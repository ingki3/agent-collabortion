# 개발 계획 — v1 구현 순서 (SSOT)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.5 — `plan/DEV_PLAN_03.md`와 동일. 이전 버전·리뷰는 `plan/`. `plan/DEV_PLAN_REVIEW_02.md` 반영: 게이트 모델 마감(게이트 예산·컷 2 위치·12주 도달 동작), P2 전환 시점 G4, 역방향 채널, 클럭 주입. 변경 내역은 §11 |
| 작성일 | 2026-09-05 |
| 근거 | `PRD.md` v0.11 §8 아키텍처·§9 NFR·§10 로드맵·§11 지표·§12 스파이크, `SCREEN.md` v0.3, `COMPONENTS.md` v0.4, `design/DESIGN_REVIEW_03.md`(K2 HITL 카드 분리) |
| 범위 | v1(MVP) 17항목 전부. 일정은 **게이트 9개**로 재고, 달력 **12주는 상한**(§6). v1.1 이후는 §9에 자리만 |
| 확정 사항 | 기술 스택 **Go + Next.js + Postgres**(PRD §8.1). 개발 주체는 **AI 에이전트 오케스트레이션**(사람은 Director 한 명), 오케스트레이션 도구 **Orca**, Reviewer 런타임 **Hermes**(§10) |
| 목적 | **무엇을 어떤 순서로 만드는지**와 **왜 그 순서인지**를 정한다. 각 단계에 완료 판정(DoD)을 두어 "됐다"를 사람마다 다르게 읽지 않게 한다 |

> **한 줄 요약.** 스파이크로 아키텍처를 확정한 뒤 계약을 고정하고, "사람이 멘션 → Claude Code가 답글"이라는 **가장 얇은 수직 슬라이스**를 실시간으로 돌린 뒤, 협업 코어(lane·합류·질문·종료 판정) → HITL·재개·예산 → 코드 작업(worktree·GC·재바인딩) → 마감 순으로 쌓는다. 되돌리기 비싼 것(재개 경로, 고아 프로세스, 워크트리 오염)은 그 기능을 켜는 단계에서 같이 만든다. 일정은 주가 아니라 **Director 게이트 9개**로 잰다 — 에이전트 팀에서 구현 시간은 압축되지만 Director의 판정, 실제 런타임 실험, 사람 실측은 압축되지 않기 때문이다. 게이트마다 **예산**(`blocked`·PR 라운드·토큰)이 있어 초과는 게이트 실패와 같다. 달력 12주는 상한이고 닿았을 때의 동작이 정해져 있다. 컷은 시간이 아니라 **게이트 실패**로 발동한다(§6).

---

## 1. 계획 원칙

1. **의존성과 리스크가 순서를 정한다.** 기능의 화려함이 아니다. ACP 하네스가 없으면 아무것도 안 돌고, 라우터가 없으면 lane이 의미 없고, 재개 경로가 없으면 HITL이 작업을 잃는다.
2. **수직 슬라이스를 먼저 끝까지 뚫는다.** 서버·데몬·웹을 각각 완성한 뒤 붙이면 통합에서 시간이 사라진다 — 통합은 에이전트 병렬성으로 압축되지 않는다. P1 끝에 "멘션 하나가 런타임을 거쳐 답글로 돌아오는" 경로가 실제로 돌아야 한다. 그 뒤로는 그 경로를 넓히는 일이다.
3. **스파이크가 아키텍처를 정하고, 계약은 그 뒤에 고정한다.** `claude-code-acp`가 불안정하면 CLI 어댑터가 v1에 들어오고 하네스가 두 벌이 된다(PRD §12-1). 툴 차단 방식과 브리프 전달 경로도 하네스 인터페이스를 바꾼다. **스파이크 1·2·3 판정 전에는 계약을 고정하지 않는다** — 같은 주에 둘 다 하면 하나는 다시 쓴다.
4. **데이터 유실 계열은 해당 단계 안에서 끝낸다.** 고아 프로세스·토큰 폐기(FR-9.1)는 격리 종류와 무관하므로 **P1**, 재개·재시도의 부분 실행 처리(FR-7.1)는 P3, 워크트리 오염(§8.4)은 P4. "나중에"가 없다.
5. **인터페이스 계약이 곧 에이전트 간 대화다.** 서버 API, `task_event` 스키마, `colab` CLI 명령·토큰, 데몬↔서버 프로토콜, 큐 인터페이스. 네 스트림의 에이전트는 이것만 보고 병렬로 간다.
6. **컷 순서는 이미 정해져 있다**(PRD §10). 이 계획은 컷 대상을 **컷이 실제로 작업량을 줄이는 단계**에 배치하고, 게이트(§6.2)에서 발동 여부만 판단한다.
7. **결정적 로직은 테스트가 먼저다.** 라우터, 상태 머신, 종료 조건 판정은 PRD에 표로 있다. 테스트를 별도 작업으로 먼저 위임한다.

---

## 2. 시스템 분해와 스트림

PRD §8.1의 구성을 따른다. 기술 스택은 **Go 서버 · Next.js 웹 · Postgres**로 확정했다. 데몬과 `colab` CLI도 Go — 단일 바이너리 배포가 필수라 서버와 언어를 맞추면 프로토콜 타입을 공유한다.

| 스트림 | 구성 요소 | 비고 |
|---|---|---|
| **S (서버)** | API, Router, 세션·lane·task 상태 머신, **종료 조건 판정**, 큐(인터페이스 뒤에 Postgres SKIP LOCKED), 실시간(WS/SSE), Platform LLM 클라이언트(§8.5), 인박스, GC 스케줄러 | 동기 API 경로에서 LLM 호출 없음(§8.1) |
| **D (데몬)** | 페어링·probe, task claim, workdir 준비, 브리프 파일, **ACP 하네스**, (조건부) CLI 어댑터, `task_event` 전송, heartbeat, **고아 정리(P1)** | 단일 바이너리. multica 데몬 구조 참고 |
| **C (colab CLI + MCP)** | `colab session/message/lane/hitl/artifact/decision/status/review` 명령, 같은 기능의 MCP 서버, `COLAB_TASK_TOKEN` 인증 | 데몬과 함께 배포. 에이전트가 플랫폼에 되돌아오는 유일한 경로 |
| **W (웹)** | S1~S17 화면, 도메인 컴포넌트 8종, 실시간 갱신, 활동 피드 렌더러 | `COMPONENTS.md`의 컴포넌트를 그대로 React 컴포넌트로 |

**병렬성.** 계약 고정 후 S·D·W·C를 **각각 별도 에이전트**가 동시에 만든다(§10). 각 단계의 DoD는 네 스트림이 붙은 상태에서만 판정한다 — 스트림별 "내 쪽은 됐다"는 완료가 아니다. 에이전트는 서로의 작업을 암묵적으로 알지 못하므로 계약이 사람 팀일 때보다 훨씬 더 중요하다. **통합과 판정은 병렬성으로 줄지 않는다** — 일정은 그래서 게이트로 잰다(§6).

---

## 3. 단계별 계획

단계의 길이는 적지 않는다. 각 단계는 **Director 게이트**로 끝나고(§6.1), 그 안의 구현은 에이전트가 병렬로 한다. 아래 "사람 팀 환산"은 작업량의 크기 감각을 위한 것이지 일정이 아니다.

| 단계 | 게이트 | 목표 | 사람 팀 환산 | 압축 안 되는 것 |
|---|---|---|---|---|
| P0 | G1 스파이크 판정 → G2 계약 승인 | 스파이크 → 계약 | 1.5주 | 스파이크 1의 실제 런타임 100턴, Director 판정 4회(스파이크 3건 + 계약) |
| P1 | G3 수직 슬라이스 DoD | 수직 슬라이스 | 2주 | E2E·kill -9 검증을 Integrator가 실제로 돌리는 시간 |
| P2 | G4 중간(A 단일 런타임) → G5 A 8단계 + Hermes | 협업 코어 (P2a 테스트 → P2b 구현) | 3주 — **가장 큰 단계**(§6.2 예산이 가장 큼) | 골든 테스트 해석 차이의 `blocked` 왕복, Hermes 실기, **템플릿 3분 Director 실측** |
| P3 | G6 C·D + 중복 0 (= 컷 2·3 판정) | HITL · 재개 · 예산 | 3주 | HITL 왕복은 Director가 직접 답해야 검증됨 |
| P4 | G7 B | 코드 작업 (컷 3 대상) | 1.5주 | 7일 오프라인 유예(시뮬레이션), 스파이크 5 |
| P5 | G8 F1 실측 → G9 출시 판정 | 마감 | 1주 | **신규 사용자 5명의 달력 시간** — G8 실패 시 재실측도 달력(5명 × 다시) |

### P0 · G1 → G2 — 스파이크, 그 다음 계약

**목표**: 아키텍처 결정 3개를 내린 **뒤에** 계약을 고정한다. 두 부분으로 나눈다.

**P0-a — 스파이크와 골격 (→ G1)**

| 작업 | 산출물 | 담당 |
|---|---|---|
| **스파이크 1** `claude-code-acp` 성숙도 — **시간 상자 3일** | §4-1 실험 결과 + **결정: CLI 어댑터 v1 포함 여부** | D |
| **스파이크 2·3** ACP 툴 차단 수단, `session/new` 시스템 프롬프트 필드 | 결정: HITL 툴 차단 방식, 브리프 전달 경로 | D |
| **스파이크 4a** 런타임 resume 능력 — ACP resume이 이전 컨텍스트를 유지하는가, Hermes 유실 감지가 되는가 | 런타임별 resume 가능 여부 표. **4c(콜드 스타트 정성 평가)는 P3 착수 전으로** | D |
| **스파이크 6** Claude Code CLI 플래그 재확인 (반나절) | 부록 C 대조표 | D |
| 스키마 v0 | PRD §7 전체를 마이그레이션으로. `message.state`, `task.budget_override`, `lane.runtime_session_ref` 등 v1.1용 필드도 지금 넣는다 — 스파이크 결과와 무관 | S |
| 프로젝트 골격 | 모노레포, CI, `make dev`(Postgres·서버·데몬·웹), 관측 스켈레톤 | S+W |
| 디자인 토큰 | 변수 19개(색 18 + 글꼴)·글리프·soft/solid를 CSS 변수와 Badge 컴포넌트로(SCREEN §9-1) | W |

**스파이크 5**(워크트리 지시 파일)는 **P4 착수 전**으로 옮긴다. worktree 종속이고, 컷 3이 발동하면 아예 필요 없다.

**P0-b — 계약 고정 (→ G2). G1 이후에만 시작한다.**

| 작업 | 산출물 | 담당 |
|---|---|---|
| 하네스 인터페이스 | 스파이크 1·2·3 결과 반영: `transport: acp \| cli`, 권한 응답 정책, 툴 차단 옵션, 브리프 전달 경로 | D+Lead |
| 데몬↔서버 프로토콜 | claim·heartbeat·event push·**토큰 폐기 통보(서버 → 데몬** — 재큐잉을 서버가 하므로). **큐 인터페이스**를 추상화(§7-3) | S+D |
| `task_event` 스키마 | class·verb·object_ref·outcome | S+D+W |
| OpenAPI 초안 | P1~P3에서 쓸 리소스 전부 | S+W |
| **주입 가능한 클럭** | 서버·데몬의 시간 의존 로직(런타임 유예 7일, HITL 기한 24h, deputy 12h, `overdue`, workdir 보존 만료) 전부 클럭 인터페이스 경유. P3·P4 테스트가 시계를 조작해야 하므로 골격에 지금 넣는다 | S+D |
| `colab` CLI 계약 | FR-7.4 명령 표 + 토큰 형식 + "턴을 끝내라" 반환 규약 | C+D |

**DoD**
- 스파이크 1·2·3 판정이 문서화되고 PRD §10 조건부 항목이 확정됨. 로드맵과 §6 컷 예약 갱신.
- `make dev`로 전부 뜨고, 데몬이 페어링되어 `probe()` 결과가 DB에 들어감. **확인 화면은 P1 S11에서** — P0에서는 `colab`이 아닌 관리용 curl/psql로 확인해도 된다(스크립트로 남긴다).
- 계약 문서 5종 + 클럭 인터페이스가 PR로 머지됨. 이후 계약 변경은 Director 승인 PR로만.

**이 단계에서 하지 않는 것**: 화면 그리기, 라우팅 규칙, 스파이크 결과 전의 계약 고정.

---

### P1 · → G3 — 수직 슬라이스

**목표**: 사람이 `@Lead`를 멘션하면 Claude Code가 ACP로 돌고, `colab message post`로 답글을 올리고, 그것이 웹에 실시간으로 보인다. **lane·HITL·격리 없이** 이 한 경로만. 그리고 그 경로에서 **데몬이 죽어도 데이터가 안 깨진다.**

| 스트림 | 작업 |
|---|---|
| S | 워크스페이스·멤버 CRUD + **초대 링크·수락**(S2·S3의 백엔드), 에이전트 CRUD(프로파일 1개), 세션 생성(`none` 격리·`runtime_id`·participants), 메시지 게시, **라우터 규칙 2·6만**(명시 멘션 → task, 그 외 → assignee), task 상태 머신 `queued → dispatched → running → completed/failed`, `task_event` 수신·저장, WS 브로드캐스트, `COLAB_TASK_TOKEN` 발급·검증·**재큐잉 시 폐기** |
| D | task claim(long-poll), `none` workdir 준비, 브리프 파일(§8.4 [1]~[5]), **ACP 하네스 핵심**: `initialize` → `session/new` → `session/prompt`, `allow_once` 탐색으로 권한 자동 응답(없으면 `reject_once` + 피드 기록), `session/update` → `task_event` 정규화(class·verb·object_ref·outcome — 렌더러는 P2), `turn_end` 처리, 프로세스 그룹 관리, heartbeat 15초, **고아 프로세스(FR-9.1)**: pgid 디스크 기록, 재시작 시 claim 전 정리, 폐기 토큰 협조 |
| C | `colab session get`, `colab session messages`, `colab message post`, MCP 서버 동일 3개 |
| W | 앱 셸(S1 로그인, **S2 회원가입, S3 초대 수락**, App Nav), **S11 최소**(런타임 카드: 이름·온라인·CLI 목록) + **S12**(설치 명령 + 연결 대기 4단계 — F1 최대 이탈 지점이므로 지금부터 실측 대상), S5 최소(상태 배지 + 제목 + 새 세션 버튼), **S7 중앙 열만** — 타임라인 + 작성창(멘션 자동완성) + 활동 피드 **원본 레일**, 실시간 갱신 |

**DoD** (E2E 자동화)
- 웹에서 "@Lead 인사해줘" 게시 → 2초 안에 데몬이 claim → Claude Code가 ACP로 실행 → `colab message post`로 답글 → 웹 타임라인에 스트리밍 도착. **E2E 20회 반복**의 첫 출력 중앙값 10초 이내 — §9의 p50은 분포 지표라 표본 수를 적는다. 부하 조건의 p50은 P5.
- 데몬을 `kill -9`하고 재시작하면: `running` task가 heartbeat 만료 후 재큐잉, **살아남은 런타임 프로세스가 정리되고**, 폐기된 토큰의 `colab message post`가 거부되어 같은 메시지가 **두 번 게시되지 않는다**(idempotency `task_id + seq` + 토큰 폐기 — 둘 다 필요).
- 취소(§8.2.2 절차)가 프로세스 트리를 남기지 않는다.
- 초대 링크로 두 번째 멤버가 워크스페이스에 들어온다(§10의 Director·Reviewer가 같은 워크스페이스에 있어야 하므로).
- 신규 머신에서 S12 절차로 데몬 페어링까지 실측 1회(P5 실측의 기준선).

**왜 이것부터인가.** ACP 하네스·큐·실시간·CLI 토큰·고아 정리 — 이 다섯이 나머지 전부의 기반이다. 이 단계에서 하네스의 추상화 누수(권한 옵션 형식, 이벤트 형식 차이)가 드러나야 P2에서 Hermes를 붙일 때 다시 짜지 않는다. 고아 정리를 여기 두는 이유는 원칙 4 — `none` 격리에서도 데몬이 죽으면 런타임이 살아남아 게시를 계속한다.

---

### P2 · G4 → G5 — 협업 코어

**목표**: 여러 에이전트가 lane으로 병렬 실행되고, 위임·합류·질문이 규칙대로 돌며, **세션이 종료 조건으로 끝난다.** 시나리오 A(`none` 격리)가 8단계 끝까지 통과한다.

**P2a — 결정적 로직의 테스트 먼저.** Reviewer 에이전트가 쓴다 — 구현 에이전트와 해석이 갈리는 지점이 먼저 드러난다.

| 테스트 | 근거 |
|---|---|
| 라우터 골든 테이블: (메시지 유형 × 작성자 × 멘션 × 스레드 위치 × lane 상태) → (트리거 대상, lane, 병합 여부) | FR-3.3 8규칙 × lane 해소 4규칙, 규칙 8 억제 범위, 병합 단위 = lane |
| 상태 머신 전이 표: 세션 6·task 10·lane 7 상태 + 불법 전이 거부 | PRD §7. `waiting_human → running` 직접 전이 거부 |
| 종료 조건 판정: 4종의 AND/OR 조합 → `completing` 진입 여부 | FR-6.x, 시나리오 A 8단계·B `agent_approval` |
| 루프 상한 3종 → `paused` | FR-3.5 |

**P2b — 구현**

| 스트림 | 작업 |
|---|---|
| S | **라우터 전체**: 규칙 1~8 + lane 해소 4단계 + 병합(lane 단위) + 트리거 미리보기 API(`suppress_agent_ids`), lane/workdir 분리, 합류 그룹(`delegated_from_task_id`) + **1회 발화** + 서버 통보, **`blocked` 경로**(질문 카드 + 즉시 시스템 메시지 + 위임자 없으면 인박스 `lane_blocked`), 루프 상한 3종 → 세션 **`paused` 상태 전이**(사유별 배너·재개 UX는 P3), 동시성 상한(세션·에이전트·데몬), 호출 권한 게이트(`respond_to`, 세션 참여 = 허용), 결정 기록 저장, **종료 조건 판정 4종 + 세션 `active → completing → completed`** — `artifact_submitted` 충족 시 플랫폼이 `user_approval` HITL 발행(HITL 전이 자체는 P3이므로 P2에서는 인박스 항목 + 승인 API까지), **요약 생성은 P4** |
| D | 프로파일 다중 + 같은 머신 내 폴백, **Hermes ACP 어댑터**(`mcpCapabilities` 필터, 유실 감지·stderr 스니핑·250ms 정적 대기 — §8.2.5), 능력 광고 기반 판정(§8.2.1), 브리프 [6]~[8] 완성 |
| C | `colab lane delegate`, `colab status set working/blocked/done`, `colab decision record`, `colab artifact submit/get`, `colab review approve/reject` |
| W | S7 **좌열**(참여자 칩 — 파생 상태, lane 보드 7상태 카드), 작성창 트리거 미리보기 칩 + × 억제 + "새 lane으로 보내기" 토글, 질문 카드(`blocked_q`) + 스레드, S7 우열(goal·종료 조건 진행률·아티팩트·결정 기록·비용), **활동 피드 렌더러 5클래스**(컷 1 대상 — 발동 시 2클래스 + 오류), S9·S10(프로파일 편집기·킬 스위치), **팀 템플릿 3종**(FR-1.4 — 역할·instruction 프리셋 + 프로파일 자동 매핑; S4 3단계와 시나리오 A 셋업의 전제), S6 마법사(`none`만) |

**DoD**
- **시나리오 A E2E 8단계 전부**: Lead가 Researcher에 3항목 위임 → lane 3개 병렬 → 각각 완료 → **합류가 정확히 한 번** → Lead 종합 → Writer 초안 → `artifact_submitted` 충족 → `user_approval` 인박스 → 승인 → `completed`. Lead가 깨어난 횟수 = 위임 1 + 합류 1 + Writer 완료 통보 1.
- P2a 테스트 전부 초록. 규칙 8이 제3자 멘션은 통과시킴.
- `blocked` 왕복: Backend 질문 → Lead 즉시 깨어남(합류 전) → 답글 → Backend 재진입 → 같은 lane.
- 루프: 두 에이전트를 서로 멘션하게 두면 5왕복에서 세션 `paused` + Director 알림.
- Hermes 프로파일로 같은 시나리오 A 통과. Claude Code ↔ Hermes 폴백 전환 시 workdir 유지.
- 템플릿에서 시나리오 A 팀(Lead·Researcher·Writer) 생성이 3분 이내 — **Director가 직접 클릭해서 실측 1회.** 에이전트가 API로 만드는 시간은 이 지표가 아니다(F1 15분의 부분 지표).
- **P2 말 결정**: HITL 카드 본문 하위 컴포넌트 분리(§8-4). P3가 HITL 카드를 두 곳에 만들기 전에.

**G4 (중간 게이트)**: P2a 통과 + 시나리오 A **Claude Code 단일 런타임** 통과. 미달이면 Hermes 어댑터를 P3로 이월(§6).

---

### P3 · → G6 — HITL · 재개 · 예산

**목표**: 사람이 개입하는 모든 경로. 세션이 멈추고 다시 이어지되 **작업을 잃지 않는다.** 시나리오 C·D 통과.

**착수 전**: 스파이크 4c — 브리프 + 히스토리 + 결정 기록만으로 콜드 스타트한 에이전트가 작업을 이어가는가(정성 평가). 실패면 콜드 스타트 프롬프트에 직전 활동 피드 요약을 주입.

| 스트림 | 작업 |
|---|---|
| S | HITL `question`·`approval`: 툴 호출 → `pending_hitl` → `turn_end`에 `waiting_human` 전이 → task당 1개 제한, 인박스 Needs Action 질의 + 심각도 7종, deputy(기한 절반 후 응답 가능, 취소는 즉시), `approval`은 절대 자동 진행 안 함 + `overdue`, **재개 = 새 attempt**(lane `runtime_session_ref` resume 우선 → 콜드 스타트), **재시도 공통 경로**(같은 workdir, `attempt ≥ 2` 프롬프트에 "이미 게시한 메시지 목록"), 재지시 = 새 task(`restarted_from_task_id`), 세션 `paused` **사유 5종 구분 + 재개**(드레인 vs 취소), `paused` 중 `queued` dispatch 금지, **턴 중 예산 강제** → `paused(budget)` + `task.budget_override`, 추정치 런타임은 하드 컷 대신 `paused`, 시스템 발행 HITL(`source: system`) — P2의 `user_approval` 인박스 항목을 정식 HITL로 승격 |
| D | `usage_update` 누적 → 예산 초과 시 §8.2.2 취소, resume 거부 감지 → `resume_rejected` 보고, `<resumed>` 프롬프트 구성 |
| C | `colab hitl ask --default`, `approve-request`, `request-info` — 호출 시 "턴을 끝내라" 반환 |
| W | S8 인박스 전체(7종 + deputy 노출 시점), HITL 카드(타임라인·인박스 — P2 말 결정대로 분리), S7 상단 액션(일시정지·재개·종료·참여자·Director 교체), lane 카드 "중단하고 다시 지시"/"중단", `paused` 배너 5종, **S7-P·S7-D 변형**(권한 비활성 + 툴팁), task 이력 펼침 |
| 테스트 | **부분 실행 시뮬레이터**(편집 N + 게시 M 후 강제 종료 → 재개 → 중복 카운트)를 구현보다 먼저 — P2a와 같은 방식 |

**DoD**
- HITL 왕복: Writer 질문 → 턴 종료(프로세스 없음, 슬롯 미점유) → Director 답변 → 새 attempt → **resume 성공 시 이전 대화를 기억**(스파이크 4a 기준) → 콜드 스타트 시에도 작업 이어감(4c 기준).
- **중복 0**: 파일 절반 편집 + 메시지 2개 게시 상태에서 데몬 kill → 재큐잉 → 재개 → 같은 메시지 재게시 0, 같은 편집 중복 적용 0. §11 "< 1%"를 CI에서 0으로.
- 예산: task 예산 초과 → `paused(budget)` → Director 상향 승인 → 같은 lane·workdir로 재개. 에이전트 `budget_per_task`는 불변.
- deputy: 발행 후 12h 전에는 승인 버튼 비활성 + "22:31부터", 취소는 즉시.
- 시나리오 C: Director 메시지가 실행 중 턴을 **절대** 죽이지 않음. 버튼만 죽임.
- 시나리오 A의 마지막 `user_approval`이 정식 HITL 카드로 돈다.

**왜 P2 뒤인가.** HITL 재개는 lane의 `runtime_session_ref`와 workdir 재사용 위에 선다. P2 없이 만들면 콜드 스타트만 되는 반쪽이 된다.

---

### P4 · → G7 — 코드 작업과 운영 안전장치

**목표**: `worktree` 격리와 그에 딸린 유실 방지, 그리고 세션 요약. 시나리오 B 통과. **컷 3 대상** — G6에서 착수 여부를 판정한다.

**착수 전**: 스파이크 5 — 추적 중인 `CLAUDE.md`에 마커 append + `skip-worktree`. 런타임이 읽는가, 에이전트가 편집하면, 복원 후 diff. 실패면 별도 파일(`CLAUDE.local.md` 등) + import 지시로 우회.

| 스트림 | 작업 |
|---|---|
| S | **세션 요약**(Platform LLM) + `session_summary` 메시지 — **§8.5 클라이언트**(모델·캐싱·`stop_reason == "refusal"` 검사·`stop_details.category` 피드 기록), 컨텍스트 재사용(첨부 + 상한), workdir GC 스케줄러(보존 기한·용량 상한·미병합/미커밋 차단·브랜치 보존), 런타임 오프라인 유예(7일) → `paused` → **재바인딩**(remote URL 판정, diff 아티팩트 재적용 프롬프트) / 종료, 활성 세션 걸린 런타임 삭제 차단, `task_event` 페이로드 마스킹 옵션 |
| D | `worktree` 준비(`git worktree add`, 브랜치 `colab/<session>/<agent>`), **브리프 오염 방지**(추적 파일 `skip-worktree` / 미추적 `.git/info/exclude`, 마커 구간만 복원), probe에 remote URL·브랜치·클린 여부, **worktree 이중 쓰기 검증**(P1 고아 정리가 worktree에서도 성립하는지) |
| C | `colab artifact submit --type diff` |
| W | S6 마법사 전체(격리 → 런타임 필터, 저장소 검증), S11 전체(저장소·유예·재바인딩), S13 workdir 관리, **S17 재바인딩 다이얼로그**, 활동 피드 파일 편집·셸 카드 상세 |

**DoD**
- **시나리오 B E2E**: PM 스펙 → Backend/Frontend 워크트리 병렬 → diff 아티팩트 → QA 리뷰(다른 워크트리 접근 없이 아티팩트만) → 수정 요청 스레드 답글 → **같은 lane·같은 워크트리** 재진입 → `agent_approval` → `completed` + 요약 게시.
- 저장소가 `CLAUDE.md`를 추적하는 상태에서 세션 실행 후 `git status` 클린, 원본 무손상, 에이전트가 정당하게 고친 부분 보존.
- 데몬 `kill -9` 후 재시작: worktree에서 같은 디렉토리 이중 쓰기 0.
- 런타임 오프라인 7일 시뮬레이션 → `paused` → 재바인딩 → diff 재적용 프롬프트로 콜드 스타트.
- GC: 미병합 커밋 워크트리는 만료돼도 삭제 안 됨 + 알림.
- 요약 생성에서 `refusal`이 오면 피드에 오류로 남고 세션은 `completed`로 끝난다(요약 없이).

---

### P5 · G8 → G9 — 마감

| 작업 | 판정 |
|---|---|
| 온보딩 S4(템플릿은 P2에 이미 있음) — **F1 15분** 실측 5회, P1 기준선과 비교 | 중앙값 < 15분 |
| 테스트 채팅(FR-1.8.1 — 세션 아님·토큰 없음) | |
| S14 설정 8탭(기본값 PRD §7) | |
| 관측 대시보드 — **§11 지표 10개 전부**: F1 시간, 자동 완료 비율, HITL 응답 시간, 위임 자율 처리 비율, 병렬 wall-clock 단축, 런타임별 task 성공률, 재개 후 중복률, resume 성공률, **`blocked` 응답 시간(< 5분)**, 주간 활성 세션 | 대시보드에서 §11 표를 그대로 읽을 수 있음 |
| 성능: p50 지연 §9, 동시 task 50 | 부하 테스트 |
| 보안 점검: originator 기준 권한, 토큰 범위, 마스킹 | |
| 컷 발동 여부 최종 판정 | §6 |

**DoD**: 신규 사용자 5명이 문서 없이 F1을 15분 안에 끝낸다. 시나리오 A·B·C·D E2E가 CI에서 초록.

---

## 4. 스파이크 상세

결과가 이후 설계를 바꾸므로 실험 방법과 분기를 미리 적는다. **위치**가 v0.2에서 바뀌었다.

| # | 위치 | 실험 | 통과 기준 | 실패 시 분기 |
|---|---|---|---|---|
| 1 | P0-a, **시간 상자 3일** | `claude-code-acp`로 Claude Code를 100턴 구동. resume 20회, 취소 20회, 권한 요청 50회 | 크래시 0, resume 성공 ≥ 95%, **`allow_once` 부재율 < 5%**(PRD §8.2.2가 부재 시 `reject_once`를 이미 정했으므로 "항상 존재"는 조건이 아니다), 프로토콜 버전 고정 가능 | **CLI 어댑터를 v1에 포함**. 하네스 인터페이스는 유지, Claude Code만 `transport: "cli"`. P1에 어댑터 작업 추가 → §6 컷 1 예약 |
| 2 | P0-a | ACP에서 `AskUserQuestion` 차단: 어댑터 옵션 / 설정 파일 / `--disallowedTools` 전달 | 차단된 툴 호출이 에이전트에게 오류로 돌아옴 | 브리프에 "쓰지 마라" + 호출 시 피드 경고(`tool_disallow: false`) |
| 3 | P0-a | `session/new`에 시스템 프롬프트 넣고 브리프 없이 실행 | 에이전트가 브리프 내용을 인지 | §8.4 1번 경로 삭제, 지시 파일이 유일 경로 |
| 4a | P0-a | (a) resume + "질문/답변" 프롬프트 → 이전 작업 맥락 유지? (b) Hermes `state.db` 삭제 후 resume → 유실 감지? | (a) 90%, (b) 100% 감지 | resume 불가 런타임은 콜드 스타트 전용으로 표시 |
| 4c | **P3 착수 전** | 브리프 + 히스토리 + 결정 기록만으로 콜드 스타트 → 작업 이어감? | 정성 평가 통과 | 콜드 스타트 프롬프트에 직전 활동 피드 요약 주입 — **§8.4 턴 프롬프트 계약 변경 PR**이 필요하므로 P3 착수 전 Director 게이트 1회 추가 |
| 5 | **P4 착수 전** (컷 3 시 생략) | 추적 중인 `CLAUDE.md`에 마커 append + `skip-worktree` → 런타임이 읽는가 / 편집하면 / 복원 후 diff | 런타임 인지, `git status` 클린, 복원 무손실 | 별도 파일 + import 지시로 우회 |
| 6 | P0-a, 반나절 | `--effort` `--max-budget-usd` `--append-system-prompt-file` 실재 확인 | 부록 C 표와 일치 | §8.2.4 갱신 |

**1번의 시간 상자는 3일 — 이것은 달력이다.** 실제 런타임을 100턴 돌리는 시간은 에이전트가 압축하지 못한다. 3일 안에 판정 못 하면 "불안정"으로 간주하고 CLI 어댑터를 포함한다 — 판정을 미루는 것이 최악이다. 스파이크 4c·5를 뒤로 뺀 결과 P0 D 에이전트의 부하가 절반으로 준다.

---

## 5. 테스트 전략

| 계층 | 방법 | 대상 | 시점 |
|---|---|---|---|
| 라우터 | **골든 테이블 테스트** — 규칙이 결정적이라 표로 다 적을 수 있다 | FR-3.3 8규칙, lane 해소 4규칙, 규칙 8 억제 범위, 병합 단위 | P2a (구현 전) |
| 상태 머신 | 전이 표 테스트 + 불법 전이 거부 | 세션 6·task 10·lane 7 상태 | P2a |
| 종료 조건 | 4종 AND/OR 조합표 | FR-6.x | P2a |
| 하네스 | **가짜 ACP 서버**(스크립트 응답) + 실제 런타임 계약 테스트 | 권한 옵션 변형, `turn_end` 지연, 마지막 청크가 응답 뒤에 오는 경우(Hermes), resume 거부 문구 | P1 |
| 고아 | 데몬 SIGKILL + 런타임 생존 → 재시작 → pgid 정리, 토큰 거부 | FR-9.1 | **P1** |
| 시간 의존 | **주입 가능한 클럭**으로 시계를 앞당겨 유예·기한·만료를 시험 | 런타임 유예 7일(P4), HITL 24h·deputy 12h·`overdue`(P3), 보존 만료(P4) | P0-b 골격, P3~P4 사용 |
| 재개·재시도 | 부분 실행 시뮬레이터 → 중복 카운트 | §11 "< 1%" — CI에서 0 | P3 (구현 전) |
| E2E | 시나리오 A·B·C·D를 실제 Claude Code·Hermes로 야간 실행(비용 상한) | §11 지표 | P2~ |
| 화면 | 컴포넌트 8종 상태별 스토리 + 권한 비활성 뷰 스냅샷 | `COMPONENTS.md` 조합표 = 스토리 목록 | 각 단계 |

**원칙**: 결정적 로직의 테스트는 Reviewer 에이전트가 구현 전에 쓴다(§10.3). 규칙이 문서에 표로 있으니 테스트가 곧 문서다. **테스트 파일은 구현 PR에서 못 건드린다.**

---

## 6. 일정 — 게이트, 컷, 상한

### 6.1 왜 주가 아니라 게이트인가

v0.3은 12주 기준·10주 컷 경로를 적었다. 개발 주체가 에이전트이므로 이 산정은 반만 맞다.

| | 사람 팀 | 에이전트 팀 |
|---|---|---|
| 단계 안 구현 (라우터, 화면, 명령, 테스트) | 주 단위 | **시간 단위** — 압축된다 |
| Director 판정 (스파이크, 계약, DoD, 컷, 계약 변경 PR) | 주당 몇 번 | **그대로** — 이것이 시계다 |
| 실제 런타임 실험 (스파이크 1 100턴, 야간 E2E, Hermes 실기) | 며칠 | **그대로** — 외부 시스템 속도 |
| 사람 실측 (F1 15분 × 5명) | 며칠 | **그대로** |
| 단계 순서 의존 (P1 전에 P2 불가) | 있음 | **있음** |

그래서 일정을 **Director 게이트 9개**로 잰다. 한 단계의 길이 = 그 단계 안에서 Director가 답해야 하는 횟수(게이트 + `blocked` + 계약 PR) × Director 응답 주기. §10.6의 하루 세 번이 지켜지면 대부분의 단계는 **며칠**이고, 압축 안 되는 항목이 있는 단계(P0 스파이크 1, P5 실측)만 달력에 묶인다.

**달력 12주는 상한이다.** "Director가 12주 안에 게이트 9개를 다 넘겨준다"는 약속이지 산정이 아니다. 상한에 닿는다면 원인은 코딩이 아니라 §6.3의 리뷰 적체다.

### 6.2 게이트와 예산

게이트로 재기로 했으면 게이트마다 **"여기까지 얼마를 쓰는가"**가 있어야 한다. 없으면 Director가 답하지 않는 동안 에이전트는 무한히 돈다 — 골든 테스트를 통과시키려 구현을 20번 고치는 식으로. §6.3 적체 임계값은 Director 쪽 병목만 잡는다. 아래 예산은 에이전트 쪽 폭주를 잡는다. **초과는 게이트 실패와 같이 취급한다** — PRD FR-7.3이 예산 초과를 `paused`로 만드는 것과 같은 원칙이다.

| 게이트 | 단계 | Director가 판정하는 것 | 예산 (`blocked` / PR / 토큰) | 통과 못 하면 |
|---|---|---|---|---|
| **G1** | P0-a | 스파이크 1·2·3 판정 (CLI 어댑터 포함 여부, 툴 차단, 브리프 경로) | 3 / 6 / P0 실측 | 3일 상자 초과 = 불안정 → 어댑터 포함, **컷 1 예약** |
| **G2** | P0-b | 계약 5종 + 클럭 승인. **G1 이후에만.** **토큰 예산 단위 확정** | 3 / 8 / P0 실측 | 계약 없이는 P1 fan-out 금지 |
| **G3** | P1 | 수직 슬라이스 DoD + kill -9 + 고아 정리 + 20회 중앙값 | 10 / 30 / G2에서 | **P2를 시작하지 않는다.** 기반이 흔들리면 뒤가 전부 흔들린다 |
| **G4** | P2 중간 | P2a 골든 테스트 + 시나리오 A Claude Code 단일 | 8 / 20 / G2에서 | Hermes 어댑터를 P3로 이월(하네스는 유지) → **G5의 Hermes 조건이 G6로 옮겨간다** |
| **G5** | P2 | 시나리오 A 8단계 + Hermes + 템플릿 3분 Director 실측 | 15 / 40 / G2에서 — **가장 큰 예산** | Hermes 미통과면 Reviewer의 colab Hermes 전환(§10.1)도 미룸 |
| **G6** | P3 | 시나리오 C·D + 중복 0 (+ G4에서 이월된 Hermes) | 15 / 40 / G2에서 | **컷 3 판정** — 미통과면 P4를 시작하지 않고 P3를 마감 |
| **G7** | P4 | 시나리오 B + `git status` 클린 + 재바인딩 + 요약 | 8 / 25 / G2에서 | **컷 2 판정** — B는 됐는데 요약이 안 됐으면 요약을 뺀다. 재바인딩 미통과면 v1.1로 |
| **G8** | P5 | F1 15분 실측 5회 (달력) | 5 / 15 / G2에서 | 온보딩 수정 후 재실측 — **재실측도 달력**(5명 × 다시). 12주 상한을 먹는 유일한 재시도 |
| **G9** | P5 | 출시 판정: A·B·C·D CI 초록 + §11 대시보드. **달력 12주 도달 시에도 열린다** — 그때 미통과 게이트 이하의 기능은 v1.1, 통과한 게이트까지의 범위로 출시 판정(단 G8 F1 실측은 생략 불가) | — | |

**예산 값에 대해.** `blocked` 상한과 PR 상한은 초기값이고, **토큰 상한은 지금 적지 않는다** — 근거 없는 숫자는 계획이 아니라 장식이다. P0-a에서 작업 단위(§10.3 형식 하나)당 실제 소비를 재어 단위 `u`를 얻고, G2에서 게이트별 토큰 예산을 `u`의 배수로 확정한다. PR당 리뷰 라운드 상한 3(§10.4)은 별도로 유지된다.

**컷은 시간이 아니라 게이트 실패로 발동한다.** 컷 대상은 컷 판정 게이트 **이후** 단계에 있고, 컷의 근거는 그 게이트에서 실제로 보인다.

| 컷 | PRD 대상 | 위치 | 발동 게이트 | 근거가 그 게이트에 있는 이유 |
|---|---|---|---|---|
| 1 | 활동 피드 5클래스 → 2클래스 + 오류 | P2 W 렌더러 | G1(어댑터 포함 시) 또는 G4 | G1: 어댑터가 P1 예산을 먹는다. G4: P2 예산 소진 속도가 보인다 |
| 3 | `worktree` → v1.1 (P4 전체 + 스파이크 5) | P4 | **G6** | C·D가 안 됐으면 worktree를 시작할 여력이 없다 |
| 2 | 요약 생성만 (판정은 P2 잔존 — PRD v0.11) | P4 S | **G7** | G6에 두면 컷 3 발동 시 자동으로 따라오고, 미발동 시 판정 근거가 없다. "B는 됐는데 요약이 안 됐다"가 컷 2의 자리 — PRD v0.11 "가장 늦게 발동"과 일치 |

### 6.3 리뷰 적체 — Director 쪽 일정 지표

모든 게이트에서 함께 본다: 미리뷰 PR **> 8** 또는 **24시간 넘게 대기한 PR·`blocked` 존재** → Director의 하루(§10.6) 재배분, Reviewer 에이전트 증원, 또는 개발용 deputy 발동(§10.6). §6.2 예산이 에이전트 쪽, 이 숫자가 Director 쪽이다. 둘 중 하나가 넘치면 일정이 늘어난다 — **이 두 숫자가 곧 진행률이다.**

---

## 7. 이 계획이 기대는 가정

1. **`claude-code-acp`가 존재하고 현재 Claude Code 버전과 호환된다.** 스파이크 1이 이것부터 본다.
2. **Hermes ACP가 `session/resume`과 `usage_update`를 실제로 준다** — multica 코드에서 확인했으나 우리 버전에서 재확인(스파이크 4a).
3. **Postgres SKIP LOCKED 큐가 동시 task 50에서 충분하다.** 넘으면 Redis — 그러면 claim·heartbeat 프로토콜이 바뀌므로 **P0-b에서 큐 인터페이스를 추상화**해 둔다. "스키마는 안 바뀐다"가 아니라 "계약 뒤에 숨긴다"가 대응이다.
4. **통합·판정·실기 실험은 에이전트 병렬성으로 줄지 않는다.** 그래서 게이트로 잰다(§6.1).
5. **Director 한 명이 하루 세 번 개입할 수 있다**(§10.6). 이것이 단계 길이를 정한다. 안 되는 날은 §10.6의 개발용 deputy가 받는다.

---

## 8. 결정 대기 (착수 전)

| # | 결정 | 근거 | 기한 |
|---|---|---|---|
| ~~1~~ | ~~12주 vs 10주~~ | §6.1 | **완료** — 17항목 전부, 달력 12주는 상한, 일정은 게이트 |
| ~~2~~ | ~~오케스트레이션 도구~~ | §10.0 | **완료** — **Orca** (P0~P2 외부 도구, 이후 colab으로 전환) |
| ~~3~~ | ~~Reviewer 런타임~~ | §10.1 | **완료** — **Hermes**. P1부터 Orca에서 직접 띄우고(P0은 Lead 겸임), G5 후 colab Hermes 프로파일로 전환 |
| 4 | SCREEN Q7 — 에이전트 수정·삭제 권한 | SCREEN §8.2 | P2b 착수 전 (권한 게이트 구현 시) |
| 5 | SCREEN Q2·Q3·Q5·Q6 — 세션 삭제, 아카이브 표시, 푸시 알림, 모바일 | SCREEN §8.2 | P3 착수 전 |
| 6 | HITL 카드 본문 하위 컴포넌트 분리 | COMPONENTS §2.3, 리뷰 #03 K2 | **P2 말** (P3가 두 곳에 만들기 전) |

---

## 9. v1.1 이후 (자리만)

컨테이너 격리(스파이크 7), Antigravity CLI 어댑터(스파이크 8), `.agent.md` 팀 팩, Build with AI, lane DAG 의존성, `supervised` 모드, HITL `choice`·`info`, 부분 합류, 채널 계층, 위험 명령 사전 승인, 세션 긴급 정지, `criteria_met` — PRD §10 v1.1 목록 그대로. v1 스키마에 이미 자리를 잡아둔 것(`message.state`, `task.budget_override`, `session_context`)은 마이그레이션 없이 켠다. 컷 3 발동 시 `worktree` 전체가 여기로 온다.

---

## 10. 개발 주체가 에이전트일 때 — 오케스트레이션 운영

이 제품은 "에이전트 여러 개가 공유 컨텍스트에서 협업하고 사람은 Director로 개입한다"는 모델이다. **개발 자체를 같은 모델로 한다.** 그래서 PRD가 정한 규칙 — 위임·합류·질문 경로·루프 상한·HITL — 이 개발 과정에도 적용된다. 잘 안 되는 지점이 곧 제품의 약점이므로, 개발 중 겪은 마찰은 PRD 리스크(§12)로 되먹인다.

### 10.0 무엇으로 돌리는가 — 단계별 전환

v0.2의 §10은 "P0부터 `colab`으로 개발한다"로 읽혔다. 그럴 수 없다 — `colab status set blocked`는 P2, HITL은 P3에서 만든다. **v1을 만드는 데 v1이 필요한 순환**을 끊으려면 단계별로 무엇을 어떤 도구로 돌리는지 명시해야 한다.

전환점은 "P2 시작"이 아니라 **G4 통과**다. 위임·질문·합류를 colab으로 갈아타려면 그 셋이 실제로 돌아야 하는데, 그것을 만드는 단계가 P2b다 — P2b의 S 에이전트가 합류 그룹을 만드는 동안 Lead는 Orca 합류로 기다린다. G4(P2a 골든 테스트 + 시나리오 A 단일 런타임)가 "위임·합류·blocked가 돈다"는 증거이므로 그 뒤에 갈아탄다.

| 협업 기능 | P0 ~ G4 | G4 ~ G5 (전환 기간) | G5 ~ G6 | G6 이후 |
|---|---|---|---|---|
| 위임 (Lead → 스트림) | **Orca** 작업 DAG | **colab** `lane delegate` (Orca 백업) | colab | colab |
| 질문 (`blocked`) | Orca ask/reply | **colab** `status set blocked` + 질문 카드 (Orca ask/reply **백업 유지**) | colab | colab |
| 합류 1회 | Orca DAG 완료 훅 | **colab** 합류 그룹 (Orca 백업) | colab | colab |
| Director HITL (반려 상신·계약 승인) | Orca + PR 리뷰 | Orca + PR 리뷰 | Orca + PR 리뷰 (HITL은 P3) | **colab** `hitl ask`·`approve-request` |
| 토큰 예산 (§6.2) | Orca / 수동 집계 | Orca | Orca | **colab** 예산 강제 |
| 리뷰 반려 루프 상한 | 수동(Lead가 센다) | **colab** `max_pair_roundtrips` | colab | colab |
| Reviewer 런타임 | Hermes (Orca 터미널) | Hermes (Orca 터미널) | **colab Hermes 프로파일** | colab |

**역방향.** 전환 기간(G4~G5)에는 Orca ask/reply를 **백업 채널로 유지**한다. colab으로 갈아탄 위임·질문·합류가 **24시간 이상 막히면** Lead가 Orca로 Director에게 HITL을 올리고, Director가 해당 기능을 Orca로 되돌린다. `blocked` 자체가 colab이면 막힌 것을 알릴 수단도 없어지므로 백업 채널이 필요하다. 되돌린 항목은 §10.7 되먹임에 기록한다 — 이것이 없으면 전환 마찰이 데이터가 아니라 정체가 된다.

**전환 자체가 dogfooding의 시작점이다.** G4 후 위임·질문·합류를 `colab`으로 갈아탈 때 난 마찰 — "Orca에서는 됐는데 colab에서는 안 되는 것" — 이 §10.7 되먹임의 첫 데이터다. 갈아탄 뒤 Orca로 되돌아간 항목이 있으면 그것이 PRD 결함이다. Orca는 병렬 워크트리·터미널 모델이라 PRD의 lane·workdir 격리와 같은 구조다 — 그래서 전환 비교가 공정하다.

### 10.1 역할

| 역할 | 수 | 하는 일 | 하지 않는 일 |
|---|---|---|---|
| **Director** (사람) | 1 | §8 결정, 스파이크 판정, 단계 DoD 승인, HITL 응답, 컷 발동, 계약·테스트 변경 PR 머지 | 코드 작성 |
| **Lead** (오케스트레이터 에이전트) | 1 | 단계별 작업 분해(§10.3), 스트림 에이전트에 위임, 합류 후 통합 상태 보고, 계약 변경 조율, 충돌 판단을 Director에게 HITL | 직접 구현 — PRD §8.3 Lead 프로토콜 그대로 |
| **S / D / W / C 에이전트** | 4 | 각자 스트림 구현. 워크트리 하나씩(격리), 작업 단위마다 PR | 남의 스트림 코드 수정 — 계약 변경이 필요하면 Lead에게 `blocked` |
| **Reviewer** | 1~2 | 모든 PR 리뷰: 계약 준수, 테스트 존재·통과, PRD/SCREEN 대조. **P2a·P3의 결정적 로직 테스트 작성.** **Hermes** — 구현 에이전트(Claude Code)와 다른 런타임·모델로 PRD 목표 6(벤더 혼합)을 개발 과정에서 검증. P1부터 Orca 터미널로 직접 띄우고(P0은 Lead 겸임 — 스파이크는 Director가 판정하고 PR이 골격뿐이라), G5 후 colab Hermes 프로파일로 전환(전환 마찰이 Hermes 어댑터 검증 데이터). **24h 후 자동 머지의 주체**(§10.6) | 직접 수정 — 반려와 사유만. **반려 = PRD FR-3.4의 "중단하고 다시 지시"**: 새 task(`restarted_from_task_id`)로 기록되어 리뷰 루프가 task 이력에 남는다 |
| **Integrator** | 1 | 단계 말에 네 스트림을 붙여 E2E(§5)를 돌리고 DoD 판정 자료를 만든다. **P1부터 분리** — P1 DoD(kill -9 재큐잉, 프로세스 트리)는 구현을 조율한 Lead가 검증하면 "자신 있게 틀림"을 잡을 사람이 없다 | 기능 추가 |

P0만 Lead가 Reviewer·Integrator를 겸한다(스파이크는 Director가 판정하므로).

### 10.2 이 구성이 사람 팀과 다른 점

| 항목 | 사람 팀 | 에이전트 팀 | 계획에 미치는 영향 |
|---|---|---|---|
| 암묵 지식 | 복도 대화로 전파 | **없다** | 계약·SSOT 문서가 유일한 공유 컨텍스트. P0-b 계약 고정이 필수 조건 |
| 작업 단위 | 며칠짜리 티켓 | **반나절 이하**, 입력·출력·DoD가 명시된 것 | Lead가 §10.3 기준으로 분해. 큰 작업을 주면 스코프가 새거나 컨텍스트가 넘친다 |
| 병렬성 | 인원수 | 사실상 무제한 | 병목이 코딩 → **리뷰·통합·결정**으로 이동. §6.3 리뷰 적체 임계값 |
| 실패 양상 | 느림 | **자신 있게 틀림** — 계약을 임의 해석, 테스트를 통과시키려 테스트를 고침 | Reviewer가 다른 모델, 테스트 파일 변경은 별도 PR로 강제, Integrator 분리 |
| 컨텍스트 한계 | 없음 | 있음 | 에이전트 브리프에 PRD 전체를 넣지 않는다. 해당 FR·계약·이 계획의 해당 단계만 |
| 일정 | 코딩 시간 | 리뷰 대기 시간 + 통합 시간 | 게이트로 잰다(§6.1). 달력 12주는 상한 |

### 10.3 작업 단위 규칙

Lead가 위임할 때 지키는 형식이다. 이 형식을 못 채우면 작업이 아직 분해되지 않은 것이다.

```
작업: <한 줄>
스트림: S | D | W | C
입력: 읽어야 할 문서 절 (PRD FR-x.y, 계약 파일, 이 계획 §3 Px)
출력: PR 하나 — 변경 파일 범위, 새 테스트
DoD: 통과해야 할 테스트 이름 / 확인 명령
금지: 건드리면 안 되는 것 (남의 스트림, 계약 파일, 기존 테스트)
막히면: blocked — 계약 변경이 필요할 때 (도구는 §10.0 단계별)
```

- **반나절 이하.** 넘으면 쪼갠다.
- **테스트를 먼저** — 결정적 로직은 Reviewer가 별도 작업으로 먼저 쓴다(P2a, P3 시뮬레이터).
- **계약 파일과 테스트 파일은 구현 PR에서 건드리지 못한다.** 바꿔야 하면 `blocked`로 Lead에게, Lead가 Director에게.

### 10.4 실행 구조

- **격리**: 스트림 에이전트마다 git 워크트리 하나. 모노레포에서 `server/` `daemon/` `cli/` `web/`를 각각 브랜치로. PRD FR-6.4의 worktree 격리와 같은 모델.
- **위임과 합류**: 단계 시작 시 Lead가 스트림별 작업을 fan-out, 전부 PR이 열리면 Integrator가 합류. 개별 완료마다 Lead를 깨우지 않는다(PRD FR-6.5).
- **질문 경로**: 구현 에이전트가 계약이 모호하다고 판단하면 임의 해석하지 말고 `blocked`. Lead가 답할 수 있으면 답하고, 제품 결정이면 Director.
- **리뷰 루프 상한 = 3** (PRD FR-3.5 기본 `max_pair_roundtrips`는 5). 다른 이유: 리뷰 반려는 Director가 보는 PR 라운드라 비용이 임의의 에이전트 핑퐁보다 크고, 3회 반려는 대개 계약 해석 차이라 에이전트끼리 더 돌아도 안 풀린다. **개발에서 3이 맞았다면 그것이 되먹임 데이터다**(§10.7) — 제품 기본값을 "리뷰 관계에서는 낮게"로 조정할 근거.
- **리뷰 게이트**: PR 머지 조건 = Reviewer 승인 + CI 초록 + (계약·테스트 변경 시) Director 승인.
- **예산**: 게이트별 `blocked`·PR·토큰 예산(§6.2). 초과 시 Lead가 멈추고 Director에게 보고 — 게이트 실패와 같이 취급. PRD FR-7.3과 같다.

### 10.5 단계별 에이전트 배정

| 단계 | Lead | S | D | C | W | Reviewer | Integrator |
|---|---|---|---|---|---|---|---|
| P0-a | 스파이크 조율 | 스키마 v0, 골격 | 스파이크 1·2·3·4a·6 **전담** | — | 토큰·Badge | Lead 겸 | — |
| P0-b | 계약 초안·조율 | 프로토콜·OpenAPI | 하네스 인터페이스 | CLI 계약 | — | Lead 겸 | — |
| P1 | 분해·조율 | API·큐·WS·라우터 2규칙·토큰 폐기 | 하네스 핵심·고아 정리 | 3개 명령 + MCP | 셸·S2·S3·S11 최소·S12·S7 중앙 | **분리** (Hermes, Orca에서) | **분리** (E2E·kill -9) |
| P2a | 조율 | — | **Hermes 어댑터 착수**(골든 테스트와 독립이라 병행) | — | — | **골든 테스트 작성** | — |
| P2b | 〃 | 라우터·lane·합류·blocked·종료 판정 | Hermes·프로파일 폴백 | 5개 명령 | S7 좌·우열·피드 5클래스·S6·S9·S10·템플릿 | 〃 → G5 후 **colab Hermes 프로파일로** | 시나리오 A |
| P3 | 〃 | HITL·재개·예산·paused | 예산 강제·resume 감지 | hitl 3개 | S8·S7-P·S7-D·배너 | 시뮬레이터 작성 | 시나리오 C·D |
| P4 | 〃 | 요약·GC·재바인딩 | worktree·오염 방지 | artifact diff | S6 전체·S11 전체·S13·S17 | 〃 | 시나리오 B |
| P5 | 마감 조율 | 관측·성능 | — | — | S4·S14 | 〃 | F1 실측 |

### 10.6 Director가 매일 하는 것, 그리고 없을 때

| 시점 | 일 | 근거 |
|---|---|---|
| 아침 | 인박스 — `blocked` 질문·HITL 응답. **30분 안에** | §11 "HITL 응답 중앙값 < 30분"을 개발 과정에서 먼저 지킨다 |
| 정오 | 리뷰 적체 PR 머지 판정 | 계약·테스트 변경 PR은 Director만 |
| 저녁 | Lead의 통합 보고 확인, 다음 날 분해 승인 | |
| 단계 말 | DoD 판정, 컷 발동 여부(§6.3) | |

**Director 부재 시 — 개발용 deputy.** PRD FR-5.2를 개발 과정에도 둔다. Director가 한 명이라 휴가·회의 중 `blocked`가 쌓이면 §6.3 리뷰 적체가 곧바로 넘친다.

| 대상 | deputy | 조건 |
|---|---|---|
| `blocked` 질문 응답 | **Lead 에이전트** | 기한 **4h**의 절반(2h) 경과 후. 계약 해석 범위 안에서만 — 계약을 바꾸는 답은 못 한다 |
| 구현 PR 머지(계약·테스트 미변경) | **Reviewer** (승인 + CI 초록) | 24h 경과 후. Integrator가 아니다 — Integrator가 머지하면 다음 E2E에서 자기가 머지한 것을 검증한다 |
| 계약·테스트 변경 PR, 컷 발동, DoD 승인, 스파이크 판정, 게이트 예산 초과 | **위임 없음** | Director 복귀까지 대기 — PRD가 `approval`을 절대 자동 진행하지 않는 것과 같다 |

**`blocked` 기한 4h는 PRD FR-5.2 기본 24h와 다르다.** 에이전트는 사람보다 빨리 막히고, 막힌 채 기다리는 동안 다른 스트림이 그 답에 의존한다. 24h면 하루가 통째로 선다. 4h가 맞았는지가 되먹임 데이터다(§10.7).

**deputy 판정의 사후 검증.** "계약 해석 범위 안"인지를 판정하는 것은 Lead 자신이다 — §10.2 "자신 있게 틀림"이 여기서 난다. 그래서 deputy로 답한 `blocked`는 **Director 복귀 시 목록으로 보고**하고, Director가 뒤집으면 그 답 위에 만든 PR은 재지시(FR-3.4, `restarted_from_task_id`)로 처리한다. 사후 검증이 있으면 Lead의 판정 오류가 회복 가능하다.

### 10.7 되먹임

개발 중 에이전트 협업에서 겪은 것을 PRD로 돌린다. 예상되는 것:

- **P2 전환 마찰**(§10.0) — 외부 도구에서 되던 것이 `colab`에서 안 되는 항목 → 해당 FR 결함
- Lead가 위임 후 턴을 끝내지 않고 기다리는 문제 → PRD §8.3 프로토콜 문구 강화
- 구현 에이전트가 계약을 임의 해석하는 문제 → "모호하면 `blocked`" 규칙의 효과 측정 → FR-6.2.1
- 리뷰 반려 루프가 3에서 잡히는지 5까지 가는지 → FR-3.5 기본값 재검토
- Director 응답 지연이 일정에 미치는 영향, deputy 발동 빈도 → §11 지표와 FR-5.2 기한 기본값의 현실성
- Reviewer를 Hermes로 돌렸을 때 리뷰 품질 → 목표 6 벤더 혼합의 실효성
- **`blocked` 기한 4h vs PRD 24h** — 에이전트 팀에서 적정 기한, deputy 답변이 뒤집힌 비율 → FR-5.2 기본값
- **전환 역방향 발동**(§10.0) — Orca로 되돌린 항목과 이유 → 해당 FR 결함
- **게이트 예산 초과 빈도**(§6.2) — 어느 게이트에서 어느 예산이 먼저 닿는지 → FR-7.3 예산 단위(task vs 세션)의 적절성

이 되먹임이 제품 검증의 일부다. **자기 제품으로 자기를 만드는 것이 v1의 첫 사용자 테스트다.**

---

## 11. 변경 내역

### v0.2 → v0.3 (`DEV_PLAN_REVIEW_01.md` 반영)

`DEV_PLAN_REVIEW_01.md`의 지적 21건(C 3, O 7, m 10, §1 대조 4)을 전부 확인했다. 21건 모두 사실관계가 맞았다. 반영 내용과, 리뷰 권고와 다르게 한 곳.

### 반영

| # | 조치 |
|---|---|
| C1 | v0.3: 기준 12주 + 10주 컷 경로. **v0.4에서 게이트 기준으로 대체**(§6.1) — Director 결정 "12주, 단 에이전트 팀에서 주 단위의 의미 재검토". 중간 게이트 G4, 리뷰 적체 임계값은 유지 |
| C2 | **§10.0 신설** — 단계별 "무엇으로 돌리는가" 표. P0~P1 외부 도구, P2 위임·질문·합류 전환, P3 HITL·예산 전환. 전환 마찰 = 되먹임 첫 데이터 |
| C3 | P0을 **1.5주 P0-a/P0-b**로 — 스파이크 1·2·3 판정 **후** 계약 고정(원칙 3 개정). 스파이크 4c → P3 전, 5 → P4 전 |
| §1.2 | 스파이크 4를 4a(P0, 런타임 능력)·4c(P3 전, 콜드 스타트)로 분리 |
| §1.3 | S2·S3를 P1에 명시 |
| §1.4 | P5 대시보드에 §11 지표 10개 전부(`blocked` 응답 시간 포함) |
| O1 | S11 최소 + S12 → P1. P1 DoD에 신규 머신 페어링 실측(기준선) |
| O2 | 팀 템플릿 3종 → P2 W. DoD "시나리오 A 팀 생성 3분" |
| O3 | 종료 조건 판정 + `active → completing → completed` → P2 S(P2a 골든 테스트 대상). 요약만 P4. 컷 2 절감을 "요약"으로 갱신(§6.2). 시나리오 A DoD를 8단계 전부로 |
| O4 | 고아 프로세스 + 토큰 폐기 → P1 D/S. P1 DoD에 "살아남은 런타임 정리 + 폐기 토큰 거부". P4에는 worktree 이중 쓰기 검증만 |
| O5 | 활동 피드 렌더러 5클래스 → P2 W. 컷 1 = "2클래스 + 오류". 컷 1이 실제로 P2를 줄이도록 |
| O6 | P2a(Reviewer가 골든 테스트, 3일) → P2b 분리. P3에도 시뮬레이터 선행 명시 |
| O7 | Reviewer 반려 = FR-3.4 재지시(`restarted_from_task_id`) 대응 명시. Reviewer Hermes 전환 = 목표 6 검증. 반려 3 vs 5 이유 기록(아래 "다르게 한 곳") |
| m1 | P4 S에 §8.5 클라이언트 + `refusal` 처리, DoD에 refusal 케이스 |
| m2 | S5 최소 = 상태 배지 + 제목 + 새 세션 버튼 |
| m3 | 루프 상한 → `paused` 상태 전이는 P2, 사유별 배너·재개 UX는 P3로 구분 |
| m4 | 스파이크 1 통과 기준 "`allow_once` 부재율 < 5%" |
| m6 | §7-3 "큐 인터페이스를 추상화"로 정정, P0-b 작업에 반영 |
| m7 | HITL 하위 컴포넌트 결정 기한 → P2 말 |
| m8 | Integrator·Reviewer P1부터 분리, P0만 Lead 겸임 |
| m9 | §10.6 개발용 deputy 표 |
| m10 | 헤더 근거에 `DESIGN_REVIEW_03.md` |

### 리뷰 권고와 다르게 한 곳

| # | 리뷰 권고 | 이 문서 | 이유 |
|---|---|---|---|
| C1 | "P2·P3 각 3주 **또는** 컷 1 선제 발동" | 12주를 기준으로 쓰고 10주 경로를 별도 명시, **선택은 Director** | 리뷰의 O2·O3·O5가 P2에 작업을 **더한다**(템플릿·종료 판정·피드 5클래스). 컷 1만으로는 10주로 안 돌아온다 — 컷 3까지 필요하고, 그것은 시나리오 B를 v1.1로 보내는 제품 결정이라 계획이 대신 내릴 수 없다 |
| C3 | 스파이크 6도 P0에서 뺀다 | P0-a에 유지 | 반나절이고 결과가 CLI 어댑터 계약(P0-b)에 들어간다. 빼면 P0-b에서 다시 찾아본다 |
| O7 | 반려 상한을 5로 맞추거나 이유를 적는다 | **3 유지**, 이유 기록 | 리뷰 반려는 Director가 보는 PR 라운드라 비용이 다르고, 3회면 대개 계약 해석 차이. 3이 맞았는지가 되먹임 데이터 |
| m9 | Lead에게 "기한 절반 후 머지 권한" | `blocked` 응답만 Lead, 구현 PR 머지는 Integrator(24h 후), **계약·테스트·컷·DoD는 위임 없음** | 에이전트에게 머지 권한을 주는 범위를 좁혔다. PRD가 `approval`을 자동 진행하지 않는 것과 같은 원칙 |

### PRD 후속 — 반영 완료 (PRD v0.11, 2026-09-05)

- §10 컷 표 2번 행 "절감" 열: "종료 조건 판정·요약 생성" → "요약 생성"(판정은 결정적 로직으로 P2 잔존).
- §12 스파이크 4를 4a/4c로, 5의 시점을 "worktree 착수 전"으로.

### v0.3 → v0.4 (착수 전 결정 반영, 2026-09-05)

| 결정 | 내용 | 반영 |
|---|---|---|
| 일정 | Director: "12주. 그런데 에이전트가 진행하는데 주 단위가 의미가 있나?" | **없다 — 반만.** 구현은 압축되지만 Director 판정·실기 실험·사람 실측은 압축되지 않는다. 일정 단위를 **게이트 9개**로 바꾸고 12주는 상한으로만. 컷 발동을 시간 → 게이트 실패로. §3 표, §6 전면 개정 |
| 오케스트레이션 도구 | **Orca** | §10.0 표에 명시. Orca의 워크트리·터미널 모델이 PRD lane·workdir와 같은 구조라 P2 전환 비교가 공정하다는 점 추가 |
| Reviewer 런타임 | **Hermes** | Orca에서 직접(P2까지 기다릴 이유 없음), G5 후 colab Hermes 프로파일로 전환. §10.1·§10.5. ~~P0부터~~ → v0.5 m5에서 P1부터로 정정 |

### v0.4 → v0.5 (`DEV_PLAN_REVIEW_02.md` 반영, 2026-09-05)

리뷰 27건을 대조했다. **25건이 맞았고 2건은 사실과 다르다.**

**반영**

| # | 조치 |
|---|---|
| C1 | 컷 2 발동 게이트를 G6 → **G7**로. 근거 열 추가 — G6에 두면 컷 3 발동 시 자동으로 따라오고 미발동 시 판정 근거가 없다 |
| C2 | §6.2에 **게이트당 예산** 열(`blocked` / PR / 토큰). 초과 = 게이트 실패. §10.4 예산 항목을 §6.2로 연결, deputy 위임 없음 목록에 추가 |
| C3 | G9에 **달력 12주 도달 동작** — 미통과 게이트 이하 v1.1, 통과 범위로 출시 판정, G8 생략 불가 |
| C4 | §10.0 전환 시점을 P2 시작 → **G4 통과 후**로. 표를 P0~G4 / G4~G5 / G5~G6 / G6 이후 4열로 |
| C5 | §10.0 **역방향**: G4~G5 동안 Orca ask/reply 백업 유지, 24h 막히면 Director HITL → 되돌림 → 되먹임 기록 |
| O1 | §10.6에 `blocked` 4h vs PRD 24h 이유, §10.7에 되먹임 항목 |
| O2 | §10.6 deputy 사후 검증 — 복귀 시 목록 보고, 뒤집히면 재지시 |
| 2.4 | §4 4c 행에 "계약 변경 PR → Director 게이트 1회 추가" |
| O3 | §3 표 P2에 "가장 큰 단계", G5 예산이 가장 큼을 명시 |
| O4 | P1 DoD "E2E 20회 반복의 중앙값". 부하 p50은 P5 |
| O5 | P2 DoD 템플릿 3분 = **Director 실측 1회**, §3 압축 안 되는 것 열에 추가 |
| O6 | **주입 가능한 클럭**을 P0-b 골격에, §5에 시간 의존 행. 유예 7일·HITL 24h·deputy 12h·`overdue`·보존 만료 |
| m2 | §3 표 P0 판정 횟수 4회 |
| m3 | G4 이월 시 G5 Hermes 조건이 G6로 옮겨가는 연쇄를 표에 |
| m4 | G8 재실측이 달력이며 12주 상한을 먹는 유일한 재시도임을 명시 |
| m5 | Reviewer Hermes "P0부터" → **P1부터**(P0은 Lead 겸임). §8·§10.1 통일 |
| m6 | §10.5 P2a에 D의 Hermes 어댑터 병행 |
| m7 | 24h 후 자동 머지 주체를 Integrator → **Reviewer**. Integrator는 검증만 |
| m9 | 토큰 폐기 통보 방향 **서버 → 데몬** |
| m8·m10 | 확인만, 조치 없음 |

**리뷰와 다르게 한 곳**

| # | 리뷰 | 이 문서 | 이유 |
|---|---|---|---|
| m1 | "`design/` 디렉토리가 없다" | 미반영 | **사실과 다름.** `design/DESIGN_REVIEW_01~03.md`와 FOLLOW_UP 3개가 존재한다. 헤더 경로는 맞다 |
| O7 | "COMPONENTS.md 조합표가 Lane Card에만" | 미반영 | **사실과 다름.** 디자인 리뷰 #03 K3가 이미 반영되어 Lane Card 상태별, Message Card kind별, Inbox Item 타입별 조합표가 전부 있다. P2b W 전제 조건 불필요 |
| C2 | 게이트당 토큰 상한을 지금 적는다 | `blocked`·PR 상한만 초기값, **토큰은 G2에서 확정** | 근거 없는 숫자는 장식이다. P0-a에서 작업 단위당 실소비 `u`를 재고 배수로 정한다 |
