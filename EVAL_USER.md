# EVAL_USER — 사용자 시나리오 중심 테스트 케이스

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 |
| 작성일 | 2026-09-05 |
| 근거 | `SCREEN.md` v0.3 (§2 역할·권한, §3.3 흐름 F1~F4, §4 화면, §7 빈 상태·오류), `PRD.md` v0.11 §3 시나리오 A~D, `EVAL.md` v0.1 |
| 짝 문서 | `EVAL.md`는 **규칙** 중심 — 한 행이 규칙 하나. 이 문서는 **사람** 중심 — 한 여정이 사용자가 목표를 이루는 경로 하나. 같은 동작을 두 문서가 다른 각도에서 본다. 뒤에서 일어나는 것은 `EVAL.md` 행 번호로 가리키고 반복하지 않는다 |
| 목적 | "화면이 규칙대로 동작하는가"가 아니라 **"사용자가 헤매지 않고 목표에 도달하는가"**를 판정한다. 각 여정에 이탈 신호를 적어 실측(PLAN G8)에서 무엇을 관찰할지 정한다 |

## 0. 읽는 법

**페르소나 셋.** 워크스페이스 하나를 같이 쓴다.

| 이름 | 누구 | 워크스페이스 역할 | 세션 역할 | 기기·습관 |
|---|---|---|---|---|
| **민지** | 마케팅 PM. 비개발자. 터미널을 써본 적이 거의 없다. Claude Code는 동료가 깔아줬다 | owner | Director | 노트북. 하루에 서너 번 확인. 알림은 이메일 |
| **준호** | 백엔드 개발자. Claude Code·Hermes 둘 다 쓴다. 저장소 `~/dev/app` | admin | Director (개발 세션) | 데스크탑 + 노트북 2대. 터미널 상주 |
| **서연** | 기획자. 민지 팀. 세션을 읽고 의견을 남긴다 | member | deputy (민지 세션) / 멤버 | 노트북. 인박스만 본다 |

**여정 형식.**

| 칸 | 뜻 |
|---|---|
| 단계 | 순서 |
| 사용자 행동 | 클릭·입력·기다림. 사용자 관점의 동사 |
| 화면 | SCREEN.md 번호 |
| **보이는 것** | 그 순간 화면에 **정확히** 있어야 하는 것. 문구·배지·버튼 상태·숫자 |
| 뒤에서 | 시스템 동작. `EVAL.md` 행 번호 |

**성공 기준**은 측정 가능하게(시간·클릭 수·이동 횟수). **이탈 신호**는 실측에서 관찰할 "헤맴"의 구체적 형태 — 뒤로 가기, 같은 화면 반복 방문, 도움말 검색, 30초 이상 정지.

---

## U1. 민지의 첫 15분 — 회원가입에서 첫 세션 시작까지 (F1) · G8

**목표**: 아무 문서 없이 15분 안에 세션 하나가 돌기 시작한다.
**전제**: 동료가 노트북에 Claude Code를 설치해 로그인까지 해둔 상태. Hermes 없음. 계정 없음.

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 가입 링크 열고 이메일·비밀번호 입력 | S2 | 필드 2개 + 가입 버튼. 소셜 로그인 없음(v1) | 계정 생성 |
| 2 | 가입 직후 | S4-1 | 온보딩 1단계 "워크스페이스 이름". **건너뛰기** 링크 있음 | 워크스페이스 없음 → S4 자동 진입 |
| 3 | "마케팅팀" 입력 → 다음 | S4-2 | 2단계 "컴퓨터 연결": **설치 명령 2줄** + 복사 버튼 + 상태 표시 `대기 중` | 페어링 토큰 발급 |
| 4 | 복사 버튼 → 터미널 열어 붙여넣기 → 실행 | S4-2 | 화면은 그대로. 상태가 `대기 중 → 연결됨 → CLI 감지 중 → 준비 완료`로 **자동 갱신**(새로고침 불필요). 준비 완료 시 "Claude Code 감지됨 · 로그인됨 · 모델 N개", "Hermes 없음" | E11-08 probe |
| 5 | 다음 | S4-3 | 3단계 "에이전트": **템플릿 카드 3장이 맨 위**(리서치 팀 / 개발 팀 / 콘텐츠 팀), 그 아래 "직접 만들기". 각 카드에 포함 에이전트 이름·역할 3줄 | — |
| 6 | "리서치 팀" 선택 | S4-3 → S9 | "Lead · Researcher · Writer 생성됨. 프로파일: Claude Code" 확인. Hermes 프로파일은 **감지되지 않아 생략됨** 문구 | E15-12 자동 매핑 |
| 7 | "첫 세션 만들기" CTA | S6-1 | 마법사 1단계 goal. 제목·goal 필드, 예시 placeholder("국내 B2B SaaS 결제 시장 조사 보고서 10페이지") | — |
| 8 | goal 입력 → 다음 | S6-2 | Director = 본인(기본). deputy 비움 | — |
| 9 | 다음 | S6-3 | 격리: `none` **기본 선택**, `worktree`는 "저장소가 필요합니다" 설명. `container` 없음 | — |
| 10 | 다음 | S6-4 | 런타임: "자동 선택(첫 실행 시 고정)" 기본. 아래에 방금 연결한 노트북 1대 | — |
| 11 | 다음 | S6-5 | 참여자: 템플릿 3개가 **이미 체크됨**, assignee = Lead 기본 | — |
| 12 | 다음 | S6-6 | 종료 조건 기본값 체크됨: `Writer가 artifact 제출` AND `Director 승인`. 문장으로 읽힘 | — |
| 13 | 다음 → 요약 → 시작 | S6-7 → S7 | 한도 기본값, `autonomy: guided` "질문 기한이 지나면 계속 기다립니다". 시작 후 S7: 타임라인에 goal 시스템 메시지 1개, lane 보드 빈 상태 **"아직 위임이 없습니다. assignee가 계획을 세우는 중입니다"**, 참여자 칩 Lead `working` | E16-A 1단계 |

**성공 기준**: 가입 → 13단계 도달 **중앙값 < 15분** (5명). 마법사 7단계에서 **기본값만으로 통과 가능**(입력한 것은 goal 하나).
**이탈 신호**: 4단계에서 3분 이상 `대기 중` → 문제 해결 안내가 **자동으로 펼쳐져야** 함(S12). 5단계에서 "직접 만들기"를 먼저 누름 → 템플릿이 충분히 앞에 없다는 뜻. 9~10단계에서 뒤로 가기 → 격리·런타임 설명 부족.
**변형**: 4단계에서 데몬이 붙었는데 CLI 0개(로그인 안 됨) → "Claude Code 감지됨 · **로그인 필요**" + 명령 안내. 다음 버튼은 활성(건너뛰기 가능)이되 홈 체크리스트에 남음.

---

## U2. 민지가 세션이 도는 것을 지켜본다 — 시나리오 A의 사용자 시점 · G4·G5

**목표**: 화면을 읽기만 해도 "지금 무엇을 하고, 잘 되고 있고, 내가 끼어들 일이 없다"를 안다.
**전제**: U1 직후. Lead가 계획을 세우는 중.

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 기다림(1~2분) | S7 | 타임라인에 Lead의 계획 메시지. 활동 피드에 "Lead가 Researcher에게 3건 위임함" 한 줄. lane 보드에 **카드 3장** `running`, 각 카드에 브리프 한 줄 | E16-A 2단계 |
| 2 | 카드 하나 클릭 | S7 | 그 lane의 스레드로 스크롤·강조. 활동 피드 원본 레일 펼침 가능 | — |
| 3 | 참여자 칩 확인 | S7 좌열 | Researcher 칩 `working`(lane 3개 중 running이 있으므로). Lead 칩 `idle` | E5-11 |
| 4 | 5분 뒤 | S7 | lane 카드 2장 `done` ✓, 1장 `running`. Lead 칩 여전히 `idle` — **Lead는 깨어나지 않음** | E3-01 |
| 5 | 마지막 lane 완료 | S7 | Lead 칩 `working`. 타임라인에 시스템 메시지 "3개 lane 결과가 Lead에게 전달됨" **한 번**. 활동 피드 "Lead 종합 중" | E3-02 |
| 6 | Lead가 `@Writer` | S7 | lane 카드 4장째 Writer `running`. 우열 종료 조건 진행률 `0/2` | E16-A 5단계 |
| 7 | Writer가 질문 | S7 + 내비 | lane 카드 Writer `waiting_human` ⏳. 타임라인에 **HITL 카드**(질문·제안 기본값 "투자자"·기한 24h). 내비 Inbox 뱃지 **1**. 이메일 도착 | E7-03 |

**성공 기준**: 7단계까지 민지가 클릭한 횟수 ≤ 2(카드 열기 외 불필요). "내가 뭘 해야 하나"를 7단계 전에는 느끼지 않음 — 실측에서 질문.
**이탈 신호**: 4단계에서 "Lead가 왜 아무것도 안 하지"라고 물음 → lane 카드에 "완료 후 Lead에게 한 번에 전달" 안내 필요. 활동 피드가 비어 있음 → 런타임 이벤트 미지원 문구가 있어야 함(§7).

---

## U3. 민지가 인박스에서 답한다 — 세션을 열지 않고 (F2) · G6

**목표**: 세션 화면으로 가지 않고 인박스 카드 안에서 답을 끝낸다. 30분 안에.
**전제**: U2 7단계. 민지는 회의 중 이메일 알림을 봤다.

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 이메일 링크 클릭 | S8 | 인박스 최상단 `hitl_request` `!` 카드. 카드 안에: 세션 제목, **goal 한 줄**, 에이전트 Writer, 질문 전문 "타깃 독자가 투자자인지 내부 경영진인지", **"에이전트 제안: 투자자"**, 기한 "23시간 남음", 자유 텍스트 입력 + 제출 | E7-03 |
| 2 | 카드만 읽고 "투자자" 입력 → 제출 | S8 | 카드가 목록에서 **사라짐**. 뱃지 0. 토스트 "Writer가 재개됩니다" | E7-07 |
| 3 | (선택) 세션 열기 | S7 | lane Writer `running`, HITL 카드에 답변 "투자자 — 민지" 표시. 결정 기록 우열에 1건 | E8-01 |

**성공 기준**: 세션 이동 없이 답 완료. 알림 → 제출 **< 2분**. 카드에 "더 보려면 세션 열기" 링크가 있되 **누르지 않아도 됨**.
**이탈 신호**: 2단계 전에 세션 열기를 누름 → 카드 정보 부족(goal·제안이 없음). 답한 뒤 카드가 남아 있음 → 처리됐는지 불안.
**변형 (approval)**: 시나리오 A 8단계 — 카드에 "승인 대상: 보고서.pdf(아티팩트 링크)", 버튼 **승인 / 거절(사유 필수)**. 제안 기본값 없음. 거절 시 사유 입력 없이는 제출 불가. 거절 후 세션 `active` 유지, 세션에 "거절 — 사유" 결정 기록 (E6-04).

---

## U4. 민지가 방향을 바꾼다 (F3, 시나리오 C) · G6

**목표**: 진행 중인 작업을 죽이지 않고 지시를 추가하거나, 필요할 때만 명시적으로 중단한다.
**전제**: Researcher lane 1개 `running` 중.

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 작성창에 `@Researcher 범위를 한국 시장으로 좁혀줘` 입력 | S7 | 전송 전 **트리거 미리보기 칩**: "Researcher를 트리거합니다 · 프로파일 Claude Code". 칩 옆에 "실행 중 → 현재 턴 종료 후 처리됩니다" 문구 | E15-06 |
| 2 | 전송 | S7 | 메시지 게시. lane 카드 Researcher에 **"대기 1"** 표시. 활동 피드 계속 흐름(턴 안 죽음) | E2-09 |
| 3 | 급해서 lane 카드 → **"중단하고 다시 지시"** | S7 | 확인 없이 **작성창이 열리고 `@Researcher`가 채워짐**. 안내: "전송하면 진행 중인 턴을 취소하고 이 메시지만으로 다시 시작합니다" | — |
| 4 | 새 지시 입력 → 전송 | S7 | 활동 피드 "사람이 중단하고 다시 지시함". lane `running` 유지. task 이력 펼치면 "task #2 (재지시, #1에서)" | E8-06 |
| 5 | 다른 lane 카드 → **"중단"** | S7 | 확인 다이얼로그 "이 lane을 중단합니다. 새 지시 없이 종료됩니다". 확인 후 카드 `failed` ✕ "사람이 중단함" | E10-04 |
| 6 | 파일 편집 중이던 lane에 "중단" | S7 | 카드에 "편집 완료 대기 중 (최대 30초)" 표시 후 종료 | E10-01 |

**성공 기준**: 2단계에서 "죽었나?"라고 묻지 않음 — "대기 1" 표시가 그 답. 3단계 진입 → 전송까지 클릭 3회 이하.
**이탈 신호**: 2단계 후 새로고침 반복. 3단계 후 작성창에서 멘션을 지움 → 멘션이 자동인 이유 안내 부족.

---

## U5. 세션이 끝난다 — 승인, 요약, 다음 세션으로 · G5·G7

**전제**: Writer가 artifact 제출.

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 알림 | S8 | `hitl_request` approval 카드: "종료 조건 1/2 충족 — Writer 제출 완료. 승인하면 세션이 완료됩니다". 아티팩트 링크 | E6-01 |
| 2 | 아티팩트 열어 보고 승인 | S8 → S7 | 세션 배지 `마무리 중` → 수십 초 후 `완료`. 타임라인 끝에 **`session_summary`**: 결정 기록·아티팩트·비용·타임라인 4구역 | E6-03 |
| 3 | S5로 | S5 | 행 배지 `완료`, 진행률 `2/2`, 비용 "$1.20 / $10 (12%)" | — |
| 4 | 새 세션 마법사 1단계에서 "이전 세션 첨부" | S6-1 | 완료 세션 목록. 선택 시 "요약 약 1,400 토큰 (상한 2,000) · 아티팩트는 링크만" | E15-09 |

**성공 기준**: 2단계 요약을 읽고 "무엇이 결정됐는지"를 30초 안에 말할 수 있음(실측 질문).
**변형 (agent_approval 단독, 준호)**: 마법사 6단계에서 경고 **"사람 승인 없이 완료됩니다"**가 체크박스 옆에 즉시 표시. QA 승인 시 인박스에 `session_completed` `i` 카드만 옴 — 승인 요청 없음 (E6-05).

---

## U6. 준호의 개발 세션 — worktree, diff, 리뷰, 정리 (시나리오 B) · G7

**목표**: 저장소를 더럽히지 않고 에이전트 둘이 병렬로 구현하고, 리뷰를 거쳐 끝난 뒤 준호가 병합한다.
**전제**: 준호 데스크탑 데몬 연결. `~/dev/app` 클린. 개발 팀 템플릿(PM·Backend·Frontend·QA).

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 마법사 3단계 `worktree` 선택 | S6-3 | 저장소 선택 드롭다운(데몬 probe 결과: 경로·remote·브랜치·클린). **바인딩 그림**: "에이전트당 워크트리 1개 — 같은 에이전트의 lane은 순차 실행" | E13-01 |
| 2 | 4단계 | S6-4 | 런타임 후보가 **그 저장소가 있는 머신만**. 노트북(저장소 없음)은 비활성 + "저장소 없음". "자동 선택" **비활성** | E13-17 |
| 3 | 6단계 `QA 승인` 단독, 7단계 예산 $20 | S6-6·7 | "사람 승인 없이 완료됩니다" 경고. 예산 옆 "초과 시 완료가 아니라 일시정지됩니다" | — |
| 4 | 시작 후 PM 위임 | S7 | lane 카드 Backend·Frontend `running`. 카드에 **브랜치명** `colab/<S>/backend`. 활동 피드에 파일 편집 카드(경로·+/-줄), 셸 카드(명령·종료 코드) | E16-B 2 |
| 5 | 터미널에서 `git status` (호기심) | 터미널 | **클린**. `git worktree list`에 2개. `CLAUDE.md` 변경 없음 | E13-03 |
| 6 | Backend가 diff 제출 | S7 우열 | 아티팩트 `diff` 1개: 파일 수·+/-, "보기" → diff 뷰어 | — |
| 7 | QA가 Frontend 스레드에 수정 요청 | S7 | Frontend 카드 `done → running`, 카드에 **"재진입 1회"**. 새 카드 **없음** | E16-B 5 |
| 8 | Frontend 새 diff → QA 승인 | S7 | 배지 `완료`. 우열 "워크트리 2개 보존 중 · 14일 후 정리 대상 · 병합은 직접" | E16-B 7·8 |
| 9 | Runtimes → 데스크탑 카드 → Workdir 관리 | S13 | 행 2개: worktree · 소유 에이전트 · 브랜치 · 용량 · **보존 만료일** · 상태 `보존 중`. 삭제 버튼 | — |
| 10 | 병합 전에 Frontend 행 삭제 시도 | S13 | **경고 + 기본 차단**: "미커밋 변경이 있습니다(diff만 제출됨). 삭제하면 작업 결과를 잃습니다" | E13-13 |
| 11 | 터미널에서 브랜치 병합 후 다시 삭제 | S13 | 삭제됨. **브랜치는 남음** 안내 | E13-10 |

**성공 기준**: 5단계에서 준호가 놀라지 않음(클린). 10단계 차단 문구를 읽고 이유를 말할 수 있음.
**이탈 신호**: 2단계에서 노트북을 고르려 시도 → 비활성 사유 문구 부족. 8단계 후 "그래서 코드가 어디 있지?" → 우열 안내 부족.

---

## U7. 예산이 넘쳤다 · G6

**전제**: 시나리오 B, Backend task가 $20 세션 예산의 잔여를 넘김.

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 알림 | S8 | `session_paused` `▲` 카드: "예산 초과 — $20.40 / $20. Backend 작업이 멈췄습니다". 버튼 **계속 승인**(금액 입력) / 세션 열기 | E9-04 |
| 2 | 세션 열기 | S7 | 상단 **배너 `일시정지 · 예산`**: 같은 문구 + "계속 승인" 버튼. lane Backend `paused` ⏸(soft). 다른 lane도 dispatch 안 됨 ("일시정지 중 대기 2") | E5-04 |
| 3 | 계속 승인, $30 입력 | S7 | 배너 사라짐. Backend `running`. 우열 예산 "$20.40 / **$30** (task 상한 재설정)". Agents 화면의 Backend `budget_per_task`는 **그대로** | E9-02 |

**성공 기준**: 1단계 카드만으로 "얼마를 얼마로 올릴지" 결정 가능. 3단계 후 에이전트 설정을 확인하러 가지 않음.
**변형 (추정치 런타임)**: 카드 문구 "추정 비용이 상한에 도달했습니다(정확한 사용량을 보고하지 않는 런타임)". 작업은 취소되지 않고 드레인됨 (E9-05).

---

## U8. 에이전트가 민지에게 묻는다 — 위임자가 없는 lane (F4) · G5

**전제**: 민지가 마법사 아닌 S7에서 직접 `@Researcher 경쟁사 5곳 정리해줘`로 lane을 만듦(위임자 없음).

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | Researcher가 `blocked` | S8 + S7 | 인박스 `lane_blocked` `!` 카드: "Researcher: '국내만인가요, 글로벌 포함인가요?'" + **답글 작성** 인라인. S7 lane 카드 `blocked` **?** 배지, 스레드에 질문 카드 | E3-08 |
| 2 | 인박스에서 "국내만" 답글 | S8 | 카드 사라짐. Researcher 재개 | E3-09 |

**변형 (위임자 있음)**: Lead가 위임한 lane이 `blocked`면 **민지 인박스에 오지 않음**. S7 lane 카드 `blocked` + 스레드 질문 카드 + Lead가 즉시 답하는 것이 타임라인에 보임 (E3-05). 민지가 끼어들 필요 없음 — 실측에서 "내가 답해야 하나?"를 묻지 않으면 성공.

---

## U9. 서연의 하루 — deputy · G6

**전제**: 민지 세션의 deputy. 민지 출장. Writer HITL이 오전 10:00 발행(기한 24h).

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 10:30 인박스 확인 | S8 | 카드 **없음**. "지금 답할 것이 없습니다" | E7-09 (deputy 인박스는 절반 전 비표시) |
| 2 | 11:00 세션 열어 HITL 카드 | S7 | 카드 보임. 응답 버튼 **비활성** + 툴팁 **"Director 응답 대기 중 · 22:00부터 응답 가능"** | E7-09 |
| 3 | 같은 화면, Researcher lane 폭주 | S7 | lane 카드 "중단" 버튼 **활성**(deputy는 취소 즉시). 클릭 → 확인 → `failed(cancelled)` | E10-06 |
| 4 | 22:05 알림 | S8 | 카드 나타남, 라벨 **"위임됨 · 지금부터 응답 가능"**. 이메일 도착 | E7-10 |
| 5 | 답변 | S8 | 처리됨. 결정 기록에 "서연(deputy)" | — |
| 6 | 세션 일시정지 시도 | S7 상단 | 버튼 **비활성** + "Director만" | S7-D |

**성공 기준**: 2단계 툴팁을 보고 "언제 가능한지"를 말할 수 있음. 3단계와 2단계의 차이(취소는 되고 승인은 안 됨)를 이상하게 여기면 툴팁에 이유 한 줄 필요.

---

## U10. 서연이 일반 멤버로 세션에 참여한다 · G5

**전제**: 준호의 개발 세션. 서연은 deputy 아님.

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | S5에서 세션 열기 | S7-P | 전부 열람 가능. 상단 액션(일시정지·종료·참여자) **비활성 + 사유 "Director만"**. lane 카드 버튼 비활성. HITL 카드 응답 비활성 "Director·deputy만". **화면을 숨기지 않음** | §7 권한 없음 |
| 2 | 작성창에 `@QA 접근성도 봐줘` | S7-P | 미리보기 칩 "QA를 트리거합니다". 전송 가능 | E1-08(멤버 메시지도 라우팅) |
| 3 | 전송 | S7-P | QA lane 생성·실행. 서연의 메시지가 트리거로 표시 | — |
| 4 | 자신을 멘션한 메시지가 오면 | S8 | `mention` `i` 카드. 뱃지에는 **안 셈** | S8 뱃지 규칙 |

**성공 기준**: 1단계에서 "왜 못 누르지"가 아니라 사유를 읽음. 2단계에서 게시가 된다는 것을 시도 전에 앎(작성창 활성).

---

## U11. 준호가 에이전트를 만들고, 시험하고, 정지시킨다 · G5

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | Agents → 새 에이전트 | S10 | 3구역: 정체성 / instruction / 실행. 역할 `lead` 선택 시 "코디네이션 프로토콜이 자동 추가됩니다" | — |
| 2 | 프로파일 추가: Hermes | S10 실행 | 모델 목록이 **데몬 probe 결과**(데스크탑에서 감지된 것). `effort` 옵션 비활성 + "Hermes 미지원" | — |
| 3 | 폴백 프로파일로 Claude Code 지정 | S10 | "같은 머신 안에서만 폴백됩니다" 안내 | E8-08 |
| 4 | **테스트 채팅** | S10 → 채팅 패널 | 1:1 대화. 응답 아래 "경로: ACP · 토큰 1,230". 세션 목록에 **아무것도 생기지 않음** | E15-03 |
| 5 | 테스트 채팅에서 "@Writer에게 위임해봐" | 채팅 | 에이전트가 위임할 수 없음을 답함(토큰 없음). 오류가 아니라 답변 | E15-04 |
| 6 | 저장 후 세션에서 폭주 발견 → `respond_to: nobody` | S10 위험 영역 | 확인 다이얼로그: **"실행 중인 턴이 취소되고 대기 중 task가 취소됩니다. 열린 HITL은 남습니다"** | E10-07 |
| 7 | 확인 | S9 | 카드 상태 `disabled` ⊘ (offline과 다른 글리프) | E5-15 |
| 8 | 다시 `owner` | S9 | `idle`. 남아 있던 HITL의 답이 오면 그때 재개 | E10-08 |

**성공 기준**: 4단계에서 "이게 세션인가?"를 묻지 않음. 6단계 문구를 읽고 HITL이 남는다는 걸 앎.
**변형 (서연이 준호의 에이전트 수정 시도)**: SCREEN Q7 미결 — 결정 전까지 "소유자만" 비활성으로 표시하고 실측에서 불만 빈도를 기록.

---

## U12. 준호가 노트북을 닫고 출장을 갔다 — 런타임 오프라인 · G7

**전제**: 노트북 데몬에 묶인 `none` 세션 1개, `worktree` 세션 1개. 노트북 8일간 꺼짐.

| # | 시점 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 1일차 | S11 | 노트북 카드 `오프라인 · 1일 경과 · 유예 6일 남음`. 세션은 `active`(queued 대기) | E14-01 |
| 2 | 7일차 | S8 + S5 | `runtime_offline` `▲` 카드 ×2 "노트북 7일 오프라인 · 세션 N 일시정지". S5 배지 **`일시정지 · 런타임 오프라인`** | E14-02 |
| 3 | 세션 열기 | S7 | 배너 "노트북이 7일간 오프라인 · **다른 런타임으로 재바인딩** / 세션 종료" | — |
| 4 | `none` 세션 재바인딩 | S17 | 후보: 온라인 런타임 전부(데스크탑). 유실 경고 **없음**. 확인 → 세션 `active`, 데스크탑에서 재개 | E14-03 |
| 5 | `worktree` 세션 재바인딩 | S17 | 후보: **remote URL이 같은 저장소를 가진 머신만**. 데스크탑에 같은 remote 있음 → 후보. **유실 경고**: "완료된 lane의 코드도 원래 머신의 브랜치에만 있습니다. diff 아티팩트 3개를 순서대로 적용해 복구합니다. 커밋 이력은 복원되지 않습니다" | E14-04·06 |
| 6 | 확인 | S7 | 첫 task 활동 피드에 "아티팩트 3개 재적용 중". 콜드 스타트 | E14-06 |
| 7 | 노트북을 삭제하려 함 | S11 | 재바인딩 전이면 **차단** + "세션 N개가 걸려 있음" 목록 | E14-08 |

**성공 기준**: 5단계 경고를 읽고 "커밋은 없어진다"를 말할 수 있음. 4와 5의 차이를 이해.

---

## U13. 민지가 서연을 초대한다 · G3

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | Settings → 멤버 → 초대 | S14 | 이메일 + 역할(member 기본) | — |
| 2 | 서연이 링크 클릭 | S3 | "마케팅팀에 초대되었습니다" + 가입 또는 로그인. 완료 후 **S4 건너뛰고** S5로(워크스페이스 이미 있음) | P1 DoD 초대 |
| 3 | 민지가 서연을 세션 deputy로 | S7 참여자 액션 | deputy 선택. 안내 "취소는 즉시, 승인은 기한 절반 후" | — |

**변형**: 서연(member)이 Settings 진입 → 내비에 Settings **없음**. URL 직접 접근 시 "owner·admin만" 표시, 숨기지 않음.

---

## U14. 설정을 바꾸면 무슨 일이 생기는지 안다 · G5

| # | 사용자 행동 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | Settings → 루프 상한 → `max_pair_roundtrips` 5→2 | S14 | 항목 옆 영향 문구 **"낮추면 정상적인 리뷰 왕복이 막힐 수 있습니다"**. 기본값 표시 | E4-09 |
| 2 | Workdir → 보존 14→3일 | S14 | "3일 후 병합되지 않은 워크트리는 삭제되지 않고 알림만 갑니다" | E13-12 |
| 3 | 보안 → 페이로드 마스킹 켬 (owner만) | S14 | "이후 diff·셸 출력은 요약만 저장됩니다. 기존 로그는 그대로" | — |
| 4 | 알림 → 세션 구독 "HITL만" | S14 | 이후 `mention`·`session_completed` 이메일 없음, `hitl_request`만 | — |

**성공 기준**: 각 항목의 영향 문구가 **한 줄**이고 사용자가 바꾸기 전에 읽음(실측: 바꾼 뒤 되돌리는 비율).

---

## U15. 실수와 막힘 — 시스템이 미리 잡아주는 것들 · G4·G5

| # | 상황 | 화면 | 보이는 것 | 뒤에서 |
|---|---|---|---|---|
| 1 | 민지가 `@Editor`(워크스페이스에 있지만 세션 비참여) 멘션 | S7 | 미리보기 칩 **경고** "Editor는 이 세션 참여자가 아닙니다 — 트리거되지 않습니다". 전송은 가능. 게시 후 메시지에 경고 배지 | E1-04 |
| 2 | "새 lane으로 보내기" 토글이 켜진 채로 두 번째 메시지 | S7 | 전송 버튼 옆 **"새 lane으로 전송됨"** 상시 표시. 토글은 전송 후 **자동 해제** | E2-07 |
| 3 | `@all 다들 상황 공유` | S7 | 미리보기 "트리거 없음 — 기록만". 전송 후 Lead 안 깨어남 | E1-05 |
| 4 | 마법사 3단계 worktree, 저장소에 미커밋 변경 | S6-3 | 저장소 옆 **"클린 아님 — 변경 3개"** + 다음 버튼 비활성 + "커밋하거나 stash 후 다시 시도" | E13-01 |
| 5 | 마법사 5단계, `respond_to: allowlist`에 민지 없음인 에이전트 | S6-5 | 목록에 비활성 + "초대 권한 없음 (소유자: 준호)" | E10-11 |
| 6 | 런타임 0개인 워크스페이스에서 새 세션 | S5 | CTA 대신 **"먼저 컴퓨터를 연결하세요"** + S12 링크. 세션 생성 비활성 | §7 |
| 7 | S12에서 3분 넘게 `대기 중` | S12 | 문제 해결 안내 자동 펼침: 방화벽 / 권한 / CLI 미설치 3항목 + 재시도 | S12 |
| 8 | 루프 상한 도달 | S7 + S8 | 배너 `일시정지 · 루프` "Lead와 Researcher가 5회 연속 주고받았습니다". 인박스 `session_paused`. 재개 버튼 + "메시지를 하나 보내면 카운터가 초기화됩니다" | E4-03·05 |
| 9 | 세션 `paused` 중 메시지 게시 | S7 | 게시됨. 작성창 위 "일시정지 중 — 재개 후 처리됩니다". lane 카드 "대기 N" | E5-04 |
| 10 | 에이전트 실행 실패(인증) | S8 + S9 | `run_failed` `▲` "Researcher: 인증 오류 — 재시도하지 않습니다". 버튼 **다시 지시** / 세션 열기 (일반 "재시도" 버튼 **없음**). S9 카드 `error` ⚠ | E8-10·E5-17 |
| 11 | Claude Code 사용량 한도(rate limit) | S8 + S7 | `run_failed` "Researcher: 쿼터 초과 — HH:MM 리셋". lane `failed`. 자동 재시도 없음. 리셋 후 **다시 지시**로 재개 | E8-10 |
| 12 | 인박스 비어 있음 | S8 | **"지금 답할 것이 없습니다"** (부정형 아님) | §7 |

---

## U16. 실측 프로토콜 (G8)

| 항목 | 내용 |
|---|---|
| 대상 | 신규 사용자 5명. 최소 2명은 터미널 비숙련(민지형), 2명은 개발자(준호형) |
| 과제 | U1 전체 → U2 관찰 → U3 응답 → U5 승인. 문서·도움 없이. 진행자는 답하지 않고 기록만 |
| 측정 | U1 소요 시간(중앙값 < 15분), U3 알림→제출 시간, 단계별 이탈 신호 발생 횟수, 사후 질문 3개("Lead가 언제 깨어났나요?" "답을 안 하면 어떻게 되나요?" "코드는 어디 있나요?" — 개발자만) |
| 통과 | 5명 중 4명이 U1을 15분 안에, 이탈 신호 합계가 단계당 평균 1 미만, 사후 질문 정답률 ≥ 80% |
| 실패 시 | 이탈 신호가 몰린 단계의 화면 문구·순서를 고치고 **같은 5명이 아닌** 새 5명으로 재실측(달력, PLAN §6.2 G8) |

---

## 부록. 여정 ↔ 게이트 ↔ EVAL 절

| 여정 | 게이트 | 주로 기대는 EVAL 절 |
|---|---|---|
| U1 | G8 | E11, E15-12, E17-07 |
| U2 | G4·G5 | E3, E5 |
| U3 | G6 | E7 |
| U4 | G6 | E2-09, E8-06, E10 |
| U5 | G5·G7 | E6 |
| U6 | G7 | E13, E16-B |
| U7 | G6 | E9 |
| U8 | G5 | E3-08·09 |
| U9 | G6 | E7-09·10, E10-06 |
| U10 | G5 | E1, 권한 매트릭스 |
| U11 | G5 | E15-03·04, E10-07 |
| U12 | G7 | E14 |
| U13 | G3 | P1 DoD |
| U14 | G5 | E4-09, E13-12 |
| U15 | G4·G5 | E1-04·05, E2-07, E13-01 |
| U16 | G8 | E17-07 |
