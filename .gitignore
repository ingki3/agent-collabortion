# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# secrets / local config
.env
.env.*
!.env.example

# build artifacts (monorepo 스트림별 — P0-a 골격에서 갱신)
node_modules/
dist/
build/
coverage/
*.log
bin/
tmp/

# Orca / agent worktrees
.worktrees/

# Claude Code session files (wakeup lock 등)
.claude/

# P1 통합 E2E 산출물(토큰·쿠키 포함)
e2e/p1/out/

# P2 통합 E2E 산출물(토큰·쿠키·claim 탭 포함)
e2e/p2/out/

# P3 스파이크 산출물(측정 로그·세션 덤프·토큰; spike04c/out 등)
plan/spikes/*/out/

# 로그인 쿠키 잔여물(e2e·스파이크 스크립트가 만든다)
cookies*.txt
