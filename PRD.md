# PRD — Agent Collaboration Messaging Service (working title: **Colab**)

| 항목 | 내용 |
|---|---|
| 문서 버전 | **v0.19.7 (Draft)** — `[FOLDERS]` **Director 승인 2026-09-26(D1~D8)**: 작업 폴더를 **방 → 미션 → 에이전트**로(FR-6.1 바인딩·폴더 배치·공유 규칙, FR-6.4 준비·GC, §8.4 `<folders>` 블록; 경로 슬러그는 한글 보존 PathSlug, 새 체크아웃 브랜치 `colab/<Slug(방)>-<room_id 앞 8자리>/<Slug(에이전트)>`, **D8 B — 미션이 닫혀도 바로 지우지 않고 `last_used_at + workdir_retention_days` 뒤 GC**; 계약 daemon-protocol v0.10.0 · harness v0.9.7 · openapi v0.3.4, 근거 `plan/research/MISSION_FOLDERS.md`). **v0.19.6** — FR-3.1.3 표 8행 보완: **보고에 대한 보고는 없다** — 턴을 깨운 메시지가 이미 보고였고 그 보고를 받은 쪽이 말하면 그 턴의 말은 요청(Director 승인 2026-09-25, T-AGENTFIX B6). **v0.19.5** — FR-2A.2.1 **완료 승인은 일이 멈춘 뒤에** — 종료 조건에서 Director 승인만 남아도 그 일에 실행·대기 중인 task 가 있으면 승인 요청을 보류하고, 마지막 task 가 끝날 때 연다. FR-2A.2.2 **열린 완료 승인은 한 일에 하나** — 판정은 「무엇이 바뀌었는가」로 하고, 지정되지 않은 제출은 조건도 승인 요청도 건드리지 않는다(Director 요청 2026-09-25, 실사용 실측 — 여섯 건 발행·다섯 동시 open). v0.19.4 — FR-2.1.2 **방 이름·설명 바꾸기**(방 머리에서 바로 · 방 설정 · 방 카드 메뉴, 시스템 메시지로 남긴다, Director 요청 2026-09-25). v0.19.3 — FR-3.1.3 **타임라인은 대화다 — 누가 → 누구에게 · 무엇을(지시·위임·보고·질문·답)**, 받는 쪽·말의 종류는 **서버가 쓰는 순간 판정해 내려준다**(openapi v0.3.2 D24 `Message.speech`·`addressees`, Lead 판정 2026-09-25; Director 요청 2026-09-25). v0.19.2 — FR-3.1.2 **에이전트 메시지의 세 층(대화 · 작업 내용 · 작업 과정)**, 기본은 대화만 보이고 작업은 펼침(Director 요청 2026-09-25, STO 실사용 테스트). v0.19.1 — FR-3.3 lane 해소 규칙 1 대상 한정. **v0.19** — **구조 전환: 세션 → 방(Room) + 일(Work)**(Director 요청 2026-09-22). §3.1 이 새 뼈대이고 §5 머리의 대응표가 나머지 FR 을 읽는 법을 정한다. v0.18 — v1.1 첫 라운드: FR-1.9.1 역할별 행동 부분집합(K-19, 표), FR-7.2 빈 턴 판정·기록(K-18), §11 관찰 표의 op(`getWorkspaceObservations`). v0.17 — OASIS 리뷰 반영(Director 확정 2026-09-15): §11 별도 「관찰」 표 5행(트리거 사슬 규모·깊이·합류 폭·라우팅 집중·빈 턴), FR-7.2 빈 턴 카드, §12 라우팅의 구조적 집중; C5 역할별 행동 부분집합은 v1.1. v0.16 — 스파이크 5(PR #153) 판정: §8.4 M3 표를 "추적 여부 무관, 별도 미추적 `COLAB_BRIEF.md`" 한 행으로 바꾸고 `skip-worktree` 안 폐기, 턴 프롬프트 첫 줄 브리프 경로 지시 고정, §12 위험 표 정정. v0.15는 K-5 결정: FR-3.3 규칙 8의 억제 기간과 합류 뒤 위임자 기상 한도(FR-3.4 병합)를 명시. v0.14는 P2a 골든 테이블 작성에서 드러난 공백 2건: FR-3.5에 **재개 시 루프 카운터** 규칙과 상한 종류 구분 요구 추가, FR-3.3 lane 해소 "3단계" 오기를 4단계로 정정. v0.13은 G1 판정 반영: CLI 어댑터 v1 제외 확정, 어댑터 패키지 개명, `rate_limited` 분류, Hermes 유실 감지 규칙. v0.12는 §7 agent.status. 이전 버전은 `prd/` |
| 작성일 | 2026-09-03 |
| 파생 문서 | **`SCREEN.md`** (화면 설계 SSOT) — 이 PRD를 화면으로 옮기며 드러난 공백이 v0.9 변경 요약에 반영되어 있다. **`PLAN.md`** (개발 계획 SSOT, 이전 버전·리뷰는 `plan/`) — §10·§12를 주차로 펼치며 드러난 것이 v0.11에 반영되어 있다 |
| 리뷰 이력 | `PRD_REVIEW_01` → v0.5 · `PRD_REVIEW_02` → v0.6 · `PRD_REVIEW_03` → v0.7 · `PRD_REVIEW_04` (`blocked` 경로 공백 6 / 소소한 것 8) → v0.8. **전건 반영, 반대 항목 없음** |
| 위치 | **이 파일이 SSOT다.** 버전별 원본과 리뷰 문서는 `prd/`에 보관한다 (`prd/PRD_05.md`가 이 문서의 원본) |
| 대조 문서 | `prd/PRD_00_other-agent.md` — 같은 요구사항으로 작성된 다른 설계안. 큰 결정은 대체로 수렴하며, 차이와 판단은 §12 "검토했으나 v1에 넣지 않은 것" |
| 참고 프로젝트 | [multica-ai/multica](https://github.com/multica-ai/multica) (이슈 기반 협업, CLI 런타임 데몬), [block/buzz](https://github.com/block/buzz) (ACP 하네스, 활동 피드, 승인 게이트), Orca (병렬 워크트리) |
| 실행 스택 | **ACP 1차 + CLI 폴백** (Claude Code, Hermes, Antigravity) + 플랫폼 내부 기능은 Anthropic Claude API |

### v0.19 변경 요약 (구조 전환 — 방과 일, Director 요청 2026-09-22)

**무엇이 바뀌나.** 협업의 단위가 **세션(하나의 goal)** 에서 **방(Room, 지속되는 대화 공간)** 으로 바뀐다. 방은 이름만으로 만들어지고(마법사 없음), 사람과 에이전트를 아무 때나 초대하며, 그 안에서 **여러 개의 일(Work)** 이 시작되고 끝난다. 방의 히스토리·결정·산출물은 **그 방의 모든 에이전트가 같은 것을 본다.** 한 에이전트는 여러 방에 참여하고, 방마다 자기 맥락(런타임 세션·작업 폴더)을 따로 갖는다. 다른 방의 맥락이 필요하면 **요청해서 읽는다**(FR-4.5, 권한은 사람 originator 기준).

| 바뀌는 것 | 전(v0.18) | 후(v0.19) |
|---|---|---|
| 협업 단위 | 세션 1개 = goal 1개, 끝나면 닫힌다 | **방**은 지속되고, 그 안의 **일**이 goal·종료 조건을 갖는다 |
| 만들기 | 7단계 마법사(격리·런타임·참여자·종료 조건·한도·autonomy) | **이름 한 칸.** 나머지는 방 설정의 기본값(워크스페이스 상속), 필요할 때 바꾼다 |
| 참여 | 세션 생성 시 참여자 확정 | 방에 **언제든 초대·퇴장**(사람·에이전트 모두) |
| 종료 조건·Director | 세션에 붙는다 | **일**에 붙는다. 방에는 **방장**(멤버·설정)만 있다 |
| 맥락 | 세션 안에서 공유, 다른 세션은 생성 시 첨부만 | 방 안에서 공유 + **실행 중에 다른 방을 읽는다**(FR-4.5) |
| 에이전트 | 세션마다 참여 | 여러 방에 동시 참여, **방마다 자기 맥락** |

**왜.** v1 의 마법사는 "일 하나"를 전제로 만들어졌는데, 실제로 사람은 **한 팀과 계속 이야기하면서 그때그때 일을 시킨다**(Director 실사용). 세션 모델에서는 이어지는 대화가 매번 새 세션이 되어 맥락이 끊기고, 같은 팀을 매번 다시 모아야 했다.

**전환 원칙**
1. **방은 완료되지 않는다.** 보관(archive)·삭제만 있다. 완료·요약·승인은 일의 것이다.
2. **모든 메시지가 일이 되지는 않는다.** 멘션은 방 안에서 task 를 만들지만, 종료 조건을 걸고 추적할 때만 **일**을 연다(FR-2A).
3. **기존 세션은 "일 하나를 가진 방"으로 이관한다**(§10 이관).
4. 라우팅(FR-3)·실행(FR-6·7)·HITL(FR-5)의 규칙은 **그대로다**. 바뀌는 것은 그 규칙이 매달리는 단위다.

### v0.17 변경 요약 (OASIS 리뷰 반영 — Director 확정 2026-09-15)

**Director 확정(2026-09-15)**: ① 관찰 지표(C1~C3)는 §11 의 G9 지표 10개와 **별도 「관찰」 표**로 두고 목표치를 걸지 않는다(대시보드도 별도 표, G9 뒤 구현). ② 역할별 행동 부분집합(C5)은 **v1.1**. 근거와 대응표·후보 전체는 `plan/research/OASIS_REVIEW.md`(T-R1). 본문의 해당 자리에 `[v0.17]` 꼬리표로 문장을 **덧붙였고** 기존 문장은 바꾸지 않았다. 셋 다 **관찰만** 이며 §11 의 G9 지표 10개와 계약(`getWorkspaceMetrics`)은 건드리지 않는다.

| # | 제안 | 근거(OASIS) | 반영 위치 |
|---|---|---|---|
| C1 | 세션 트리거 사슬 관찰 지표 3개(규모·깊이·폭, 목표치 없음) — FR-3.5 상한(8·60·5)이 맞는지 **분포**로 본다 | 정보 확산 지표 scale·depth·max breadth(논문 §4.1) | §11 |
| C2 | 라우팅의 구조적 집중(규칙 6·7 폴백 비율, 에이전트별 트리거 점유율) 관찰 + 리스크 행 | RecSys 절제 실험 — 노출 규칙이 집단 결과를 좌우한다(논문 §5) | §11, §12 |
| C3 | 빈 턴(메시지·플랫폼 조작·편집 0 으로 끝난 턴)도 렌더하고 비율을 관찰 | `do_nothing` 이 1급 행동으로 trace 에 남는다 | FR-7.2, §11 |

### v0.2 변경 요약

| # | 결정 | 반영 위치 |
|---|---|---|
| 1 | 세션마다 사람 한 명을 **Director**로 지정. HITL 요청의 수신자. | §3, FR-2.1, FR-5 |
| 2 | Lane 격리는 **워크트리 / 컨테이너 둘 다** 지원, 세션 생성 시 선택. | FR-2.1, FR-6.4 |
| 3 | 세션 도중 **에이전트 동적 생성 금지**. 등록된 풀 안에서만 위임. | FR-1.5, §8.3 프로토콜 |
| 4 | 이전 세션 요약의 컨텍스트 재사용 길이 상한은 **설정에서 조정**. | FR-4.4, §6 Settings |
| 5 | 에이전트는 **CLI 런타임 + 모델 설정**이 기본. 런타임별 **멀티 프로파일** 지원. | FR-1.1, FR-1.6 |
| 6·7 | v1은 **CLI 런타임 데몬**으로 시작. 초기 지원 후보: Claude Code, Antigravity, Hermes (v0.6에서 v1은 2종으로 축소). | §2, §8, §10 |

### v0.3 변경 요약

| 결정 | 반영 위치 |
|---|---|
| 세 런타임의 헤드리스 실행 방식·resume·출력 파싱·MCP·지시 파일을 multica 어댑터 코드와 공식 문서로 확인해 확정. | §8.2, §12, 부록 C |

### v0.4 변경 요약 (block/buzz 분석 반영)

| # | 결정 | 반영 위치 |
|---|---|---|
| A | **ACP(Agent Client Protocol)를 1차 실행 경로로 채택.** 런타임별 플래그 조립은 ACP 어댑터가 없는 런타임(Antigravity)의 폴백으로 강등. | §8.2 전면 재작성, §8.1, §10 |
| B | **턴 진행 중 새 메시지 처리 정책** 추가: `steer`(기본) / `queue` / `interrupt` / `director-interrupt`. | FR-3.4 **(v0.5·v0.6에서 변경)** |
| C | **활동 피드(Activity Feed) 설계** 도입: 동사·목적어·결과 문장, 렌더 클래스, 제자리 갱신, 침묵도 렌더. | FR-7.2 |
| D | **승인 토큰 모델**: HITL 요청은 토큰 해시만 저장하고 서명 응답으로 해소, 재개 지점 명시, 기본 기한 24h, 후속 분기. | FR-5.4 **(v0.5에서 제거)** |
| E | **`.agent.md` 이식 가능 정의 파일** + 정의(템플릿)/인스턴스 분리. | FR-1.7, FR-1.8 |
| F | 부수 채택: 호출 권한 게이트 + 형제 에이전트, 빈 확인 메시지 금지 규칙, 메모리 조회 실패와 부재 구분, 고빈도 이벤트 비영속 분리. | FR-1.1, §8.3, FR-4.2, §7 |

### v0.5 변경 요약 (리뷰 #01 반영)

**설계 결정을 바꾼 것 (Critical)**

| # | 이전 (v0.4) | 변경 | 위치 |
|---|---|---|---|
| C1 | HITL이 run을 "일시 정지"하고 `resume_at`(다음 단계)부터 재개 | **HITL 툴 호출 = 턴 종료.** 재개는 새 attempt이며 런타임 세션 resume 우선, 실패 시 콜드 스타트. `resume_at` → `runtime_session_ref`. 대기 중 동시성 슬롯을 점유하지 않는다 | FR-5.4, FR-7.1, §7 |
| C2 | `steer`(기본) = 취소 후 재프롬프트. 런타임 `steering` 능력에 의존 | **정책명 `restart-with-context`로 변경, 기본값은 `queue`.** 취소는 **명시 멘션에만** 적용(작성자 무관), Director의 중단은 UI 버튼. `steering` 능력 제거 | FR-3.4, §8.2.1 **(v0.6에서 정책 자체를 삭제)** |
| C3 | lane = 위임마다 하나. 작업 공간도 lane마다 | **lane(동시성 단위)과 workdir(작업 공간)를 분리.** workdir 바인딩은 격리 방식이 정한다: `worktree`는 에이전트당 1개 공유, `container`·`none`은 lane당 1개 | FR-6.1, FR-6.4, §7 |
| C4 | `runtime_id` 비우면 자동 선택 | **세션에 `runtime_id` 고정.** `worktree`·`container`면 필수. 프로파일 폴백은 같은 머신 안에서만 | FR-1.6, FR-2.1, §7 |
| C5 | `respond_to: owner` + 형제 규칙이 기본 | **세션 참여 = 세션 내 트리거 허용.** `respond_to`는 세션 **초대** 권한과 세션 밖 멘션만 통제 | FR-1.9 |
| C6 | v1에 컨테이너·Antigravity·`.agent.md`·Build with AI 포함 | 모두 **v1.1로 이동** | §10 |

**해소한 규칙 충돌·누락 (Major)** — M1 합류 1회로 통일, M2 시스템 발행 HITL 스키마, M3 예산·시간을 종료 조건에서 limits로 이동, M4 승인 토큰 제거(인증 세션 + 멱등 레코드), M5 재시도의 부분 실행 처리, M6 브리프 전달 경로 단일화 + 워크트리 오염 방지, M7 보안 문구 정정, M8 루프 상한을 체인 깊이·시간당 빈도로, M9 예산 강제 시점, M10 `pending_approval` 스키마 선반영, M11 ACP 툴 차단 스파이크, M12 `proposed_default` 추가.

**리뷰가 지적하지 않았으나 함께 고친 것** — 작업 공간 GC 정책 부재(FR-6.4), 지연 폴백(규칙 7)과 턴 중 메시지 정책의 상호작용 미정의(FR-3.3).

**문서 정합성** — m1~m15 전건 반영(FR-2 헤더 복원, 오픈 이슈 재번호, 목표·지표 수치 통일, 스키마 필드 보강 등).

### v0.6 변경 요약 (리뷰 #02 반영)

리뷰 #02가 지적한 신규 충돌 4건은 **v0.5가 리뷰 #01을 고치면서 만든 것**이다. 규칙을 한 곳에서 바꾸고 그 규칙에 의존하는 다른 곳을 놓친 패턴이 반복됐으므로, 이번에는 각 수정마다 영향받는 위치를 문서에 함께 적었다.

**v0.5에서 생긴 충돌 해소**

| # | 문제 | 조치 | 위치 |
|---|---|---|---|
| N1 | 합류를 1회로 통일했으나 §8.3 규약이 "위임자에게 회신 멘션"을 계속 지시해 무효화 | 규약을 "완료 보고는 상태 갱신 + 아티팩트로, 위임자를 멘션하지 않는다"로 교체. **서버가 자식→위임자 멘션을 트리거하지 않고 합류 묶음에 싣는다.** 합류 그룹 = 같은 `delegated_from_task_id`에서 나온 자식 집합 | §8.3, FR-3.3, FR-6.5 |
| N2 | 취소 판별자를 "명시 멘션"으로 바꿨더니 에이전트·일반 멤버의 멘션도 남의 턴을 죽임 | **`mid_turn_policy`를 삭제하고 취소를 UI 버튼 전용으로.** 어떤 메시지도 진행 중인 턴을 취소하지 않는다 (리뷰 원안 대신 수정안, 아래 설명) | FR-3.4, FR-2.1, §6, §7 |
| N3 | 트리거가 어느 lane에 붙는지 규칙 없음 | **격리 방식과 무관한 4단계 lane 해소 규칙** 추가 (리뷰 원안 대신 수정안) | FR-3.3 |
| N4 | 상태 머신이 `waiting_human ↔ running` 양방향이라 "프로세스는 이미 종료" 서술과 모순 | `running → waiting_human → queued` 단방향. HITL 호출은 `pending_hitl` 플래그만 세우고 `turn_end`에 전이. task당 열린 HITL 1개 | FR-7.1, FR-5.4 |

**Major 11건** — M1 작업 디렉토리를 **Workdir**로 개명(용어 충돌), M2 Claude Code CLI 어댑터를 v1 조건부 항목으로 명시, M3 `.git/info/exclude`는 추적 파일에 무효 → `skip-worktree` + 마커 구간만 복원, M4 GC 조건에 "작업 트리 클린" 추가, M5 중단 버튼 스펙(대상 lane·입력), M6 서버측 억제 휴리스틱 **제거**(수정안), M7 승인은 자동 진행 금지 + deputy 승인 권한 + `overdue` 플래그, M8 `nobody`의 실행 중·대기 task 처리, M9 예산 초과를 `paused(budget)` 재개 가능 상태로, M10 첫 dispatch 시 `runtime_id` 고정, M11 데몬 크래시 시 고아 프로세스 정리.

**두 리뷰가 모두 놓친 것** — 세션이 묶인 런타임이 영구히 사라지면 복구 경로가 없었다. 런타임 재바인딩·세션 포기 정책을 추가했다(FR-9).

**리뷰 원안 대신 수정안을 채택한 3건**

- **N2**: 리뷰안은 "명시 멘션 **AND** 작성자가 Director"였다. 조건을 셋(정책 설정 + 명시 멘션 + 작성자)이나 겹치느니 **`mid_turn_policy`를 없애고 취소를 UI 버튼 하나로** 모았다. 이미 "지금 중단하고 다시 지시" 버튼이 있고 그것이 Director의 의도를 가장 분명하게 표현한다. 설정 축이 하나 줄고 "어떤 메시지도 진행 중인 작업을 죽이지 않는다"는 불변식이 생긴다.
- **N3**: 리뷰안 3번은 "격리가 `worktree`면 그 에이전트의 유일 lane"인데, worktree가 에이전트당 하나인 것은 **workdir**이지 lane이 아니다(C3). 격리에 의존하지 않는 규칙으로 바꿨다.
- **M6**: 리뷰안의 억제 기준(멘션 제거 후 80자 미만 + `?`·URL·코드·숫자 없음)은 `@QA 리뷰 부탁해` 같은 **정상 위임을 억제한다**. v0.5의 기준("진행·결과·질문 신호 없음")도 같은 결함이 있었다. 길이가 아니라 "요청을 하는가"가 판별자여야 하는데 휴리스틱으로는 어렵다. 억제를 빼고 `max_pair_roundtrips`로 핑퐁을 잡는다 — 오탐으로 일을 잃는 것보다 몇 턴 낭비가 낫다.

### v0.7 변경 요약 (리뷰 #03 반영)

v0.6이 "규칙을 줄여 충돌을 없앤" 결과, 이번에는 **줄인 규칙이 덮지 못하는 경로**가 드러났다. 합류를 1회로 묶으면서 자식이 위임자에게 물어볼 길이 막혔고(A), 취소를 UI로 옮기면서 재지시된 task의 정체성이 흐려졌다(B). 공백을 메우되 규칙 수는 늘리지 않는 방향으로 고쳤다.

**설계 공백 7건**

| # | 문제 | 조치 | 위치 |
|---|---|---|---|
| A | 규칙 8이 자식→위임자 멘션을 **전부** 억제해, 작업 중 질문("API 응답 형식을 JSON으로 할까요?")이 위임자에게 닿지 않음. 서버는 완료 보고와 질문을 구분할 수 없다 | **lane 상태에 `blocked` 추가**하고 합류 판정에서 **종료 상태로 취급**. 자식은 `colab status set blocked --note "<질문>"`으로 턴을 끝내고, 위임자는 합류 묶음에서 질문을 받아 **스레드 답글**로 그 lane을 재진입시킨다. 규칙 8의 범위도 "위임자만 억제, 함께 멘션된 제3자는 트리거"로 명시 | FR-6.1, FR-6.2, FR-6.5, FR-3.3 |
| B | "중단하고 다시 지시"가 같은 task의 `attempt`만 올려, 재시도(같은 지시)와 재지시(다른 지시)가 구분되지 않음 | 재지시는 **새 task**를 만들고 `restarted_from_task_id`로 잇는다. `runtime_session_ref`만 물려받는다. `attempt`는 같은 트리거의 재시도 전용 | FR-3.4, FR-7.1, §7 |
| C | 큐 병합 단위가 "에이전트"라 lane 3개를 가진 Researcher의 서로 다른 lane 메시지가 하나로 합쳐짐 | 병합 단위를 **lane**으로 교정. lane 해소 규칙이 먼저 lane을 정하고 그 다음 병합 | FR-3.4 |
| D | **deputy가 v1 기능 5곳에서 쓰이는데 로드맵은 v1.1**. 승인 자동 진행 금지(M7)와 겹치면 v1에서 Director 부재 시 세션이 완료 불가 | deputy를 **v1으로 확정**. 이미 v1 기능이 참조하고 있었으므로 로드맵 오류를 바로잡는 것이다 | §10, §12 |
| E | 재진입한 lane이 끝났을 때 누구에게 알리는지 규칙 없음(시나리오 B 5단계) | **합류는 그룹당 한 번만 발화**하고, 재진입 lane의 완료는 **재진입을 일으킨 메시지의 작성자**에게 회신 멘션으로 알린다. 작성자가 위임자면 규칙 8에 걸리므로 시스템 메시지로 깨운다 | FR-6.5 |
| F | 런타임 재바인딩 시 "진행 중이던 lane만 콜드 스타트"라고 했으나, worktree 격리에서는 **완료된 lane의 코드도** 죽은 머신에만 있다 | 복구 프롬프트에 "이 세션의 diff 아티팩트를 순서대로 적용한 뒤 이어가라"를 넣는다. "같은 저장소" 판정은 경로 문자열이 아니라 **remote URL**로 하고 probe 보고 항목에 추가 | FR-9, FR-9.2 |
| G | `lane delegate`(새 lane)와 최상위 멘션(최근 lane 재사용)의 차이를 Lead가 모름 | §8.3에 사용 구분을 명시 | §8.3 |

**정합성 14건** — s-1·s-2는 **새 규칙과 정반대를 말하던 문장**이라 우선 교체했다(부록 B의 "취소는 명시 멘션에만", §12의 "서버측 빈 확인 메시지 억제"). Workdir 개명 잔재 3곳(FR-6.1 제목·표 헤더, FR-6.3), 상태 열거값 누락(`task.status`의 `paused`, `lane.status` 전체, `runtime_offline_grace`, probe 저장소 정보), `expired` → `auto_answered` 개명 등 나머지도 반영했다.

**리뷰 원안 대신 수정안을 채택한 1건 — 일정 컷 순서**

리뷰는 컷 3순위로 **Hermes를 v1.1로** 미루자고 했다. 반대한다. ACP 하네스를 만드는 이유가 "런타임 추가 비용을 프리셋 한 줄로 낮춘다"인데, **런타임이 하나뿐이면 그 가치를 검증할 수 없고** 두 번째를 붙일 때 드러났을 추상화 누수를 v1.1까지 모른다. Hermes는 ACP 네이티브라 어댑터 비용도 가장 낮다 — 아끼는 것에 비해 잃는 정보가 크다.

대신 3순위를 **`worktree` 격리 → v1.1**로 바꿨다. 이걸 자르면 GC(FR-6.4)·`skip-worktree` 처리(§8.4)·재바인딩 복구(F)·미병합 커밋 판정이 통째로 빠져 실제 공수 절감이 훨씬 크다. 코드 작업 시나리오는 뒤로 밀리지만 목표 2·3·4(공유 컨텍스트·HITL·병렬)는 `none` 격리로 전부 검증된다. 자세한 순서는 §10.

### v0.8 변경 요약 (리뷰 #04 반영)

v0.7은 `blocked` 상태를 **추가**했지만 그것이 기존 규칙들과 만나는 지점을 따라가지 않았다. 리뷰 #04가 그 경로를 실제로 걸어보니 세 곳이 끊겨 있었다 — 질문이 가장 느린 형제를 기다리고, 답글을 달 스레드가 없고, 위임자 없는 lane은 갈 곳이 없다. **전건 반영했고 반대 항목은 없다.**

세 수정(1·4·6)이 서로 맞물려 **규칙이 오히려 줄었다**: `blocked` 시 즉시 시스템 메시지가 위임자를 깨우고 → 그 메시지가 스레드 루트가 되어 답글 대상을 만들고 → lane에 있는 `runtime_session_ref` 덕에 그 답글이 resume을 타고 → 합류 후에는 억제가 풀려 일반 멘션으로 돌아온다. E 표는 3행에서 2행으로 줄었다.

| # | 문제 | 조치 | 위치 |
|---|---|---|---|
| 1 | 질문이 **가장 느린 형제를 기다린다.** Backend가 2분 만에 물어도 Frontend가 40분 걸리면 Lead는 40분 뒤에 답한다 | `blocked`는 상태 변경이라 완료 보고와 **구조적으로 구분**되므로 규칙 8의 억제 근거가 적용되지 않는다. **합류와 별개로 즉시 시스템 메시지로 위임자를 깨운다.** 합류 1회 불변식은 유지 | FR-6.2.1, FR-6.5 |
| 2 | Lead가 **답글 달 곳이 없다.** `status set blocked`는 상태 갱신이지 메시지가 아니라 스레드 루트가 없다 | `status set blocked`가 lane 스레드에 **카드를 메시지로 게시**한다. `lane.blocked_note`는 캐시일 뿐이고 이력은 메시지가 갖는다 | FR-6.2.1, FR-7.4, §7 |
| 3 | **위임자 없는 lane**(Director가 직접 만든)의 `blocked`는 깨울 대상이 없다 | Director 인박스로 보낸다. 브리프에도 명시 | FR-6.2.1, FR-8 |
| 4 | **재진입마다 콜드 스타트.** `runtime_session_ref`가 task에 있는데 재진입은 전부 새 task라, ref를 물려받는 규칙이 재지시(B)에만 있었다 | **`runtime_session_ref`를 lane으로 옮긴다.** 런타임 세션은 "한 에이전트의 한 작업 흐름"이고 그것이 lane의 정의다. 재지시의 "ref만 물려받는다"도 이 규칙의 결과가 된다 | §7, FR-5.4, FR-7.1 |
| 5 | 재진입 완료 통보가 **프롬프트 규칙인지 서버 동작인지** 없었다 | 합류처럼 **서버가 한다.** 프롬프트에 의존하지 않는다 | FR-6.5 |
| 6 | 규칙 8이 자식→위임자 멘션을 **영구히** 억제해, 합류 후 재진입한 자식도 위임자를 못 깨운다 | 억제 범위를 **"합류 그룹이 발화하기 전까지"** 로 좁힌다. 합류 후에는 일반 멘션 | FR-3.3 규칙 8, FR-6.5 |

**소소한 것 8건** — t-1 `blocked`도 동시성 계산에서 제외, t-2 Director가 새 lane을 만드는 수단(`/lane` 접두), t-3 deputy의 취소·승인 권한 시점 통일, t-4 종료 조건 "3종"을 실제로 열거, t-5 Director 교체 주체 명시, t-6 컷 1의 클래스 표기 정정, t-7 v0.5 요약의 마지막 `workspace` 잔재, t-8 합류 프롬프트에 "답을 기다리는 자식이 있으면 먼저 답하라".

**함께 명시한 것** — 1번(즉시 알림)을 넣으면 "`blocked`를 합류 판정에서 종료로 취급"하는 이유가 약해 보이지만, **유지해야 한다.** 종료로 치지 않으면 질문한 자식 때문에 나머지 형제의 합류가 영원히 막힌다. 즉시 알림은 "질문을 빨리 전달"하는 것이고, 종료 취급은 "형제들의 합류를 막지 않는" 것이라 목적이 다르다.

### v0.9 변경 요약 (화면 설계 리뷰 반영)

`SCREEN.md` 초판(`screen/SCREEN_01.md`)을 쓰면서 **PRD를 화면으로 옮길 수 없는 지점** 7건이 드러났다. 리뷰(`screen/SCREEN_REVIEW_01.md` §5)가 정리한 것을 반영한다. 대부분 "정의가 없어서 화면 문서가 임의로 만들었고, 그것이 PRD 규칙과 충돌한" 경우다.

| # | 공백 | 조치 | 위치 |
|---|---|---|---|
| P1 | 결정 기록이 v1인지 v1.1인지 모순 (본문은 v1, 로드맵은 v1.1) | **기록·표시는 v1, 전용 UI만 v1.1.** 지나간 세션의 결정은 나중에 복원할 수 없다 | §10 |
| P2 | 테스트 채팅이 §6에만 있고 로드맵·정의에 없음 | **FR-1.8.1 신설.** 세션이 아니며 `colab` 토큰을 발급하지 않는다(되돌아올 대상이 없으므로) | FR-1.8.1, §10 |
| P3 | **루프 상한이 로드맵 어디에도 없었다** | v1 라우팅 항목에 복원. 핑퐁 방지는 v1 필수 | §10 |
| P4 | 컨텍스트 재사용의 v1/v1.1 경계 불명확 | 첨부와 상한 설정은 v1, **주입 내용 미리보기·취사선택 UI**가 v1.1 | FR-4.4 |
| P5 | task에는 `paused`가 있으나 lane에는 없어 lane 보드가 그릴 수 없었다 | **lane 상태에 `paused` 추가.** 예산 초과는 실패가 아니라 사람의 판단 대기 | FR-6.2, §7 |
| P6 | §6 UX 표가 v1/v1.1 구분 없이 나열해 화면 문서가 오해 | Agents·격리·마법사 행에 버전 표기 | §6 |
| P7 | **사람의 세션 역할이 정의되지 않았다.** FR-5.3 한 문장뿐이라 화면 문서가 `participant`/`observer`를 만들었고, PRD가 허용한 게시를 금지하는 결과가 됐다 | **FR-5.3 재작성.** 사람은 Director·deputy·워크스페이스 멤버 셋. 멤버는 열람·게시 가능, 비공개 세션은 v1.1. `participant`는 에이전트 전용 용어 | FR-5.3 |

**교훈으로 남길 것** — P7은 "PRD가 말하지 않은 것을 파생 문서가 채우면, 그 채움이 PRD 규칙을 위반해도 아무도 모른다"는 사례다. 앞으로 파생 문서가 새 개념을 도입하면 **PRD에 없는 개념임을 명시**하고 되돌려 확인받는다. `screen/SCREEN_01.md` §8.2가 그 역할을 했다.

### v0.13 변경 요약 (G1 판정 반영 — `plan/G1_DECISION.md`)

| # | 문제 | 조치 | 위치 |
|---|---|---|---|
| G1-1 | 조건부 항목 "Claude Code CLI 어댑터" 미결 | 스파이크 1 통과 → **v1 제외 확정**, 폴백 스펙은 문서 유지 | §10 |
| G1-2 | 어댑터 패키지 `@zed-industries/claude-code-acp`가 2026-02 이후 동결 | `@agentclientprotocol/claude-agent-acp@0.74.0`으로 개명·고정 | §8.2.3, §12 |
| G1-3 | 계정 사용 한도 오류가 재시도 분류에 없음 | `rate_limited` — 리셋 시각까지 재큐잉(`task.not_before`) | FR-7.1 |
| G1-4 | Hermes 유실 감지 규칙 | `session/load` null 또는 provenance 불일치가 1차, refusal 규칙은 보조 | §8.2.5, §12 |
| G1-5 | 어댑터가 사용자 전역 설정(MCP·hooks)을 세션에 싣는다 | 하네스 계약에서 `settingSources` 제한 + `strictMcpConfig` — `contracts/harness.md` | §8.2 |

### v0.12 변경 요약 (스키마 v0 구현 반영)

| # | 문제 | 조치 | 위치 |
|---|---|---|---|
| S1′ | §7 agent 표에 `status` 컬럼이 있었으나 FR-1.3(v0.10)은 "저장하지 않고 파생·계산"으로 확정 | §7에서 컬럼 제거, ENUM 값 집합만 유지. `error` 파생에 필요한 실패 분류는 `task.failure_kind`(auth·quota·config·network·runtime_offline·stall·timeout·cancelled·other)로 | §7 |

### v0.11 변경 요약 (개발 계획 리뷰 #01 반영)

§10 로드맵과 §12 스파이크를 주차별 계획(`PLAN.md`)으로 펼치면서 드러난 것 둘이다.

| # | 문제 | 조치 | 위치 |
|---|---|---|---|
| P1′ | 컷 2순위의 절감 열에 "종료 조건 판정"이 있었으나, 판정은 결정적 로직이고 시나리오 A 8단계(`artifact_submitted` → `user_approval` → `completed`)가 요구하므로 자를 수 없다 | 절감을 **요약 생성만**으로 축소. 발동 우선순위 근거 명시 | §10 컷 표 |
| P2′ | 스파이크 4(HITL 재개)를 1주차에 두었으나 콜드 스타트 검증은 lane `runtime_session_ref`와 재개 프롬프트가 있어야 실제 조건이 된다. 스파이크 5(워크트리 지시 파일)는 worktree 종속이라 컷 3 발동 시 불필요 | 4를 **4a(1주차, 런타임 능력)·4c(HITL 착수 전, 콜드 스타트)**로 분리. 5를 **worktree 착수 전**으로 | §12 스파이크 |

### v0.10 변경 요약 (디자인 리뷰 #01·#02 반영)

와이어프레임을 그리고 리뷰하면서 드러난 것들이다. 리뷰 #01은 FR-1.3에 문장 하나를 추가하게 했고, 리뷰 #02는 그 문장이 **기존 FR-5.2와 모순**임을 잡아냈다.

| # | 문제 | 조치 | 위치 |
|---|---|---|---|
| C1′ | FR-1.3 "에이전트 상태는 lane을 집계하지 않는다"(리뷰 #01 반영)와 FR-5.2 "task가 waiting_human이면 에이전트도 waiting_human"이 충돌 | **에이전트 상태를 task 상태에서 파생되는 계산값으로 정의.** 우선순위 표 6단계. `blocked`·`paused`는 파생에 안 넣고, `error`는 "실행 불가"(인증·쿼터·설정)로 좁혀 task 실패와 분리 | FR-1.3, FR-5.2 |
| C2′ | task 예산 초과 승인이 에이전트의 `budget_per_task`를 바꾸는지 불명 | **그 task에만** 적용. `task.budget_override` 필드 신설 | FR-7.3, §7 |
| C3′ | 세션 `paused` 중 `queued` task 처리 미정 | dispatch하지 않고 재개 시 순서대로 | FR-2.3 |
| C4′ | 마법사에서 런타임(3단계)이 격리(5단계)보다 먼저라, 격리가 런타임 후보를 제한하는데 나중에 고름 | **격리 → 런타임 → 참여자** 순으로 변경. 와이어프레임 S6-3의 "5단계에서 다시 골라야 함" 주의문이 순서 오류의 증거였다 | §6 |

디자인 리뷰 #01에서 반영한 FR-1.3 문장(v0.9 사후 수정, 변경 요약 누락 — 리뷰 #02 m10 지적)도 이 표의 C1′에 흡수됐다.

---

## 1. 개요

### 1.1 한 줄 정의

**복수의 AI 에이전트를 등록하고 역할을 부여한 뒤, 하나의 공유 컨텍스트(세션) 안에서 서로 채팅·멘션하며 사용자가 준 goal을 병렬로 수행하는 메시징 서비스.** 에이전트의 실제 실행은 사용자 머신의 CLI 런타임(Claude Code 등)이 담당한다.

### 1.2 문제

- 여러 에이전트(리서처, 기획자, 개발자, 리뷰어…)를 쓰려면 각각 별도 터미널/채팅창을 열고 같은 컨텍스트를 반복 설명해야 한다.
- 에이전트 간 결과물 전달은 사람이 복사·붙여넣기로 중계한다. 결정의 근거와 히스토리가 흩어진다.
- 에이전트가 언제 사람의 판단을 필요로 하는지(HITL) 명시적인 채널이 없다. 사용자는 항상 화면을 지켜봐야 한다.
- 작업을 병렬로 돌리고 싶어도 에이전트끼리 서로의 진행 상황을 모른 채 충돌한다.

### 1.3 해결

multica가 "이슈 트래커"를 협업 표면으로 삼았다면, 본 서비스는 **"메시징 세션(채널)"** 을 협업 표면으로 삼는다.

- 사용자는 **세션**을 만들고 **goal**, **종료 조건**, **Director**(결정권자)를 설정한다.
- 세션에 **에이전트**들을 초대한다. 각 에이전트는 이름·역할·instruction·런타임 프로파일을 가진다.
- 에이전트들은 세션 안에서 **채팅하고 서로 @멘션**하며 일을 나눈다. 모든 메시지는 공유 컨텍스트가 된다.
- 에이전트가 사람의 결정이 필요하면 **HITL 요청**을 올리고, Director가 인박스에서 답한다.
- 서로 독립적인 하위 작업은 **병렬 lane**으로 실행되며, 종료 조건이 충족되면 세션이 자동으로 닫힌다.

### 1.4 multica에서 가져오는 것 / 바꾸는 것

| multica | 본 서비스 | 이유 |
|---|---|---|
| 이슈(Issue)가 작업 단위, 댓글이 대화 | **세션(Session)** 이 작업 단위, **메시지/스레드**가 대화 | 목표 하나에 여러 에이전트가 자유롭게 대화하는 구조가 핵심 |
| `[@Name](mention://agent/<uuid>)` 형식의 멘션이 에이전트 트리거 | 동일 방식 채택 (`mention://agent|user|all`) | 이름 충돌 없이 결정적으로 라우팅 가능 |
| 스쿼드 리더가 "코디네이트만 하고 구현하지 않는" 프로토콜 | **Lead 역할** 에이전트에 동일 프로토콜 부여 | 검증된 위임 패턴 |
| 데몬(runtime)이 사용자 머신의 CLI를 구동, 26개 CLI 지원 | **동일한 데몬 모델**. v1은 Claude Code · Hermes 2종 (Antigravity는 v1.1) | 사용자가 이미 로그인한 CLI를 그대로 사용, 코드가 머신 밖으로 나가지 않음 |
| 에이전트 1개 = 런타임 1개 + 모델 1개 | 에이전트 1개 = **런타임 프로파일 N개** (세션/lane마다 선택) | 같은 역할을 빠른 모델/깊은 모델로 바꿔 쓰기 |
| 이슈 상태(in_progress → in_review → done)가 리뷰 게이트 | **세션 종료 조건 + Director 승인 게이트** | goal 단위 종료를 명시적으로 정의 |
| 태스크는 이슈당 순차, 스쿼드는 리더 경유 | **DAG 기반 병렬 lane** (Orca 스타일) | 독립 작업의 wall-clock 단축 |
| 인박스는 사람 전용, 에이전트가 사람을 멘션/`blocked` 로 호출 | **HITL Request** 를 1급 객체로 승격, 수신자는 **Director** | Director가 "무엇을 결정해야 하는지" 명확히 알 수 있게 |

---

## 2. 목표와 비목표

### 2.1 목표 (Goals)

1. 사용자가 데몬 설치부터 첫 세션 완료까지 15분 안에 도달할 수 있다(§11 지표와 같은 수치).
2. 에이전트 간 위임·보고가 **전부 세션 메시지로 남아** 사람이 언제든 따라잡을 수 있다.
3. 에이전트가 사람의 결정 없이 진행하면 안 되는 지점을 스스로 식별해 Director에게 HITL 요청을 올린다.
4. 독립적인 하위 작업이 병렬로 실행되어 단일 에이전트 대비 총 소요 시간이 줄어든다.
5. 세션은 사용자가 정의한 종료 조건에 따라 **자동으로 완료**되며, 결과물이 정리된 형태로 남는다.
6. 서로 다른 벤더의 CLI를 같은 세션에서 섞어 쓸 수 있다 (v1: Claude Code · Hermes, v1.1: Antigravity 추가).

### 2.2 비목표 (Non-goals, v1)

- VCS 통합(PR 자동 생성, 리뷰 코멘트 동기화) — lane이 워크트리를 쓰더라도 커밋/PR은 사람 또는 에이전트가 CLI로 직접. v2.
- 사람 간 메시징(Slack 대체) — 사람은 Director 역할(지시·승인·질문 응답)에 집중.
- 에이전트의 세션 중 동적 생성 — 금지(결정 3).
- 클라우드 호스팅 런타임 — v1은 사용자 머신의 데몬만. 클라우드 런타임은 v2.
- 모바일 앱.

---

## 3. 핵심 개념 (Glossary)

| 용어 | 정의 |
|---|---|
| **Workspace** | 사용자·에이전트·런타임·세션을 담는 최상위 단위. 멤버 권한(owner/admin/member). |
| **Runtime** | 데몬이 설치된 머신. 설치·로그인된 CLI 목록(`claude`, `antigravity`, `hermes`)을 서버에 보고하고 task를 받아 실행한다. multica의 `agent_runtime`에 대응. |
| **Runtime Profile** | 에이전트에 붙는 실행 설정 1벌: `{런타임 종류, 모델, effort/옵션, 환경변수, 인자}`. 에이전트는 프로파일을 여러 개 가질 수 있고 하나가 기본. |
| **Agent** | 이름·역할·instruction·런타임 프로파일들을 가진 등록된 AI 참여자. 워크스페이스 소속, 여러 세션에 참여 가능. |
| **Role** | 역할 태그와 역할 설명. `lead`, `researcher`, `writer`, `engineer`, `reviewer`, `custom`. `lead`는 코디네이션 프로토콜을 추가로 받는다. |
| **Room** (방) | **협업의 기본 단위(v0.19).** 지속되는 대화 공간. 참여자(사람·에이전트), 메시지 스트림, 공유 맥락, 방 설정(런타임·격리·한도 기본값)을 가진다. 이름만으로 만들어지고 완료되지 않는다 — 보관·삭제만. |
| **Work** (일 · 화면 이름 **미션**) | 방 안에서 **goal 과 종료 조건을 갖는 추적 단위**. `[V19-C]` **소유자는 사람(`director_user_id`), 실행자는 에이전트(`assignee_agent_id`)** — 맡기는 것은 위임이지 소유권 이전이 아니다(Linear 의 delegate≠assignee 와 같은 구분). 화면은 둘 다 보인다. 이전 버전의 Session 이 여기 대응한다. 한 방에 여러 개가 동시에 열릴 수 있고, 끝나면 요약이 방에 남는다. |
| **방장** | 방의 멤버·설정을 바꿀 수 있는 사람. 생성자가 기본이며 변경 가능. 워크스페이스 owner·admin 도 같은 권한. |
| **Session**(옛 용어) | v0.18 까지의 협업 단위. v0.19 부터 **방 + 일 하나**로 읽는다. 옛 문장이 "세션"이라 말할 때 어느 쪽을 가리키는지는 §5 머리의 대응표. 런타임 쪽 `runtime_session_ref`(CLI 런타임의 대화 세션)는 **다른 뜻**이며 그대로다. |
| **Director** | **일마다** 지정되는 사람 한 명(v0.19). HITL 요청의 수신자이자 `user_approval` 종료 조건의 승인자. 일을 연 사람이 기본값이며 변경 가능. 일에 속하지 않은 방 안 질문은 **방장**에게 간다. |
| **Goal** | **일**의 목표 문장 + 성공 기준(acceptance criteria). 방에는 goal 이 없다(설명 한 줄만). |
| **Completion Condition** | **일**의 종료 조건. 산출물 제출, 리뷰어 승인, Director 승인, 기준 충족의 조합. 예산·시간 상한은 종료가 아니라 `paused`이므로 여기 속하지 않는다(FR-2A.2). |
| **Message** | 세션 내 발화. 작성자는 user / agent / system. 스레드(parent) 구조 지원. |
| **Mention** | `[@표시명](mention://agent/<id>)` 형식의 링크. 에이전트 트리거의 유일한 결정적 수단. |
| **Task (Run)** | 하나의 트리거(멘션, 배정, 응답)로 생성되어 특정 런타임에 dispatch되는 에이전트 실행 단위. |
| **HITL Request** | 에이전트가 Director에게 올리는 구조화된 요청 (질문 / 선택 / 승인 / 정보 요청). 응답 전까지 해당 task는 `waiting_human`. |
| **Artifact** | 세션에서 생성된 산출물 (문서, 파일, 코드 diff, 표). 종료 조건의 대상. |
| **Shared Context** (방 맥락) | 방의 설명·첨부 자료·아티팩트·메시지 히스토리·결정 기록의 집합. **그 방의 모든 에이전트가 같은 것을 본다**(FR-4.1). |
| **Agent Context** (에이전트 맥락) | 에이전트가 **방마다** 따로 갖는 것: 런타임 대화 세션(`lane.runtime_session_ref`), 작업 폴더, 그 방에서 자기가 한 일. 방이 다르면 섞이지 않는다 — 한 에이전트가 여러 방에 있어도 A 방의 작업이 B 방 프롬프트에 자동으로 들어가지 않는다. |
| **Room Read** (다른 방 읽기) | 실행 중인 에이전트가 **요청해서** 다른 방의 요약·히스토리·산출물을 읽는 것(FR-4.5). 권한은 그 task 를 일으킨 **사람 originator** 기준이며, 읽은 사실은 양쪽 방에 남는다. |
| **Lane** (화면 이름 **서브 미션**) | 병렬 실행 트랙. 위임 하나가 lane 하나를 만들며, 같은 에이전트가 여러 lane을 가질 수 있다. 동시성·의존성(DAG)·보드 표시의 단위. **lane 은 방에 속하고, 일이 있으면 그 일에도 매인다**(v0.19). |
| **Workdir** (작업 공간) | 에이전트가 실제로 파일을 읽고 쓰는 디렉토리. lane과 **분리된 개념**이며, 격리 방식이 바인딩을 정한다 — `worktree`는 **방×에이전트** 하나를 공유하고, `container`·`none`은 **미션×에이전트** 하나(`[FOLDERS]` 제안 — 옛 규칙은 lane당 하나, FR-6.1). **Workspace(최상위 테넌트)와 혼동하지 말 것** — 문서 전체에서 작업 디렉토리는 항상 Workdir로 쓴다. |


### 3.1 구조 — 방과 일 (v0.19)

```
Workspace
 └─ Room (방)  ── 지속. 이름 + 설명 한 줄. 완료 없음(보관·삭제만)
     ├─ 참여자: 사람(멤버) + 에이전트  ← 언제든 초대·퇴장
     ├─ 방 맥락: 메시지·결정 기록·산출물·첨부        ← 방 안 모든 에이전트가 같은 것을 본다
     ├─ 방 설정: 런타임·격리·한도·autonomy(워크스페이스 기본값 상속)
     ├─ Work (일) ×N  ── goal · 종료 조건 · Director · 예산 · 상태(draft→active→…→completed)
     │    └─ Lane ×N → Task ×N
     └─ Lane ×N → Task ×N   ← 일에 속하지 않은 실행(질문·잡일)도 있다
```

**세 문장으로.** 방은 **누구와 이야기하는가**, 일은 **무엇을 끝내는가**, lane·task 는 **누가 지금 무엇을 하는가**다.

**FR 이 매달리는 단위(v0.19)**

| 규칙 | 단위 | 근거 |
|---|---|---|
| 멘션 라우팅·루프 상한(FR-3) | **방** | 대화가 방 단위라 트리거·억제도 방 안에서 판정된다. 한 사람 메시지가 여는 사슬은 방 안에서 센다. `[V19-B]` **주의**: 지금 구현은 hop 을 최근 200행 창으로 읽고 "창 안에 사람 hop 이 없으면 깊이 0"을 택했다 — 세션에서는 안전했지만 **오래 사는 방에서는 깊이 상한이 조용히 꺼진다**. 창을 시간(최근 1시간)과 사람 hop 기준으로 바꾸거나, 사람 hop 이 없으면 **깊이를 0 이 아니라 상한 근처로** 읽어야 한다(R1 필수) |
| 병렬 lane·합류(FR-6) | **방**(일이 있으면 일에도 매인다) | 위임은 대화 중에 일어난다. 일 밖 위임도 가능해야 한다 |
| 종료 조건·요약·Director 승인(FR-2A·FR-5) | **일** | 끝이 있는 것에만 끝 판정이 있다 |
| 예산·시간 상한(FR-2A.3) | **방** ∩ **일** | 둘 다 걸 수 있고, 실행은 **작은 쪽**을 따른다 |
| `queued` 사유 | **task** | 큐에 걸린 이유를 `task.queued_reason`(`room_lanes` · `agent_global` · `runtime` · `workspace`)에 남긴다 — 방 화면이 「다른 방에서 작업 중」을 말하려면 이 칸이 있어야 한다(SCREEN G-5) |
| 동시 lane 상한(FR-6.3) | **방만** | 일마다 다른 병렬도를 두면 같은 방의 두 일이 서로의 슬롯을 굶긴다. 일은 상한을 갖지 않는다 |
| 런타임 고정·격리·작업 폴더(FR-6.1·FR-9) | **방** | 한 방의 에이전트들은 같은 머신에서 같은 저장소를 본다. 방이 다르면 폴더도 다르다 |
| 맥락 공유(FR-4) | **방**(안) · **요청**(밖) | 방 안은 자동, 다른 방은 FR-4.5 로 읽는다 |

**방과 세션의 관계(이관).** v0.18 의 세션 하나는 **일 하나를 가진 방 하나**와 같다. 그래서 이관은 기계적이다(§10). 반대로 방을 세션으로 되돌릴 수는 없다 — 일이 둘 이상이면 나눌 기준이 없다.

### 3.2 화면 용어 (Director 확정 2026-09-23)

**코드·계약의 식별자와 화면의 말은 다르다.** 아래 왼쪽은 DB·openapi·CLI 가 쓰는 이름이고, 오른쪽은 사람이 보는 말이다. 화면 문구는 이 표를 자물쇠로 못박는다(`COMPONENTS.md` §8.4 — R1.5 문구 전환 라운드에서 반영).

| 코드·계약 | 화면 이름 | 왜 |
|---|---|---|
| `room` | **방** | 지속되는 대화 공간 |
| `work` | **미션** | "일"은 너무 일반적이라 `task`(할 일)와 한 글자 차이로 붙어 버린다. 미션은 **끝이 있는 목표**라는 뜻이 이름에 들어 있다 |
| `lane` (위임으로 갈라진 갈래) | **서브 미션** | **미션을 위해 쪼개인 일**. 위임 하나가 갈래 하나이고, 브리프가 그 갈래의 목표다(FR-6.1). 옛 화면 용어 「작업 줄기」를 대체한다 |
| `task` | **할 일** | 한 번의 실행(턴). 기존 용어 그대로 |
| `workdir` | **작업 폴더** | 기존 용어 그대로 |
| `artifact` · `decision` | **아티팩트** · **결정 기록** | 기존 화면 용어 그대로(실측: 화면은 「아티팩트」를 쓴다 — 「산출물」로 바꾸지 않는다) |

- **서브 미션은 미션 안에서만 그렇게 부른다.** 미션에 속하지 않은 갈래(일 밖 실행, FR-2A.1)는 같은 카드로 보이되 미션 자리에 **「미션 없음」**을 적는다.
- 방 화면 상단은 **「미션 3개 · 서브 미션 5개 · 할 일 12개」** 처럼 세 층이 한눈에 구분된다 — 이것이 이 이름을 고른 이유다.
- **문서(PRD·계약)는 `work`·`lane` 을 계속 쓴다.** 번역은 화면에서만 한다 — 식별자를 바꾸면 계약·골든·e2e 가 한 번에 흔들린다(§12.1-3 과 같은 판단).



---

## 4. 사용자 시나리오

아래 시나리오는 v0.18 문장 그대로다. v0.19 로는 **각 시나리오가 방 하나 안의 일 하나**이고, 달라지는 것은 시작 방식(마법사 → 방에서 「이걸 일로」)과 끝난 뒤(세션이 닫히는 대신 **일이 닫히고 방은 남아** 다음 일로 이어진다)뿐이다. 시나리오 E 가 새 구조가 여는 흐름이다.

### 시나리오 A — 시장 조사 보고서 (비개발)

1. 사용자가 노트북에 데몬 설치 → Claude Code, Hermes가 감지되어 런타임으로 등록.
2. 에이전트 3개 등록: `Lead`(lead, Claude Code + opus), `Researcher`(researcher, 프로파일 2개: Hermes 기본 / Claude Code 대체), `Writer`(writer, Claude Code).
3. 세션 생성. Goal: "국내 B2B SaaS 결제 시장 조사 보고서 10페이지". Director: 본인. 런타임: 노트북. 격리: `none`(v1.1부터는 컨테이너 선택 가능). 종료 조건: `Writer가 artifact 제출` AND `Director 승인`.
4. Lead가 자동 트리거 → 계획을 메시지로 올리고 `@Researcher`에게 3개 조사 항목을 병렬 위임. **lane 3개가 생기고, 격리 `none`의 작업 폴더는 미션×에이전트이므로 Researcher 폴더 하나를 세 lane이 함께 쓴다**(FR-6.1 `[FOLDERS]` — 옛 규칙은 lane마다 폴더 3개).
5. Researcher 3개 lane이 동시에 조사 → 각각 결과를 스레드에 게시. 세 lane이 모두 끝나면 **합류가 한 번** 일어나 Lead가 종합 run으로 깨어난다(FR-6.5).
6. Lead가 종합 → `@Writer` 초안 요청. Writer가 "타깃 독자가 투자자인지 내부 경영진인지"를 **HITL 질문**으로 올리고 `--default "투자자"`를 제안하며 **턴을 종료**한다 → Director 인박스.
7. Director가 "투자자" 선택 → Writer가 **새 attempt로 재개**(런타임 resume 우선) → 보고서 artifact 제출.
8. `artifact_submitted` 충족 → **플랫폼이** Director에게 `user_approval` HITL을 발행 → 승인 → 세션 `completed` → 요약 자동 게시.

### 시나리오 B — 기능 구현 (개발)

1. 에이전트: `PM`(lead), `Backend`(engineer), `Frontend`(engineer), `QA`(reviewer). 모두 Claude Code 프로파일(v1은 Claude Code + Hermes 지원).
2. 세션 생성. Goal: "회원 탈퇴 기능 구현". **런타임: 저장소가 있는 개발 머신(필수)**. 격리: **워크트리**(`~/dev/app`). 종료 조건: `QA 승인`. 예산 상한 $20(초과 시 완료가 아니라 `paused`).
3. PM이 스펙 작성 → `@Backend` `@Frontend` 병렬 위임 → lane 2개. **격리가 워크트리이므로 workdir는 에이전트당 하나** — `colab/<session>/backend`, `colab/<session>/frontend` 브랜치.
4. 각자 구현 후 `colab artifact submit --type diff`로 diff를 제출한다. **QA의 판정 대상은 아티팩트다** — 같은 미션 동료의 체크아웃은 맥락을 위해 읽을 수 있지만 쓰지 않는다(FR-6.1 `[FOLDERS]`).
5. QA가 Frontend의 diff 스레드에 수정 요청 → **기존 lane이 `done → running`으로 재진입**해 같은 워크트리에서, **lane의 런타임 세션을 resume해** 이전 대화를 이어받은 채 작업한다 → 새 diff 제출 → **서버가 QA를 깨우고**(FR-6.5) QA가 승인.
6. 세션 `completed`. 워크트리는 14일 보존되며 병합은 Director가 수행한다. 미병합 커밋이 있으면 GC가 삭제하지 않는다(FR-6.4).

### 시나리오 C — Director 중간 개입

- Director가 "범위를 한국 시장으로 좁혀줘"라고 쓰면 — 멘션이 있든 없든 — 대상 에이전트가 실행 중이면 **큐잉된다**. 진행 중인 턴은 죽지 않는다.
- 지금 당장 방향을 틀어야 하면 lane 보드에서 그 lane의 **"중단하고 다시 지시"** 를 누른다. 멘션이 채워진 작성창이 열리고, 전송하면 턴이 취소되고 새 지시로 다시 시작한다(FR-3.4).
- 그냥 멈추기만 할 때는 **"중단"** 을 누른다. lane이 `failed(cancelled)`가 된다.

### 시나리오 D — 프로파일 전환

- Researcher의 Hermes 프로파일이 실패 → 데몬이 **같은 머신 안에서** 대체 프로파일(Claude Code)로 재시도한다. 워크트리와 아티팩트 디렉토리를 잃지 않는다.
- 같은 머신에 대안이 없으면 task는 `queued`로 대기하고 Director에게 알린다. **다른 머신으로 넘기지 않는다**(C4).

### 시나리오 E — 계속 쓰는 방 (v0.19 가 여는 것)

1. Director 가 방 **「결제팀」** 을 이름만으로 만들고 Lead·Researcher·Writer 를 초대한다. 설정은 손대지 않는다(워크스페이스 기본값).
2. 잡담처럼 묻는다 — "@Researcher 경쟁사 수수료 정책 어떻게 돼?" 일은 열리지 않는다. task 하나가 돌고 답이 방에 남는다.
3. 이틀 뒤 같은 방에서 "이걸로 보고서 쓰자" → 그 메시지에서 **「이걸 일로」**. goal·종료 조건·Director 가 붙고 Writer 가 일한다. 2번의 답은 **방 맥락에 이미 있어 다시 설명하지 않는다**.
4. 일이 끝나면 요약이 방에 남고 **방은 그대로**다. 다음 주 "지난번 보고서 업데이트" 는 같은 방에서 이어진다.
5. 옆방 **「인프라」** 의 결정이 필요해지면 Writer 가 `colab room read --room 인프라` 로 읽는다 — Director 가 두 방 모두의 멤버라서 허용되고, 읽은 사실이 양쪽 방에 남는다.
6. 같은 Writer 는 **「마케팅」** 방에도 참여 중이다. 두 방의 작업 폴더·런타임 대화 세션은 따로라 서로 섞이지 않는다.

---

## 5. 기능 요구사항

**v0.19 를 읽는 법 — "세션"이 나오면 이 표로 옮겨 읽는다.** 아래 FR 들은 v0.18 에 쓰인 문장을 그대로 두고, 매달리는 단위만 바뀌었다(§3.1). 규칙 자체는 바뀌지 않았다.

| 옛 문장의 "세션" | v0.19 에서 가리키는 것 |
|---|---|
| 세션 생성·참여자·초대·런타임 고정·격리·작업 폴더·라우팅·루프 상한·인박스 알림·SSE 구독 | **방**(Room) |
| goal·acceptance criteria·종료 조건·완료·`completing`·요약·Director 승인·세션 비용 집계 | **일**(Work) |
| `paused`(예산·시간) | **일** 이 멈춘다. 방 예산이 바닥나면 **그 방의 모든 일**이 멈춘다(FR-2A.3). **루프 상한은 방 단위**(§3.1)라 `room.blocked_reason: loop` 로 **방**이 멈춘다 |
| `paused`(런타임 소실·유예 만료, FR-9.2) | **방**이 멈춘다 — 그 방의 모든 일과 일 밖 task. 재바인딩하거나 일들을 닫을 때까지 |
| `session_id`(DB·계약) | 이관 뒤에도 당분간 **방 id**를 가리킨다 — 일 id 는 `work_id` 로 새로 둔다(§7·§10) |
| `runtime_session_ref`·`session/new`·`session/load`(ACP) | **런타임의 대화 세션**. 방·일과 무관하다 — 이름만 같다 |

### FR-1. Agent 등록 및 관리

multica의 Agent 등록 페이지(`agents-create`)를 참고하되, **역할 정의**와 **런타임 프로파일**을 분리해 둘 다 전면에 둔다.

**FR-1.1 등록 폼 필드**

| 필드 | 필수 | 설명 |
|---|---|---|
| `name` | ✓ | 표시 이름. 멘션 라벨로 사용. 워크스페이스 내 유일. |
| `role` | ✓ | 역할 태그 (`lead`, `researcher`, `writer`, `engineer`, `reviewer`, `custom`). |
| `role_description` | ✓ | 역할 설명 1~3문장. 다른 에이전트의 로스터에 노출되어 "누구에게 위임할지" 판단 근거가 된다. |
| `instructions` | ✓ | 시스템 프롬프트. 페르소나·작업 방식·제약. 매 run마다 주입. |
| `profiles[]` | ✓ (1개 이상) | 런타임 프로파일 목록 (FR-1.6). 하나를 `default`로 지정. |
| `tools` | | 허용 도구/스킬/MCP 서버 목록. 런타임이 지원하는 범위 내에서 적용. |
| `max_concurrent_tasks` | | 동시 실행 가능한 task 수 (기본 3). |
| `avatar` | | 이미지. |
| `respond_to` | | **누가 이 에이전트를 세션에 초대할 수 있는가**: `owner`(기본) / `allowlist` / `workspace` / `nobody`(킬 스위치). 세션 안에서의 상호 트리거는 참여 자체가 허용한다. FR-1.9. |
| `budget_per_task` | | task 1건당 토큰/비용 상한. |

**FR-1.2 Build with AI** — 자연어 설명("보수적인 재무 분석가")을 입력하면 이름·역할·instruction 초안을 생성. 플랫폼 내부 Claude API 호출(구조화 출력)로 폼 JSON을 받는다. 프로파일은 사용자가 직접 선택.

**FR-1.3 Agent 상태** — `idle` / `working` / `waiting_human` / `error` / `offline` / `disabled` 여섯 값. 세션 화면의 참여자 목록과 에이전트 페이지에 실시간 표시한다.

에이전트 하나가 lane을 여럿 가지므로(FR-6.1) 에이전트 상태는 **task 상태에서 파생되는 값**이며, 저장하지 않고 계산한다. 위에서부터 첫 번째로 맞는 것이 상태다 (디자인 리뷰 #02 C1′).

| 순서 | 조건 | 상태 |
|---|---|---|
| 1 | `respond_to: nobody` | `disabled` — 사람이 정지시킴 |
| 2 | 세션 런타임 연결 끊김 | `offline` |
| 3 | 실행 자체가 불가능한 오류 — 인증·쿼터·설정 (FR-7.1이 재시도하지 않는 분류) | `error` |
| 4 | `running` task가 하나라도 있음 | `working` |
| 5 | `waiting_human` task가 하나라도 있음 | `waiting_human` — 인박스와 짝이 맞는 정보 |
| 6 | 그 외 | `idle` |

- **`blocked`·`paused` lane은 파생에 넣지 않는다.** 그 lane의 프로세스는 이미 끝났고, 위임자나 사람의 답을 기다리는 것은 `idle`과 구분할 이유가 약하다. 어느 lane이 왜 멈췄는지는 lane 카드가 말한다.
- **`error`는 task 실패가 아니다.** task 하나가 `failed(timeout)`했다고 에이전트를 `error`로 두면 "다시 지시"할 때까지 끈적하게 남는다. `error`는 "이 에이전트는 지금 실행할 수 없다"만 뜻한다.
- `offline`과 `disabled`는 원인이 다르므로 아이콘과 문구를 구분해 표시한다.

**FR-1.4 템플릿** — 자주 쓰는 팀 구성(리서치 팀, 개발 팀, 콘텐츠 팀)을 프리셋으로 제공. 프리셋은 역할·instruction만 담고, 프로파일은 사용자의 런타임에 맞춰 첫 실행 시 매핑.

**FR-1.5 동적 생성 금지** — 에이전트 생성·수정은 Agents 화면(사람)에서만 가능하다. 에이전트에게 제공되는 툴셋에 에이전트 생성 툴은 없으며, `delegate`의 대상은 세션 `participants`로 제한된다. 로스터에 없는 역할이 필요하면 에이전트는 `ask_human`으로 Director에게 참여자 추가를 요청한다.

> 참고: Buzz는 에이전트가 새 에이전트를 **초안으로 제안**하고 소유자가 폼에서 검토·확정하는 중간 경로를 둔다. 우리는 v1에서 이를 채택하지 않는다 — 위의 `ask_human` 경로가 이미 사람의 승인을 거치는 같은 효과를 내고, 초안 검토 UI를 추가할 만큼 수요가 확인되지 않았다. v2 재검토 대상.

**FR-1.6 런타임 프로파일**

```
profile:
  name: "deep"                 # 에이전트 내 유일
  runtime_kind: claude_code    # claude_code | antigravity | hermes
  model: claude-opus-5         # 런타임이 지원하는 모델 ID
  options: { effort: xhigh }   # 세션이 광고한 능력 범위 내에서만 유효 (§8.2.6)
  env: { ... }                 # 추가 환경변수
  args: [ ... ]                # 추가 CLI 인자
  fallback_profile: "fast"     # 이 프로파일 실패 시 대체할 프로파일 이름 (선택)
  is_default: true
```

- **프로파일은 머신을 고르지 않는다.** 실행 머신은 세션의 `runtime_id`가 정한다(FR-2.1, C4). 프로파일은 "그 머신 위에서 어떤 CLI를 어떤 모델로 돌릴지"만 규정한다.
- 세션 참여자 추가 시, 또는 `delegate` 호출 시 프로파일을 지정할 수 있다. 미지정 시 `default`.
- **폴백은 같은 머신 안에서만 일어난다.** 세션 런타임에 해당 `runtime_kind`가 없거나 실패하면 `fallback_profile`로 전환한다. **다른 머신으로 넘기지 않는다** — 워크트리와 아티팩트 디렉토리를 잃기 때문이다. 같은 머신에 대안이 없으면 task는 `queued`로 대기하고 Director에게 알린다.
- 지원 모델 목록은 데몬이 런타임별로 보고한다(§8.2의 probe 결과).
- `transport`는 프로파일에 두지 않는다. ACP 어댑터 사용 가능 여부는 데몬이 판단하고 실패 시 CLI로 폴백한다(§8.2). 사용된 경로는 활동 피드에 기록된다.

**FR-1.7 이식 가능한 정의 파일 (`.agent.md`)** — 에이전트 정의는 DB 레코드인 동시에 **하나의 파일로 내보내고 가져올 수 있다**. Buzz의 `.persona.md`와 같은 형태로, YAML frontmatter가 메타데이터이고 마크다운 본문이 `instructions`다.

```markdown
---
name: Researcher
role: researcher
role_description: 1차 자료를 찾아 출처와 함께 정리한다. 결론을 내리지 않는다.
profiles:
  - name: default
    runtime_kind: claude_code
    model: claude-opus-5
    options: { effort: high }
  - name: fast
    runtime_kind: hermes
    model: "anthropic:claude-sonnet-5"
tools: [web_search, web_fetch]
respond_to: owner
max_concurrent_tasks: 3
---

너는 리서처다. 주장마다 출처 링크를 남기고, 확인되지 않은 것은
"확인 필요"로 표시한다. 결론과 권고는 Lead에게 맡긴다.
```

- **왜**: 팀이 에이전트 구성을 git으로 버전 관리하고, PR로 리뷰하고, 워크스페이스 간에 공유할 수 있다. UI 폼과 파일은 같은 스키마의 두 표현이다.
- **팀 묶음**: 여러 `.agent.md`와 README를 디렉토리로 묶어 **팀 팩**으로 배포한다. 팩 가져오기는 에이전트를 한꺼번에 등록한다.
- **검증**: 가져오기 시 스키마 검증 후, 참조된 `runtime_kind`가 이 워크스페이스에 없으면 등록은 하되 `unmapped`로 표시하고 (런타임 연결이 끊긴 `offline`과 구분한다) 프로파일 매핑을 요구한다.

**FR-1.8 정의와 인스턴스의 분리** — `.agent.md`(또는 템플릿)는 **정의**이고, 워크스페이스에 등록된 에이전트는 **인스턴스**다. 인스턴스는 정의에서 복사된 필드 외에 자기 것만 갖는다: `workspace_id`, `owner_id`, `status`, 누적 사용량, `definition_source`와 `definition_version`.

- 정의가 갱신되어도 **실행 중인 task에는 영향을 주지 않는다.** 새 설정은 다음 run부터 적용된다. 진행 중인 에이전트의 설정을 갈아끼우는 동작은 제공하지 않는다.
- 인스턴스는 `definition_version`을 보관해 "이 팩의 v3에서 왔고 현재 v5가 있음"을 표시하고, 사용자가 명시적으로 업데이트를 적용한다.

**FR-1.8.1 테스트 채팅 (P2)** — 에이전트 편집 화면에서 세션을 만들지 않고 그 에이전트와 1:1로 대화해 설정을 검증한다.

- **세션이 아니다.** `session` 레코드를 만들지 않으므로 세션 목록·비용 집계·합류·lane에 관여하지 않는다. 별도 경량 채널(`test_chat`)로 저장하고 워크스페이스 비용에만 합산한다.
- workdir은 임시 디렉토리이며 대화 종료 시 삭제한다. `worktree`·`container` 격리를 쓰지 않는다.
- `colab` CLI 토큰은 발급하지 않는다 — 플랫폼에 되돌아올 대상(세션·lane)이 없기 때문이다. 따라서 에이전트는 메시지 게시·위임·HITL을 쓸 수 없고 순수 응답만 한다.
- 실행 경로(ACP/CLI)와 사용 토큰을 함께 보여준다. 설정이 틀렸는지 확인하는 것이 목적이다.

**FR-1.9 호출 권한 게이트** — **세션 참여가 곧 세션 내 트리거 허용이다.** 에이전트를 세션에 초대하는 행위가 권한 부여이며, 세션 안에서는 Director와 다른 참여 에이전트가 `respond_to`와 무관하게 서로 멘션할 수 있다.

`respond_to`가 통제하는 것은 **세션 밖**이다.

| 값 | 의미 |
|---|---|
| `owner` (기본) | 소유자만 이 에이전트를 세션에 **초대**할 수 있다 |
| `allowlist` | 지정한 멤버들이 초대할 수 있다 |
| `workspace` | 모든 워크스페이스 멤버가 초대할 수 있다 |
| `nobody` | **킬 스위치.** 새 초대를 막고, 이미 참여 중인 세션에서도 트리거를 중단한다. 정의는 지우지 않는다 |

- 이 정의가 없으면 멤버 A가 만든 Lead와 멤버 B가 만든 Researcher가 같은 세션에 있어도 서로 위임하지 못한다. 팀 워크스페이스에서 기본값이 협업을 막는 상태가 된다.
- 권한 판정은 언제나 **체인 최상단의 사람 originator** 기준이다. 에이전트가 다른 에이전트를 호출해도 권한이 상승하지 않는다. 세션 초대 자체가 originator의 권한으로 이뤄졌으므로, 세션 내 상호 트리거는 그 권한 안에 있다.
**`nobody`(킬 스위치)의 즉시 효과 (M8)** — 폭주하는 에이전트를 멈추는 것이 목적이므로 대기만 시켜서는 안 된다.

| 대상 | 처리 |
|---|---|
| `running` 턴 | **취소한다** (§8.2.2 절차). 활동 피드에 "소유자가 정지시킴"으로 남긴다 |
| `queued` task | `cancelled`로 보낸다 |
| `waiting_human`의 열린 HITL | **남긴다.** 사람이 답할 기회를 뺏지 않는다. 답이 오면 `answered`로 기록하되 **재큐잉만 보류한다**. 다시 활성화하면 그때 이어진다 |
| workdir | 보존한다 |

에이전트 상태는 `disabled`로 표시하며 `offline`(런타임 연결 끊김)과 구분한다. 다시 `owner` 등으로 되돌리면 새 트리거부터 정상 동작한다.

**FR-1.9.1 역할별 행동 부분집합 (v1.1, K-19 — OASIS 리뷰 C5, Director 확정 2026-09-15)** — 에이전트가 플랫폼에 할 수 있는 말(`colab` 명령)은 **역할이 정한다.** 프롬프트(§8.3)로 "하지 말라"고 이르는 것이 아니라 **표면**으로 막는다: 데몬이 MCP 툴 목록을 역할의 부분집합으로 자르고, CLI 는 그 밖의 명령을 서버에 보내기 전에 거부하며(exit 3 `command_not_allowed`), 서버도 같은 표로 `403 command_not_allowed` 를 낸다(세 층이 같은 표를 본다). 표는 `openapi` `Agent.allowed_commands`(읽기 전용 파생값)·`CliContext.allowed_commands`·데몬 번들 `task.allowed_commands` 로 흐른다.

| 역할 | 허용 명령 | 막는 것과 이유 |
|---|---|---|
| `lead` | 전부 | 코디네이터 |
| `researcher` · `writer` · `engineer` | `session get/messages` · `artifact get/submit` · `message post` · `status set` · `decision record` · `hitl ask/request-info` | `lane delegate`(위임은 Lead 의 일 — 실무자가 위임하면 사슬이 깊어진다, FR-3.5) · `review approve/reject`(자기 산출물을 자기가 승인하지 않게) · `hitl approve-request`(완료 승인 요청은 Lead 가) |
| `reviewer` | `session get/messages` · `artifact get` · `message post` · `status set` · `decision record` · `review approve/reject` · `hitl ask/request-info` | `lane delegate` · `artifact submit`(리뷰어는 산출물을 내지 않는다 — 반려 사유는 `review reject --reason`) · `hitl approve-request` |
| `custom` | 전부 | Director 가 instructions 로 정한다 |

세션 참여 자체(FR-1.9)와는 독립이다 — 참여했어도 역할 밖 명령은 없다. 표를 바꾸는 것은 계약 변경(`colab-cli.md` §2.5).
### FR-2. Room (방)

**FR-2.1 방 만들기 — 칸 하나.**

| 필드 | 설명 |
|---|---|
| `name` | 방 이름 (필수, 이것만 있으면 만들어진다) |
| `description` | 한 줄 설명 (선택). goal 이 아니다 — 무엇을 끝내는지는 일이 정한다 |

나머지(런타임·격리·한도·autonomy·참여자)는 **방 설정**이며 워크스페이스 기본값을 상속한다. 만들기 화면에서 묻지 않고, 방을 연 뒤 바꾼다. **마법사는 없앤다** — v1 의 7단계는 "일 하나"를 전제로 했는데 방은 계속 쓰는 공간이라 그 시점에 정할 수 있는 것이 이름뿐이다.

**FR-2.1.2 방 이름·설명은 언제든 바꾼다 (v0.19.4, Director 요청 2026-09-25)** — 방은 끝나지 않고 계속 쓰는 공간이라, 만들 때 붙인 이름이 하는 일과 어긋나게 된다(「STO 시장 리서치」가 토큰증권 전반을 다루게 되는 식). 이름은 방을 찾고 부르는 유일한 손잡이이므로 **바꾸는 길이 방 안에 있어야 한다.**

- **누가**: 방장·부방장·ws owner·admin(방 설정과 같은 권한, `updateRoom`). 그 밖에게는 이름이 읽기 전용이고 편집 수단이 보이지 않는다.
- **어디서**: ① 방 머리의 이름을 그 자리에서 고친다(가장 짧은 길) ② 방 설정 맨 위 「이름·설명」 ③ 방 목록 카드 「…」 메뉴 「이름 바꾸기」. 세 곳이 같은 규칙·같은 문구를 쓴다.
- **규칙**: 앞뒤 공백을 떼고 1~200자, 비면 저장하지 않는다(「방 이름을 적어 주세요」). **같은 이름의 방이 있어도 막지 않는다** — 워크스페이스 안에서 이름은 고유 키가 아니다(id 가 키). 설명은 0~500자, 비워도 된다. 바뀌지 않았으면 저장하지 않는다(시스템 메시지도 없다).
- **남긴다**: 타임라인 시스템 메시지 「〈사람〉이 방 이름을 〈옛 이름〉에서 〈새 이름〉(으)로 바꿨습니다」 · 설명이면 「〈사람〉이 방 설명을 바꿨습니다」 + `activity_log`. 멘션·링크·검색은 방 **id** 로 이어지므로 옛 이름으로 적힌 과거 메시지·요약은 고치지 않는다(기록은 그때의 말이다).
- **실시간·다른 자리**: `room.updated` 로 방 목록 카드·내비·방 머리·받은 요청·다른 방의 참고 방 링크·S23 기록에 새 이름이 뜬다(이 자리들은 이름을 저장하지 않고 방에서 읽는다).
- **에이전트**: 브리프 [4] 방 맥락이 다음 턴부터 새 이름을 싣는다 — [1]~[5] 바이트 동일(E12-11)은 이름을 바꾼 그 한 번 깨진다(미션이 바뀔 때와 같은 의도된 대가, harness §10). 진행 중인 턴은 옛 이름으로 끝난다.

**FR-2.1.1 방을 만들 때 확정되는 것과 확정되지 않는 것 (V19-A P0-5)** — 칸 하나로 만드는 대가로 **사용자가 고르지 않은 값이 나중에 되돌릴 수 없게 확정된다.** 그 사실을 **두 번** 알린다.

- 만들기 화면 아래 한 줄: 「격리 없음 · 컴퓨터는 첫 실행 때 정해집니다 — 방 설정에서 미리 바꿀 수 있습니다」(워크스페이스 기본값을 그대로 읽어 쓴다).
- **첫 task dispatch 로 `runtime_id` 가 고정되는 순간** 방 타임라인에 시스템 메시지: 「이 방은 〈컴퓨터 이름〉에서 돕니다. 격리 방식과 컴퓨터는 이제 바꿀 수 없습니다 — 작업 폴더가 거기 묶여 있습니다.」
- **저장소를 쓰는 방은 첫 dispatch 전에 격리를 고르게 한다.** `isolation: none` 인 채 **저장소가 있는 런타임**으로 첫 dispatch 가 일어나면 방장에게 승인 HITL 을 발행한다 — 「이 컴퓨터에는 저장소가 있습니다. 에이전트들이 **같은 폴더를 함께 고치게** 됩니다. 워크트리로 나눌까요?」(기본 제안 `worktree`). 이 요청은 `hitl_request`(`type: approval`, `source: system`, `approver_spec: room_owner`, 부재 위임 FR-2A.3)이고 인박스 타입은 **`isolation_confirm`** 이다 — 승인 = `worktree`, 거절 = `none` 유지. **답이 올 때까지 그 방의 첫 dispatch 를 보류하고 `runtime_id` 를 고정하지 않는다.** 이것이 없으면 잡담 한 마디가 개발 방의 격리를 영구히 `none` 으로 굳힌다.

**FR-2.2 참여 — 언제든 초대·퇴장**

- 방장·부방장(또는 워크스페이스 owner·admin)이 사람과 에이전트를 **아무 때나** 초대하고 내보낸다. 초대 자체가 그 방 안에서의 트리거 허용이다(FR-1.9 의 규칙을 방에 적용).
- **초대된 에이전트는 방 맥락을 처음부터 본다**(FR-4.1) — 합류 시점 이전의 히스토리도 읽을 수 있다. 사람이 새 팀원에게 스레드를 보여 주는 것과 같다. 민감한 방은 초대를 막는 것으로 통제한다.
- **사람의 퇴장**: 열린 미션의 Director 인 사람은 **먼저 Director 를 넘겨야** 나갈 수 있다(거부 + 안내). 방장은 부방장이나 다른 참여자에게 방장을 넘긴 뒤 나간다(§12.1-4 승계 규칙은 워크스페이스를 떠날 때의 자동 경로다). 일반 참여자는 언제든 나간다.
- 퇴장한 에이전트의 lane·task 는 남고(기록), 새 트리거만 막힌다. 퇴장은 취소가 아니다 — 진행 중 턴은 FR-3.4 로 중단한다.
- 사람은 워크스페이스 멤버만 초대할 수 있다. 방 참여는 워크스페이스 권한을 **넘지 않는다**.
- **참여자는 본인에 대해 언제든 「이 방에서 나가기」를 할 수 있다**(위 사람 퇴장 규칙의 거부 조건은 그대로).
- 참여자 초대·퇴장, `visibility` 변경, 방장·부방장 변경, 미션 열림·닫힘, `manual` 멈춤 걸기·풀기는 **타임라인 시스템 메시지와 `activity_log`** 에 남긴다 — 로스터가 바뀌면 이후 위임 판단이 달라진다.

**FR-2.3 방 설정**(전부 선택, 워크스페이스 기본값 상속)

| 설정 | 기본 | 뜻 |
|---|---|---|
| `runtime_id` | 없음 → **첫 task dispatch 때 고정** | 이 방의 일은 한 머신에서 돈다(C4 그대로, 단위만 방) |
| `isolation` | 워크스페이스 기본(`none`) | `worktree` 면 저장소 경로. **작업 폴더는 방×에이전트 단위**(FR-6.1) |

`[V19-B]` **브랜치와 작업 폴더의 수명.** `worktree` 격리의 브랜치는 `colab/<방 slug>/<에이전트 slug>` 이고 **방이 사는 동안 하나**다 — 일이 바뀌어도 같은 브랜치의 다음 커밋이다. 병합 단위는 브랜치가 아니라 일이 낸 diff 아티팩트다(FR-4.3). **보존 기한은 방의 끝이 아니라 `workdir.last_used_at + workdir_retention_days`** 로 센다 — 방은 완료되지 않으므로 옛 기준점(세션 완료 시각)을 쓰면 GC 가 영원히 0행이고 워크트리가 무한히 쌓인다(FR-6.4 도 같이 읽는다).
| `limits` | 워크스페이스 기본 | 방 예산·시간·**동시 진행 일 수**(`max_concurrent_works`, 기본 3)·동시 lane 상한. 예산·시간은 일에도 걸 수 있고 **작은 쪽**이 이긴다. 동시 일 상한을 넘겨 새 일을 열면 `409` — 「열린 일을 닫거나 상한을 올리세요」 |
| `autonomy` | `guided` | 방 기본값. 일이 덮어쓸 수 있다 |
| `default_director` | **없음 → 연 사람** | 지정하면 새 일의 Director 기본값 |

**FR-2.4 방 상태** — `active` ⇄ `archived`(읽기 전용, 새 트리거 없음) + 삭제(FR-2.6). **방은 `completed` 되지 않는다.**

`[V19-B]` **방을 멈추는 칸.** FR-2A.3 의 "방 상한을 넘기면 그 방의 모든 일과 일 밖 task 가 멈춘다"·FR-9.2 의 런타임 소실을 강제할 자리가 필요하다. `status` 에 `paused` 를 더하지 않고 **`room.blocked_reason`**(`budget` · `runtime_offline` · `loop` · **`manual`** · null)을 둔다. **`manual` 은 사람이 건다**(SCR-A G-11 — v1 「세션 취소」의 자리): 방장·부방장·워크스페이스 owner·admin 이 「이 방 멈춤」을 누르면 진행 중인 모든 턴이 FR-3.4 의 「중단」과 같은 방식으로 끝나고 새 트리거가 막힌다. 해제도 같은 사람이 한다 — 상태 머신을 늘리면 `archived` 와의 조합이 폭발하고, 큐 게이트는 한 줄이면 되기 때문이다(claim 이 `blocked_reason IS NOT NULL` 인 방의 task 를 주지 않는다). `budget`·`loop`·`runtime_offline` 의 해제는 승인 HITL(또는 재바인딩)의 결과이고, **`manual` 은 건 사람과 같은 권한자가 직접 푼다**(HITL 없음). **R0 계약의 `Room` 스키마가 이 칸을 알아야 한다** — §12.1 의 결정 항목.

`[V19-C]` **보관의 뜻.** `archived` 는 **새 활동만 닫고 과거를 남긴다** — 메시지·일·아티팩트·결정 기록이 그대로 보존되고 **검색에도 계속 걸린다**. 보관된 방에서는 새 메시지·새 일·새 트리거가 거부되고, 진행 중 task 가 있으면 보관을 거부한다. **보관은 되돌릴 수 있다**(해제하면 참여자까지 그대로) — 해제 권한은 보관과 같다(방장·부방장·워크스페이스 owner·admin). 삭제(FR-2.6)만 영구적이다 — 이 구분이 없으면 사람이 "지우기 아까운 방"을 삭제한다.

**FR-2.5 방 요약(선택)** — 방이 길어지면 사람이 「여기까지 정리」를 눌러 그 시점까지의 요약을 방에 남긴다. 자동 요약은 **일**이 끝날 때만 한다(FR-2A.4) — 방은 끝이 없어 자동 요약의 기준 시점이 없다.

`[V19-C]` **범위와 출처.** 요약은 **범위를 사람이 고르고 그 범위를 요약 메시지에 기록한다** — 「최근 7일」 또는 직접 고른 기간, 그리고 `{from_message_id, to_message_id}` + 인용한 메시지 id 목록. 요약이 틀렸을 때 원 메시지로 내려갈 수 있어야 하고 같은 범위는 같은 입력을 봐야 한다. 「내가 안 읽은 것만」 기준은 **사람마다 달라 방에 남기지 않는다**(본인 미리보기로만) — 방 메시지가 보는 사람에 따라 달라지면 그것은 더 이상 방 맥락이 아니다.

**FR-2.6 방 삭제** — 방장 또는 워크스페이스 owner·admin. **진행 중인 일이 있으면 거부**(먼저 끝내거나 취소). 메시지·일·lane·task·활동·HITL·아티팩트·결정·비용이 함께 사라지고 워크스페이스 집계에서도 빠진다. 미병합·미커밋 작업 폴더가 있으면 거부한다(FR-6.4 M4 와 같은 이유). `activity_log` 에 `room.deleted` 한 줄만 남는다. 작업 폴더는 병합·클린이면 즉시 GC 대상으로 넘긴다(아니면 위 문장대로 삭제 자체를 거부).

### FR-2A. Work (일 · 화면 이름 **미션**) — goal 과 종료 조건

**FR-2A.1 일 시작** — 방 안에서 누구나(사람) 연다. 화면에서는 「**새 미션**」·「**이걸 미션으로**」다(§3.2). 세 가지 길이 있고 전부 같은 레코드를 만든다.

| 길 | 어떻게 |
|---|---|
| 메시지에서 | 메시지 옆 「**이걸 미션으로**」(화면 문구) → goal 은 그 메시지 본문이 기본값 |
| 새 일 버튼 | goal 입력 + (선택) 종료 조건·Director·예산 |
| 에이전트가 제안 | Lead 가 `colab work propose --goal` → **사람이 확인해야 열린다**(방 참여자 누구나 열거나 거절하고, 거절하면 타임라인 시스템 메시지로 제안한 에이전트에게 알린다. 기한은 없다 — `expired` 없음)(에이전트가 스스로 일을 열지 못한다 — 무한 자기 위임을 막는다) |

기본값: goal 만 필수, Director = 방 설정 `default_director`, **비어 있으면(기본) 연 사람**, 예산 = 방 설정. **종료 조건 기본값은 `assignee` 가 정해진 뒤에만 `artifact_submitted(assignee) AND user_approval` 이다**(V19-A P0-3) — 어느 길로 열든 assignee 를 고르지 않으면 **`user_approval` 단독**으로 연다. 대상 없는 `artifact_submitted` 는 아무도 충족시킬 수 없어 일이 영영 안 닫힌다(v1 의 S-84 와 같은 사고).

**일이 아닌 실행도 있다.** 방에서 에이전트를 멘션하면 task 가 돈다 — 종료 조건을 걸고 추적하고 싶을 때만 일을 연다. 일에 속하지 않은 task 의 HITL 은 **방장**에게 간다.

**FR-2A.2 종료 조건** — v0.18 FR-2.2 의 표·규칙을 그대로 쓴다(원자 4종 + AND/OR, `agent_approval` 은 리뷰어 지정 필수, `criteria_met` 단독 불가). 단위만 일이다.

**FR-2A.3 한도와 `paused`** — 예산·시간·루프 상한은 종료 조건이 아니다(v0.18 과 같다). 바뀐 것은 범위다.

- **일의 상한**을 넘기면 그 일이 `paused` 되고 Director 에게 계속 진행 승인 HITL 이 간다.
- **방의 상한**을 넘기면 **그 방의 모든 일과 일 밖 task 가 멈춘다**. 방장에게 승인 HITL 이 간다. **방장이 답하지 않으면**(SCR-A G-10) 미션의 deputy 규칙과 같게 **기한 절반 경과 후 부방장**이, 부방장이 없으면 **워크스페이스 owner 중 가장 오래된 한 명**이 답할 수 있다 — 일 밖 task 의 HITL(FR-2A.1, `room_owner`)도 같은 경로를 탄다. 방장 한 사람의 부재로 방이 영구 정지하지 않게 한다.
- 실행 시점 유효 상한은 `min(방 잔여, 일 잔여, task 상한)`. 데몬에 싣는 값도 이 최솟값이다(daemon-protocol §4.1 `limits`).
- **기본은 상한 없음이고, 없다는 사실이 보여야 한다**(T-BUDGETCAP, Director 2026-09-25 — 상한 없는 방이 80분에 $92.77). 방·일 비용 줄은 상한이 없으면 「상한 없음」과 그 자리의 「상한 걸기」를 보인다(SCREEN §4.6). 워크스페이스 기본값 둘은 **새로 만드는 것에만** 걸린다 — 새 방 `room_defaults.limits.budget_usd`, 새 일 `budget_policy.default_session_budget_usd`(일을 열 때 예산을 비우면 이 값, 명시적 null 은 상한 없음).

**FR-2A.2.1 승인 요청은 일이 멈춘 뒤에 뜬다 (v0.19.5, Director 요청 2026-09-25)** — 종료 조건에서 **Director 승인(`user_approval`)만 남고 나머지가 충족**됐더라도, 그 일에 **실행·대기 중인 task**(`queued` · `dispatched` · `preparing` · `running`)가 있으면 승인 요청 HITL 을 **열지 않고 보류한다**. 마지막 task 가 끝나는 순간(정상 종료 · 취소 · 실패 모두) 종료 조건을 다시 판정해 그때 연다.

실측(2026-09-25, 실사용 「게임 제작 방」): 담당 에이전트가 한 턴 안에서 게임을 만들며 13:41:01 에 v1 을 제출하자 — 같은 턴이 계속 돌고 있는데 — 그 순간 「종료 조건이 모두 충족되었습니다. 승인하시겠습니까?」가 떴다. 33초 뒤 같은 에이전트가 「v1 올렸고 계속 통합한다」고 보고했다. **아직 만들고 있는 것을 끝내도 되냐고 묻는 질문**이어서, 사람은 답할 근거가 없다.

- 보류 중에는 진행률 행이 **「조건 충족 — 진행 중인 작업이 끝나면 승인을 요청합니다」**로 말한다(openapi `CompletionProgress.conditions[].held_reason: running_tasks`). 조건이 막힌 것이 아니라 **차례가 아직 오지 않은** 것이므로 ✗ 도 막힘 사유도 아니다.
- **보류는 `user_approval` 에만 적용된다.** `agent_approval`(리뷰어 승인)·`criteria_met`·`artifact_submitted` 의 판정은 그대로다 — 리뷰어는 작업 중에도 검토할 수 있고, 그것이 다음 작업의 입력이다.
- **거절된 승인은 다시 열리지 않는다.** Director 가 거절하면(FR-4.2 · E6-04) 아무것도 다시 부르지 않으므로, 보류 표식이 있는 요청만 나중에 열린다.
- `paused`·`waiting_human` task 는 보류시키지 않는다 — 사람을 기다리는 task 가 사람의 승인을 막으면 서로 기다린다.

**FR-2A.2.2 열린 완료 승인은 한 일에 하나 (v0.19.5, 실사용 결함)** — 같은 일의 `user_approval` 요청은 **동시에 하나만** 열려 있다. 열린 요청이 있으면 새로 열지 않는다 — **이미 그 질문을 하고 있기 때문**이다. 갱신도 대체도 하지 않는다: 질문 문장(「종료 조건이 모두 충족되었습니다. 승인하시겠습니까?」)이 같고, 답이 가리키는 것도 같은 일이며, 닫았다 다시 여는 것은 사람이 읽고 있던 카드를 지우고 같은 카드를 새로 놓는 일이다(받은 요청의 기한 절반 위임 시계도 그때마다 되감긴다 — FR-5.2).

실측(2026-09-25, 「게임 제작 방」): 아티팩트가 제출될 때마다 새 요청이 열려 13:41 · 13:45 · 14:04 · 14:08 · 14:21 · 14:30 여섯 건, 그중 **다섯이 동시에 open** 이었다. 받은 요청 화면에 같은 질문이 다섯 줄 쌓이고, 하나에 답해 일이 닫혀도 넷이 남는다.

- **판정은 「무엇이 바뀌었는가」로 한다.** 플랫폼이 승인을 청하는 때는 **트리를 실제로 움직인 이벤트**(아직 미충족이던 원자를 충족시킨 제출·리뷰 승인)와 **명시적 재판정**(조건 변경 FR-2A.2 · 보류 해제 FR-2A.2.1)뿐이다. 「`user_approval` 만 남았는가」는 한 번 참이 되면 사람이 답할 때까지 참이므로, 그 물음만으로는 **그 뒤 도착하는 모든 이벤트**가 요청을 다시 연다. 위 여섯 건이 그 결과다.
- **이미 충족된 원자를 다시 충족시키는 일은 아무것도 바꾸지 않는다.** 같은 담당이 v2·v3 를 내도 `artifact_submitted` 는 이미 참이다 — 새 질문이 아니라 같은 질문이다.
- **지정되지 않은 에이전트의 제출은 조건을 건드리지 않는다**(E6-02 그대로). 실측에서 조건은 「아티팩트 제출(who=assignee → Lead)」인데 **Writer** 의 제출 두 건(13:45 · 14:08)에도 요청이 열렸다. 저장은 되고 충족은 되지 않는 것이 원래 규칙이고, **승인 요청도 열리지 않아야** 규칙이 완결된다.

**FR-2A.4 일 상태와 요약** — v0.18 FR-2.3 의 상태 머신(`draft → active → (paused ⇄ active) → completing → completed | cancelled`)을 일에 그대로 쓴다. `completing` 에서 요약을 만들어 **방에 메시지로 남긴다** — 일은 닫히고 방은 계속된다.

**FR-2A.5 여러 일의 동시 진행** — 한 방에 일이 여러 개 열려 있을 수 있다. 화면은 방 상단에 「진행 중인 일」 칩으로 보이고, 메시지·lane 은 자기가 속한 일을 가리킨다(없으면 "일 없음"). 동시 실행 상한은 방 설정이 정한다.

`[V19-B]` **아티팩트 이름은 방 단위다.** 같은 이름을 다시 제출하면 **일이 달라도** v2 가 된다(유니크 키가 방×이름). 일마다 v1 부터 매기지 않는다 — 방이 산출물의 보관소이고, 사람은 "그 보고서의 최신본"을 방에서 찾는다.

**일 밖 실행의 결과도 방에 자리가 있다(V19-A P1-8).** 방 사이드바는 **일에 따라 바뀌는 칸**(goal·종료 조건 진행률·그 일의 비용)과 **방 전체 칸**(아티팩트·결정 기록·방 누적 비용·방 설정)으로 나뉘고, 일 밖 task 가 만든 아티팩트·결정은 방 전체 칸에 **「일 없음」 묶음**으로 보인다.

**FR-2A.6 일 삭제·취소** — 취소는 v0.18 과 같다(`active`·`paused` → `cancelled`). 삭제는 일만 지우고 방과 메시지는 남긴다 — 메시지는 방의 것이기 때문이다.

### FR-3. 메시징 및 멘션 라우팅

**FR-3.1 메시지 구조** — 세션은 하나의 메인 타임라인 + 스레드(reply). 작성자 타입 `user | agent | system`. 마크다운 지원. `source_task_id`로 어떤 run에서 나온 메시지인지 추적.

**FR-3.1.1 메시지가 어느 일에 속하는가 (v0.19, V19-A P1-7)** — `message.work_id` 는 다음 순서로 정해지고, **사람이 보내기 전에 확인할 수 있어야 한다**(FR-3.6 트리거 미리보기와 같은 칩에 표시).

1. 작성창에서 사람이 고른 일(고르면 그것으로 고정).
2. 그 메시지가 어떤 일의 스레드 안이면 그 일.
3. 멘션 대상 에이전트가 이 방에서 **지금 실행 중인 lane** 을 갖고 그 lane 이 일에 매여 있으면 그 일.
4. 그 밖이면 **일 없음**(`work_id = null`).

에이전트 메시지는 자기 task 의 lane 이 매인 일을 따른다.

**미션을 열 때의 사후 귀속(V19-GAP P-J).** 「이걸 미션으로」로 연 미션은 원 메시지와 그 스레드 중 `work_id = null` 인 것들을 **한 번** 이 미션으로 귀속하고, 그 스레드에서 트리거된 lane 의 `work_id` 도 함께 채운다(그 뒤의 비용·브리프가 미션에 잡히게). 이미 다른 미션에 속한 메시지는 바꾸지 않고, 그런 메시지에서는 「이걸 미션으로」를 쓸 수 없다. 미리보기 칩은 「@Writer 를 트리거합니다 · 일 〈보고서 초안〉에 들어갑니다」처럼 **귀속될 일을 함께** 적는다(일 없음이면 그렇게).


**FR-3.1.2 에이전트 메시지의 세 층 — 대화 · 작업 내용 · 작업 과정 (v0.19.2, Director 요청 2026-09-25)** — 에이전트 메시지는 사람이 훑는 **대화**와 필요할 때만 펼치는 **작업**을 나눈다. 타임라인은 에이전트끼리 무엇을 주고받는지가 먼저 읽혀야 한다. STO 실사용 테스트(2026-09-24)에서 Researcher 조사 결과(1.5만 자)·Writer 초안 전문(2.8만 자)이 「@Lead 조사 끝났습니다」 한 줄과 한 본문에 섞여, 타임라인이 대화가 아니라 문서 더미로 읽혔다.

| 층 | 무엇 | 기본 표시 | 저장 |
|---|---|---|---|
| **대화** | 누구에게 · 무엇을 · 결론 · 다음 할 일. 5줄 안팎 | **항상 보인다** | `message.content`(기존) |
| **작업 내용** | 이 턴이 만든 중간 산출 — 조사 결과 · 초안 전문 · 표 · 긴 목록 · 대조표 | **접힘** — 한 줄 요약(「작업 내용 · 1.5만 자 · 표 3개」) + 첫 줄 미리보기 | `message.detail`(새 칸, 마크다운, 없을 수 있음) |
| **작업 과정** | 도구 호출 · 파일 편집 · 셸 · 플랫폼 조작(FR-7.2 활동 피드) | **접힘** — 한 줄 요약(「작업 과정 · 도구 12회 · 파일 2개 편집 · 4분」) | `task_event`(기존) |

- **에이전트가 나눈다.** `colab message post --body <대화> --detail <작업 내용>`(또는 `--detail-file <경로>`). 브리프 [2] 가 「대화는 짧게, 조사 결과·초안 전문·표는 작업 내용으로, 제출물은 아티팩트로」를 가르친다(§8.4).
- **에이전트가 나누지 않았으면 화면이 나눈다(폴백).** `detail` 이 없고 본문이 **1,200자 또는 25줄**을 넘으면 첫 문단(최대 400자)을 대화로 보이고 나머지를 「작업 내용 · 자동으로 접음」에 넣는다. **저장은 바꾸지 않는다** — 화면의 표시만. 표·코드 블록 도중에서 자르지 않는다(그 앞 문단 경계로).
- **사람 메시지·시스템 메시지·요약·HITL·질문 카드는 접지 않는다.** 사람의 말은 사람이 쓴 그대로 보이고, 나머지는 이미 짧거나 카드 모양이 따로 있다.
- **제출물은 여전히 아티팩트다.** `detail` 은 대화 속 중간 산출이고 종료 조건(`artifact_submitted`)·리뷰·버전은 아티팩트만 갖는다. 같은 글이 둘 다에 있으면 메시지는 아티팩트 카드로 가리키고 전문을 다시 붙이지 않는다.
- **방 단위 보기 전환.** 타임라인 머리에 「**대화만** / 작업 내용 펼침」 — 기본은 대화만, 보는 사람마다 기억한다(브라우저). 펼침은 「작업 내용」만 연다. 「작업 과정」은 메시지마다 따로 연다(양이 많고 개입 판단용이라).
- **에이전트가 읽는 쪽은 줄이지 않는다.** 일을 넘겨받는 에이전트가 그 산출을 읽어야 한다. 턴 프롬프트의 **트리거 메시지는 `detail` 전문**을 싣고, `<history>` 의 다른 메시지는 `detail` 첫 400자 + 「(작업 내용 N자 — `colab room messages --thread <id>` 로 전문)」 한 줄. `colab room messages`·`room read` 는 `detail` 을 싣는다.
- **받은 요청·알림·검색 미리보기·미션 요약**은 대화(`content`)만 쓴다.
- **계약 변경은 R5**(Director 승인 뒤) — openapi `Message.detail`·`MessageCreate.detail`, colab-cli `message post --detail/--detail-file`·MCP `detail`, harness §10 브리프 [2] 문장과 `<history>` 규칙.

**FR-3.1.3 타임라인은 대화다 — 누가 → 누구에게 · 무엇을 (v0.19.3, Director 요청 2026-09-25)** — 「어떤 에이전트가 어떤 에이전트에게 명령을 한 건지 구분이 잘 되지 않아.」 FR-3.1.2 가 한 메시지 **안**을 대화로 만들었다면, 이 FR 은 메시지 **사이**를 대화로 만든다. 지금 카드 머리는 작성자 이름뿐이라, 받는 쪽은 본문 속 멘션 칩으로만 드러나고 위임·보고·질문·답이 같은 모양이다.

1. **머리 한 줄 = 보낸 쪽 → 받는 쪽 · 말의 종류.** 「**@Lead** ‹위임› → **@Researcher**」. 받는 쪽이 여럿이면 칩 여럿(셋까지, 나머지는 「외 N명」), 없으면 「방 전체」.
2. **판정은 서버가 판정해 내려준다(openapi D24) — 추측 금지.** 서버는 아래 표의 근거가 비면 그 판정을 하지 않고 다음 행으로 내려간다. 본문 문장(「조사 끝났습니다」 등)을 읽어 종류를 짐작하지 않는다. 화면·CLI 는 `speech`·`addressees`·`responds_to_message_id`·`delegated_lane_id` 만 읽는다.

서버가 이 순서로 판정해 `Message.speech`·`addressees` 에 저장한다(D24). 「판정 근거」는 서버가 그때 보는 것이다.

| 순서 | 말의 종류 | 판정 근거(서버가 쓰는 순간 보는 것) | 받는 쪽 |
|---|---|---|---|
| 1 | (시스템 줄) | `kind = system` | — 가운데 작은 줄, 머리 없음 |
| 2 | (HITL 카드) | `kind = hitl` | 카드가 이미 적는다(SCREEN §4.6 「Director 〈이름〉에게」) |
| 3 | **질문** | `kind = blocked_q` | `mentions`(위임자). 없으면 그 lane 의 `waiting_for` |
| 4 | **요약** | `kind = summary` | 방 전체 |
| 5 | **답** | 부모(`parent_id`)가 `blocked_q` 인 스레드 답글 | 질문 카드 작성자(+ `mentions`) |
| 6 | **메모** | `is_note`(`/note ` 접두) | 「기록만」 — 받는 쪽 없음 |
| 7 | **위임** | `colab lane delegate` 가 쓴 메시지 — **`router.Delegate` 가 자기가 쓰는 줄 안다**(본문을 되짚지 않는다). 만든 lane 이 `delegated_lane_id` | 그 lane 의 에이전트. 머리 옆에 그 서브 미션의 **지금 상태** 칩 |
| 8 | **보고** | 에이전트 메시지이고, 그 턴을 깨운 메시지(`task.trigger_message_id` — 게시하는 task 가 자기 것을 안다)의 작성자가 사람·에이전트(시스템 제외 — 이 사람이 **요청자**)이며, **요청자가 받는 쪽에 있거나 받는 쪽이 비었다**. 그 메시지 id 가 `responds_to_message_id`. **단 그 트리거가 이미 보고였고 지금 작성자가 그 보고의 받는 쪽이면 이 말은 보고가 아니라 요청(10번)이다 — 보고에 대한 보고는 없다**(v0.19.6, 아래 문단). 그 보고 안에서 본문 멘션 칩으로만 불린 에이전트(받는 쪽이 아니다)가 돌려주는 결과는 그대로 보고다 | **요청자 한 명.** 같이 부른 다른 에이전트는 라우팅(FR-3.3)이 깨우되 본문 멘션 칩으로만 보인다. 머리 아래 「↩ 〈요청자〉의 「첫 줄」에 대한 보고」 링크 → 그 메시지로 이동 |
| 9 | **지시** | 사람 메시지가 에이전트를 멘션(`mentions` 에 `agent`), `/note` 아님 | 멘션된 에이전트 |
| 10 | **요청** | 에이전트 메시지가 다른 에이전트를 멘션(7·8 아님) | 멘션된 에이전트 |
| 11 | (대화) | 그 밖 | `mentions` → 스레드 대상(부모 작성자) → 「방 전체」 순 |

   **보고에 대한 보고는 없다 (v0.19.6, Director 승인 2026-09-25 T-AGENTFIX B6)**: 턴을 깨운 메시지가 **이미 보고(`speech = report`)**이고 **지금 작성자가 그 보고의 받는 쪽(`addressees`)에 있다면**, 8번의 다른 전제를 다 갖춰도 그 턴의 말은 **요청**이다 — 받는 쪽은 그대로 보고한 쪽 한 명, `responds_to_message_id` 는 비운다. 보고를 받은 쪽이 보고한 쪽에게 하는 말은 다음 일의 지시이지 보고가 아니다. 그 요청으로 깨운 턴의 답은 다시 보고다(보고 → 요청 → 보고). 받는 쪽이 아닌 에이전트 — 보고 안의 본문 멘션 칩으로 라우팅(FR-3.3)이 깨운 쪽 — 는 보고를 받은 것이 아니라 부탁을 받은 것이므로, 그 턴에서 보고한 쪽에게 돌려주는 결과는 **원래 보고**다(`responds_to_message_id` = 그 보고, PR #343 리뷰). 실측(게임 제작 방 14:05·14:30): Lead 의 턴은 Writer 의 보고로 깨어났고, 그 턴에서 Writer 에게 한 새 지시가 「보고(→ Writer)」로 보였다. 서버는 쓰는 순간 트리거의 저장된 `speech`·`addressees` 를 보고 판정하고, 이미 쓰인 행은 같은 규칙으로 한 번 다시 계산한다(멱등).

   **메모(6번)가 위임·보고보다 위인 이유**: `/note` 는 FR-3.3 규칙 1 의 「기록만」이다 — 누구도 깨우지 않으니 받는 쪽도 없고, 트리거된 턴에서 쓴 `/note` 도 보고가 아니다.

   받는 쪽의 공통 규칙: 작성자 자신은 빼고, 같은 대상은 한 번만, `@all` 은 「모두」. **`detail`(작업 내용) 속 멘션은 받는 쪽이 아니다** — 라우팅(FR-3.3)과 같은 이유.
3. **보고는 지시와 한 흐름으로 읽힌다.** 8번의 「↩ … 에 대한 보고」는 원래 지시가 화면 밖(이전 페이지·다른 미션 거르개)이어도 누르면 `?around_message_id=` 로 그 메시지에 간다. 위임 메시지(7번)의 상태 칩은 그 서브 미션이 끝나면 「완료」로 바뀐다 — 지시를 보면 보고가 왔는지 안다.
4. **대화다운 배치.** 사람은 오른쪽 말풍선, 에이전트는 왼쪽(아바타 + 말풍선), 시스템은 가운데 작은 줄, 요약·질문·HITL 카드는 가운데 열 전폭 카드. **같은 작성자 · 같은 받는 쪽 · 같은 종류가 5분 안에 이어지면 머리를 한 번만** 그린다(묶음). 스레드 안 답글은 좌우를 나누지 않고 들여쓴 채 같은 머리를 쓴다.
5. **FR-3.1.2 세 층·미션 라벨·스레드·HITL 카드는 그대로다.** 이 FR 은 머리와 배치만 바꾼다 — 접힘 줄은 말풍선 안 대화 아래에 그대로 있다.
6. **판정은 서버가 한다 — `speech`·`addressees` 로 내려준다 (openapi v0.3.2 D24, Lead 판정 2026-09-25).** 서버는 **쓰는 순간** 안다: `router.Delegate` 는 자기가 위임을 쓰는 줄 알고(→ `delegate`·`delegated_lane_id`), 에이전트 게시는 자기 task 의 `trigger_message_id` 를 안다(→ `report`·`responds_to_message_id`). 화면이 나중에 되짚으면 「본문이 lane brief 로 끝나는가」 같은 짐작이 되고, 옛 턴마다 `listLaneTasks` 를 불러 방이 커질수록 요청이 는다. 그래서 위 표는 **서버 판정 규칙의 정본**이고(구현 `server/internal/messages/speech.go`), 목록·`getMessage`·SSE `message.created`·CLI `room messages` 가 같은 네 칸을 싣는다. 화면·CLI 는 그 칸만 읽는다 — 규칙이 화면마다 갈라지지 않는다. 이 마이그레이션 전에 쓰인 행은 같은 규칙으로 한 번 채운다(실사용 방의 타임라인이 머리 없이 남지 않게).

**FR-3.2 멘션 문법** — multica와 동일하게 링크 형식으로 고정.

```
[@Researcher](mention://agent/6f1a…)     에이전트
[@김민수](mention://user/…)               사람 (Director가 아니어도 알림)
[@all](mention://all/all)                  전원
```

UI에서 `@` 입력 시 자동완성이 링크를 삽입한다. 에이전트에게는 로스터에 붙여넣기용 링크를 제공한다.

**FR-3.3 라우팅 규칙** (multica `computeCommentAgentTriggers` 를 세션 모델로 옮김. 위에서부터 우선 적용)

1. `/note` 접두 메시지 → 트리거 없음 (기록만).
2. 에이전트 명시 멘션 → 각 멘션된 에이전트에 task 생성. 같은 에이전트 중복 멘션은 1개로 병합. 세션 참여자가 아닌 에이전트 멘션은 무시하고 경고 표시.
3. `@all` 또는 사람만 멘션 → 암묵적 라우팅 억제, 에이전트 트리거 없음.
4. 에이전트가 작성한 메시지는 **멘션이 있을 때만** 다른 에이전트를 트리거 (암묵 라우팅 없음). 하위 lane의 완료 보고는 **개별로 위임자를 깨우지 않는다** — 합류는 FR-6.5가 한 번만 수행한다(M1).
5. 사용자가 에이전트 메시지에 답글 → 그 에이전트. 스레드 안의 답글 → 스레드 소유 에이전트.
6. 그 외 사용자 메시지 → 세션 `assignee`.
7. 규칙 5로 비-assignee가 트리거된 경우, assignee 폴백 task를 5분 지연 예약하고 주 에이전트가 응답하면 취소.
8. **자식 lane이 자기 위임자를 멘션해도 트리거하지 않는다.** 게시는 하되 합류 묶음(FR-6.5)에 실어 한 번만 전달한다. §8.3이 위임자 멘션을 금지하지만 프롬프트 규칙은 어겨질 수 있으므로 서버에도 같은 규칙을 둔다(N1). **억제는 합류 그룹이 발화하기 전까지다(v0.15, K-5 결정).** 합류 뒤 자식이 위임자를 멘션하면 **일반 멘션으로 라우팅**하되, 위임자가 한 줄마다 깨어나는 것은 FR-3.4의 **lane 단위 병합**이 막는다 — 첫 멘션이 위임자 task 하나를 만들고 위임자가 그 task를 마치기 전의 후속 멘션은 그 `queued` task에 병합된다(`coalesced_message_ids`). 새 규칙을 두지 않는 이유: "위임자가 다시 지시할 때까지 억제"는 자식의 후속 발견을 조용히 묻고, 억제 해제 시점을 lane 상태 외에 한 곳 더 기억해야 한다. 합류 1회 불변식(FR-6.5)은 **합류 시스템 메시지**에 대한 것이지 합류 뒤 일반 대화에 대한 것이 아니다.
   - **억제 범위는 위임자 한 명이다.** 자식이 `@PM @QA`를 함께 멘션하면 PM(위임자)만 억제되고 QA는 정상 트리거된다.
   - **억제 기간은 그 lane이 속한 합류 그룹이 발화하기 전까지다** (리뷰#04-6). 합류가 끝난 뒤에는 일반 멘션으로 동작한다 — 그때는 묶어서 전달할 합류가 더 없으므로 억제할 이유도 없다. 이 한정 덕에 "합류 후 재진입한 자식이 위임자에게 말을 걸 수 없는" 막다른 길이 사라지고, FR-6.5의 통보 규칙도 한 줄 줄어든다.
   - 억제 기간 동안 자식이 위임자에게 **질문**하려면 `colab status set blocked`를 쓴다(FR-6.2.1). 서버는 질문과 완료 보고를 구분할 수 없지만, 상태 변경은 구분할 수 있다.

**lane 해소 규칙** — 규칙 1~8은 "task를 만든다"까지만 정한다. 그 task가 **어느 lane에 속하는지**는 다음으로 결정한다. 격리 방식과 무관하게 적용된다(격리는 workdir 바인딩만 정한다, FR-6.1).

| 순서 | 조건 | 결과 |
|---|---|---|
| 1 | 스레드 답글이고 그 스레드의 루트가 특정 lane의 task에서 나왔고, **트리거 대상이 그 lane의 에이전트다** | **그 lane** (v0.19.1 — 대상이 다른 에이전트면 규칙 1을 건너뛰고 최상위 멘션처럼 3·4로. 남의 lane에 task가 붙거나 그 lane의 queued task에 병합돼 대상이 영영 깨지 않던 결함, T-THREAD 실측) |
| 2 | `colab lane delegate`로 만들어졌다 (`--depends-on` 포함) | **항상 새 lane** — 위임의 정의가 그것이다 |
| 3 | 최상위 멘션이고 그 에이전트가 이 세션에 이미 lane을 갖고 있다 | **가장 최근 lane 재사용.** `done`·`blocked`면 재진입(`reentry_count` 증가) |
| 4 | 그 외 | **새 lane** |

- 규칙 1이 시나리오 B를 결정한다. QA가 리뷰 스레드 안에서 Frontend에게 수정을 요청하면 Frontend의 **기존 lane**으로 가고, 같은 workdir에서 이어 작업한다.
- 규칙 2가 시나리오 A를 결정한다. Lead가 `@Researcher`에게 세 항목을 각각 위임하면 lane 3개가 생긴다. 같은 에이전트라도 위임이 셋이면 lane도 셋이다.
- 규칙 3이 없으면 Director가 무심코 던진 후속 멘션마다 lane이 늘어난다.
- **사람이 새 lane을 만드는 수단** (t-2): 규칙 3이 항상 재사용이므로 Director는 같은 에이전트에게 병렬 작업을 줄 방법이 없다(에이전트와 달리 `colab lane delegate`를 쓸 수 없다). 메시지 작성창에 **"새 lane으로 보내기"** 토글을 두고, 켜면 규칙 3을 건너뛰고 규칙 4(새 lane)로 간다. 켜져 있으면 전송 버튼 옆에 그 사실을 표시해 실수로 병렬 작업이 생기지 않게 한다.

**FR-3.4 턴 진행 중 도착한 메시지 처리** — **어떤 메시지도 진행 중인 턴을 취소하지 않는다.** 작성자가 누구든, 명시 멘션이든 암묵 라우팅이든, 실행 중인 에이전트를 향한 메시지는 **항상 큐잉된다.**

- **병합 단위는 lane이다 (C).** 같은 **lane**에 대한 `queued` task를 하나로 병합한다. 병합된 메시지 id는 `coalesced_message_ids`에 보존하고, 다음 run의 턴 프롬프트에 도착 순서대로 인용한다. lane 해소 규칙이 먼저 lane을 정하고 그 다음 병합한다 — 에이전트 단위로 병합하면 lane 3개를 가진 Researcher(시나리오 A)의 서로 다른 lane 메시지가 엉뚱한 lane에서 실행된다.
- lane 하나당 진행 중인 턴은 항상 1개다(FR-6.3). 여러 lane은 동시에 진행된다.

**취소는 UI 조작으로만 한다.** 세션 화면의 lane 보드에서 `running` 상태인 lane 카드마다 두 개의 동작을 제공한다(§6, M5).

| 동작 | 절차 | 결과 |
|---|---|---|
| **중단하고 다시 지시** | 그 lane의 에이전트를 멘션한 메시지 작성창이 열린다. 전송하면 진행 중인 턴을 취소하고 **새 메시지만으로** 다시 시작한다 | lane은 `running` 유지. **새 task**를 만들고 `restarted_from_task_id`로 잇는다 (B) |
| **중단** | 확인 후 진행 중인 턴만 취소한다. 새 지시 없음 | lane `failed(cancelled)`, 활동 피드에 "사람이 중단함" |

**재지시는 새 task다 (B).** 재시도(같은 지시를 다시)와 재지시(다른 지시)를 `attempt` 하나로 겹치면 안 된다. `task.trigger_message_id`는 단일 필드이므로 같은 행에 덮어쓰면 트리거가 바뀐 이력을 잃고, `<resumed>` 프롬프트가 "이전 시도가 중단됨"이라고만 말해 에이전트가 **이전 지시를 이어가려 한다.**

| 구분 | 처리 |
|---|---|
| **재시도** (실패·타임아웃·예산) | 같은 task, `attempt` 증가. 같은 `trigger_message_id` |
| **재지시** (중단하고 다시 지시) | **새 task**, `attempt`는 1부터. `restarted_from_task_id`로 이전 task를 가리킨다. 런타임 세션은 lane이 갖고 있으므로(§7) 따로 물려받을 것이 없다 |

재지시 프롬프트는 `<resumed>`가 아니라 **새 지시만** 담는다(§8.4). 이전 지시는 히스토리에 남아 있고, 사람이 방향을 바꾼 것이므로 이어가면 안 된다.

- **되돌리기 어려운 작업 중에는 취소를 보류한다.** 마지막 `task_event`가 파일 편집 또는 셸 명령이고 그 완료 이벤트가 아직 오지 않았으면 완료를 기다렸다가 취소한다. 최대 30초를 넘기면 그대로 취소하고 그 사실을 활동 피드에 남긴다.
- 취소 절차는 §8.2.2를 따른다(권한 요청 응답 → `session/cancel` → 드레인). 프로세스를 즉시 죽이지 않는다.
- 취소 권한은 **그 lane 이 매인 미션의** Director·deputy, 그리고 **방장·부방장**에게 준다(v0.19) — 미션 밖 lane 은 방장·부방장만 멈출 수 있다. 다른 참여자는 버튼이 비활성이다. **deputy는 취소를 즉시 쓸 수 있다** — 폭주하는 작업을 멈추는 것은 지연될수록 손해가 커지므로, 기한을 기다리게 하는 승인(FR-5.4)과 달리 시점 제한을 두지 않는다(t-3).

> **왜 정책 설정을 없앴나 (v0.6).** v0.4는 `steer`를 기본값으로 두어 Director의 "좋아요, 계속해" 한 마디가 진행 중인 턴을 죽였다. v0.5는 판별자를 "명시 멘션"으로 좁혔지만, 이번엔 **에이전트끼리의 멘션**이 남의 턴을 죽였다 — QA가 작업 중인 Backend에게 "이 부분 확인해줘"라고 쓰면 Backend의 턴이 취소된다. 에이전트 간 멘션은 사람보다 훨씬 빈번하므로 이 정책을 켠 세션은 턴이 계속 끊긴다.
>
> 리뷰 #02는 "명시 멘션 **AND** 작성자가 Director"를 제안했다. 맞는 방향이지만 조건이 셋(정책 설정 + 명시 멘션 + 작성자)이나 겹친다. 그럴 바에는 **메시지에서 취소를 완전히 떼어내고 UI 조작 하나로 모으는 편**이 낫다. 설정 축이 하나 줄고, "어떤 메시지도 진행 중인 작업을 죽이지 않는다"는 불변식이 생겨 사용자가 규칙을 외울 필요가 없다. Director가 방향을 바꾸고 싶으면 버튼을 누르면 되고, 그 한 번의 클릭이 파괴적 동작에 대한 명시적 동의가 된다.

**FR-3.5 루프 방지** — 에이전트 간 상호 멘션은 허용하되 세 가지 상한을 둔다. **세션 누적 hop 수는 쓰지 않는다** — 정상적인 세션도 라운드를 몇 번 돌면 누적치에 닿아 멀쩡한 작업이 멈춘다.

| 상한 | 기본값 | 의미 |
|---|---|---|
| `max_chain_depth` | 8 | 사람의 메시지에서 시작해 멘션이 연쇄된 **깊이**. 사람이 개입하면 0으로 리셋 |
| `max_hops_per_hour` | 60 | 세션의 **시간당** 에이전트 간 트리거 수 |
| `max_pair_roundtrips` | 5 | 같은 두 에이전트가 **연속으로** 주고받은 횟수. 사이에 제3자나 사람이 끼면 리셋 |

- `max_chain_depth`는 FR-6.2의 위임 DAG 깊이보다 커야 한다. Lead → 실무자 → 리뷰어 → Lead 정도가 깊이 4이므로 기본 8이면 정상 위임을 막지 않는다.
- 초과 시 **방** `blocked_reason: loop` + **방장**에게 HITL(부재 위임 FR-2A.3, v0.19). 상한은 워크스페이스 설정에서 조정. **어느 상한에 걸렸는지 구분해 기록한다** — `paused(loop)` 하나로 뭉치면 Director가 무엇을 조정해야 할지 알 수 없다.
- **재개했을 때 카운터.** Director가 `paused(loop)` 세션을 재개하면 `max_chain_depth`와 `max_pair_roundtrips`는 **0으로 리셋**되고, `max_hops_per_hour`는 **리셋되지 않는다**(시간 창이 지나야 준다). 위 두 개는 이미 "사람이 개입하면 리셋"이 정의이고 재개는 사람의 개입이다 — 리셋하지 않으면 재개하자마자 같은 상한에 다시 걸려 재개가 무의미해진다. 반대로 셋 다 리셋하면 재개를 반복해 상한을 무한히 우회할 수 있으므로, **시간당 상한이 그 우회를 받아낸다.**
- **서버측 억제는 구조적 규칙으로만 한다.** 자식→위임자 완료 보고 멘션은 트리거하지 않는다(규칙 8). 이것은 관계로 판정하므로 오탐이 없다.
- **내용 기반 억제(빈 확인 메시지 휴리스틱)는 넣지 않는다.** "본문이 짧고 요청 신호가 없으면 억제"는 `@QA 리뷰 부탁해` 같은 **정상 위임을 조용히 삼킨다** — 짧고, 물음표도 숫자도 없다. 길이가 아니라 "요청을 하는가"가 판별자여야 하는데 LLM 없이는 판정할 수 없고, §8.1에 따라 동기 API 경로에서는 LLM을 호출하지 않는다. 핑퐁은 위의 `max_pair_roundtrips`가 결정적으로 잡는다. **오탐으로 일을 잃는 것보다 몇 턴 낭비가 낫다.**
- §8.3의 "빈 확인 메시지 금지"는 프롬프트 규칙으로 남긴다. 어겨도 상한이 받아낸다.

**FR-3.6 트리거 미리보기** — 메시지 전송 전 "이 메시지는 A, B를 트리거합니다 (프로파일: …)" 표시. `suppress_agent_ids`로 개별 억제 가능.

### FR-4. 공유 컨텍스트

**v0.19**: 맥락은 **방 안에서 자동으로 공유**되고, 다른 방은 **요청해서 읽는다**(FR-4.5). 에이전트 자신의 맥락(런타임 대화 세션·작업 폴더)은 **방마다 따로**다(§3 용어표 Agent Context).

**FR-4.1 컨텍스트 구성** — 각 run에 다음이 제공된다. multica처럼 **안정적인 브리프는 파일**(런타임의 프로젝트 지시 파일, 예: Claude Code의 `CLAUDE.md`)로, **턴별 내용은 프롬프트**로 전달한다.

| 구간 | 내용 | 전달 방식 |
|---|---|---|
| 안정 브리프 | 에이전트 identity + instructions, 워크스페이스 규칙, **방 설명 + (그 턴이 속한) 일의 goal/criteria/종료조건**, 참여자 로스터(멘션 링크·역할 설명 포함), 첨부 컨텍스트 요약, 툴/CLI 사용 규약 | **런타임 지시 파일이 기본 경로**(Claude Code `CLAUDE.md`, Antigravity·Hermes `AGENTS.md`). 시스템 프롬프트 전달 수단이 있는 경로에서는 그쪽을 우선한다 — 판정 규칙은 §8.4 |
| 결정 기록 | HITL 답변, 확정된 결정 목록 | 브리프 파일 뒤쪽 섹션 |
| 방 히스토리 | **세 묶음**(v0.19, V19-A P1-6): ① 방의 최근 `room_history.recent_messages` 개(워크스페이스 설정, 기본 50) ② **그 턴이 속한 일의 메시지 전부**(일 밖이면 생략) ③ 방의 **결정 기록 전부** + 가장 최근 「여기까지 정리」 요약. 방은 끝나지 않아 ①만으로는 오래된 결정이 조용히 잘린다 — **잘린 구간이 있으면 브리프에 한 줄로 적는다**. 합류 이전 메시지도 읽을 수 있다(FR-2.2) | 턴 프롬프트 |
| 트리거 | 트리거 메시지(들) 원문 인용, 위임 브리프, 프로파일 정보 | 턴 프롬프트 |

에이전트는 `colab room get`, `colab room messages --thread <id> --tail 30` 로 필요한 만큼 읽는다(옛 `session get`·`session messages` 는 같은 것을 가리키는 별칭으로 한 판 유지 — `colab-cli.md`).

**FR-4.2 결정 기록(Decision Log)** — HITL 응답과 에이전트의 `colab decision record` 호출을 세션 단위 목록으로 보존. 새 에이전트가 합류해도 "왜"를 알 수 있게 한다.

- **부재와 장애를 구분한다.** 결정 기록이나 이전 세션 요약을 조회할 때, "확실히 비어 있음"과 "조회 실패"는 다르게 처리한다. 조회가 **실패**하면 브리프에 해당 구간을 아예 넣지 않는다. 빈 구간을 넣으면 에이전트가 "결정된 바 없음"으로 읽고 이미 내려진 결정을 뒤집는다. 실패 사실은 활동 피드에 오류로 남긴다.

**FR-4.3 아티팩트 저장소** — `colab artifact submit --name --type --file`. 버전 관리(같은 이름 재제출 시 v2). 세션 사이드바에 노출. 워크트리 격리 세션에서는 diff/브랜치 참조도 아티팩트 타입으로 등록 가능.

**FR-4.4 컨텍스트 재사용** — **v0.19 에서 버린다**(§12.1-7 확정): 같은 팀과 이어 쓰는 것은 방의 정의이고, 다른 방은 FR-4.5 로 읽는다. **정리 대상 셋**(R0 에서): §7 `room_context` 표, 계약 `Session.context[]`·`context_reuse_override`, 워크스페이스 설정의 「컨텍스트 재사용 상한」. 아래 v0.18 원문은 기록으로만 남긴다.

완료된 세션을 새 세션의 컨텍스트로 첨부 가능 (요약 + 아티팩트 링크). 요약 길이 상한은 **워크스페이스 설정** `context_reuse.max_summary_tokens` (기본 2,000) 와 `context_reuse.include_artifacts` (기본: 링크만) 로 조정한다. 세션 생성 시 개별 오버라이드 가능.

- **v1 범위 (P4)**: 세션 생성 폼에서 이전 세션을 골라 첨부하는 것과 상한 설정은 **v1**이다. v1.1의 "세션 컨텍스트 재사용 UI"는 **어떤 부분이 주입됐는지 미리보고 취사선택하는 화면**을 말한다 — v1은 요약 전체를 상한까지 넣는다.

**FR-4.5 다른 방 읽기 (v0.19)** — 실행 중인 에이전트가 **요청해서** 다른 방의 맥락을 읽는다. 자동 주입은 하지 않는다: 방이 늘수록 프롬프트가 부풀고, 읽을 이유가 없는 방까지 섞이면 어느 방의 결정인지 에이전트가 헷갈린다.

```
colab room list [--query <말>]                     → 읽을 수 있는 방 목록(이름·설명·마지막 활동·내가 참여 중인가)
colab room read --room <id> [--tail N] [--query]   → 그 방의 요약 + 최근 메시지 + 결정 기록 + 산출물 목록
colab artifact get <id>                            → (기존) 산출물 본문
```

**권한 — 사람 originator 기준(FR-9 보안 원칙 그대로).** 에이전트가 읽을 수 있는 방은 다음을 **둘 다** 만족하는 방이다.

1. **그 task 를 일으킨 사람 originator** 가 그 방의 참여자다 — 에이전트를 거쳐 권한이 오르지 않는다.
2. 그 에이전트가 그 방의 참여자이거나, 현재 방에 **참고 방 링크**(`room_link`)로 그 방이 연결되어 있다.

`[V19-B]` **originator 가 없는 턴**(합류 통보·질문 기상·재진입 통보·폴백 재시도로 깨어난 턴)은 **자기를 깨운 task 의 originator 를 물려받는다.** 그래도 없으면 다른 방을 읽을 수 없고(`403`), 에이전트에게는 "이 턴은 사람의 요청에서 시작하지 않아 다른 방을 읽을 수 없습니다"가 간다. **방장으로 대체하지 않는다** — 그것이 바로 FR-4.5 가 막으려는 권한 상승이다.

둘 중 하나라도 아니면 `403`이고 **목록에도 보이지 않는다**(존재를 숨긴다). **originator 가 그 방을 떠난 뒤**(HITL 24시간 답, 재바인딩 뒤 재개 등) 읽기가 갑자기 막히는 경우는 사유를 구분해 알린다 — "요청자가 그 방의 참여자가 아닙니다"(에이전트 자신의 권한 문제와 헷갈리지 않게). **사람 쪽에도** 같은 사실이 간다 — 읽으려 한 방의 활동에 「〈사람〉이 〈방〉을 떠나 〈에이전트〉의 참고 읽기가 막혔습니다」(SCREEN G-7). 이 문장은 **존재 숨김의 예외**다 — 그 사람이 한때 참여했던 방이라 이름이 이미 알려져 있고, 사람이 원인을 알아야 풀 수 있다. originator 가 한 번도 참여하지 않은 방이면 방 이름을 적지 않는다. 참고 방 링크는 방장이 건다 — "이 방은 저 방을 참고한다"를 사람이 명시하는 자리이며, 1번은 그래도 검사한다.

- `[V19-C]` **권한은 읽는 시점에 다시 검사한다.** 1·2 조건은 dispatch 때가 아니라 **`colab room read`·`room list` 를 호출한 그 순간** 판정한다. task 는 오래 산다(HITL 24시간, 재시도, `rate_limited` 대기) — dispatch 때 한 번만 보면 **이미 나간 사람의 권한으로 읽는 창**이 열린다.
- **읽은 사실을 남긴다.** **읽은 방**에는 그 task 의 `task_event`(`class=status`, 사람이 읽는 문장은 `payload.args.note`)와 활동에, **읽힌 방**에는 `activity_log` + **타임라인 시스템 메시지 한 줄**: 「〈사람 이름〉의 요청으로 〈에이전트〉가 이 방을 읽었습니다(요약 + 최근 N건)」. 맥락이 새어 나가는 경로를 **읽힌 쪽 사람도** 봐야 한다.
- **거부도 말한다.** 권한 두 조건 중 하나라도 어긋나 `403` 이면 **읽으려 한 방의 활동**에 남긴다(어느 방인지는 적지 않는다 — 존재를 숨기는 규칙 그대로): 「〈에이전트〉가 읽을 수 없는 방을 조회했습니다」.
- **참고 방 링크의 대상은 거는 사람이 참여자인 방이어야 한다**(아니면 `403 not_participant_of_target`) — 모르는 방의 존재를 링크로 탐색하지 못하게 한다.
- **참고 방 링크는 방 설정에서 방장·부방장(또는 ws owner·admin)이 건다.** 걸고 푸는 것을 **양쪽 방 모두에** 시스템 메시지로 남긴다 — 한쪽만 알면 유출 경로가 한쪽에만 보인다.
- **읽기는 읽기다.** 다른 방에 메시지를 쓰거나 일을 열 수 없다. 옮기고 싶으면 사람이 그 방에서 하거나, 산출물로 제출한다.
- **양·비용**: 한 턴에 읽을 수 있는 방 수·분량에 상한(워크스페이스 설정 `room_read.max_rooms_per_turn` 기본 3, `max_tokens` 기본 4,000). 넘으면 잘라 주고 그 사실을 알린다.
- 읽은 내용은 그 턴의 프롬프트에만 들어간다 — **에이전트의 방 맥락에 영구 편입되지 않는다**. 필요하면 에이전트가 `colab decision record` 로 현재 방에 옮겨 적는다.

### FR-5. HITL (Human-in-the-loop)

multica에서는 에이전트가 사람을 멘션하거나 `blocked`로 상태를 바꾸는 것이 HITL의 전부다. 본 서비스는 이를 1급 객체로 만들고 수신자를 **Director**로 고정한다.

**FR-5.1 HITL Request 타입**

| 타입 | 에이전트 CLI | Director 응답 UI |
|---|---|---|
| `question` | `colab hitl ask --question --context --default <제안>` | 자유 텍스트 |
| `choice` | `colab hitl ask --question --option A --option B --default A` | 라디오/체크 + 기타 |
| `approval` | `colab hitl approve-request --summary --artifact <id>` | 승인 / 거절(사유) |
| `info` | `colab hitl request-info --what --why` | 파일/링크/텍스트 첨부 |

`--default`(스키마 `proposed_default`)는 `question`·`choice`에서 **필수**다. 기한이 지났을 때 진행할 값이 없으면 세션이 무기한 멈춘다. Director UI에도 "에이전트 제안: …"으로 함께 보여준다.

**FR-5.2 동작**

- **HITL 툴 호출은 턴을 끝낸다.** 툴은 "요청이 등록되었으니 턴을 종료하라"를 반환하고 에이전트는 정상 종료한다. 프로세스가 답을 기다리며 살아 있지 않는다(FR-5.4).
- task는 `waiting_human`이 되고, 에이전트 상태는 FR-1.3의 파생 규칙에 따라 `waiting_human`으로 계산된다(running task가 없을 때). 세션 타임라인에 카드로 게시.
- Director 인박스에 `action_required`로 등록. 이메일/푸시 알림 옵션. 다른 멤버는 카드를 볼 수 있지만 응답 버튼은 Director에게만 활성화.
- Director가 부재일 때를 위해 세션에 `deputy_director_user_id`(선택)를 둘 수 있다. 기한의 절반이 지나면 deputy에게 위임 알림.
- 응답이 확정되면 **새 attempt로 재개**하고, 응답은 결정 기록에 저장한다(FR-5.4).
- `due_in` 지정 가능(기본 24시간). 기한 초과 시 (a) 대기 유지 (b) `proposed_default`로 진행 — **`question`·`choice`에만 해당하며** 세션 `autonomy` 설정에 따른다. `approval`·`info`는 항상 대기한다(FR-5.4).
- 다른 에이전트는 대기 중인 HITL과 무관한 lane을 계속 진행할 수 있다.

**FR-5.3 세션에 참여하는 사람 (P7)** — 사람의 역할은 **셋뿐이다.**

> **`[v0.19]` 방에서 사람의 참여는 초대로 정해진다(V19-A P0-1).** v0.18 까지는 "세션에 초대 절차는 사람에게 없다"(워크스페이스 멤버는 누구나 열람·게시)였다. 방은 지속되는 공간이라 **누가 이 대화에 있는가**가 명시되어야 한다 — 방 설정 `visibility` 로 둘 다 살린다: `workspace`(기본 — 워크스페이스 멤버 누구나 열람·게시, v0.18 과 같다) · `invited`(초대된 사람만). 어느 쪽이든 **방장이 사람을 초대**할 수 있고, 워크스페이스 owner·admin 은 감사 목적으로 모든 방을 **열람**한다(게시는 참여 뒤). `invited` 방의 감사 열람은 `activity_log` 에 남는다.
>
> **v0.19 단위(R1-2)**: 이 표의 열은 두 단위로 갈린다 — **열람·게시·lane 취소는 방**, **HITL 응답·승인과 일시정지·종료는 일**이다. 그리고 v0.19 부터 **사람도 방의 참여자다**(`room_participant` 한 표, FR-2.2) — 아래 "초대라는 절차는 사람에게 없다"는 문장은 v0.18 의 것이고, v0.19 에서는 **방에 초대된 사람(과 워크스페이스 owner·admin)이 열람·게시한다**. `participant` 라는 말은 이제 사람·에이전트 둘 다에 쓴다.

| 역할 | 열람 | 게시·멘션 | HITL 응답·승인 | lane 취소 | 세션 일시정지·종료 |
|---|---|---|---|---|---|
| **Director**(일) | ✅ | ✅ | ✅ | ✅ | ✅ |
| **deputy** (선택) | ✅ | ✅ | 기한 절반 경과 후 | **즉시** | ❌ |
| **방 참여자**(사람) | ✅ | ✅ | ❌ | ❌ | ❌ |
| **방장** | ✅ | ✅ | 일 밖 task·방 상한 HITL 만 | ✅ | 방 보관·삭제(진행 중 일 없을 때) |
| **부방장**(선택, 방마다 한 명) | ✅ | ✅ | 일 밖 task·방 상한 HITL — **기한 절반 경과 후** | ✅ | 「이 방 멈춤」·해제 · 보관·해제 (삭제 ❌) |

- **부방장은 참여자 초대·퇴장·방 설정·참고 방 링크도 방장과 같이 한다**(V19-GAP P-A). 방 삭제와 방장 넘기기는 방장(과 ws owner·admin)만이다.

- v0.18: 워크스페이스 멤버는 누구나 세션을 열람·게시했고 사람에게 초대 절차가 없었다. **v0.19: 방의 참여자(사람)만 열람·게시하고, 초대는 방장이 한다**(FR-2.2). 워크스페이스 owner·admin 은 모든 방에 대해 같은 권한을 갖는다. 멤버의 메시지는 FR-3.3 규칙대로 라우팅된다.
- **비공개 방**(워크스페이스 멤버 전체가 아닌 특정 사람만 열람)은 v0.19 의 방 참여자 모델로 자연히 성립한다 — v0.18 의 "비공개 세션은 v1.1" 항목은 이 전환으로 해소된다. (v0.18 원문:)
- **비공개 세션**(특정 멤버만 열람)은 v1에 없다. 필요해지면 v1.1에서 `visibility` 필드와 `observer` 역할을 함께 도입한다.
- Director는 언제든 메시지로 지시할 수 있고, `supervised` 모드에서는 Lead의 모든 위임 메시지가 Director 승인 후에만 전송된다(v1.1).
- HITL 응답과 승인, lane 취소, 세션 종료는 위 표대로 제한된다. 권한이 없는 사람에게도 **버튼은 보이되 비활성**으로 두고 사유를 표시한다.

**FR-5.4 HITL 일시 정지와 재개 모델** — 우리 run은 CLI 프로세스 하나이고 **재개 가능한 "단계"가 없다.** 재개 수단은 런타임 세션 resume뿐이며 그마저 런타임에 따라 신뢰도가 다르다(§8.2.5). 따라서 다음과 같이 정의한다.

- **요청 = 턴 종료.** HITL 툴은 요청을 등록하고 "턴을 끝내라"를 반환한다. 프로세스가 답을 기다리며 블로킹하지 않는다. 블로킹하면 Antigravity의 `--print-timeout`이 끊고, heartbeat(FR-7.1의 3분 무응답)와 충돌하며, 워크트리·컨테이너·동시성 슬롯을 최대 24시간 점유한다.
- **`waiting_human`은 동시성 슬롯을 점유하지 않는다.** 에이전트의 `max_concurrent_tasks`와 세션 `max_parallel_lanes` 계산에서 제외한다. 반면 **작업 공간(워크트리·컨테이너)은 보존한다** — 재개 시 이어서 작업해야 하기 때문이다.
- **응답 = 새 attempt.** 재개는 다음 순서로 시도한다.
  1. **런타임 세션 resume** — **lane의** `runtime_session_ref`(런타임 종류 + 세션 id + provenance)로 이전 세션을 이어받고, "질문 / 답변 / 승인 여부 / 사유"를 담은 턴 프롬프트를 보낸다. 이 경로는 HITL 응답뿐 아니라 **모든 재진입**이 공유한다.
  2. **콜드 스타트 폴백** — resume이 불가하거나 거부되면(§8.2.5) 브리프 파일 + 세션 히스토리 + 결정 기록으로 새 세션을 시작하고, 같은 답변 프롬프트에 "이전 시도가 중단되었으니 작업 공간의 현재 상태를 먼저 확인하라"를 덧붙인다.
- **후속 분기**: 재개 프롬프트에 `approved: true|false`와 사유가 들어가 거절 경로를 분기할 수 있다. 거절은 정상 흐름이며 실패가 아니다.
- **응답 기록은 멱등하다.** 서버는 (a) 아직 `open`이고 (b) 기한이 지나지 않았으며 (c) 응답자가 `approver_spec`에 부합하는지 검증한 뒤 저장한다. 같은 요청에 대한 두 번째 응답은 오류가 아니라 무시된다.
- **`approver_spec`**: `director`(기본) / **`room_owner`**(v0.19 — **일에 속하지 않은 task 의 HITL 기본값**: 일이 없으면 Director 가 없으므로 방장이 받는다, FR-2A.1) / `any_member` / 특정 사용자 id. **역할 기반 지정(예: "리뷰어 아무나")은 v1에서 지원하지 않으며, 지원하지 않는 spec은 저장 시점에 거부한다** — 조용히 아무나 승인 가능해지는 상태를 만들지 않는다(fail closed).
- **`director` spec의 정확한 의미는 "Director, 그리고 기한의 절반이 지난 뒤에는 deputy"다 (M7).** deputy에게 위임 알림만 보내고 응답 권한을 주지 않으면 알림이 무의미하다.

**기한 만료 시 동작 (M7)** — 타입에 따라 다르다.

| 타입 | 기한 만료 시 |
|---|---|
| `question` · `choice` | 세션 `autonomy`에 따라 (a) 계속 대기 (b) `proposed_default`로 진행 |
| `approval` · `info` | **항상 계속 대기한다. 자동 진행하지 않는다.** |

**승인은 어떤 경우에도 자동으로 나지 않는다.** `autonomy: autonomous`에서도 마찬가지다. 침묵을 승인으로 해석하면 사람 게이트의 의미가 사라지고, 거절로 해석하면 정상 작업이 기한 때문에 죽는다. 둘 다 틀렸으므로 기다린다. 대신 기한이 지나면 상태를 바꾸지 않고 **`open`을 유지한 채 `overdue` 플래그**를 세운다 — 늦었어도 답할 수 있어야 하고, 인박스는 `overdue`를 맨 위로 올린다.

상태 값은 `open` / `answered`(사람이 답함) / `auto_answered`(`question`·`choice`가 기한 만료로 `proposed_default`로 진행됨) 셋이다. **`expired`라는 값은 두지 않는다** — 만료가 곧 종료를 뜻하지 않기 때문이다(s-9).
- **"Needs Action" 질의**: 인박스는 "나에게 열려 있는 HITL 요청 + 기한이 임박한 항목"의 합집합 질의로 만든다. 이것이 사람 쪽 진입점의 유일한 정의다.

> **승인 토큰을 두지 않는 이유.** v0.4는 랜덤 토큰의 SHA-256 해시를 저장해 "저장소가 유출되어도 위조 불가"를 노렸다. 그러나 응답은 **인증된 Director 세션**으로 들어오고 서버가 `approver_spec`을 검증하므로, 토큰이 추가로 막는 것이 없다. DB에 쓸 수 있는 공격자라면 `approved=true`를 직접 쓰면 된다. 토큰이 의미를 갖는 경우는 **로그인 없는 원클릭 승인 링크**뿐이고, 그때는 링크 유출이 곧 승인이므로 1회성·짧은 수명·재확인 페이지가 함께 필요하다. v1은 원클릭 승인을 넣지 않으므로 토큰을 제거하고 인증 세션 + 멱등 응답 레코드만 유지한다. 원클릭 승인을 도입할 때 토큰 모델을 다시 설계한다.

### FR-6. 병렬 실행 (Lanes)

Orca의 워크트리 병렬 모델을 메시징 세션에 적용한다.

**FR-6.1 Lane과 Workdir의 분리** — v0.4는 lane 하나가 "동시성 단위"와 "작업 공간"을 겸했다. 그 결과 같은 에이전트에게 항목 3개를 병렬 위임하면(시나리오 A) 워크트리가 3개 생기고, 리뷰어가 남의 워크트리를 어떻게 보는지도 정의되지 않았다. 두 개념을 분리한다.

| 개념 | 정의 | 수명 |
|---|---|---|
| **Lane** | 실행 트랙. 위임 하나가 lane 하나를 만든다. 같은 에이전트가 여러 lane을 동시에 가질 수 있다. 동시성·의존성(DAG)·보드 표시의 단위 | 위임 → 완료 |
| **Workdir** | 실제 작업 디렉토리. 격리 방식이 바인딩 규칙을 정한다(FR-6.4) | GC까지(미션 닫힘 또는 `last_used_at` + 보존 기한, FR-6.4) |

**바인딩 규칙**

| 격리 | Workdir 단위 | 이유 |
|---|---|---|
| `worktree` | **방×에이전트당 1개** (`colab/<room>/<agent>` 브랜치, v0.19) | 같은 에이전트의 여러 lane이 같은 체크아웃을 공유한다. 코드 작업에서 한 에이전트가 자기 작업을 여러 갈래로 동시에 쓰는 경우는 드물고, 워크트리 폭증과 병합 지옥을 막는다. 같은 에이전트의 lane들은 순차 실행된다(FR-6.3) |
| `container` | **미션×에이전트당 1개** (`[FOLDERS]`, 옛 규칙: lane당 1개) | 아래 `none`과 같다 |
| `none` | **미션×에이전트당 1개** 하위 폴더 + 미션 공용 `_shared` (`[FOLDERS]`, 옛 규칙: lane당 1개) | 같은 미션의 동료가 서로의 결과를 찾을 수 있어야 한다. 옛 lane당 폴더에서는 코드 협업 미션의 Developer 가 동료 파일을 못 찾았고 다른 에이전트는 절대 경로를 grep 해 우회했다(게임 제작 방 실측, 2026-09-25). 같은 에이전트의 병렬 lane(시나리오 A Researcher 3-way)은 그 폴더를 **함께 쓰며 병렬을 유지**한다 — 턴 프롬프트가 새 파일 이름에 갈래 표지를 넣으라고 지시한다 |

**폴더 배치 (`[FOLDERS]` — 설계 제안, Lead 승인 전. 근거·선택지 `plan/research/MISSION_FOLDERS.md`)** — 작업 폴더는 **방 → 미션 → 에이전트** 순서로 놓인다.

```
<workdir_root>/rooms/<방>/<미션>/<에이전트>/   ← 에이전트 cwd (none·container)
<workdir_root>/rooms/<방>/<미션>/_shared/       ← 미션 공용
<workdir_root>/rooms/<방>/_room/<에이전트>/     ← 미션 밖 턴
<workdir_root>/rooms/<방>/_worktrees/<에이전트>/ ← worktree 격리 체크아웃(방×에이전트)
```

- 각 조각은 `<슬러그>-<id 앞 8자리>` 이며 **만들 때 한 번 정해 저장한다.** 방·미션·에이전트 이름이 바뀌어도(FR-2.1.2) 폴더는 옮기지 않는다 — 실행 중인 lane 의 cwd 와 런타임 세션 resume 이 경로에 매여 있다. 화면은 현재 이름을 보여 준다.
- **경로는 격리와 무관하게 서버가 짓는다**(probe `workdir_root` + 위 규칙). 옛 배치(`sessions/<방>/<lane>/`, `worktrees/<슬러그>/<에이전트>/`)의 폴더는 **옮기지 않는다** — 그 행에 매인 lane 은 옛 경로로 계속 일하고, 새 행부터 새 배치다. 옛 폴더는 아래 GC 규칙으로 자연히 사라진다.
- 테스트 채팅(FR-1.8.1)은 방이 없으므로 `.colab/testchat/<id>` 그대로다.

- **재위임은 기존 lane을 재사용한다.** 이미 `done`인 에이전트에게 수정을 요청하면(시나리오 B의 "Frontend에 수정 요청") 그 에이전트의 **기존 lane이 `done → running`으로 재진입**하고 같은 workdir에서 이어 작업한다. 새 lane을 만들지 않는다 — 같은 작업의 이력을 한 lane에 모으기 위해서다. (workdir은 격리 방식이 정하므로, `worktree`에서는 새 lane을 만들어도 워크트리는 어차피 같다.)
- **같은 미션 안에서는 폴더로 읽고, 미션·방 경계를 넘을 때는 아티팩트로 읽는다 (`[FOLDERS]`, 옛 문장 「크로스 lane 읽기는 아티팩트로만」을 대체).**

  | 대상 | 읽기 | 쓰기 |
  |---|---|---|
  | 자기 폴더 · 같은 미션 `_shared` | ✅ | ✅ |
  | 같은 미션 동료의 폴더(`worktree` 면 동료의 체크아웃) | ✅ 참고용 | ❌ |
  | 다른 미션 · 미션 밖 · 다른 방 | ❌ — 아티팩트, 다른 방은 FR-4.5 방 읽기 | ❌ |

  쓰기를 자기 폴더와 `_shared`로 묶는 이유는 동시 쓰기 충돌이다 — `worktree`에서 두 에이전트가 한 트리에 쓰면 저장소 손상이다(E13-08). **리뷰·병합의 판정 대상은 여전히 제출된 아티팩트다**(FR-4.3, 시나리오 B) — 동료 폴더 읽기는 맥락을 얻는 길이지 판정 근거가 아니다. 서버는 동료 폴더 경로를 **턴 프롬프트 `<folders>` 블록**으로 알린다(§8.4). 경로를 알리지 않는 것이 격리를 지키지 못한다는 것은 실측이 보였다 — 경로를 grep 해 찾아낸다.

**FR-6.2 lane 상태와 의존성** — `colab lane delegate --agent --brief --depends-on <lane_id> --profile <name>` 으로 DAG를 구성. 의존 lane이 완료되면 자동 시작.

lane 상태: `queued / running / waiting_human / blocked / paused / done / failed`. lane 보드(칸반)에 그대로 표시한다. `paused`는 task의 `paused(budget)`를 lane 층에 올린 것이다(P5) — 예산 초과는 실패가 아니라 사람의 판단을 기다리는 상태이므로 `failed`와 구분해 보여야 한다.

| 상태 | 의미 | 합류 판정 |
|---|---|---|
| `queued` `running` | 대기·실행 중 | 진행 중 |
| `waiting_human` | Director의 HITL 응답 대기(FR-5.4) | 진행 중 |
| **`blocked`** | **위임자의 답을 기다린다.** 에이전트가 `colab status set blocked --note "<질문>"` 으로 턴을 끝냈다 | **종료로 취급** |
| **`paused`** | 예산·시간 상한 초과로 멈췄다. Director가 계속을 승인하면 재개된다(FR-7.3) | 진행 중 |
| `done` `failed` | 완료·실패 | 종료 |

- **`blocked`는 종료 상태로 취급한다(A).** 그래야 나머지 자식이 끝났을 때 합류가 발화해 위임자가 질문을 받는다. 종료로 치지 않으면 질문한 자식 때문에 합류가 영원히 막힌다.
- lane 상태 머신은 `done → running`, `blocked → running` 재진입을 허용한다(`reentry_count` 증가).
- `blocked`와 `waiting_human`은 기다리는 대상이 다르다 — 전자는 **위임한 에이전트**, 후자는 **사람(Director)**. 답을 줄 수 있는 주체가 다르므로 상태도 나눈다.

**FR-6.2.1 자식이 위임자에게 묻는 경로 (A)** — 규칙 8은 자식의 위임자 멘션을 억제한다. 서버가 "완료 보고"와 "질문"을 구분할 수 없기 때문인데, 그렇다고 질문할 길이 없으면 Backend가 "API 응답 형식이 스펙에 없는데 JSON으로 할까요?"를 물을 방법이 사라진다. 다음 경로를 쓴다.

1. 자식이 `colab status set blocked --note "<질문>"` 으로 **턴을 끝낸다.** lane은 `blocked`, workdir은 보존된다.
2. **서버가 그 lane의 스레드에 질문 카드를 메시지로 게시한다** (리뷰#04-2). 상태 갱신만으로는 답글을 달 대상이 없기 때문이다 — 이 메시지가 스레드 루트가 되고, id는 `lane.blocked_message_id`에 캐시한다. `lane.blocked_note`는 마지막 값 캐시일 뿐이고 **이력은 메시지가 갖는다.**
3. **서버가 위임자를 즉시 깨운다** (리뷰#04-1). 그 질문 카드를 인용한 시스템 메시지를 보내며, 프롬프트에 "이것은 질문 알림이며 합류가 아니다. 답만 하고 턴을 끝내라"를 명시한다. 합류를 기다리면 2분 만에 나온 질문이 가장 느린 형제 때문에 40분 뒤에 도착한다.
4. 위임자는 답을 담아 **그 질문 카드에 답글**을 단다. lane 해소 규칙 1에 따라 그 lane으로 라우팅되어 `blocked → running` 재진입하고, lane의 `runtime_session_ref`로 이전 대화를 이어받는다(§7).

**`blocked`는 즉시 알림을 보내면서도 합류 판정에서는 계속 종료로 취급한다.** 둘은 목적이 다르다 — 즉시 알림은 "질문을 빨리 전달"하는 것이고, 종료 취급은 "질문한 자식 때문에 형제들의 합류가 막히지 않게" 하는 것이다. 종료로 치지 않으면 답이 올 때까지 그룹 전체가 멈춘다.

**위임자가 없는 lane** (Director가 직접 멘션해 만들어 `delegated_from_task_id`가 빈 경우, 리뷰#04-3): 깨울 위임자가 없으므로 질문 카드를 **그 lane 이 매인 미션의 Director** 인박스에, **미션 밖 lane 이면 방장** 인박스에(부재 위임 FR-2A.3) `action_required`로 올린다. Director가 답글을 달면 같은 규칙 1로 재진입한다. 브리프에도 "위임자가 없는 작업에서 `blocked`는 Director에게 간다"를 적는다.

Director가 답해야 하는 질문(범위·리스크·트레이드오프)은 `blocked`가 아니라 `colab hitl ask`로 올린다. 둘을 혼동하면 위임자가 답할 수 없는 질문이 위임자에게 가거나 그 반대가 된다.

**FR-6.3 동시성 제한** — **방** `max_parallel_lanes`(기본 5, v0.19 — 일에는 없다), 에이전트 `max_concurrent_tasks`, 런타임(데몬) 전역 상한, 워크스페이스 전역 상한. 초과분은 큐 대기. **`waiting_human`과 `blocked`는 어느 계산에도 포함하지 않는다** — 둘 다 프로세스가 이미 종료되어 있다(t-1). **`worktree` 격리에서는 같은 에이전트의 lane들이 workdir를 공유하므로 항상 순차 실행된다.**

**FR-6.4 격리와 작업 공간 정리** — 세션 생성 시 선택한 `isolation`에 따라 데몬이 workdir를 준비한다.

| 방식 | 준비 | 적합 |
|---|---|---|
| `worktree` | 지정 저장소에서 `git worktree add <room>/<agent>` (브랜치 `colab/<room>/<agent>`; v0.19 — 옛 `<session>` 자리에 방 id 가 온다. 이미 만들어진 폴더·브랜치는 같은 uuid 라 그대로 동작하고 이름만 새것부터 바뀐다). 브리프 파일은 `.git/info/exclude`에 등록(§8.4). `[FOLDERS]`: 체크아웃 위치는 `<workdir_root>/rooms/<방>/_worktrees/<에이전트>/`, 브랜치 슬러그는 **방 이름**에서 온다(현재 구현은 에이전트의 첫 미션 제목을 쓴다 — 바로잡을 결함, MISSION_FOLDERS FINDING-1). 이미 있는 체크아웃은 옮기지 않는다. 미션 공용 `_shared/` 는 저장소 밖 `rooms/<방>/<미션>/_shared/`. | 코드 작업 |
| `container` | 지정 이미지로 컨테이너 생성, 세션 컨텍스트 파일과 아티팩트 디렉토리 마운트. CLI는 컨테이너 안에서 실행(이미지에 CLI와 인증 포함 필요). | 리서치·문서, 보안 격리 |
| `none` | `<workdir_root>/rooms/<방>/<미션>/<에이전트>/` (미션 밖이면 `_room/<에이전트>/`) + 미션 공용 `_shared/`. 경로는 서버가 지어 번들에 싣는다(`[FOLDERS]` — 옛 배치 `sessions/<방>/<lane>/` 은 데몬이 지었다). | 가벼운 작업 |

세 방식 모두 lane 종료 시 아티팩트 디렉토리를 서버에 동기화한다.

**정리(GC) 정책** — 작업 공간은 무한정 쌓이면 안 된다. 데몬이 다음을 수행한다.

- `container`·`none`: 그 lane 이 매인 **미션이 닫히면 즉시 삭제**한다(v0.19). 미션 밖 lane 은 `last_used_at + workdir_retention_days`. 아티팩트는 이미 서버에 동기화되어 있다. **`[FOLDERS]` 배치에서는 삭제 단위가 lane 이 아니라 미션 폴더 `<미션>/` 하나(에이전트 폴더 전부 + `_shared`)** 다. `worktree` 방의 미션 `_shared`(저장소 밖, 커밋 없음)도 미션이 닫히면 즉시 지운다. 미션 닫기 확인은 「이 미션의 작업 폴더 N개(〈용량〉)가 정리됩니다 — 남길 것은 아티팩트로 제출하세요」를 적는다.
- `worktree`: 세션 종료 후에도 **기본 14일 보존**(워크스페이스 설정 `workdir_retention_days`). 병합·정리는 Director가 수행한다. 보존 기한이 지나면 다음 **둘 중 하나에 해당할 때만** 자동 삭제한다.
  - 브랜치가 병합되었고 작업 트리가 클린하다.
  - 커밋이 없고 **작업 트리도 클린하다**.

  즉 **미병합 커밋 또는 미커밋 변경이 있으면 삭제하지 않고** Director에게 알린다. 시나리오 B처럼 에이전트가 커밋 없이 diff만 제출하는 흐름에서는 "커밋 없음"이 기본 상태이므로, 작업 트리 조건이 없으면 작업 결과를 지운다(M4).
  - **삭제는 워크트리만 하고 브랜치는 남긴다.** 브랜치는 가볍고, 잘못 지우면 복구가 어렵다.
- 데몬은 workdir 루트의 디스크 사용량을 보고하고, 워크스페이스 설정의 상한(`workdir_disk_quota_gb`)에 도달하면 그 컴퓨터로의 **새 dispatch** 를 막고 **방장**에게 정리를 요청한다 — 전달은 받은 요청의 `runtime_offline` 과 같은 층의 **`workdir_quota`**(attention) 항목과 방 전폭 배너(`blocked_reason` 은 쓰지 않는다 — 방이 아니라 컴퓨터의 상태다)(v0.19 — 방 만들기는 막지 않는다. 컴퓨터 없이도 만들어진다).
- Runtimes 화면에 머신별 작업 공간 목록·용량·마지막 사용 시각을 표시하고 수동 삭제를 제공한다.

**FR-6.5 합류(Join)** — 위임자에게 결과를 전달하는 **유일한 경로**다. 자식 lane이 개별적으로 위임자를 깨우지 않는다(FR-3.3 규칙 4·8, §8.3 규약).

**합류 그룹의 정의**: 같은 위임 task(`delegated_from_task_id`)에서 만들어진 자식 lane 집합. Lead가 두 번에 나눠 위임하면 합류도 두 번 일어난다. 이 정의가 없으면 나중에 추가된 자식이 앞선 합류를 무한정 지연시킨다.

- 그룹의 모든 자식 lane이 종료 상태(`done` 또는 `failed`)가 되면 위임자 에이전트에 `progress_update` 시스템 메시지로 결과 묶음이 **한 번** 전달되어 종합 run이 트리거된다.
- **일부가 실패해도 나머지를 기다린다.** 실패한 lane은 실패 사유와 함께 묶음에 포함된다. 위임자가 재시도·우회·에스컬레이션을 판단한다.
- `blocked` 자식은 **종료로 취급**하므로 합류를 막지 않는다(FR-6.2.1). 질문은 이미 즉시 알림으로 전달됐고, 결과 묶음에도 다시 실린다.
- **합류 프롬프트에 미답변 질문 수를 명시한다** (t-8): "답을 기다리는 자식 N개가 있다. 답하기 전에 작업을 종료하지 마라." 즉시 알림을 놓쳤거나 답을 미룬 위임자가 `status set done`으로 그냥 끝내는 것을 막는다.
- 자식이 `waiting_human`이면 종료 상태가 아니므로 합류는 대기한다. 그 대기가 세션 시간 상한에 걸리면 `paused`가 되고 Director에게 알린다. 자식 하나의 HITL이 24시간을 끌면 나머지가 다 끝나도 위임자가 깨어나지 못하므로, **"대기 중인 자식을 제외하고 먼저 종합" 옵션(부분 합류)을 v1.1 후보로 둔다**(m-n).

**합류는 그룹당 한 번만 발화한다 (E).** 그러면 재진입한 lane이 끝났을 때는 누가 알림을 받는가 — 그 그룹의 합류는 이미 발화했다.

**이 통보는 서버가 한다** (리뷰#04-5). lane이 `done`이 될 때 그 task의 트리거 작성자가 위임자가 아니면, 서버가 작성자를 멘션한 시스템 메시지를 게시한다. 에이전트의 프롬프트 규칙에 맡기면 어겨질 수 있고, 그러면 QA는 Frontend가 끝난 것을 영영 모른다. 합류와 같은 방식으로 결정적이다.

| 상황 | 통보 |
|---|---|
| 재진입 lane이 완료 | 서버가 **재진입을 일으킨 메시지의 작성자**를 멘션. 시나리오 B 5단계에서 Frontend가 새 diff를 내면 QA가 깨어난다. 작성자가 위임자 본인이어도 합류는 이미 발화했으므로 규칙 8의 억제가 풀려 있다(규칙 8 셋째 항) |
| 작성자가 사람(Director) | 인박스 알림. 에이전트 트리거 없음 |

재진입은 새 합류 그룹을 만들지 않는다. 만들면 위임자가 자식 하나가 고쳐질 때마다 종합을 다시 돌린다.

### FR-7. Task 실행 및 관찰

**FR-7.1 상태 머신** (multica 준용)

```
                        ┌──────────────────────────────┐
                        ↓                              │ (응답·기한 만료)
deferred → queued → dispatched → preparing → running → waiting_human
                        │                       │
                        │                       ├──→ completed
                        │                       └──→ failed | cancelled
                        │                            paused(budget)
                        └── (재개·재시도는 queued로 되돌아온다)
```

- `preparing`: 데몬이 workdir를 준비하는 단계. 재개·재시도에서는 기존 workdir를 재사용한다.
- `dispatched` 5분 초과 → `failed(timeout)`. `running`은 15초 heartbeat, 3분 무응답 시 런타임 offline 처리.
- **`waiting_human`에서 나가는 유일한 경로는 `queued`로의 재큐잉이다(N4).** 프로세스는 이미 종료되어 있으므로 `running`으로 직접 돌아갈 수 없다. 응답이 오거나 기한이 만료되면 `queued → dispatched → preparing → running`을 다시 거친다.
- 이 상태에서는 heartbeat를 요구하지 않고 동시성 슬롯도 점유하지 않는다. workdir는 보존한다.

`paused(budget)`은 예산 초과로 취소된 task의 상태다(FR-7.3). Director가 계속을 승인하면 `queued`로 되돌아온다.

**HITL 전이 시점** — HITL 툴 호출이 곧바로 상태를 바꾸지 않는다.

1. 툴 호출 시 task에 `pending_hitl` 플래그만 세우고 "턴을 끝내라"를 반환한다.
2. 에이전트가 지시를 따르지 않고 작업을 계속할 수 있다. 그동안 게시된 메시지와 편집은 **그대로 기록한다** — 이미 일어난 일이다.
3. **`turn_end`가 도착하면** `waiting_human`으로 전이한다.
4. **task당 열린 HITL은 하나로 제한한다.** 두 번째 호출은 "이미 대기 중인 요청이 있다"로 거부하고, 활동 피드에 남긴다. 하나의 턴이 답을 두 개 기다리면 재개 프롬프트가 모호해진다.
- 재시도: `runtime_offline`, 네트워크, 프로세스 stall은 2~3회 자동(같은 머신 안에 대체 프로파일이 있으면 전환), 인증/쿼터/설정 오류는 재시도 없음. **`rate_limited`(G1 F3)**: 계정 사용 한도는 `session/prompt`의 JSON-RPC 오류 `-32603 "You've hit your limit … resets HH:MM"`으로 오고 프로세스는 살아 있다 — 쿼터 오류가 아니라 **리셋 시각까지 `queued`로 되돌리는 재시도 가능 분류**로 다룬다(`task.not_before`).

**재시도는 처음부터 다시 하지 않는다 (M5).** 중단된 task는 이미 파일을 절반 고치고 메시지를 두 개 게시했을 수 있다. `task_id + seq` 멱등 키는 메시지 중복 게시만 막고 파일 편집·셸 명령은 막지 못한다. 따라서 재시도는 HITL 재개(FR-5.4)와 **같은 코드 경로**를 쓴다.

1. **같은 workdir를 재사용한다.** 새로 만들지 않는다.
2. **런타임 세션 resume을 먼저 시도**하고, 불가하면 콜드 스타트로 폴백한다.
3. `attempt ≥ 2`의 턴 프롬프트에 다음을 주입한다: 이전 시도가 중단되었다는 사실, **이미 게시한 메시지 목록**, "작업을 이어가기 전에 workdir의 현재 상태(변경된 파일, git status)를 먼저 확인하라"는 지시.

이 규칙이 없으면 재시도가 같은 메시지를 다시 쓰거나 이미 적용된 편집을 중복 적용한다.

**FR-7.2 활동 피드 (Activity Feed)** — 데몬이 보낸 `task_event(seq)`를 사람이 **읽지 않고 훑어서 판단할 수 있는** 형태로 렌더한다. block/buzz의 활동 피드 설계를 채택한다. 이 피드의 목적은 감상이 아니라 **개입 여부 판단**이며, 모든 항목은 세 질문 중 하나에 답해야 한다: 지금 무엇을 왜 하는가(이해), 잘 되고 있는가(확신), 내가 끼어들어야 하는가(통제).

*지배 프레임* — 모든 항목은 한 문장이다: **에이전트가 [동사]를 [목적어]에 했다 → [결과].**
> "#design에 메시지를 보냈다." · "`runtime.rs`를 수정했다 (+12/−3)." · "테스트를 실행했다 → 1248개 통과." · "Director에게 승인을 요청했다 → 대기 중."

전체 인자, 원본 출력, 전체 diff는 펼쳐야 보이는 곳으로 내린다. 문장을 읽고, 궁금해질 때만 펼친다. **v0.19.2: 이 피드는 에이전트 메시지 카드의 세 번째 층 「작업 과정」이고 기본 접힘이다(FR-3.1.2)** — 타임라인 기본 보기는 대화만.

*렌더 클래스* — 모든 이벤트는 정확히 하나의 클래스에 속한다. 마지막 세 개가 "어떤 이벤트든 표현할 수 있다"는 바닥을 보장한다.

| 묶음 | 클래스 |
|---|---|
| 중추 (항상 읽힘) | 메시지(에이전트의 목소리), 플랫폼 조작(`colab` 호출), 파일 편집, 셸 명령, 툴 상태·턴 생명주기 |
| 판단 근거 (필요할 때 참조) | 사고 과정, 계획·할 일 목록, 권한 요청, 오류 |
| 안전망 (드물게 읽히지만 반드시 존재) | 일반 툴(정직한 폴백), 원본 레일, 억제된 잡음 |

*원칙*

- **전송 방식이 아니라 의미로 렌더한다.** MCP 툴로 보낸 메시지와 `colab message post` 셸로 보낸 메시지는 **같은 카드**로 보인다. 에이전트가 플랫폼에 어떻게 도달했는지는 배관이고, 무엇을 했는지가 계약이다. (§8.2에서 런타임마다 경로가 다르므로 이 원칙이 특히 중요하다.)
- **결과를 먼저 말한다.** 성공·실패·결과값이 앞에 온다. 원본 덤프는 폴백이지 헤드라인이 아니다.
- **제자리에서 갱신한다.** 진행 중인 동작은 자기 행을 `대기 → 실행 중 → 완료|실패`로 바꾼다. 하나의 동작은 하나의 항목이며, 상태 줄을 쌓지 않는다.
- **절대 캄캄해지지 않는다.** 이벤트의 부재도 정보다. 침묵·유휴·타임아웃은 "대기 중…", "응답 없음"으로 **렌더되는 상태**이지 빈 화면이 아니다. 우리가 에이전트에게 요구하는 규칙과 같다 — 보여주지 않았으면 일어나지 않은 것이다.
- **실패는 크게, 읽기는 작게.** 눈에 띄는 정도가 결과의 무게를 따라간다. 쓰기·관리 동작·오류는 크게, 조회와 사고 과정은 조용하게. 묻힌 오류는 고장난 피드다.
- **참조를 해소한다.** `#design`, "Director의 메시지", 파일명으로 보여준다. 원본 id나 uuid를 노출하지 않는다.
- **스트림을 합친다.** 청크로 도착한 텍스트는 하나의 항목이다. 사람은 메시지를 읽지 패킷을 읽지 않는다.
- **부풀리지 않는다.** 인식된 동작은 의미 카드를, 인식하지 못한 동작은 깨끗한 일반 행을 받는다. 더 풍부해 보이려고 의미를 지어내지 않는다.
- **기본은 정제, 원본은 요청 시.** 원본 레일은 안전망이고, 둘 사이의 전환은 같은 진실의 확대·축소이지 다른 피드가 아니다.
- `[v0.17]` **빈 턴도 렌더한다.** 턴이 메시지 0·플랫폼 조작(`colab` 호출) 0·파일 편집 0 으로 `end_turn` 하면 "아무것도 하지 않고 턴을 끝냈다" 를 **정보 카드**(오류 아님)로 남긴다. 위의 "절대 캄캄해지지 않는다" 는 턴 **중**의 침묵을 다루고, 이 문장은 턴이 **끝난 뒤** 남는 공백을 다룬다 — 멘션이 낭비됐는지(§8.3 규약 위반) 정당한 무응답인지는 Director 가 카드를 보고 판단한다. refusal 은 별도(재시도, D-13). 판정 위치(서버 finish 시)와 `task_event` 키는 계약 결정(`plan/research/OASIS_REVIEW.md` C3). **판정과 기록(v1.1, K-18)**: 서버가 `finish` 에서 그 attempt 의 `task_event` 에 `status`(플랫폼 조작) 0건·`tool edit_file` 0건·게시 메시지 0건이면 `{class: status, verb: turn_end, object_ref: "empty_turn", outcome: info, payload: {command: "turn_end", args: {note: "아무것도 하지 않고 턴을 끝냈습니다"}}}` 한 행을 남긴다 — 닫힌 스키마 안(S-52), 새 키 없음. 관찰 표 `empty_turn_rate` 가 이 행을 센다.

*강등* — 런타임이 구조화 이벤트를 주지 못하면(§8.2.6) 메시지 카드와 원본 레일만 남는다. 이때도 "이 런타임은 툴 단위 로그를 제공하지 않습니다"를 명시해 침묵과 구분한다.

**FR-7.3 비용 집계와 강제** — task/agent/세션/런타임 단위 토큰·비용 집계. 런타임이 사용량을 보고하지 않으면(능력 `usage=false`) 추정치로 표시하고 "추정" 배지를 단다.

**강제 시점 (M9)** — 예산은 task 사이에서만 검사하면 단일 task가 크게 초과할 수 있다.

- **턴 중 강제**: 데몬이 `usage_update`(ACP) 또는 각 이벤트의 사용량을 누적해, `budget_per_task` 또는 세션 잔여 예산을 넘으면 §8.2.2의 취소 절차를 밟는다. 상태는 `failed`가 아니라 **`paused(budget)`** 다 (M9) — 예산 초과는 오류가 아니라 정책이고, Director가 예산을 늘려 승인하면 **같은 재개 경로**(런타임 resume 우선, 같은 workdir)로 이어가야 작업을 잃지 않는다.
- **task 예산 승인은 그 task에만 적용된다** (C2′). 승인 시 입력한 새 상한은 `task.budget_override`에 저장되고, 에이전트의 `budget_per_task`는 바뀌지 않는다. 에이전트 기본값을 영구히 올리려면 에이전트 편집 화면에서 한다 — HITL 한 번으로 설정이 바뀌면 다음 세션에서 예상 밖의 비용이 난다.
- Director가 "계속"을 승인하면 해당 task를 `queued`로 되돌린다. 새 트리거를 기다리지 않는다. **이 HITL은 `source: system`이지만 `task_id`를 반드시 채운다** — 재개할 task를 특정해야 하기 때문이다(s-13).
- CLI 폴백 경로에서 런타임이 자체 예산 플래그를 지원하면(예: Claude Code `--max-budget-usd`) 함께 전달해 이중으로 건다.
- **추정치로는 하드 컷을 하지 않는다.** `usage=false` 런타임(Antigravity)은 추정 비용이 상한의 100%에 도달하면 **세션을 `paused`로 만들고 Director에게 알린다**. 부정확한 추정으로 작업을 중간에 죽이지 않는다. 이 차이는 세션 생성 시 격리·런타임 선택 화면에 표시한다.
- 세션 예산 소진은 종료 조건이 아니라 `paused`다(FR-2.2).

**FR-7.4 에이전트 툴 표면 (`colab` CLI)** — multica가 `multica issue comment add`로 에이전트가 플랫폼에 되돌아오게 하듯, 본 서비스는 `colab` CLI를 데몬과 함께 설치한다. 어떤 런타임이든 셸을 실행할 수 있으면 동일하게 사용한다. Claude Code처럼 MCP를 지원하는 런타임에는 같은 기능의 **MCP 서버**도 제공한다.

| 명령 | 설명 |
|---|---|
| `colab session get` / `colab session messages` | 세션 정보·히스토리 조회 |
| `colab message post --content-file --parent <id>` | 메시지/답글 게시 (멘션 포함 가능) |
| `colab lane delegate --agent --brief --depends-on --profile` | lane 생성 + 멘션 메시지 자동 작성 (대상은 참여자로 제한) |
| `colab hitl ask --default <제안>` / `approve-request` / `request-info` | HITL (수신자 Director). **호출하면 턴을 종료하라는 응답이 돌아온다** — 답을 기다리며 블로킹하지 않는다(FR-5.4) |
| `colab artifact submit` / `get` | 아티팩트 |
| `colab decision record --summary --rationale` | 결정 기록 |
| `colab status set working\|blocked\|done --note` | 자기 상태 갱신. **`blocked`는 위임자에게 질문하고 턴을 끝내는 경로**(FR-6.2.1) — 서버가 질문 카드를 스레드에 게시하고 위임자를 즉시 깨운다. 사람이 답할 질문은 `colab hitl ask`를 쓴다 |
| `colab review approve\|reject --artifact --comments` | 리뷰어 승인/반려 |

CLI는 task마다 발급되는 단기 토큰(`COLAB_TASK_TOKEN`)으로 인증하며, 해당 task의 세션·에이전트 범위 밖에는 접근할 수 없다.

### FR-8. 인박스 및 알림

- 사용자 전용 인박스. 항목 타입(v0.19): `hitl_request`(action_required, Director·방장) · **`isolation_confirm`**(FR-2.1.1) · **`lane_blocked`**(위임자가 없는 lane 의 질문 — 미션 안이면 Director, 미션 밖이면 방장, FR-6.2.1) · **`work_proposed`**(FR-2A.1) · `work_paused` · **`room_paused`**(방 전체, action_required) · `work_completed` · `run_failed` · `runtime_offline` · **`workdir_quota`**(FR-6.4, attention) · `mention` · **`room_invited`**(FR-2.2). 옛 `session_completed`·`session_paused` 는 `work_*` 로 이름이 바뀐다.
- 구독 설정(v0.19, §10 R2): **방** — 전부 / 내가 참여한 미션만 / HITL만 / 끄기 · **미션** — 전부 / HITL만 / 종료만(방 설정을 덮어씀) · **서브 미션** — 켜기 / 끄기(스레드 단위). 안 읽음 배지는 방 단위 하나다(§12.1-6).
- 에이전트는 인박스가 없다. 에이전트 간 통신은 전부 세션 메시지.

### FR-9. Runtime (데몬) 관리

- **Runtimes** 화면: 연결된 머신 목록, 온라인 상태, 감지된 CLI와 버전, 로그인 상태, 실행 중 task 수.
- **Add a computer**: 설치 스크립트 + 페어링 토큰 2줄 제공 (multica 방식). 데스크톱 앱은 v2.
- 데몬은 시작 시 지원 바이너리를 탐지하고 `probe()`로 버전·모델 목록·로그인 여부를 보고한다. 이후 60초마다 갱신.
- `worktree` 세션을 위해 저장소 정보도 보고한다: `repo_path`, **remote URL**, 현재 브랜치, 워킹 트리 클린 여부. remote URL이 있어야 재바인딩 시 "같은 저장소"를 판정할 수 있다(FR-9.2, F).
- 데몬 전역 동시 task 상한(기본 10), 런타임별 상한 설정.

**FR-9.1 데몬 크래시와 고아 프로세스 (M11)** — §8.2.2의 "모든 종료 경로에서 프로세스 그룹 정리"는 데몬이 살아 있을 때의 이야기다. 데몬이 죽으면 자체 프로세스 그룹으로 띄운 런타임은 **살아남아 계속 파일을 고치고 `colab` CLI로 메시지를 게시한다.** 그동안 서버는 heartbeat 만료로 task를 재큐잉하므로, 복구된 데몬이 같은 workdir에서 두 번째 프로세스를 띄우면 **두 프로세스가 같은 디렉토리에 동시에 쓴다.**

- 데몬은 런타임 프로세스를 띄울 때 **pgid와 task id를 디스크에 기록**하고, 정상 종료 시 지운다.
- 데몬은 재시작하면 **task를 claim하기 전에** 기록된 pgid를 확인해 살아 있는 고아를 먼저 정리한다.
- 서버는 task를 재큐잉할 때 그 task의 **`COLAB_TASK_TOKEN`을 폐기한다.** 정리 전에 고아가 뭔가 게시하려 해도 서버가 거부한다. 이것이 데몬 복구와 무관하게 동작하는 마지막 방어선이다.
- 고아가 남긴 workdir 변경은 지우지 않는다. 재개 프롬프트의 "현재 상태를 먼저 확인하라"(FR-7.1)가 처리한다.

**FR-9.2 런타임이 영구히 사라졌을 때** — 세션은 `runtime_id`에 고정되므로(C4), 그 머신이 돌아오지 않으면 세션은 영원히 `queued`가 된다. 두 리뷰 모두 놓친 경로다.

> **v0.19 단위(R1-1)**: 이 절의 "세션"은 **방**이다 — 런타임 고정·유예 만료 `paused`·재바인딩·삭제 금지는 전부 방 단위다. 다만 **`cancelled` 로 닫는 것은 그 방의 진행 중인 일 전부**이고 방 자체는 남는다(FR-2.4: 방은 종료되지 않는다). 여기의 `paused` 는 예산이 아니라 **런타임 소실**이 원인이며, 재바인딩하거나 일을 닫을 때까지 그 방의 모든 일과 일 밖 task 가 멈춘다. **알림과 선택(재바인딩 / 열린 일 전부 취소)은 방장이 받고 고른다**(부재 위임 FR-2A.3) — 아래 불릿의 "Director"는 방장으로 읽는다.

- 런타임이 `runtime_offline_grace`(기본 7일)를 넘겨 오프라인이면 그 런타임에 묶인 세션을 `paused`로 만들고 Director에게 알린다.
- Director는 둘 중 하나를 고른다.
  - **재바인딩**: `isolation: none`이면 다른 온라인 런타임으로 옮긴다. `worktree`·`container`면 같은 저장소·이미지를 가진 런타임으로만 옮긴다.
    - **`worktree`에서는 진행 중이던 lane뿐 아니라 완료된 lane의 코드도 유실된다 (F).** 푸시는 비목표이므로(§2.2) 커밋은 죽은 머신의 브랜치에만 있고, 서버에는 diff 아티팩트만 남는다. 따라서 재바인딩 후 첫 프롬프트에 **"이 세션의 diff 아티팩트를 제출 순서대로 새 workdir에 적용한 뒤 이어가라"** 를 넣는(v0.19: **방 전체의 diff 를 미션과 무관하게 제출 시각순**으로 — 브랜치가 방×에이전트당 하나라 커밋 순서가 곧 제출 순서다)다. 이것 없이는 재바인딩이 복구가 아니라 처음부터 다시 하기가 된다.
    - **"같은 저장소" 판정은 remote URL로 한다.** `repo_path` 문자열 비교로는 다른 저장소를 같은 것으로 오인한다. 데몬 probe가 `repo_path`와 함께 remote URL·현재 브랜치를 보고한다(FR-9).
    - 아티팩트·메시지·결정 기록은 서버에 있으므로 대화 컨텍스트는 온전히 남는다.
  - **세션 종료**: `cancelled`로 닫고 아티팩트만 회수한다.
- 런타임을 삭제하려 할 때 활성 세션이 걸려 있으면 삭제를 막고 위 선택을 먼저 요구한다.

---

## 6. UX / 화면

| 화면 | 주요 요소 |
|---|---|
| **Runtimes** | 머신 카드(온라인, CLI 목록·버전·로그인, 실행 중 task 수), **작업 공간 목록·용량·마지막 사용 시각 + 수동 삭제**, Add a computer |
| **Agents** | 카드 목록 (이름·역할·상태·기본 프로파일), 새 에이전트, **템플릿**(v1). Build with AI·`.agent.md` 가져오기는 **v1.1** |
| **Agent 편집** | FR-1.1 폼, 프로파일 편집기(런타임 종류 → 모델 → 옵션), 권한, 툴, 테스트 채팅 |
| **Rooms**(방 목록, v0.19) | 방 카드(이름·설명·마지막 활동·안 읽음·열린 미션 수·참여자 아바타), 검색·정렬, **「새 방」은 이름 한 칸**(마법사 없음, FR-2.1) |
| **Room 상세**(방, v0.19) | 좌: 참여자(사람·에이전트) + **서브 미션 보드** · 가운데: 타임라인(+ 미션 칩으로 거르기) · 우: **미션에 따라 바뀌는 칸**(goal·종료 조건 진행률·그 미션의 비용)과 **방 전체 칸**(아티팩트·결정 기록·누적 비용·방 설정·**맥락 읽기 기록**). 상단에 **열린 미션 칩**과 「새 미션」·「이걸 미션으로」 |
| **Room 설정** | 참여자 초대·퇴장, `visibility`, 런타임·격리(첫 dispatch 전에만), 한도(예산·동시 일·동시 lane), autonomy, **참고 방 링크**, 보관·삭제 |
| **미션**(work) | 미션 열기 폼(goal·assignee·종료 조건·Director·예산), 진행률·요약, 취소·삭제, **에이전트의 미션 제안 확인**(FR-2A.1). 방 화면 안에서 열린다 — 별도 페이지는 없고 **`/rooms/:id?work=:workId`** 로 바로 열 수 있다(받은 요청·알림·시스템 메시지에서 링크하려면 필요) |
| (v0.18 의 Sessions·Session 상세는 위 넷으로 대체된다 — 마법사는 없앤다) |
| **Inbox** | HITL 응답 UI(Director·방장) — 에이전트의 제안 기본값을 함께 표시, **방·미션 바로가기**, 미션 제안 항목 |
| **Settings** | 멤버, 런타임 정책, 예산 정책, 루프 상한(체인 깊이·시간당 빈도), ~~컨텍스트 재사용 상한~~(v0.19 에서 제거 — FR-4.4 폐기), **방 기본값**(격리·한도·autonomy·visibility), 기본 격리 방식, **작업 공간 보존 기한·용량 상한**, 활동 로그 페이로드 마스킹, **활동 로그(감사, owner·admin — 다른 방 읽기·링크·감사 열람·방 삭제·방장 승계)**, 알림 |

멘션 자동완성, 트리거 미리보기, 실시간 스트리밍(에이전트 타이핑 표시)은 필수.

---

## 7. 데이터 모델

```
workspace 1─N member (user_id, role: owner|admin|member)
workspace 1─N runtime (name, host, status, daemon_version, last_seen_at,
                       capabilities(jsonb: [{kind, version, models[], logged_in}]),
                       repos(jsonb: [{path, remote_url, branch, clean}]))  -- F
workspace 1─N agent (name, role, role_description, instructions, tools,
                     owner_id, respond_to, respond_to_allowlist[],
                     avatar_url, budget_per_task, max_concurrent_tasks,
                     definition_source, definition_version,
                     -- status 컬럼 없음: FR-1.3에 따라 task 상태에서 파생·계산한다 (v0.12, 스키마 v0에서 발견).
                     -- 값 집합(idle|working|waiting_human|error|offline|disabled)은 ENUM 타입으로만 존재
                     archived_at)
agent     1─N agent_profile (name, runtime_kind, model, options(jsonb),
                             env(jsonb), args[], is_default, fallback_profile_id?)
workspace 1─N room (name, description, owner_user_id, deputy_owner_user_id?,   -- v0.19 방 (방장·부방장)
                    visibility: workspace|invited,                            -- FR-5.3
                    runtime_id?, isolation(jsonb)?, limits(jsonb)?, autonomy?,
                    default_director_user_id?, status: active|archived,
                    blocked_reason: budget|runtime_offline|loop|manual|null,  -- FR-2.4 방을 멈추는 칸(manual = 사람이 건 긴급 정지)
                    created_by, created_at)
room      1─N room_participant (agent_id? | user_id?, role: owner|deputy|member,
                                joined_at, left_at?,
                                last_read_message_id?)                -- 사람·에이전트 같은 표; 안 읽음은 방 단위(사람 행만)
room      1─N room_link (target_room_id, created_by)                  -- 참고 방(FR-4.5)
room      1─N work (title, goal, acceptance_criteria[], director_user_id, deputy_user_id?,
                    assignee_agent_id?, completion_condition(jsonb), limits(jsonb),
                    autonomy?, status: draft|active|paused|completing|completed|cancelled,
                    paused_reason?, blocked_reason?, cost_usd, created_by, finished_at?)   -- 옛 session 의 goal 쪽
room      1─N work_proposal (proposed_by_task_id, goal, rationale, trigger_message_id?,
                    status: open|accepted|rejected, decided_by?, decided_at?, reject_reason?, work_id?)      -- FR-2A.1 에이전트 제안 → 사람이 연다(S26)
room      1─N room_read_log (reader_task_id, reader_agent_id, originator_user_id?,
                    target_room_id, allowed: bool, denied_reason?, summary_bytes, recent_n, truncated: bool, created_at)  -- FR-4.5 양쪽 기록(S23). task_event 는 닫힌 스키마라 여기 둔다
room      1─N room_agent_profile (agent_id, profile_id)               -- 방에서 쓰는 프로파일
room      1─N room_context (type: doc|url|file|room, ref, summary)
room      1─N workdir (agent_id?, lane_id?, kind: worktree|container|dir,
                             path_or_ref, branch?, status: active|retained|deleted,
                             disk_bytes, last_used_at, retain_until)
room      1─N message (author_type, author_id, parent_id, content, detail?, work_id?,   -- detail: v0.19.2 FR-3.1.2 작업 내용(접힘)
                       mentions[], source_task_id,
                       kind: text|hitl|blocked_q|system|summary,
                       state: posted|pending_approval)      -- M10: supervised 모드
room      1─N lane (work_id?, parent_lane_id, agent_id, profile_id, depends_on[],
                    workdir_id,                       -- C3: 격리 방식이 공유 여부 결정
                    delegated_from_task_id,           -- 합류 그룹 키 (FR-6.5)
                    runtime_session_ref(jsonb: {kind, session_id, provenance}),
                                                      -- 리뷰#04-4: task가 아니라 lane에 있다
                    status: queued|running|waiting_human|blocked|paused|done|failed,
                    blocked_note, blocked_message_id, reentry_count)
lane      1─N task (agent_id, profile_id, trigger_message_id,
                    delegated_from_task_id, restarted_from_task_id,   -- B: 재지시
                    originator_user_id,
                    coalesced_message_ids[], attempt, max_attempts,
                    pending_hitl,                                      -- N4
                    budget_override,                                   -- C2′: task 예산 승인값
                    status: deferred|queued|dispatched|preparing|running
                            |waiting_human|paused|completed|failed|cancelled,
                    queued_reason: room_lanes|agent_global|runtime|workspace|null,   -- v0.19 §3.1, 서브 미션 대기 사유
                    failure_kind, started_at, finished_at)
task      1─N task_event (seq, class, verb, object_ref, outcome,
                          tool, input, output, usage, superseded_by)
task      1─1 task_usage (input_tokens, output_tokens, cache_read, cost_usd, estimated)
room      1─N hitl_request (work_id?, task_id?, source: agent|system, type,   -- approver_spec: director|room_owner|any_member|user_id
                            question, options[], proposed_default,
                            approver_spec: director|room_owner|any_member|user_id,   -- v0.19 room_owner(D-4)
                            due_at, overdue,
                            status: open|answered|auto_answered,
                            approved, answer, answered_by, answered_at)
room      1─N artifact (work_id?, name, version, type, storage_ref, submitted_by_task_id)
room      1─N decision (work_id?, summary, rationale, source: hitl|agent, ref_id)
member    1─N inbox_item (type, severity, session_id, work_id?, lane_id?, ref_id, read_at)   -- session_id 는 방 id 로 읽는다(다른 자식 표와 같은 규칙); work_id·lane_id 는 미션·서브 미션 구독 칸(R1 에 미리)
workspace 1─1 workspace_settings (loop_limits, budget_policy, context_reuse,
                                  default_isolation, runtime_policy,
                                  workdir_retention_days, workdir_disk_quota_gb,
                                  runtime_offline_grace)
workspace 1─N activity_log
```

- **v0.19 이관 규칙**: 옛 `session` 행 하나 → `room` 하나 + `work` 하나. 자식 표(message·lane·task·artifact·decision·hitl·workdir)의 `session_id` 는 **방 id 로 읽는다**(열 이름은 당분간 유지, §10). goal 쪽 칸(goal·acceptance_criteria·completion_condition·director_user_id·status·cost_usd)은 `work` 로 옮긴다. `work_id` 는 nullable — 일에 속하지 않는 메시지·lane·task 가 있다(FR-2A.1).
- **사람과 에이전트를 같은 표(`room_participant`)에 둔다.** 방에서 둘의 차이는 "트리거되는가"뿐이고, 초대·퇴장·목록·권한 검사는 같은 코드가 처리해야 갈라지지 않는다.
- 멘션은 `message.mentions[]`에 `{kind, id}`로 정규화해 저장하고, 본문에는 원문 링크를 유지한다.
- `agent`는 정의에서 온 필드와 인스턴스 필드를 함께 갖는다(FR-1.8). `definition_source`(파일 경로·팩 id·수동)와 `definition_version`을 보관해 업데이트 여부를 표시한다.
- `session.director_user_id` 변경, `agent.respond_to` 변경은 `activity_log`와 시스템 메시지에 남긴다.
- **`workdir`가 lane과 작업 공간을 분리한다(C3).** `worktree` 격리에서는 `agent_id`만 채워 같은 에이전트의 lane들이 하나의 행을 공유하고, `container`·`none`에서는 `lane_id`를 채워 lane마다 하나씩 갖는다. GC는 이 테이블의 `retain_until`·`disk_bytes`를 기준으로 동작한다(FR-6.4).
- **`hitl_request`에 토큰이 없다(M4).** 응답은 인증된 세션으로 들어오고 `approver_spec` 검증 + 멱등 저장으로 처리한다. `task_id`는 nullable이며, 플랫폼이 발행한 요청은 `source: system`이다(M2).
- **`lane.runtime_session_ref`가 재개의 유일한 근거다(C1, 리뷰#04-4).** 런타임 세션은 "한 에이전트의 한 작업 흐름"이고 그것이 lane의 정의이므로, ref는 task가 아니라 lane이 갖는다. 그래야 **모든 재진입**(스레드 답글·최근 lane 재사용·재지시·HITL 응답·재시도)이 예외 없이 resume을 탄다 — task에 두면 재진입마다 새 task가 생기므로 매번 콜드 스타트가 된다.
  - task는 시작할 때 자기 lane의 ref로 resume을 시도하고, 성공하면 그 결과로 lane의 ref를 갱신한다.
  - resume이 거부되면(§8.2.5) lane의 ref를 비우고 콜드 스타트한다.
  - `provenance`는 세션 동일성 검증에 쓴다(Hermes 유실 감지).
- `message.state`는 `supervised` 모드에서 Director 승인 전 메시지를 담는다. v1.1 기능이지만 마이그레이션을 피하려고 스키마는 v1에 넣는다(M10).
- `task_event`는 활동 피드의 렌더 단위다(FR-7.2). `class`는 렌더 클래스, `verb`/`object_ref`/`outcome`은 한 문장 렌더용이며, 제자리 갱신은 새 행을 쓰고 이전 행의 `superseded_by`를 채우는 방식으로 이력을 잃지 않고 표현한다. **서버가 스스로 만드는 이벤트도 닫힌 스키마를 지킨다(v0.16, S-52)**: 사람의 플랫폼 조작·플랫폼이 사람에게 하는 말(cancel·set_status·hitl·gc.refused·명령 TTL 만료)은 `class=status` 에 두고 사람이 읽는 문장은 `payload.args.note` 에, 서버 자체 진단(preview 드리프트·미가격 비용·예산 강제 실패·스키마 거절)은 `class=runtime` + `detail` 에 둔다. 서버 insert 경로도 데몬 경로와 같은 검증을 거친다.

**고빈도 이벤트는 영속화하지 않는다.** 프레즌스, 타이핑·생성 중 표시, 토큰 델타 같은 신호는 실시간 채널로만 흐르고 `task_event`·`activity_log`·검색 색인에 들어가지 않는다. 영속 대상은 "나중에 누가 왜 그랬는지 물을 수 있는 것"으로 한정한다. 이 구분이 없으면 감사 로그가 스트리밍 잡음에 묻힌다.

---

## 8. 아키텍처

### 8.1 구성

```
[Web Client (Next.js)]
      │ WebSocket / SSE
[API Server (Go)]  ──  [Router: 멘션 파싱 → task enqueue]
      │                        │
[Postgres]              [Task Queue (Postgres SKIP LOCKED)]
      │                        │  long-poll / WS
[Platform LLM (Claude API)]   [Runtime Daemon (사용자 머신)]
  Build with AI, 세션 요약,       │
  criteria 판정, 미리보기 요약     ├─ ACP Harness ──stdio JSON-RPC──> claude-code-acp
                                 │   (1차 경로)                      hermes acp
                                 │                                   codex-acp / goose …
                                 ├─ CLI Adapter (폴백) ─────────────> agy -p …
                                 ├─ isolation: worktree | container | none
                                 └─ `colab` CLI + MCP server (에이전트가 호출)
```

- **Router**: 메시지 생성 시 FR-3.3 규칙을 적용하고 task를 enqueue. 동기 API 경로에서 LLM 호출 없음.
- **Daemon**: task를 claim → lane workdir 준비 → 브리프 파일 작성 → **ACP 하네스**(또는 폴백 CLI 어댑터)로 런타임 프로세스 구동 → 이벤트를 `task_event`로 전송 → 종료 사유·사용량 보고. 데몬은 stateless, 상태는 서버.
- **Platform LLM**: 에이전트 실행이 아닌 플랫폼 기능(Build with AI, 세션 요약, `criteria_met` 판정, 트리거 미리보기 요약)만 Claude API를 직접 호출한다.
- **기술 스택 확정** (2026-09-04): Go 서버·데몬·CLI, Next.js 웹, Postgres. 개발은 에이전트 오케스트레이션으로 진행하며 사람은 Director 한 명 — `PLAN.md` §10.

### 8.2 런타임 실행 계층 — ACP 1차, CLI 폴백

**결정 (v0.4):** 런타임 통합의 1차 경로는 **ACP(Agent Client Protocol)** 이고, 런타임별 CLI 플래그 조립은 ACP 어댑터가 없는 런타임의 **폴백**이다.

**근거.** block/buzz는 `buzz-acp` 하네스 하나로 Goose·Claude Code·Codex·자체 에이전트를 동일하게 구동하며, 프리셋에 Hermes를 포함한 십여 개 런타임이 등록되어 있다. 우리가 필요한 세 동작 — 세션 생성/재개, 권한 요청 응답, 툴 호출·텍스트·사용량 스트리밍 — 은 ACP에 모두 표준화되어 있다. CLI 플래그 방식은 런타임마다 인자·출력·resume·오류 분류가 전부 달라 어댑터 3개가 사실상 서로 다른 프로그램이 된다(§8.2.4의 폴백 스펙이 그 증거다). ACP를 1차로 두면 런타임 추가 비용이 "프리셋 한 줄 + 실행 커맨드"로 줄어든다.

**8.2.1 통합 하네스 인터페이스**

```
interface RuntimeHarness {
  transport: "acp" | "cli"
  probe(): { installed, version, logged_in, models[], capabilities }
  capabilities: {
    permission_negotiation: boolean  // 권한 요청/응답을 프로토콜로 처리
    resume: boolean                  // 세션 재개
    structured_events: boolean       // tool call 단위 이벤트 (활동 피드 렌더 가능)
    usage: boolean                   // 토큰/비용 보고
    model_select: boolean            // 세션 내 모델 지정
    effort: boolean                  // 사고 깊이 옵션 (카탈로그가 광고할 때만)
    tool_disallow: boolean           // 특정 툴 차단 가능 (AskUserQuestion 등, M11)
    mcp: boolean                     // MCP 서버 주입
  }
  start(workdir, profile): SessionHandle      // ACP: initialize + session/new|resume
  prompt(handle, turn): AsyncIterable<TaskEvent>
  cancel(handle)                                // FR-3.4의 취소·재프롬프트가 이것만 쓴다
}
```

`steering`(진행 중 턴에 메시지 주입) 능력과 `steer()` 메서드는 v0.5에서 제거했다. FR-3.4의 재지시 버튼은 취소 후 새 지시로 재시작하므로 `cancel()`만 있으면 되고, 실제 주입을 지원하는 런타임이 확인되지 않았다.

능력은 **런타임 이름이 아니라 세션이 광고한 값**으로 판정한다. 같은 프로토콜 키를 쓰는 서로 다른 바이너리가 존재하고(예: Hermes 키를 공유하는 다른 구현), 버전에 따라 능력이 달라지기 때문이다.

**8.2.2 ACP 경로 (기본)**

- **핸드셰이크**: `initialize`(프로토콜 버전과 클라이언트 능력 선언) → `session/new {cwd, mcpServers, model}` 또는 재개 시 `session/resume {cwd, sessionId, mcpServers}` → 필요 시 `session/set_model`, 사고 깊이 옵션 설정 → `session/prompt`.
- **권한 요청**: `session/request_permission`이 오면 제시된 옵션 중 `kind == "allow_once"` 인 것을 골라 응답한다. **optionId를 하드코딩하지 않는다** — 런타임마다 값이 다르고 버전에 따라 바뀐다. 허용 옵션이 없으면 `reject_once`로 응답하고 해당 툴 호출을 활동 피드에 거부로 기록한다.
  - **왜 전부 자동 승인하는가.** 안전장치를 격리(FR-6.4)와 사후 관찰(FR-7.2)에 두고, 실행 시점에는 막지 않는다. 매 툴 호출마다 사람을 부르면 병렬 lane의 이점이 사라지고 Director가 승인 기계가 된다.
  - **한계**: 격리 안에서는 무엇이든 할 수 있다. 워크트리 lane의 에이전트가 `git push --force`나 저장소 밖 경로 삭제를 실행하면 막을 수단이 없다. **위험 명령 패턴에 대한 사전 승인(`tool_approval` HITL)은 v1.1 후보**로 두고, v1은 "무엇이든 하되 전부 남긴다"로 간다. 판단 근거는 §12 오픈 이슈.
- **이벤트**: `session/update` 알림의 `agent_message_chunk`, `agent_thought_chunk`, `tool_call`, `tool_call_update`, `usage_update`, `turn_end` 를 그대로 `task_event`로 정규화한다(FR-7.2의 렌더 클래스에 대응).
- **취소**: 대기 중인 권한 요청에 `cancelled`로 먼저 응답 → `session/cancel` → `stopReason: "cancelled"` 수신까지 드레인. 중간에 프로세스를 죽이면 히스토리가 깨진다.
- **프로세스 위생**: 런타임 서브프로세스는 자체 프로세스 그룹으로 띄우고 모든 종료 경로에서 그룹 단위로 정리한다.
- **동시성**: 하네스 프로세스당 워커 수를 설정(기본 1, 상한 32)하고, **lane 하나당 진행 중인 턴은 항상 1개**로 제한한다(FR-6.3).

**8.2.3 지원 런타임 — 경로와 능력** (Antigravity는 v1.1)

| | Claude Code | Hermes | Antigravity |
|---|---|---|---|
| 1차 경로 | **ACP** (`@agentclientprotocol/claude-agent-acp` 어댑터 — 구 `claude-code-acp`, G1) | **ACP** (`hermes acp`, 네이티브) | **CLI 폴백** (ACP 어댑터 없음) |
| 폴백 경로 | CLI (`claude -p`, §8.2.4) | CLI (`hermes -z`) | — |
| 지시 파일 | `CLAUDE.md` | `AGENTS.md` | `AGENTS.md` (`GEMINI.md`가 있으면 우선) |
| MCP | ✓ | ✓ (런타임 `mcpCapabilities`에 맞춰 stdio/http 필터) | ✗ → `colab` CLI 셸 호출로 대체 |
| resume | ✓ | ✓ (유실 감지 필요, §8.2.5) | ✓ `--conversation <id>` (거부 감지 불가) |
| structured_events | ✓ | ✓ | ✓ stream-json (구버전은 텍스트+로그) |
| usage | ✓ 토큰 + 비용(추정) | ✓ 토큰 + 비용 | △ 토큰만, **세션 누적값** → 턴별은 차분 계산 |
| effort | ✓ low~max | △ 카탈로그가 광고할 때만 | ✓ low/medium/high |
| 권한 자동 승인 | ACP 협상, 폴백은 `--permission-mode` | ACP 협상 + `HERMES_YOLO_MODE=1` | `--dangerously-skip-permissions` |
| 헤드리스 인증 | OAuth 캐시 또는 `ANTHROPIC_API_KEY` | `~/.hermes/config.yaml` 프로바이더 설정 | 로그인 캐시, CI는 `modelProvider:"gemini"`+`GEMINI_API_KEY` |

**8.2.4 CLI 폴백 스펙** (ACP 어댑터 부재·설치 실패·프로토콜 버전 불일치 시)

*Antigravity (`agy`) — v1.1의 CLI 경로*
```
agy -p --input-format stream-json --output-format stream-json \
  --dangerously-skip-permissions --model <id> --effort <low|medium|high> \
  --print-timeout <dur> --log-file <tmp> --add-dir <lane> [--conversation <id>]
```
stdin에 `{"event":"user","message":{"content":"…"}}`. `--print-timeout`은 **기본 5분이 긴 턴을 끊으므로 항상 명시**한다. 존재하지 않는 `--model`은 조용히 무시되므로 `agy models`(줄당 `id<TAB>label`) 카탈로그로 실행 전 검증한다 — **정적 폴백 목록을 두지 않는다**. 이벤트는 `init` → `step_update`(step_type: user_input|agent_response|tool|checkpoint) → `result`(status: SUCCESS|ERROR|CANCELED|INTERRUPTED|INVALID). stream-json 미지원 구버전은 stdout 텍스트 + `--log-file` 정규식 스캔(`conversation=<uuid>`, `Print mode: timed out`, `agent executor error:`)으로 강등한다.

*Claude Code (`claude`)*
```
claude -p --input-format stream-json --output-format stream-json --verbose \
  --permission-mode bypassPermissions --disallowedTools AskUserQuestion \
  --mcp-config <colab-mcp.json> --strict-mcp-config \
  --model <id> --effort <lvl> --max-turns <N> --max-budget-usd <usd> \
  [--resume <session_id>] --settings <settings.json>
```
프롬프트는 인자가 아니라 stdin에 한 줄 JSON으로(10MB 상한). `--bare`는 `CLAUDE.md`를 읽지 않으므로 **쓰지 않는다**. 세션 id는 `system/init`과 `result`의 `session_id`에서 얻는다. `--mcp-config` 서버 기동을 최대 30초(`MCP_TIMEOUT`) 대기하고, 종료는 SIGINT를 먼저 보낸다(SIGTERM은 exit 143·턴 미완료).

*Hermes (`hermes`)*: `hermes -z "<prompt>" --model <id> --yolo --usage-file <path> --in <lane>`. `--usage-file`이 `estimated_cost_usd`·토큰·`session_id`를 남긴다. 이벤트 스트림이 없어 활동 피드가 텍스트 한 덩어리로 강등되므로 ACP 실패 시에만 쓴다.

**8.2.5 런타임별 알려진 함정** (multica 구현에서 검증된 것)

| 런타임 | 함정 | 대응 |
|---|---|---|
| Claude Code | 헤드리스에서 `AskUserQuestion`이 빈 답을 반환 | **ACP·CLI 두 경로 모두에서** disallow하고 HITL은 `colab hitl ask`로 일원화. CLI는 `--disallowedTools`, ACP는 어댑터의 툴 차단 수단(`tool_disallow` 능력)을 쓴다 — 수단 확인은 스파이크 항목(M11). 차단이 불가능하면 브리프에 "이 툴을 쓰지 말라"고 명시하고 호출 시 활동 피드에 경고를 남긴다 |
| Claude Code | resume 거부 시 "no conversation found" 류 메시지 | `resume_rejected`로 분류해 새 세션으로 재시도 |
| Hermes | `state.db`에 없는 세션을 **오류 없이 새로 생성** | **`session/load` 결과가 `null`이면 유실, 아니면 `_meta.hermes.sessionProvenance.acpSessionId == 요청 id` 확인**(스파이크 4a: 3/3 감지, 오탐 0). `stopReason=="refusal" && 턴 활동 0`은 실기에서 발동하지 않아 보조 규칙으로 강등(G1 F7) |
| Hermes | 상위 LLM이 4xx/5xx여도 `end_turn` 보고 | stderr에서 프로바이더 오류를 별도 감지 |
| Hermes | 마지막 메시지 청크가 프롬프트 응답 **뒤에** 도착 | 250ms 정적 대기 + 2초 드레인 후 완료 판정 |
| Antigravity | resume 거부를 구분할 수 없음(빈 conversation id) | 항상 새 세션 폴백 준비 |
| Antigravity | 특정 버전에서 stdout 0바이트·exit 0 완료 | 빈 출력 완료 시 실패로 간주하지 말고 재시도 후 에스컬레이션 |

**8.2.6 능력에 따른 UI 강등 규칙**

| 상황 | 표시 |
|---|---|
| `structured_events=false` (CLI 폴백 텍스트 모드) | 활동 피드가 메시지 카드 + 원본 레일만, 툴 타임라인 없음 |
| `usage` 토큰만 (Antigravity) | 비용 카드에 "추정" 배지, 워크스페이스 가격표로 계산 |
| `mcp=false` (Antigravity) | 브리프에 `colab` CLI 규약만 기재, MCP 툴 목록 미주입 |
| `tool_disallow=false` | `AskUserQuestion` 등을 프로토콜로 막을 수 없으므로 브리프 지시 + 호출 시 활동 피드 경고 |
| `effort` 미지원 | 프로파일 편집기에서 effort 필드 비활성 |

### 8.3 Lead 코디네이션 프로토콜

Lead 역할의 브리프 파일에는 다음 섹션이 추가된다 (multica Squad Operating Protocol 준용).

- 직접 구현하지 않고 위임한다. 위임은 `colab lane delegate` 또는 정확한 멘션 링크로만 한다.
- **새 독립 작업은 `colab lane delegate`로, 진행 중인 lane에 대한 추가 지시는 그 lane의 스레드 답글로 한다.** 최상위 멘션은 그 에이전트의 마지막 lane을 재사용하므로(FR-3.3 lane 해소 규칙 3) 병렬 작업을 만들 때 쓰면 안 된다.
- **위임 대상은 세션 참여자 로스터에 있는 에이전트로 한정된다. 새 에이전트를 만들거나 요청할 수 없다.** 필요한 역할이 없으면 `colab hitl ask`로 Director에게 참여자 추가를 요청한다.
- 위임 후 턴을 종료한다. **자식 lane이 전부 끝나면 합류가 한 번 일어나** `progress_update`로 결과 묶음이 돌아온다(FR-6.5). 개별 완료마다 깨어나지 않으니 중간에 폴링하지 않는다.
- 불확실하거나 범위·리스크·트레이드오프가 걸린 결정은 `colab hitl ask`로 Director에게 올린다.
- 종료 조건이 충족되었다고 판단하면 `colab artifact submit` 또는 `colab status set done`으로 신호한다. 리뷰어의 승인은 `colab review approve`다(FR-2.2의 `agent_approval`과 같은 동작).

**모든 에이전트의 공통 규약** (§8.4의 [2] 구간에 포함)

- **빈 확인 메시지를 올리지 마라.** "알겠습니다", "확인했습니다"만 담긴 메시지는 멘션한 모두를 다시 트리거해 대화를 무한히 되돌린다. 할 말이 진행 상황·결과·질문 중 하나가 아니면 메시지를 만들지 않는다.
- **보여주지 않았으면 일어나지 않은 것이다.** 오래 걸리는 작업은 시작할 때 한 줄로 알리고, 끝나면 결과를 남긴다.
- **위임받은 일이 끝나면 위임자를 멘션하지 마라.** 완료 보고는 `colab status set done --note`와 아티팩트 제출로 한다. 결과는 합류 묶음에 실려 위임자에게 **한 번에** 전달된다(FR-6.5). 개별로 멘션하면 위임자가 자식 수만큼 깨어난 뒤 합류 때 또 깨어난다.
- **작업 중 위임자의 판단이 필요하면 `colab status set blocked --note "<질문>"` 으로 턴을 끝내라.** 질문이 카드로 게시되고 위임자가 즉시 깨어난다(FR-6.2.1). 답이 오면 같은 자리에서 이어간다. 위임자를 멘션해 봐야 억제되므로 이 경로를 써야 한다. 사람만 답할 수 있는 질문(범위·리스크·트레이드오프)은 `colab hitl ask`다.
- **위임자로서 질문 알림을 받으면 답만 하고 턴을 끝내라.** 그 알림은 합류가 아니므로 종합을 시작할 시점이 아니다.

### 8.4 브리프 파일 구성 (런타임 지시 파일)

데몬이 lane workdir에 쓰는 안정 브리프. 캐시 친화적으로 **변하지 않는 순서**를 유지한다.

```
[1] Agent Identity + instructions
[2] Workspace 규칙 + 멘션 문법 + colab CLI/MCP 사용 규약 (v0.19.2: 「--body 는 대화 — 누구에게·무엇을·다음 할 일, 5줄 안팎. 조사 결과·초안 전문·표는 --detail. 제출물은 artifact submit」, FR-3.1.2)
[3] (lead만) Coordination Protocol
[4] Session: goal / acceptance_criteria / 종료 조건 / Director 이름 / 격리 방식
[5] Roster: 참여자별 이름·역할 설명·멘션 링크 (현재 상태 working/idle 는 턴마다 바뀌므로 턴 프롬프트 `<roster_status>` 로 — [1]~[5] 바이트 동일, harness v0.9.2)
[6] Context: 첨부 자료 요약, 이전 세션 요약 (설정 상한 내)
[7] Decision Log
[8] Instruction Precedence: 사용자 지시 > 세션 goal > 에이전트 instruction > 런타임 기본
```

**전달 경로 (M6)** — 경로는 하나만 고른다. 둘 다 쓰면 같은 내용이 중복 주입된다.

1. 런타임이 시스템 프롬프트 전달 수단을 제공하면 그것을 쓴다 — ACP `session/new`의 시스템 역할(**필드 존재 여부는 스파이크로 확인**, §12), 또는 CLI의 `--append-system-prompt-file` 계열.
2. 그 수단이 없으면 **지시 파일이 유일한 경로**다. 세 런타임 모두 시스템 프롬프트 인자를 무시하거나 갖고 있지 않을 수 있으므로, **v1의 기본 가정은 지시 파일**이고 1번은 스파이크로 확인된 런타임에만 적용한다.

**지시 파일이 워크트리를 오염시키지 않게 한다 (M6)** — `worktree` 격리에서 workdir는 저장소 체크아웃 그 자체다. 여기에 `CLAUDE.md`/`AGENTS.md`를 그냥 쓰면 (a) 저장소가 원래 갖고 있던 같은 이름 파일을 덮어써 프로젝트 규칙이 사라지고, (b) `git status`에 잡혀 에이전트가 커밋해버린다.

- **저장소의 지시 파일(`CLAUDE.md`/`AGENTS.md`)은 원본 상태와 무관하게 읽지도 쓰지도 않는다 (M3, v0.16 정정).** 브리프는 **별도 미추적 파일 `<workdir>/COLAB_BRIEF.md`** 에 쓰고 `.git/info/exclude` 에 등록한다. 스파이크 5(`plan/spikes/SPIKE_05.md`)가 v0.15 까지의 안(추적 파일에 마커 append + `skip-worktree`)을 떨어뜨렸다: `skip-worktree` 는 `git status` 를 클린으로 보이게 하지만 **에이전트의 정당한 편집을 커밋에서 조용히 빼고**(12런 중 커밋 0) `switch`·`merge` 를 자력으로 풀 수 없게 막는다 — 시나리오 B(브랜치에 커밋)와 정면 충돌한다.

| 원본 상태 | 브리프 위치 | 숨기기 | lane 종료 시 |
|---|---|---|---|
| 무관(지시 파일이 있든, 추적 중이든, 없든) | `<workdir>/COLAB_BRIEF.md`(마커 구간으로 감싼 전문) | `.git/info/exclude` 에 경로 등록 | 파일 삭제, 등록 해제 |

- 지시 파일이 남지 않으므로 복원할 것도 없다. 에이전트가 `CLAUDE.md`/`AGENTS.md` 를 작업의 일부로 고치고 커밋하는 것은 평범한 git 작업으로 그대로 동작한다.
- `.gitignore`는 저장소가 소유한 파일이므로 건드리지 않는다.
- `claude_code` 는 파일 경로가 아니라 `_meta.systemPrompt.append` 로 받으므로 이 절의 파일은 `instruction_file` 런타임(hermes)에만 생긴다. `settingSources: []` 아래에서 Claude Code 는 저장소의 `CLAUDE.md` 도 읽지 않는다(스파이크 5 §2.1) — 그것을 열려면 G1 F2 격리를 다시 여는 일이므로 v1 에서는 열지 않는다.

**턴 프롬프트**

- 트리거 메시지 인용, 위임 브리프, 최근 히스토리 N개, "응답은 `colab message post`로 게시하라"는 지시만 담는다.
- **`instruction_file` 런타임(hermes)의 턴 프롬프트 맨 앞 한 줄은 고정이다(v0.16, 스파이크 5 §6.3)**: "먼저 `<workdir 절대 경로>/COLAB_BRIEF.md` 를 읽어라". 브리프 [1]~[5] 의 바이트 동일 규칙(E12-11)은 그 파일 안에서 유지한다. 비용: 매 턴 앞에 파일 읽기 도구 호출 1회(실측 4/4).
- **`<folders>` 블록 (`[FOLDERS]` 제안)** — 서버가 턴마다 자기 폴더(`you`)·미션 공용(`shared`)·같은 미션 동료의 폴더(읽기 전용 표시, **실제로 만들어진 것만**)의 절대 경로를 적는다. 미션·동료 구성은 턴마다 바뀌므로 브리프가 아니라 턴 프롬프트다(`<roster_status>` 와 같은 층 — [1]~[5] 바이트 동일 유지). 브리프 [2] 에는 고정 한 줄만: 「턴 프롬프트의 `<folders>` 가 네 폴더와 같은 미션 동료의 폴더다. 쓰기는 네 폴더와 shared 에만. 다른 미션·다른 방의 것은 아티팩트나 `colab room read` 로」. 같은 에이전트의 병렬 lane 이 한 폴더를 쓸 때는 「새 파일 이름에 갈래 표지를 넣어라」를 덧붙인다(FR-6.1).
- **"중단하고 다시 지시"로 재시작할 때**(FR-3.4)는 **새 지시만** 담는다. `<resumed>` 구간을 쓰지 않는다 — 사람이 방향을 바꾼 것이므로 이전 작업을 이어가면 안 된다(B). 런타임 resume 위에서 재시작하면 이전 지시는 이미 런타임 세션 안에 있으므로 다시 넣으면 중복이 되고, 콜드 스타트라면 히스토리 구간에 이미 들어 있다(m-o).
- **재개·재시도 프롬프트**(FR-5.4, FR-7.1)에는 `<resumed>` 구간을 두고 중단 사유, 이미 게시한 메시지 목록, HITL 답변과 승인 여부, "workdir의 현재 상태를 먼저 확인하라"는 지시를 담는다.
- 히스토리 구간에는 `included` / `total` / `truncated`를 함께 적어, 잘렸다는 사실을 에이전트가 알고 필요하면 `colab session messages`로 더 읽게 한다.

### 8.5 플랫폼 내부 Claude API 사용 원칙

플랫폼 기능(에이전트 실행 아님)에만 적용된다.

| 항목 | 결정 |
|---|---|
| 모델 | `claude-opus-5` 기본. 세션 요약·미리보기 등 경량 작업은 `claude-sonnet-5`. |
| Thinking | adaptive, `output_config.effort`로 조절. |
| 구조화 출력 | Build with AI, `criteria_met` 판정, 트리거 미리보기는 `output_config.format`으로 JSON 강제. |
| 캐싱 | 워크스페이스 규칙·판정 루브릭을 안정 prefix로 두고 `cache_control`. 단 **모델별 최소 캐시 길이**(수백~수천 토큰)에 못 미치면 캐시가 조용히 적용되지 않으므로, 판정 프롬프트 길이를 확인하고 `usage.cache_read_input_tokens`로 히트를 검증한다. |
| 폴백 | `stop_reason == "refusal"` 처리 필수(응답 본문을 읽기 전에 검사). 서버사이드 폴백은 베타 헤더 `server-side-fallback-2026-07-01` + `fallbacks: "default"`로 활성한다. 거절 시 `stop_details.category`를 활동 피드에 오류로 기록한다. |
| 스트리밍 | 요약 생성 등 긴 출력은 스트리밍. |

---

## 9. 비기능 요구사항

| 항목 | 요구 |
|---|---|
| 지연 | 메시지 게시 → 데몬 claim p50 < 2s. 에이전트 첫 출력 p50은 격리 방식별로: `none` < 10s, `worktree` < 15s(워크트리 생성 포함), `container` < 60s(컨테이너 기동 + MCP 서버 대기 최대 30초 포함) |
| 동시성 | 워크스페이스당 동시 running task 50, 데몬당 기본 10 |
| 내구성 | 데몬 재시작 시 `running` task는 heartbeat 만료 후 재큐잉. 메시지 중복 게시는 idempotency key(`task_id + seq`)로 막고, **파일·셸의 부분 실행은 FR-7.1의 재개 절차로 처리**한다(멱등 키로는 막을 수 없다) |
| 보안 | 에이전트 호출 권한은 **최초 사람 originator** 기준으로 판정 (에이전트 체인이 권한 상승 불가). `colab` CLI 토큰은 task 범위·단기. **소스 트리 전체는 사용자 머신에 남는다. 다만 에이전트가 읽고 쓴 내용 — 파일 편집 diff, 셸 명령과 출력, 제출한 아티팩트 — 은 활동 로그·아티팩트로 서버에 저장된다.** 워크스페이스 설정으로 `task_event` 페이로드 마스킹(요약만 저장)과 원본 로컬 보관을 선택할 수 있다. |
| 감사 | 모든 메시지·툴 호출·HITL 응답·상태 전이·Director 변경은 activity_log. |
| 비용 | 세션·에이전트·워크스페이스 예산 상한. 초과 시 자동 `paused`. |
| 관측 | task 실패 분류별 비율, 런타임별 성공률, HITL 평균 응답 시간, 프로파일 폴백 발생률 대시보드. |
| i18n | UI 한국어/영어. 에이전트 instruction 언어 무관. |

---

## 10. 범위 및 로드맵

### v1 (MVP, 10주)

v0.4 대비 **컨테이너 격리 · Antigravity · `.agent.md` · Build with AI를 v1.1로 옮겼다**(C6). 이들은 아직 스파이크가 끝나지 않았거나(컨테이너 인증, Antigravity MCP) 대체 수단이 있다(템플릿이 Build with AI를, 폼이 파일 가져오기를 대신함). 목표 6(벤더 혼합)은 Claude Code + Hermes 둘 다 ACP로 충족된다.

- [ ] 워크스페이스·멤버 CRUD
- [ ] **데몬 + Runtimes 화면 + ACP 하네스** — 핸드셰이크, 권한 협상(`allow_once` 탐색), 이벤트 정규화, 취소 절차, 프로세스 그룹 정리
- [ ] **런타임 2종**: Claude Code(ACP), Hermes(ACP). 구현 순서: **ACP 하네스 → Claude Code → Hermes**
- [x] ~~(조건부) Claude Code CLI 어댑터~~ — **G1(2026-09-05)에서 v1 제외 확정**: 스파이크 1 통과(크래시 0, resume 11/11, `allow_once` 부재 0%). §8.2.4 폴백 스펙은 문서로 유지. 원문: 스파이크 1에서 `claude-code-acp`가 불안정하다고 판정되면 v1에 포함한다. §12의 "드리프트 시 CLI 폴백으로 강등" 완화책도 이 어댑터가 있어야 성립하므로, 스파이크 결과에 따라 **v1 필수 또는 v1.1**로 확정한다(M2)
- [ ] 에이전트 CRUD(폼), **프로파일(멀티) 편집기**, 팀 템플릿(FR-1.4), 동적 생성 금지
- [ ] 호출 권한 게이트(`respond_to` = 초대 권한, 세션 참여 = 트리거 허용)
- [ ] 세션 생성(goal, **Director**, `runtime_id`, 참여자+프로파일, **격리 worktree/none**, **종료 조건 4종: `artifact_submitted` · `agent_approval` · `user_approval` · `manual`**, 예산 상한). `criteria_met`(플랫폼 LLM 판정)만 v1.1 — 시나리오 B가 `agent_approval`을 쓰므로 이것이 빠지면 v1에서 성립하지 않는다(t-4)
- [ ] 메시지·스레드·멘션 라우팅(FR-3.3 규칙 1~8 + **lane 해소 규칙**), 트리거 병합, 자식→위임자 멘션 억제, **루프 상한 3종**(체인 깊이·시간당 빈도·연속 왕복 — FR-3.5. 핑퐁 방지는 v1 필수다)
- [ ] `colab` CLI + MCP 서버, 브리프 파일 생성(**추적/미추적 파일별 워크트리 오염 방지**), 비용 집계 + 턴 중 예산 강제
- [ ] **활동 피드** — 중추 5개 렌더 클래스 + 일반 툴 + 원본 레일, 제자리 갱신, 침묵 상태 표시
- [ ] HITL `question` / `approval` (**턴 종료 + `turn_end` 전이 + 새 attempt 재개**, Director 수신, 승인은 자동 진행 없음), **deputy director**(D), 인박스 "Needs Action" 질의
- [ ] 병렬 lane(의존성 없음), lane/workdir 분리, lane 보드 + **중단·재지시 버튼**, 합류 1회 + 서버 통보, **`blocked` 질문 경로**(질문 카드 게시 + 즉시 알림 + 위임자 없을 때 인박스), **lane 단위 런타임 세션 resume**
- [ ] workdir GC(보존 기한·용량 상한), **고아 프로세스 정리 + 토큰 폐기**(FR-9.1)
- [ ] **런타임 영구 오프라인 시 세션 재바인딩·종료**(FR-9.2)
- [ ] 세션 자동 종료 + 요약, 컨텍스트 재사용 상한 설정
- [ ] **결정 기록 저장·표시**(P1) — `colab decision record`와 HITL 응답을 세션 단위로 보존하고 세션 화면에 목록으로 노출한다. 전용 UI는 v1.1이지만 **기록 자체는 v1이다** — 나중에 쌓으려 해도 지나간 세션의 결정은 복원할 수 없다
- [ ] **테스트 채팅**(P2) — 에이전트 편집 화면에서 세션을 만들지 않고 1:1로 시험 대화. 프로파일 설정이 맞는지 확인하는 용도

> **일정 주의**: 리뷰를 거치며 데이터 유실을 막는 항목들(FR-9.1 고아 프로세스, FR-9.2 재바인딩, M3 추적 파일, A 질문 경로)이 계속 늘었다. 전부 빼기 어려운 것들이라 **10주는 빠듯하다.** 아래 컷 순서를 미리 정해 두었으니, 스파이크 1 결과가 나오는 날 즉석에서 고민하지 말고 순서대로 적용한다.

### 일정이 모자랄 때의 컷 순서

| 순서 | 컷 대상 | 대가 | 절감 |
|---|---|---|---|
| 1 | 활동 피드를 **중추 2클래스(메시지·플랫폼 조작) + 오류 + 원본 레일**로 축소 | 파일 편집·셸 카드가 원본 레일로 강등된다. 판단은 여전히 가능 | 렌더러 절반 |
| 2 | 세션 **자동 종료 + 요약** → `manual` 종료만 | 목표 5 미달. 지표 "자동 완료 비율"은 v1.1로 | **요약 생성만.** 종료 조건 판정은 결정적 로직이라 골든 테스트와 함께 남긴다 — 시나리오 A 8단계가 판정을 요구하므로 자를 수 없다(`PLAN.md` §6.2). 절감폭이 작아 1·3순위보다 먼저 발동할 이유가 없다 |
| 3 | **`worktree` 격리 → v1.1** (`none`만 남긴다) | 코드 작업 시나리오가 v1.1로. 목표 2·3·4는 `none`으로 전부 검증된다 | GC·`skip-worktree`·재바인딩 복구·미병합 판정이 통째로 빠진다 |

**리뷰 #03은 3순위로 Hermes를 미루자고 했으나 채택하지 않았다.** ACP 하네스의 존재 이유가 "런타임 추가 비용을 프리셋 한 줄로 낮춘다"인데, 런타임이 하나뿐이면 그 가치를 검증할 수 없고 두 번째를 붙일 때 드러났을 추상화 누수를 v1.1까지 모른 채 간다. Hermes는 ACP 네이티브라 어댑터 비용이 가장 낮으므로, 아끼는 공수에 비해 잃는 정보가 크다. 반면 `worktree`를 자르면 위 표대로 네 덩어리가 함께 빠진다.

3순위까지 가면 v1은 "**Claude Code + Hermes, `none` 격리, 수동 종료**"가 된다. 벤더 혼합(목표 6)과 협업 핵심(목표 2·3·4)은 살아 있으므로 제품 가설은 검증된다.

### v1.1

- [ ] **컨테이너 격리** (인증 전달 스파이크 완료 후)
- [ ] **Antigravity CLI 어댑터** — 전역 MCP 설정 주입 스파이크, Gemini 가격표 기반 비용 추정
- [ ] **`.agent.md` 내보내기/가져오기**, 팀 팩 배포, 정의 버전 업데이트 알림
- [ ] **Build with AI**
- [ ] lane 의존성(DAG), `supervised` 모드(스키마는 v1에 선반영)
- [ ] HITL `choice` / `info`
- [ ] 결정 기록 **전용 UI**(필터·검색·근거 추적), 세션 컨텍스트 재사용 UI
- [ ] 활동 피드 나머지 클래스(계획·할 일, 사고 과정, 억제된 잡음 토글)
- [ ] Hermes `HERMES_HOME` 오버레이(에이전트별 스킬·메모리 분리)
- [ ] **채널 계층** — 워크스페이스와 세션 사이에 도메인 단위(`#payments`)를 두고 멤버 권한·에이전트 로스터·기본 격리를 상속시킨다. 세션이 수십 개가 되면 평평한 목록으로는 탐색이 무너진다 (아래 참고)
- [ ] **위험 명령 사전 승인** (`tool_approval` HITL) — 패턴 매칭으로 걸러낸 위험 명령만 Director 승인 후 실행 (§8.2.2)
- [ ] **세션 긴급 정지** — 세션의 모든 lane을 한 번에 취소하는 조작. 현재는 lane 단위 중단(FR-3.4)과 에이전트 단위 킬 스위치(FR-1.9)뿐이라 세션 전체가 폭주하면 하나씩 눌러야 한다 → **v0.19 에서 v2.0 으로 흡수**: 방 단위 「이 방 멈춤」(`room.blocked_reason: manual`, FR-2.4)이 정본이다. v1.1 항목으로는 만들지 않는다.

### v2.0 — 구조 전환: 방과 일 (v0.19, 다음 메이저)

v1.1 까지의 세션 모델 위에 **방**을 얹는다. 순서는 "계약 → 서버 → 화면 → 에이전트 표면 → 이관" 이고, 각 단계는 **앞 단계가 머지된 뒤**에 시작한다(v1 에서 쓴 방식 그대로).

| 단계 | 내용 | 끝났다고 보는 기준 |
|---|---|---|
| R0 계약 | `room`·`work` 리소스, 방 참여 op(**`addParticipant` 를 `{agent_id | user_id}` oneOf 로** — 지금은 agent_id 필수라 사람을 못 넣는다), `work_proposal`·`room_read_log`·`listActivityLog`, 목록 스키마 분리(옛 `Session` 16칸 중 8칸이 미션 것), `work` 종료 조건 이동, `colab room list/read`(FR-4.5), 데몬 번들의 `room_id`·`work_id?` | openapi lint 초록 + 생성물 재생성 + **`daemon-protocol`·`harness`·`colab-cli` 개정**(번들·브리프·명령이 같은 §번호를 각자 파싱한다), 옛 `session*` op 은 **별칭으로 한 판 유지** |
| R1 서버 | 방·일 테이블(0025~ — **`session` 을 `room` 으로 RENAME 하고 goal 쪽 열만 `work` 로 빼낸다**: 자식 14표의 FK 11개가 그대로 따라오고 데이터는 0행 이동), **변환 SQL 과 검증 스크립트(행 수 대조)를 여기서 만든다**, `inbox_item.work_id`·`lane_id` 칸 미리, 라우팅·상한·HITL 의 단위 이동, FR-4.5 읽기 권한·기록 | 기존 e2e 전부 초록(세션 = 방+일 하나로 읽힌 채) + 새 e2e(방 2개·일 2개·다른 방 읽기) |
| R2 화면 | 방 목록·방 화면(일 칩·참여자 초대·설정), 일 열기/종료, 다른 방 읽기 흔적 표시, **알림·안 읽음 단위**(`[V19-C]` 방 3단 구독은 끝나지 않는 방에 너무 성기다 — **일·lane 스레드**를 구독 단위로 내린다. `inbox_item.work_id`·`lane_id` 칸은 **R1 에 미리** 넣어 나중에 마이그레이션 없이 붙인다) | 마법사 제거, S5·S7 이 방 기준으로 다시 그려짐, 안 읽음이 방 20개에서 동작 |
| R2 서브 순서(리뷰 #270 R1-3 · SCR-B §5.3~5.4) | (a0) R0 산물로 SSE·op 이름 확정 → (a) `/rooms` 신설, `/sessions/*` 는 `/rooms/*` 로 **307**(마법사 `/sessions/new` 는 제외) → (M1) 목(`lib/mock`) 개정 → (M2) `STREAM_EVENT_TYPES`·openapi enum 에 새 SSE 타입(없으면 조용히 버려진다) → (b) S5·S7 재작성 → (c) S18~S21 → (M3) S22~S26 → (M4) **안 읽음**(`markRoomRead`·배지·`room.unread`) → (M5) S8 받은 요청 개정 → (M6) S9·S11·S13·S14·S17 수정 → (M7) S15 활동 로그 승격 + `listActivityLog` → (d) **그 뒤에** 마법사 삭제 + `/sessions/new` 307 → (e) **R1.5 문구 자물쇠**(서버 Go 리터럴까지 한 PR — server-wording 자물쇠가 글자 단위로 대조하므로 쪼갤 수 없다; 규모: 화면 문자열 107·웹 테스트 90 블록·서버 Go 40파일·e2e 셸 20파일) → (M8) 스크린샷·EVAL_USER 갱신 | 각 단계가 혼자 CI 초록. **별칭 제거는 R2 에 없다 — R4 조건(설치된 CLI 갱신 뒤)에서만** |
| R3 에이전트 표면 | `colab room *` 명령·MCP 툴, **브리프 [4](세션/방 맥락) 에 방 설명 + 그 턴의 일**(`[1]` 은 Agent Identity 라 건드리지 않는다 — `[1]~[5]` 바이트 동일 규칙 E12-11 때문에 일이 바뀌면 프리픽스가 달라지는 대가를 §8.4 에 적는다), `ColabCommand` enum 의 `room_read` 는 **R0 계약에서** 추가 | 실기 1턴에서 다른 방 읽기가 권한대로 되고 기록이 남는다 |
| R1.5 문구 전환(실행 시점은 **R2 (e)** — 화면이 새 구조로 서 있어야 문구를 바꿀 수 있다) | 화면·서버 문장의 "세션"을 **방·미션·서브 미션**으로(§3.2 표를 `COMPONENTS.md` §8.4 자물쇠에 넣는다 — 「작업 줄기」→「서브 미션」 포함. 자물쇠가 한 번에 빨개지므로 한 라운드로 몰아서), 골든 빌드태그 CI 통과 | wording 자물쇠 초록(§3.2 표 전수), p3golden·p4golden 초록 |
| R4 이관·정리 | **(Director 결정 2026-09-24) 옛 세션 데이터는 이관하지 않고 지운다**(덤프를 남기고 삭제 — 실사용 DB 세션 38개 전부 테스트성). `/sessions/*` 주소·`session get/messages` 명령·SSE `session.*` 삭제(openapi v0.3.0 D22), 문서 정리 | 삭제 뒤 방·미션으로 실기 완주, 옛 주소 호출은 404 |

**이관(R4) 규칙** — *2026-09-24 Director 결정으로 쓰지 않는다(옛 데이터 삭제). 기록으로 남긴다*: 세션 1 → 방 1 + 일 1. 방 이름 = 세션 제목, 방 설명 = goal 첫 줄, 일 goal = 세션 goal. 자식 행은 `session_id` 를 방 id 로 그대로 읽고 `work_id` 를 새 일로 채운다. 완료된 세션은 **보관된 방**이 된다(일은 `completed`). 되돌리기는 없다 — 전환 전 DB 덤프를 남긴다.

**v2 에서 같이 볼 것**: 한 에이전트가 여러 방에서 동시에 도는 동안의 동시성 상한(에이전트당 `max_concurrent_tasks` 가 방을 가로질러 적용된다 — §9), 방이 많아질 때의 목록·검색, 방 단위 알림 설정.

### v2 (그 밖)

- [ ] 추가 런타임(Codex, Goose, Cursor, OpenCode 등) — ACP 어댑터가 있으면 프리셋 등록만으로 추가
- [ ] 클라우드 런타임 — 유휴 시 스스로 종료하는 수명 관리, 프레즌스를 갱신 임대(lease)로 처리
- [ ] VCS 통합(워크트리 → PR 자동 생성·리뷰)
- [ ] 스케줄 세션(autopilot), Slack 연동
- [ ] 데스크톱 앱(데몬 내장)

---

## 11. 성공 지표

| 지표 | 목표 (v1 출시 3개월) |
|---|---|
| 데몬 설치 → **첫 미션 완료**까지 시간 (신규 사용자, v0.19) | 중앙값 < 15분 — 온보딩 대본(SCREEN F1)은 **미션을 열고 끝내는 단계까지** 포함해야 이 지표를 잰다(SCR-A P-9) |
| **일** 자동 완료 비율 (수동 종료 대비) | > 60% |
| HITL 요청당 Director 응답 시간 | 중앙값 < 30분 |
| 에이전트 간 위임 메시지 중 사람 개입 없이 처리된 비율 | > 70% |
| 병렬 서브 미션을 쓴 **미션**의 wall-clock 단축 | 단일 에이전트 대비 40% |
| 런타임별 task 성공률 | Claude Code > 95%, 기타 > 85% |
| 재개·재시도 후 중복 작업(같은 메시지 재게시, 편집 중복 적용) 발생률 | < 1% |
| 재진입 시 런타임 세션 resume 성공률 (콜드 스타트로 떨어지지 않는 비율) | > 90% |
| `blocked` 질문이 위임자에게 닿아 답을 받기까지의 시간 | 중앙값 < 5분 |
| 주간 **활성 방** / 활성 워크스페이스 · 방당 주간 **열린 일** | 방 3+ · 방당 일 2+ |

**v0.19**: **방은 완료되지 않으므로**(FR-2.4) 완료·자동 완료 계열 지표는 전부 **일** 기준이다. 방 기준 지표는 활성·빈도만 센다. `[V19-B]` **분모를 못박는다**: 1(F1 시간)·2(자동 완료)·5(병렬 단축)는 **일**, 10(주간 활성)은 **방**, 나머지(HITL 응답·위임 자율·런타임 성공률·중복·resume·blocked 응답)는 단위가 없는 task·HITL 집계라 그대로다. G9 가 못박은 10개이므로 R1 구현자가 임의로 고르지 않게 여기서 정한다.

*목표 6(벤더 혼합) 검증 지표*: 두 종류 이상의 런타임을 섞어 쓰는 워크스페이스 비율. 제품 성공 지표가 아니라 목표 6이 실제 수요였는지 확인하는 용도이므로 목표치를 걸지 않고 관찰만 한다.

`[v0.17]` **관찰 행(목표치 없음, 위 10개와 별도 표)** — 위 목표 6 지표와 같은 성격이다. 데이터는 `session_hop(rule, cause_hop_id, chain_depth)`·`lane.delegated_from_task_id`·`task_event` 에 이미 있다. `getWorkspaceMetrics` 10개(G9 조건)에 섞지 않고 별도 op 또는 아래 표로 둘지는 Lead 가 정한다(`plan/research/OASIS_REVIEW.md` §3 C1~C3).

| 관찰 행 `[v0.17]` | 세는 법 | 무엇을 보려고 |
|---|---|---|
| `[v0.17]` 트리거 사슬 규모 | 사람 메시지 1건이 만든 파생 task 수(같은 리셋 구간), 중앙값·p95 | 사람 한 마디가 얼마나 많은 턴을 부르는가 |
| `[v0.17]` 트리거 사슬 깊이 | 세션이 도달한 최대 `chain_depth`, 분포 | `max_chain_depth` 8 이 정상 위임을 막는지·너무 느슨한지(S-76·S-78 류를 미리) |
| `[v0.17]` 합류 폭 | 합류 그룹의 최대 자식 lane 수, 분포 | 병렬이 실제로 쓰이는가 |
| `[v0.17]` 라우팅 집중 | task 를 만든 FR-3.3 규칙 번호 분포(특히 6·7 폴백 비율)와 에이전트별 트리거 점유율 | Director 가 멘션 없이 써서 assignee 에게 전부 쏠리는 패턴 |
| `[v0.17]` 빈 턴 비율 | 메시지·플랫폼 조작·편집 0 으로 끝난 attempt / 전체 attempt | 멘션이 낭비되는 정도(FR-7.2 빈 턴 카드) |
| `[V19-C]` 첫 활동 지연 | 멘션·트리거부터 그 task 의 **첫 `task_event`** 까지의 시간 분포(p50·p90) | "멘션했는데 반응이 없다"는 체감이 몇 초에서 시작하는지. 기계 계약(`dispatched` 5분·heartbeat 3분)은 그대로 |
| `[V19-C]` 무활동 회수 | `running` 인데 한동안 새 `task_event` 가 없다가 **다시 활동이 온** 비율 | 무응답으로 죽일지 회수 가능한 상태로 둘지를 분포로 먼저 본다 |

---

## 12. 리스크 및 오픈 이슈

| 리스크 | 완화 |
|---|---|
| **구조 전환(v0.19)이 이관 중 맥락을 잃는다** | 세션 1 → 방 1 + 일 1 은 기계적이라 손실 지점이 적다. 전환 전 덤프 + 이관 뒤 "방 수 = 옛 세션 수, 메시지 수 동일" 를 스크립트로 대조(§10 R4) |
| **다른 방 읽기(FR-4.5)가 맥락 유출 경로가 된다** | 사람 originator 권한 + 에이전트 참여(또는 방장이 건 참고 방 링크) **둘 다** 요구, 목록에서도 숨김, 읽은 사실을 양쪽 방에 기록, 턴당 방 수·분량 상한 |
| `[V19-C]` **방 모델이 "시작한 뒤 계속 덧붙이는" 사용을 유도해 결과 품질이 떨어진다** | 외부 실측(Devin 2025 리뷰: 시작 뒤 요구를 덧붙일수록 나빠진다)을 규칙 변경이 아니라 화면으로 받는다 — FR-3.4 의 `queue` 기본값 유지 + 종료 조건을 걸 만한 대화에는 그 자리에서 「**미션으로 만들기**」를 권한다 |
| `[V19-C]` **위임 체인에서 권한 주체가 바뀐다** | 규칙은 이미 "체인 최상단의 사람 originator"(§9). 구현이 **모든 층**(위임 자식·재시도·재지시·HITL 재개)에서 `originator_user_id` 를 승계하고 그 값으로 FR-4.5 읽기를 판정하는지 e2e 로 고정한다 — 외부 실패 사례는 "정책은 맞는데 한 층에서만 전달된" 것이었다 |
| **방이 오래 살면서 맥락이 부풀고 비싸진다** | 프롬프트는 최근 N 개 + 스레드만(FR-4.1), 나머지는 CLI 조회. 사람이 「여기까지 정리」로 요약을 남긴다(FR-2.5). 방 예산 상한(FR-2A.3) |
| **한 에이전트가 여러 방에서 동시에 돌아 상한이 새거나 서로의 맥락을 섞는다** | 에이전트 맥락은 **방마다** 분리(§3 용어표) — 런타임 대화 세션·작업 폴더가 방 단위. 동시성은 에이전트 `max_concurrent_tasks` 가 방을 가로질러 적용된다(§9) |
| **일 없는 대화에서 나온 task 의 책임자가 불분명** | 일 밖 HITL 은 **방장**에게 간다(FR-2A.1). 그래도 애매하면 그 자리에서 "미션으로 만들기"를 권하는 안내를 띄운다 |
| **HITL 재개가 런타임 resume에 의존하는데 그 신뢰도가 런타임마다 다름** | 콜드 스타트 폴백을 1급 경로로 설계(FR-5.4). 브리프 + 히스토리 + 결정 기록만으로 이어갈 수 있어야 한다 — 스파이크 4번으로 검증 |
| **재시도가 부분 실행을 중복 적용** | 같은 workdir 재사용 + resume 우선 + `attempt ≥ 2` 프롬프트에 "이미 한 일" 주입(FR-7.1). 성공 지표로 중복 발생률 추적 |
| Hermes가 유실된 세션을 오류 없이 새로 만들고, LLM 오류에도 `end_turn`을 보고 | 세션 provenance 비교 + "refusal & 활동 0" 감지, stderr 프로바이더 오류 스니핑 (multica 검증 방식) |
| Claude Code 헤드리스에서 `AskUserQuestion`이 빈 답 반환 | ACP·CLI 양쪽에서 disallow, HITL은 `colab hitl ask`로 일원화. ACP 차단 수단은 스파이크 2번 |
| 에이전트 간 무한 핑퐁 | FR-3.5의 체인 깊이·시간당 빈도·연속 왕복 3중 상한 + **구조적 억제**(자식→위임자 완료 보고, 규칙 8). 내용 기반 휴리스틱은 정상 위임을 삼키므로 쓰지 않는다 |
| 컨텍스트 폭증으로 비용 급증 | 히스토리 inline 최소화 + CLI 조회, 브리프 파일 순서 고정, 턴 중 예산 강제(FR-7.3), 요약 길이 상한 |
| HITL 남발로 자율성 저하 | `autonomy` 모드, `proposed_default` 필수화, 요청 빈도 지표 |
| Director 부재로 세션 정체 | **deputy director를 v1에 포함한다(D).** 승인은 어떤 경우에도 자동 진행하지 않으므로(FR-5.4) deputy가 없으면 Director 부재 시 세션이 완료될 수 없다. `question`·`choice`는 `proposed_default`로 진행 가능하지만 `approval`은 사람만 답한다. 최후 수단으로 워크스페이스 admin이 **미션**의 Director를 교체할 수 있다(FR-5.3). **Director 가 워크스페이스를 떠나면 그 방의 방장이 자동으로 Director 를 승계**하고 미션 타임라인에 시스템 메시지를 남긴다(v0.19, §12.1-4 와 같은 방식) |
| 종료 조건 판정 오류(조기 완료) | `criteria_met`은 단독 사용 금지 + 판정자를 플랫폼 LLM으로 고정 |
| 병렬 lane 간 산출물 충돌 | 격리별 workdir 바인딩(C3) + 크로스 lane 읽기는 아티팩트로만 |
| 작업 공간 디스크 무한 증가 | 보존 기한·용량 상한·자동 GC(FR-6.4). 미병합 커밋이 있는 워크트리는 삭제하지 않고 알림 |
| **ACP 어댑터가 서드파티 패키지**(`claude-code-acp`)라 버전 드리프트·중단 위험 | 어댑터 버전을 프로파일에 고정, probe에서 프로토콜 버전 불일치 감지 시 CLI 폴백으로 자동 강등(§8.2.4를 유지하는 이유) |
| ACP 권한 협상에서 `allow_once`가 없는 런타임 | `reject_once` 후 활동 피드에 거부 기록. 반복되면 해당 런타임을 CLI 경로로 전환 |
| 취소가 되돌리기 어려운 작업을 중간에 끊음 | **어떤 메시지도 취소하지 않는다**(FR-3.4). 취소는 Director·deputy의 UI 조작뿐이고, 파일 편집·셸 명령 진행 중이면 완료까지 최대 30초 대기 |
| **런타임이 영구히 사라져 세션이 고립** | 7일 오프라인이면 세션 `paused` + 재바인딩·종료 선택(FR-9.2). 활성 세션이 걸린 런타임은 삭제 차단. `worktree`는 완료된 코드도 유실되므로 복구 프롬프트가 diff 아티팩트를 재적용한다(F) |
| **자식이 위임자에게 물어볼 길이 막힘** | lane `blocked` + 질문 카드 게시 + **즉시 알림**(합류를 기다리지 않는다) + 합류에서는 종료로 취급. 규칙 8의 억제는 위임자 한 명, 합류 발화 전까지만(FR-6.2.1) |
| **재진입마다 런타임 대화가 끊겨 콜드 스타트** | `runtime_session_ref`를 lane에 둔다(§7). 모든 재진입이 예외 없이 resume을 탄다 |
| **재지시가 이전 지시를 이어감** | 재지시는 새 task(`restarted_from_task_id`)이고 프롬프트에 `<resumed>`를 넣지 않는다(B) |
| **데몬 크래시 후 고아 프로세스와 재큐잉된 task가 같은 workdir에 이중 쓰기** | pgid 디스크 기록 + 재시작 시 claim 전 정리, 재큐잉 시 `COLAB_TASK_TOKEN` 폐기(FR-9.1) |
| 지시 파일이 저장소를 오염 | 저장소의 지시 파일은 건드리지 않고 **별도 미추적 `COLAB_BRIEF.md` + `.git/info/exclude`** + 턴 프롬프트 첫 줄이 경로 지시 + 종료 시 파일 삭제(§8.4, v0.16 — 스파이크 5가 `skip-worktree` 안을 떨어뜨렸다) |
| (v1.1) 컨테이너 격리 시 CLI 인증 전달 | 이미지에 CLI 포함 + 호스트 인증 디렉토리 read-only 마운트, 스파이크로 확정 |
| (v1.1) Antigravity가 MCP를 헤드리스에서 못 받고 비용 미보고 | `colab` CLI 셸 호출로 대체, 추정 비용은 하드 컷 대신 `paused` |
| `[v0.17]` **라우팅의 구조적 집중** — 규칙 6·7이 멘션 없는 사람 메시지를 전부 assignee 에게, 합류가 결과를 전부 위임자 한 명에게 보낸다. 규칙은 결정적이라 "편향" 이 아니라 **집중**이고, Director 가 멘션을 안 쓰면 병렬이 생기지 않고 Lead 가 병목이 된다(OASIS 절제 실험: 노출 규칙이 집단 결과를 좌우) | 규칙을 바꾸지 않는다(FR-3.5 "내용 기반 억제 안 함"). §11 관찰 행(규칙 번호 분포·트리거 점유율)으로 **재기만** 하고, G8 실측 5명의 로그에서 먼저 읽는다. 규칙 변경은 그 뒤 별도 결정 |

**오픈 이슈 / 스파이크** — 1~2는 v1 착수 전, 3~5는 1주차, 6~8은 해당 기능 착수 전.

1. ~~`claude-code-acp` 어댑터의 성숙도·유지보수 상태 실측.~~ **G1 통과** (`plan/spikes/SPIKE_01.md`, `plan/G1_DECISION.md`). 지시한 `@zed-industries/claude-code-acp`는 2026-02 이후 동결, 후속 `@agentclientprotocol/claude-agent-acp` 0.74.0으로 고정. 원문: Claude Code CLI 직접 구동(§8.2.4) 대비 안정성이 떨어지면 Claude Code만 CLI를 1차로 되돌린다. 하네스 구조는 그대로 두고 경로만 바꾼다. **이 결정이 v1 범위를 좌우하므로 1주차에 내린다.**
2. **ACP 경로에서 툴을 차단하는 수단**(`AskUserQuestion`). 어댑터 옵션·설정 파일 중 무엇이 되는지 확인. 불가하면 브리프 지시 + 활동 피드 경고로 대체(M11).
3. **ACP `session/new`에 시스템 프롬프트 필드가 있는가.** 없으면 §8.4의 1번 경로를 삭제하고 지시 파일을 유일 경로로 확정(M6).
4. **HITL 재개 경로 검증** — 두 단계로 나눈다(`PLAN.md` §4). **4a(1주차)**: 런타임 능력 — Claude Code ACP resume 후 답변 프롬프트로 이전 컨텍스트가 유지되는지, Hermes 유실 감지가 실제로 동작하는지. **4c(HITL 구현 착수 전)**: 콜드 스타트만으로 작업을 이어갈 수 있는지(C1) — lane `runtime_session_ref`와 `<resumed>` 프롬프트가 있어야 실제 조건이 되므로 1주차에는 할 수 없다.
5. **워크트리에 지시 파일 append/복원**이 각 런타임에서 실제로 읽히는지 — **`worktree` 격리 착수 전에** 한다(1주차 아님). 컷 3이 발동하면 생략. **저장소가 `CLAUDE.md`를 이미 추적하는 케이스를 반드시 포함한다** — `.git/info/exclude`는 untracked에만 듣고, `skip-worktree`가 런타임의 파일 읽기·에이전트의 편집과 충돌하지 않는지 확인해야 한다(M3).
6. **Claude Code CLI 플래그 재확인**: `--effort`, `--max-budget-usd`, `--append-system-prompt-file`. §8.2.4는 multica 코드에서 확인했으나 부록 C 공식 문서로 재대조.
7. (v1.1) 컨테이너 격리에서 각 CLI의 로그인 상태를 안전하게 전달하는 방법. **대기 중(`waiting_human`) 컨테이너를 stop/start로 회수하는 방법도 함께** — 24시간 대기가 여럿이면 컨테이너가 쌓인다(m-l).
8. (v1.1) Antigravity `--output-format stream-json` 최소 버전, 전역 MCP 설정이 `-p` 모드에서 로드되는지.

**남은 설계 숙제** — 워크트리 격리 세션 종료 후 브랜치 정리·병합 UX는 v1에서 Director 수동이고, v2 VCS 통합에서 자동화한다(FR-6.4의 GC는 삭제만 다루고 병합은 다루지 않는다).

**검토했으나 v1에 넣지 않은 것** — 같은 요구사항으로 작성된 다른 설계안(`prd/PRD_00_other-agent.md`)과 대조해 확인한 항목들이다. 셋 다 방향에는 동의하며 v1.1 후보로 둔다(§10).

| 항목 | 왜 지금 넣지 않는가 | 넣을 때 필요한 판단 |
|---|---|---|
| **채널 계층** | 세션이 적은 초기에는 평평한 목록으로 충분하고, 계층을 넣으면 권한·로스터·격리 설정의 상속 규칙을 함께 설계해야 한다 | 한 워크스페이스의 활성 세션이 20개를 넘기 시작하면 도입 |
| **위험 명령 사전 승인** | 매 호출을 막으면 병렬 lane의 이점이 사라진다. 무엇을 "위험"으로 볼지 패턴을 먼저 확정해야 한다 | 격리 밖으로 나가는 동작(저장소 외부 경로 쓰기, force push, 네트워크 쓰기)만 대상으로 좁힌다 |
| ~~**세션 긴급 정지**~~ → v0.19 방 `blocked_reason: manual`(FR-2.4)로 흡수 | lane 단위 중단으로 대부분 처리되고, 폭주는 루프 상한(FR-3.5)이 먼저 잡는다 | 실제로 "여러 lane을 연속으로 눌러야 했다"는 사례가 나오면 |

**채택하지 않기로 한 것** — **에이전트별 Ed25519 키페어와 서명 이벤트 로그**(Buzz 방식). 다른 설계안은 이를 엔터프라이즈 감사의 근거로 들지만, Buzz에서 서명이 필요한 이유는 Nostr 릴레이가 **분산 신뢰 없는 환경**이기 때문이다. 우리는 서버가 단일 신뢰 지점이므로 서명이 추가로 막는 것이 없다 — DB에 쓸 수 있는 공격자는 서명 검증 로직도 바꿀 수 있다. `activity_log`와 해시 체인으로 같은 감사 요구를 충족하면서 서명 검증 오버헤드를 지지 않는다. 부록 B의 같은 판단과 일관된다.

---

### 12.1 v0.19 전환의 결정 기록 (전부 확정 — 2026-09-23)

1. ~~**방 하나 = 런타임 하나** 를 유지할지~~ → **확정(Lead 권고, Director 위임)**: 유지. 지금은 유지(§FR-2.3) — 한 방의 에이전트가 같은 저장소를 봐야 위임·리뷰가 성립한다. 방에서 머신을 섞고 싶다면 **일마다 런타임**으로 내려야 하고, 그러면 작업 폴더 규칙(FR-6.1)이 일 단위로 바뀐다.
2. ~~**일을 에이전트가 스스로 열 수 있게 할지**~~ → **확정(Lead 권고, Director 위임)**: 제안만, 사람이 연다. 지금은 제안만 하고 사람이 연다(FR-2A.1) — 자동 개설을 허용하면 목표가 스스로 번식한다.
3. ~~**옛 `session_id` 열 이름을 언제 바꿀지**~~ → **확정(Lead 권고, Director 위임)**: R4 별칭 제거 때까지 유지, 그 뒤 별도 라운드. 지금 계획은 "방 id 로 읽고 이름은 유지"(§7·§10 R4) — 이름을 바꾸면 계약·골든·e2e 가 한 번에 흔들린다.
4. ~~**방장 승계와 부재**~~ → **확정(Director, 2026-09-23)**: 권고대로 — **부방장 한 명을 선택 항목**으로 두고, 방장이 워크스페이스를 떠나면 **워크스페이스 owner 중 가장 오래된 한 명이 자동 승계**하며 승계 사실을 방에 시스템 메시지로 남긴다.
5. ~~**에이전트 동시 상한의 범위**~~ → **확정(Director, 2026-09-23)**: **전역** — 에이전트의 `max_concurrent_tasks` 는 방을 가로질러 하나다(사람이 동시에 할 수 있는 양이 정해져 있는 것과 같다). 방 단위 `max_parallel_lanes` 는 그와 별개로 따로 건다.
6. ~~**읽지 않은 것을 무엇으로 셀지**~~ → **확정(Lead 권고, Director 위임 2026-09-23)**: **방 단위** `room_participant.last_read_message_id`(사람 행만). 미션·서브 미션 단위는 구독(알림)으로만 내리고(§10 R2), 안 읽음 배지는 방에 하나다 — 두 단위의 배지가 같이 뜨면 사람이 어느 것을 지워야 하는지 모른다.
7. ~~**FR-4.4 컨텍스트 재사용을 유지할지**~~ → **확정(Lead 권고, Director 위임)**: **버린다.** 마법사와 함께 v1 범위에서 빼고, 옛 대화를 참고하는 길은 FR-4.5 다(보관된 방도 참여자면 읽을 수 있다). 화면을 두지 않는다.
8. ~~**화면에서 Work 를 무엇이라 부를지**~~ → **확정(Director, 2026-09-23)**: **미션**. 미션을 위해 쪼개인 갈래는 **서브 미션**이다(§3.2 화면 용어표).
9. ~~**방을 멈추는 칸**~~ → **확정(Director, 2026-09-23)**: **`room.blocked_reason`**(`budget` · `runtime_offline` · `loop` · `manual` · null)을 둔다. 방 `status` 는 `active|archived` 그대로 — 상태 머신을 늘리지 않는다. 큐는 `blocked_reason IS NOT NULL` 인 방의 task 를 주지 않는다. `budget`·`loop`·`runtime_offline` 의 해제는 승인 HITL(또는 재바인딩)의 결과이고, `manual` 은 건 권한자가 직접 푼다(FR-2.4).
10. ~~**§11 지표 10개의 분모**~~ → **확정(Lead 권고, Director 위임)**: 1·2·5 는 미션, 10 은 방(§11 에 반영됨). G9 가 못박은 표라 바꾸려면 근거를 남긴다.
11. ~~**방 목록이 늘어날 때의 기본 정렬·검색**~~ → **확정(Lead 권고, Director 위임)**: **마지막 활동순**, 안 읽음은 배지로만(정렬을 바꾸지 않는다 — 목록이 스스로 움직이면 사람이 방을 못 찾는다). 검색은 이름·설명 부분 일치. 보관된 방은 기본 숨김 + 토글.

---

## 부록 A. multica 참고 코드 경로

| 기능 | 경로 |
|---|---|
| 멘션 파싱 | `server/internal/util/mention.go` |
| 댓글 라우팅 규칙 | `server/internal/handler/comment.go` (`computeCommentAgentTriggers`) |
| 스쿼드 리더 프로토콜 | `server/internal/handler/squad_briefing.go` |
| 프롬프트 조립 / 브리프 파일 | `server/internal/daemon/prompt.go`, `daemon/execenv/runtime_config_sections.go` |
| 에이전트 → 플랫폼 회신 CLI | `server/internal/daemon/execenv/reply_instructions.go` |
| 태스크 상태·재시도 | `server/internal/service/task.go`, `server/pkg/taskfailure` |
| 에이전트 권한 판정 | `server/internal/handler/agent_access.go` |
| 에이전트·런타임 스키마 | `server/migrations/001_init.up.sql`, `server/pkg/db/generated/models.go` |
| 데몬/CLI 구조 | `CLI_AND_DAEMON.md`, `CLI_INSTALL.md` |
| 런타임 백엔드 인터페이스 (`Backend.Execute`, 25개 프로토콜 + omp) | `server/pkg/agent/agent.go`, `builtin_runtimes.go` |
| Claude Code 어댑터 | `server/pkg/agent/claude.go`, `claude_models.go`, `thinking.go` |
| Antigravity 어댑터 | `server/pkg/agent/antigravity.go`, `models.go` (agy models 파싱) |
| Hermes 어댑터 (ACP) | `server/pkg/agent/hermes.go`, `acp_usage.go`, `daemon/execenv/hermes_home.go`, `hermes_sessions.go` |
| 런타임별 브리프 파일 매핑 | `server/internal/daemon/execenv/runtime_config.go` |
| 가격표 | `server/internal/metrics/pricing.go` |
| 문서 | `apps/docs/content/docs/{agents,squads,mentioning-agents,tasks,inbox,issues,daemon-runtimes,providers}.mdx` |

## 부록 B. block/buzz 참고 코드 경로

v0.4에서 채택한 설계의 출처. Buzz는 Rust + Nostr 이벤트 로그 기반이라 저장소 아키텍처는 따르지 않고, 아래 항목만 참고했다.

| 채택한 것 | 경로 |
|---|---|
| ACP 하네스 전체 구조 (풀, 큐, 스코프, 프롬프트 프레이밍) | `crates/buzz-acp/src/{lib,pool,queue,scope,prompt_framing}.rs` |
| ACP 프로토콜 처리 — 핸드셰이크, `allow_once` 탐색, 취소 절차 | `crates/buzz-acp/src/acp.rs` |
| 런타임 프리셋·능력 선언 | `desktop/src-tauri/src/managed_agents/discovery.rs`, `discovery/presets.rs` |
| 활동 피드 설계 (동사·목적어·결과, 렌더 클래스, 원칙) | `VISION_ACTIVITY.md` |
| "Needs Action" 인박스 질의 | `crates/buzz-db/src/store/feed.rs` |
| 이식 가능한 에이전트 정의 파일 (`.persona.md`) | `crates/buzz-persona/src/{persona,manifest}.rs`, `examples/meadow-core/agents/*.persona.md` |
| 정의/인스턴스 분리 | `desktop/src-tauri/src/managed_agents/types.rs` |
| 호출 권한 게이트 (`respond-to`의 4단계 값) | `crates/buzz-acp/src/lib.rs`. **형제 에이전트 규칙은 채택하지 않았다** — 소유자 귀속 대신 세션 참여를 권한 근거로 삼는다(FR-1.9, C5) |
| 턴 진행 중 메시지 처리 (`multiple-event-handling`) | `crates/buzz-acp/src/config.rs`. **정책 자체를 채택하지 않았다** — Buzz는 `steer`가 기본이지만, 우리는 어떤 메시지도 진행 중인 턴을 취소하지 않고 취소를 UI 조작으로만 한다(FR-3.4, N2) |
| 승인 게이트 | `crates/buzz-workflow/src/{executor,schema}.rs`. **토큰 모델은 채택하지 않았다** — Buzz는 서명 이벤트로 승인이 들어와 토큰이 필요하지만, 우리는 인증 세션으로 받으므로 불필요하다(FR-5.4, M4) |
| 빈 확인 메시지 금지 등 공통 규약 | `crates/buzz-acp/src/base_prompt.md` |
| 메모리 조회 실패와 부재의 구분 | `crates/buzz-acp/src/engram_fetch.rs` |
| 컨텍스트 압축(자기 요약 핸드오프) | `crates/buzz-agent/src/handoff.rs` |
| 클라우드 런타임 수명 관리 (v2 참고) | `VISION_REMOTE_AGENTS.md` |

**채택하지 않은 것**: Nostr 이벤트 로그를 저장 substrate로 쓰는 구조, 키페어 기반 에이전트 아이덴티티, YAML 워크플로 엔진(우리는 lane DAG로 대체), 공유 작업 디렉토리 모델(우리는 lane 격리).

## 부록 C. 런타임 공식 문서 출처

| 런타임 | 문서 |
|---|---|
| Claude Code | [Run Claude Code programmatically (headless)](https://code.claude.com/docs/en/headless), [CLI reference](https://code.claude.com/docs/en/cli-reference) |
| Antigravity | [Headless mode](https://antigravity.google/docs/cli/headless/), [Getting started](https://antigravity.google/docs/cli/getting-started), [MCP](https://antigravity.google/docs/cli/mcp/), [Best practices](https://antigravity.google/docs/cli/best-practices/), [antigravity-cli #60 (프로젝트 로컬 MCP 무시)](https://github.com/google-antigravity/antigravity-cli/issues/60) |
| Hermes | [CLI Commands Reference](https://hermes-agent.nousresearch.com/docs/reference/cli-commands), [CLI Interface](https://hermes-agent.nousresearch.com/docs/user-guide/cli), [MCP](https://hermes-agent.nousresearch.com/docs/user-guide/features/mcp), [Sessions](https://hermes-agent.nousresearch.com/docs/user-guide/sessions) |
