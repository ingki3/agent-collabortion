# 화면 설계 #01 — 정보 구조와 화면 명세

| 항목 | 내용 |
|---|---|
| 문서 버전 | **v0.19.1 (Draft)** — v0.19 **구조 전환**(세션 → 방 + 미션) + **독립 검증 3건 반영**(SCR-A 사용성 · SCR-B 구현 · SCR-C 디자인). PRD 가 정본이고 이 문서가 따라간다 |
| 위치 | **이 파일이 화면 설계의 SSOT다.** 이전 버전과 리뷰는 `screen/` |
| 작성일 | 2026-09-22 (v0.3: 2026-09-03) |
| 근거 | `PRD.md` **v0.19** (SSOT, PR #269~#274 머지본). 모든 화면·기능·상태는 PRD의 FR 번호로 역참조한다. **PRD 와 이 문서가 어긋나면 PRD 가 이긴다** |
| 입력 | `plan/research/V19_flow.md` §2 화면 충격표·§5 제안 문장(전건 반영), `plan/research/V19_impl.md` §1.5 라우트·§6 NN1~9, **`plan/research/SCR19_usability.md`(SCR-A) §5 P-1~P-11 · `SCR19_impl.md`(SCR-B) §1 화면→op·§2 SSE·§6.1 C1~C12 · `SCR19_design.md`(SCR-C) §6 A~T·§7 우선순위**, 현재 `web/__screenshots__/` 174장(실측) |
| 범위 | v1(MVP) 화면 전체 + v1.1 이후로 미루는 화면의 자리 표시. **v0.19 는 PRD §10 의 v2.0(R0~R4)이며, 이 문서의 "v1" 표기는 그 전환이 끝난 뒤의 기준 제품을 말한다** |
| 목적 | 화면 목록, 화면 간 연결, 각 화면의 기능·표시 정보·권한을 확정해 UI 디자인과 API 설계의 공통 기준을 만든다. **계약(R0)은 화면이 부르는 것에서 나오므로 이 문서가 먼저다** |

> **읽는 법.** §1~§3은 전체 구조(화면 지도·권한·용어), §4는 화면별 상세 명세, §5~§7은 화면을 가로지르는 규칙(공통 컴포넌트·실시간·빈 상태), §8은 PRD 대조 결과와 **화면이 못 받는 조항(공백)** 이다. 화면 하나만 볼 사람은 §2.3 권한 표와 §4의 해당 절만 읽으면 된다.

> **v0.3 → v0.19 에서 무엇이 바뀌었나.** 삭제 1(S6 마법사) · 폐기 1(S16 채널) · 재작성 2(S5·S7) · 수정 7(S4·S8·S9·S11·S13·S14·S17) · 승격 1(S15 v1.1→v1) · **신규 9(S18~S26)**. 기존 S 번호는 **재사용하지 않는다** — 문서·테스트·스크린샷이 옛 번호를 가리키고 있어서다. 상세는 §8.6.
>
> **v0.19 → v0.19.1 에서 무엇이 바뀌었나.** 검증 3건을 반영했다 — **용어 정정**(「산출물」을 전부 **「아티팩트」**로 되돌림, PRD §3.2 정본) · **PRD 확정 반영**(S25 정렬·검색, 안 읽음 모델, 지표 분모, 컨텍스트 재사용 폐기, 방장 부재 위임, 방 긴급 정지 `manual`) · **파괴적 동작 봉인**(`(전체)` 보기에서 미션 동작 비활성) · **접근성**(툴팁 → `DisabledHint`, 미션 칩 라벨·배너 role·건너뛰기) · **밀도 규칙**(우열 방 전체 칸 기본 접힘, 보드 `done`·`failed` 접힘) · **신규 명세**(첫 dispatch 격리 승인 카드, 「이 방 멈춤」, 화면→op 부록). 상세는 §8.6.

---

## 1. 설계 원칙

PRD에서 직접 따라오는 제약이다. 화면을 그릴 때 이 여섯 가지가 계속 충돌 판정 기준이 된다.

1. **사람이 시간을 보내는 곳은 방 하나다.** 워크스페이스에는 에이전트·컴퓨터·방이 있고, 방은 끝나지 않는다. 나머지 화면은 방을 만들거나(방 목록) 방 밖에서 답하기 위해(받은 요청) 존재한다. **방이 곧 화면의 중심이고, 미션은 그 안에서 열리고 닫힌다**(FR-2.4 — 방은 `completed` 되지 않는다).
2. **끝이 있는 것과 없는 것을 화면에서 가른다.** goal·진행률·요약·승인·비용 상한은 **미션**의 것이고, 참여자·설정·아티팩트·결정 기록·누적 비용은 **방**의 것이다(FR-2A.5). 한 칸이 둘 다 말하면 미션이 2개 열린 순간 화면이 거짓말을 한다.
3. **파괴적 동작은 화면 조작으로만 한다.** 어떤 메시지도 진행 중인 턴을 취소하지 않는다(FR-3.4). 취소는 서브 미션 카드의 버튼이고, 그래서 서브 미션 보드는 장식이 아니라 **유일한 제어판**이다.
4. **침묵도 렌더한다.** 이벤트가 없는 것도 정보다(FR-7.2). 빈 목록·대기·응답 없음은 모두 명시적 상태로 그린다. **빈 방은 특히 그렇다** — 방 만들기가 칸 하나가 된 대가로 "이제 무엇을 하나"를 말해 줄 곳이 빈 방밖에 없다(§4.6 빈 상태).
5. **권한은 숨기지 않고 비활성으로 보인다.** 방장·Director만 쓸 수 있는 버튼도 다른 사람에게 보이되 비활성이고, **이유를 버튼 바로 아래에 글자로 적는다**(COMPONENTS §8.5, 구현 `DisabledHint`). **툴팁으로 쓰지 않는다** — 비활성 버튼은 키보드 포커스를 받지 못해 툴팁을 띄울 방법이 없고, 그러면 **권한 구조가 키보드·스크린리더 사용자에게만 안 보인다.** 누가 결정권을 갖는지가 화면에서 읽혀야 한다. **사유에는 층을 적는다** — 「Director만 가능」이 아니라 「〈민호〉가 이 미션의 Director 입니다」(§2.3).
6. **되돌릴 수 없는 확정은 두 번 말한다.** 마법사가 사라지면서 사용자가 **고르지 않은 값**(격리·컴퓨터)이 첫 실행에 굳는다. 만들 때 한 번, 굳는 순간 한 번 말한다(FR-2.1.1). 이것이 v0.19 가 새로 지는 가장 큰 사고 위험이다(V19-A W-1·W-2).

---

## 2. 사용자 상태와 접근 권한

### 2.1 인증 상태

| 상태 | 접근 가능 | 진입 시 |
|---|---|---|
| **로그아웃** | 로그인, 회원가입, 초대 수락 | 보호된 URL 접근 시 로그인으로 보내되 **원래 URL을 기억**했다가 로그인 후 그 자리로 복귀 |
| **로그인, 워크스페이스 없음** | 워크스페이스 생성, 초대 대기 목록 | 온보딩(§4.2)으로 강제 이동 |
| **로그인, 워크스페이스 있음, 컴퓨터 없음** | 전체 화면 접근 가능. **방은 만들 수 있다** | 방 만들기는 막지 않는다 — 방은 `runtime_id` 없이 만들어지고 첫 실행 때 고정된다(FR-2.3). 대신 방 화면 상단에 「연결된 컴퓨터가 없어 에이전트가 아직 일을 시작할 수 없습니다」 배너 + S12 링크 |
| **로그인, 준비 완료** | 전체 | 마지막으로 본 방 또는 방 목록 |

> **v0.3 에서 뒤집은 결정(m5)**: v0.3 은 "런타임이 없으면 세션을 만들 수 없다"를 화면 결정으로 못박았다. **v0.19 에서는 철회한다.** 방 만들기는 칸 하나이고(FR-2.1) 방은 대화 공간이므로, 컴퓨터가 붙기 전에도 사람끼리 방을 만들고 이야기할 수 있어야 한다. 막는 대신 **왜 에이전트가 조용한지**를 방 안에서 말한다 — 원래 m5 의 취지("원인을 모른 채 기다리지 않게")는 이 배너가 더 잘 지킨다.

### 2.2 역할

역할은 **네 층**으로 나뉘고 서로 독립이다. 화면 권한을 판정할 때 넷을 각각 봐야 한다. v0.3 은 세 층이었고, v0.19 가 **방 층**을 새로 넣었다.

| 층 | 값 | 근거 |
|---|---|---|
| 워크스페이스 역할 | `owner` / `admin` / `member` | §7 `member.role` |
| **방 역할** | **방장** / **부방장**(선택) / **방 참여자** | FR-2.2, FR-5.3, §12.1-4 확정 |
| **미션 역할** | `director` / `deputy` | FR-5.3 — **미션마다** 다르다 |
| 에이전트 소유 | 에이전트별 `owner_id` | FR-1.9 |

> **`참여자`는 이제 사람과 에이전트 둘 다를 가리킨다** (PRD FR-5.3 v0.19, §7 `room_participant` 한 표). v0.3 의 「`participant`는 에이전트 전용 용어다」는 **철회한다.** 방에서 둘의 차이는 "트리거되는가"뿐이고 초대·퇴장·목록·권한 검사는 같은 코드가 처리한다. 화면에서도 참여자 목록은 **한 목록**이고 사람·에이전트를 아이콘으로만 구분한다.
>
> **방에 사람을 초대하는 절차가 생겼다.** v0.3 은 "세션에 사람을 초대하는 절차는 없다"였다. v0.19 는 방 설정 `visibility` 로 둘 다 살린다(§2.4).

### 2.3 화면·동작별 권한 매트릭스

**워크스페이스 층**

| 동작 | owner | admin | member | 비고 |
|---|---|---|---|---|
| 워크스페이스 설정 변경 | ✅ | ✅ | ❌ | 예산·루프 상한·보존 기한·**방 기본값** |
| 멤버 초대·역할 변경 | ✅ | ✅ | ❌ | owner 강등은 owner만 |
| 컴퓨터 추가·삭제 | ✅ | ✅ | ❌ | 진행 중 방이 걸린 컴퓨터는 삭제 차단(FR-9.2) |
| 에이전트 생성 | ✅ | ✅ | ✅ | 생성자가 `owner_id` |
| 에이전트 수정·삭제 | ✅ | ✅ | 소유자만 | **PRD 미정 — §8.7 Q7** |
| **방 만들기** | ✅ | ✅ | ✅ | 생성자가 **방장** |
| **모든 방 열람** | ✅ | ✅ | ❌ | 감사 목적. 게시는 참여 뒤(FR-5.3) |

**방 층** — 방 안의 동작은 워크스페이스 역할이 아니라 **방 역할**이 정한다.

| 방 안 동작 | 방장 | 부방장 | 방 참여자(사람) | ws owner·admin | 근거 |
|---|---|---|---|---|---|
| 방 열람 | ✅ | ✅ | `visibility` 에 따름 | ✅ | FR-5.3, §2.4 |
| 메시지 게시·멘션 | ✅ | ✅ | ✅ | 참여 뒤 ✅ | FR-5.3 — 참여자 메시지도 라우팅된다 |
| 참여자 초대·퇴장(사람·에이전트) | ✅ | ✅ | ❌ | ✅ | FR-2.2 |
| 방 설정 변경(격리·컴퓨터·한도·autonomy·기본 Director) | ✅ | ✅ | ❌ | ✅ | FR-2.3 |
| **참고 방 링크 걸기·풀기** | ✅ | ✅ | ❌ | ✅ | FR-4.5 |
| **미션 밖 할 일의 HITL 응답·승인** | ✅ | 기한 절반 경과 후 | ❌ | 부방장이 없으면 기한 절반 후 **owner 최고참 1인** | FR-2A.1 `approver_spec: room_owner` + FR-2A.3 위임 경로 |
| **방 상한 초과 해소 승인** | ✅ | 기한 절반 경과 후 | ❌ | 〃 | FR-2A.3 — 그 방의 모든 미션이 멈춘 상태 |
| **「이 방 멈춤」·해제**(`blocked_reason: manual`) | ✅ | ✅ | ❌ | ✅ | FR-2.4 [V19-B] — v1 「세션 취소」의 자리 |
| 서브 미션 중단·재지시 | ✅ | ✅ | ❌ | ❌ | FR-3.4 — 방장은 방 전체의 폭주를 멈출 수 있어야 한다 |
| (미션에 속한 서브 미션은 **미션층 표의 director·deputy 도** 중단할 수 있다) | | | | | 아래 미션층 참조 |
| 「여기까지 정리」 | ✅ | ✅ | ✅ | ✅ | FR-2.5 — 읽기 보조라 제한하지 않는다 |
| 방 보관·보관 해제 | ✅ | ✅ | ❌ | ✅ | FR-2.4 — 진행 중 할 일이 있으면 거부 |
| 방 삭제 | ✅ | ❌ | ❌ | ✅ | FR-2.6 — 진행 중 미션이 있으면 거부 |
| 방장 넘기기 | ✅ | ❌ | ❌ | ✅ | §12.1-4 |

> **방장 부재 — 「떠남」과 「자리 비움」이 다르고 둘 다 답이 있다**(SCR-A 막힘 8 · P-6·G-10, PRD FR-2A.3·§12.1-4 확정).
>
> - **떠남**(워크스페이스 탈퇴): **워크스페이스 owner 중 가장 오래된 한 명이 자동 승계**하고 그 사실이 방 타임라인에 시스템 메시지로 남는다(§4.6 시스템 메시지 4). 화면은 승계를 **알리기만** 한다 — 사람이 누를 버튼은 없다.
> - **자리 비움**(응답 없음): 방장만 답할 수 있는 두 가지(미션 밖 할 일의 HITL·방 상한 해소 승인)는 **기한의 절반이 지나면 부방장**에게, 부방장이 없으면 **워크스페이스 owner 중 가장 오래된 한 명**에게 넘어간다 — 미션 `deputy` 와 같은 시간 위임이다. **방장 한 사람의 부재로 방이 영구 정지하지 않는다.**
> - 화면은 넘어가는 시점을 **미리 말한다** — 카드·배너에 「**방장 〈민호〉 응답 대기 · 14:30부터 〈서연〉(부방장)이 답할 수 있습니다**」(§4.6 방 멈춤 배너 · §4.14 S8).
>
> **부방장은 방장의 대리가 아니다.** 부방장은 일상 운영(초대·설정·중단·「이 방 멈춤」)을 나눠 지고, **돈과 삭제는 기한 전에는 못 만진다** — 방 상한 해소 승인은 기한 절반 이후에만, 방 삭제는 끝까지 방장(또는 ws owner·admin)만이다. 미션의 `deputy` 와는 다른 층이며 같은 사람일 필요도 없다.

**미션 층** — 미션 안의 동작은 **그 미션의** Director·deputy 가 정한다. 방에 미션이 3개면 사람마다 미션별로 권한이 다르다.

| 미션 안 동작 | director | deputy | 방장 | 방 참여자 | 근거 |
|---|---|---|---|---|---|
| HITL 응답(`question`) | ✅ | 기한 절반 경과 후 | ❌ | ❌ | FR-5.4 |
| HITL 승인(`approval`) | ✅ | 기한 절반 경과 후 | ❌ | ❌ | 자동 진행 없음 |
| 미션 예산 초과 후 계속 승인 | ✅ | 기한 절반 후 | ❌ | ❌ | FR-7.3 |
| 미션 일시정지·재개·종료(`manual`) | ✅ | ❌ | ❌ | ❌ | FR-2A.4 |
| **서브 미션 중단·재지시** | ✅ | **즉시** | ✅ | ❌ | FR-3.4, t-3 — deputy 의 즉시 취소는 PRD FR-5.3 의 비대칭 그대로(카드 본문과 일치) |
| 미션 취소·삭제 | ✅ | ❌ | ❌ | ❌ | FR-2A.6 |
| 종료 조건 고치기 | ✅ | ❌ | ❌ | ❌ | S-84 — `blocked_reason` 해소 |
| 미션 Director 교체 | ✅ | ❌ | ❌ | owner·admin만 | t-5 |
| 미션 열기(「새 미션」·「이걸 미션으로」) | 방 참여자 누구나 — 연 사람이 기본 Director | | | | FR-2A.1 |

> **v1 HITL 타입은 `question`과 `approval` 둘이다** (PRD §10). `choice`·`info`는 v1.1에서 **같은 카드 컴포넌트에 입력부만 교체**되어 추가된다. §5의 HITL 카드는 "타입별 입력부 교체 가능"으로 설계한다.
>
> **deputy의 비대칭**: 취소는 즉시, 승인은 기한 절반 이후다(t-3). UI에서 승인 버튼이 비활성일 때 "Director 응답 대기 중 · 14:30부터 승인 가능"처럼 **언제부터 가능한지**를 함께 보여준다.
>
> **권한이 미션마다 다르다는 것을 화면이 말해야 한다.** 방 화면 우열의 미션 칸에는 항상 **이 미션의 Director 이름**이 보이고, 내가 그 미션의 Director 가 아니면 버튼이 비활성 + 「〈이름〉이 이 미션의 Director 입니다」. 방 하나에 Director 가 셋인 상태가 정상이므로, 비활성 사유가 "Director만 가능"에서 멈추면 사용자가 자기 권한을 못 찾는다.

### 2.4 방의 공개 범위 (`visibility`)

| 값 | 열람 | 게시·멘션 | 방 목록에 보이나 |
|---|---|---|---|
| `workspace` (**기본**) | 워크스페이스 멤버 누구나 | 누구나 | 전원에게 보인다 |
| `invited` | 초대된 사람 + ws owner·admin | 초대된 사람만 | 초대된 사람에게만. **owner·admin 에게는 「감사 열람」 표시와 함께 보인다** |

- **기본값은 `workspace` 다** — v0.18 과 같은 열린 동작을 유지한다(FR-5.3). `invited` 는 사람이 명시적으로 고를 때만이고, 고르는 순간 방 타임라인에 시스템 메시지로 남긴다.
- `invited` 방에서 owner·admin 이 열람할 때는 상단에 **「감사 목적으로 열람 중입니다 — 게시하려면 참여해야 합니다」** 배너를 띄우고 작성창을 비활성한다. 감사 열람은 `activity_log` 에 남는다.
- **에이전트에게는 `visibility` 가 없다.** 에이전트는 초대된 방만 본다(FR-2.2). `workspace` 방이라고 모든 에이전트가 읽는 것이 아니다 — 그것은 FR-4.5 의 다른 방 읽기가 권한 두 조건으로 따로 판정한다.

---

## 3. 화면 지도

### 3.1 전체 구조

```
[로그아웃]
  로그인 ──── 회원가입
     │           │
     └─→ 초대 수락 ─┐
                    ↓
[로그인]        온보딩 (워크스페이스 없음 / 컴퓨터 없음일 때만)
                    ↓
        ┌───────────┴──────────────────────────────────────────────┐
        │              앱 셸 (좌측 내비 + 상단바)                     │
        ├──────────────────────────────────────────────────────────┤
        │  방 ──→ S18 방 만들기(이름 한 칸) ──→ S7 방 화면            │
        │   │  └→ S25 방 찾기(검색·정렬 고정·안 읽음 거르개)          │
        │   │                                                       │
        │   └─ S7 방 화면 ─┬─ S19 참여자 초대·퇴장                    │
        │                  ├─ S20 방 설정 ─→ S24 참고 방 링크         │
        │                  ├─ S21 미션 열기 ─→ S22 미션 패널          │
        │                  ├─ S26 미션 제안 확인                      │
        │                  └─ S23 맥락 읽기 기록                      │
        │                                                           │
        │  받은 요청 (S8) ───────────────→ 방·미션 바로가기            │
        │  에이전트 (S9) ──→ S10 편집 ──→ 테스트 채팅                  │
        │  연결된 컴퓨터 (S11) ──→ S12 컴퓨터 연결                    │
        │                     ├→ S13 작업 폴더 관리                   │
        │                     └→ S17 컴퓨터 바꾸기(재바인딩)           │
        │  설정 (S14, owner·admin) ──→ S15 활동 로그                  │
        └──────────────────────────────────────────────────────────┘

   ✖ S6 새 세션 마법사 — 삭제(FR-2.1)
   ✖ S16 채널 목록·상세 — 폐기(방이 그 자리에 들어앉았다)
```

**마법사 7단계가 간 곳.** 삭제는 기능의 삭제가 아니다 — 7단계가 강제로 보여 주던 것이 세 곳으로 나뉜다. 어느 것도 잃지 않는다는 것이 §8.4 자기 점검의 한 줄이다.

| 마법사 단계 | 간 곳 |
|---|---|
| 1 goal·성공 기준·첨부 | **S21 미션 열기** (goal 필수). **첨부 컨텍스트는 v1 범위에서 버렸다**(FR-4.4, §12.1-7 확정) — 옛 대화를 참고하는 길은 FR-4.5 다 |
| 2 Director·deputy | **S21 미션 열기** (기본값은 방 설정 `default_director`) |
| 3 격리 + workdir 바인딩 그림 | **S20 방 설정** + **S18 만들기 화면의 한 줄** + **첫 실행 시스템 메시지**(FR-2.1.1) |
| 4 컴퓨터(런타임) | **S20 방 설정**. 안 고르면 첫 실행 때 고정되고 그 사실을 두 번 말한다(FR-2.1.1) |
| 5 참여자·프로파일·`respond_to` 차단 사유 | **S19 참여자 초대·퇴장** |
| 6 종료 조건(사람 말 문장·리뷰어 필수·`agent_approval` 경고) | **S21 미션 열기** + **S22 미션 패널의 「조건 고치기」** |
| 7 한도·`autonomy`(세 값의 차이를 문장으로) | **S20 방 설정**(방 기본값) + **S21 미션 열기**(미션 덮어쓰기) |

### 3.2 화면 목록과 우선순위

| # | 화면 | URL | **종류** | v1 | 진입 경로 | v0.19 |
|---|---|---|---|---|---|---|
| S1 | 로그인 | `/login` | 페이지 | ✅ | 미인증 접근, 로그아웃 | — |
| S2 | 회원가입 | `/signup` | 페이지 | ✅ | 로그인 화면 | — |
| S3 | 초대 수락 | `/invite/:token` | 페이지 | ✅ | 이메일 링크 | — |
| S4 | 온보딩 | `/onboarding` | 페이지 | ✅ | 워크스페이스·컴퓨터 없음 | **수정** — 3단계 CTA가 「첫 방 만들기」 |
| S5 | **방 목록** | `/rooms` | 페이지 | ✅ | 내비, 로그인 후 기본 | **재작성** — 옛 Sessions 목록 |
| ~~S6~~ | ~~새 세션 마법사~~ | ~~`/sessions/new`~~ | — | **삭제** | — | FR-2.1 «마법사는 없앤다» |
| S7 | **방 화면** | `/rooms/:id` | 페이지 | ✅ | 목록, 받은 요청, 알림 | **재작성** — 옛 Session 상세 |
| S8 | **받은 요청** | `/inbox` | 페이지 | ✅ | 내비(뱃지), 알림 | **수정** — 항목 타입이 방/미션으로 갈린다 |
| S9 | 에이전트 목록 | `/agents` | 페이지 | ✅ | 내비 | **수정** — 카드에 「참여 중인 방 N」 |
| S10 | 에이전트 편집 | `/agents/:id` · `/agents/new` | 페이지 | ✅ | 에이전트 목록 | — (테스트 채팅은 방이 아니다, FR-1.8.1) |
| S11 | 연결된 컴퓨터 | `/runtimes` | 페이지 | ✅ | 내비, 온보딩 | **수정** — 「묶인 방 N개」 |
| S12 | 컴퓨터 연결 | `/runtimes/new` | 페이지 | ✅ | 연결된 컴퓨터 | — |
| S13 | 작업 폴더 관리 | `/runtimes/:id/workdirs` | 페이지 | ✅ | 컴퓨터 카드 | **수정** — 「쓰는 중인 방」·보존 기준이 `last_used_at` |
| S14 | 설정 | `/settings/*` | 페이지 | ✅ | 내비(owner·admin) | **수정** — 「방 기본값」 묶음 |
| S15 | **활동 로그(감사)** | `/settings/audit` | 페이지 | ✅ **승격** | 설정, S23 | **v1.1 → v1** — FR-4.5 읽힌 쪽 기록이 갈 곳이 여기뿐 |
| ~~S16~~ | ~~채널 목록·상세~~ | ~~`/channels/*`~~ | — | **폐기** | — | 방이 그 자리다. 방 **위** 계층은 §8.2 G-3 |
| S17 | 컴퓨터 바꾸기(재바인딩) | `/runtimes/:id/rebind?room=` | 페이지 | ✅ | S11 카드, S7 배너 | **수정** — `?session=` → `?room=` |
| **S18** | **방 만들기** | `/rooms/new` | **다이얼로그** | ✅ | S5 「새 방」, 빈 상태, S4 3단계 | **신규** |
| **S19** | **참여자 초대·퇴장** | `/rooms/:id/participants` | **다이얼로그** | ✅ | S7 좌열 「참여자」, 빈 방 CTA | **신규** |
| **S20** | **방 설정** | `/rooms/:id/settings` | 페이지 | ✅ | S7 상단, 우열 「방 설정」 | **신규** |
| **S21** | **미션 열기** | `/rooms/:id?work=new` · `?work=from&message=:mid` | **다이얼로그** | ✅ | S7 「새 미션」·메시지 「이걸 미션으로」 | **신규** |
| **S22** | **미션 패널** | `/rooms/:id?work=:workId` | **같은 화면의 상태**(S7 우열) | ✅ | S7 상단 미션 칩, S8 카드 | **신규** |
| **S23** | **맥락 읽기 기록** | `/rooms/:id/reads` | 페이지 | ✅ | S7 우열 「맥락 오간 기록」, 시스템 메시지 | **신규** |
| **S24** | **참고 방 링크** | `/rooms/:id/settings/links` | **다이얼로그** | ✅ | S20 | **신규** |
| **S25** | **방 찾기** | `/rooms?q=&unread=&archived=` | **같은 화면의 상태**(S5 상단 제어) | ✅ | S5 상단 | **신규** |
| **S26** | **미션 제안 확인** | `/rooms/:id?work_proposal=:id` | **다이얼로그** | ✅ | S8 `work_proposed` 항목, S7 제안 카드 | **신규** |

> **「종류」가 상태인 행(S22·S25)은 새 화면이 아니라 이미 있는 화면의 한 상태다**(SCR-C D). URL 을 갖는 이유는 아래 각주와 같다. **R2 구현을 셀 때 이 둘은 화면 수에 넣지 않는다** — 넣으면 26개로 읽히고 실제로 새로 그릴 것은 24개다. S25 에서 `sort` 파라미터가 빠진 것은 정렬이 **마지막 활동순 하나로 고정**됐기 때문이다(§4.4, PRD §12.1-11).

> **번호를 이어 붙인 이유.** S18~S26 은 비어 있던 번호가 아니라 **S17 다음**이다. 옛 번호(S6·S16)를 새 화면에 재사용하면 `EVAL_USER.md`·스크린샷 파일명(`p4-w5-01-s6-isolation.png` 등 174장)·리뷰 문서가 가리키는 대상이 조용히 바뀐다. 삭제·폐기된 번호는 **비워 둔 채로** 남긴다.
>
> **다이얼로그에도 URL을 준다.** S19·S21·S24·S26 은 방 화면 위에 뜨는 다이얼로그지만 경로를 갖는다 — 받은 요청·알림·시스템 메시지에서 **바로 그 자리로** 보낼 수 있어야 하고(F2 지표: 방을 열지 않고 답한다), 새로고침해도 돌아와야 한다. S17 이 v0.3 에서 같은 이유로 경로를 가졌다.

### 3.3 주요 사용자 흐름

**F1. 신규 사용자 → 첫 미션이 열리고 끝난다** (목표: 15분, §11 지표 1)
```
S2 회원가입 → S4 온보딩 1(워크스페이스 생성)
           → S12 컴퓨터 연결 (설치 명령 2줄 복사 → 데몬 연결 대기)
           → S11 확인 (CLI 감지)
           → S9/S10 에이전트 등록 (템플릿에서 팀 통째로)
           → S18 방 만들기 (이름 한 칸)
           → S19 참여자 초대 (에이전트 3)
           → S7 첫 멘션 전송 → 참여자 칩이 「작업 중」
           → S7 그 메시지의 「이걸 미션으로」 → S21 (goal 기본값 = 그 본문) → 「열기」
           → S8 또는 S7 우열에서 「승인」 → 미션 `completed` + 요약 카드
```
**v0.3 의 F1 과 도달 기준이 다르다.** 옛 기준은 「세션 하나가 돌기 시작한다」였고 마법사 7단계가 그 절반이었다. 방은 **만들어도 아무것도 돌지 않는다** — 누가 메시지를 쳐야 돈다.

**도달 기준은 「첫 미션 완료」다**(PRD §11 지표 1 · SCR-A P-9). 「첫 멘션 → 칩이 `working`」에서 멈추면 **신규 사용자가 「미션」이라는 말을 한 번도 만나지 않고 온보딩을 끝내는데 그 사람의 15분을 재는 지표는 「첫 미션 완료」**가 되어, G8 실측에서 지표 1 의 모수가 0 이 된다. 그래서 대본 마지막 두 단계(미션 열기 · 승인)가 F1 의 일부다. 미션을 여는 자리는 **첫 멘션이 만든 메시지의 「이걸 미션으로」**이고 — 새 화면을 더하지 않는다.

단계 수는 13 → 10 으로 줄지만, **줄어든 만큼 설명도 사라졌다.** 마법사 3단계가 유일하게 가르치던 「코드는 어디 있나요?」(U16 사후 질문)의 답이 없어지므로, **S18 만들기 화면의 한 줄**·**첫 실행 시스템 메시지**·**첫 dispatch 격리 승인 카드**(§4.14 `isolation_confirm`)가 그 자리를 대신한다(FR-2.1.1, §4.5·§4.6·§4.14). 셋이 없으면 G8 은 "시간은 통과, 정답률은 하락"으로 갈린다.

가장 이탈이 많을 지점은 여전히 **S12의 데몬 연결 대기**다. 연결될 때까지 폴링하며 "대기 중 → 연결됨 → CLI 감지됨"을 단계로 보여준다.

**F2. HITL 응답** (지표: 중앙값 30분 내)
```
알림/뱃지 → S8 받은 요청 → 항목 선택 → 인라인 응답
                                   → (맥락이 더 필요하면) 방 바로가기 · 미션 바로가기
```
받은 요청에서 **맥락 없이 답할 수 있어야** 한다. 카드 안에 질문·선택지·제안 기본값이 있고, **맥락 한 줄**은 다음 규칙으로 채운다(v0.19 에서 새로 필요해진 규칙 — 미션 goal 이 없는 항목이 생겼다).

| 항목이 속한 곳 | 맥락 한 줄 |
|---|---|
| 미션 안 | `방 이름 · 미션 goal` |
| 미션 밖 할 일 | `방 이름 · 「…」 로 시작한 대화` — **트리거 메시지 첫 줄을 둘째 줄에 인용한다**(§4.14) |
| 방 전체(방 상한 초과) | `방 이름 · 이 방의 미션 N개가 멈췄습니다` |

**F3. 방향 전환** (시나리오 C)
```
S7 → 메시지 작성 → 전송 (실행 중이면 큐잉, 턴은 죽지 않음)
   또는 서브 미션 카드 → "중단하고 다시 지시" → 멘션 채워진 작성창 → 전송 → 턴 취소 후 재시작
   또는 서브 미션 카드 → "중단" → 확인 → 서브 미션 failed(cancelled)
```

**F4. 자식 에이전트의 질문** (FR-6.2.1)
```
Backend가 blocked → 서버가 질문 카드 게시 + 위임자(Lead) 즉시 깨움
                  → Lead가 질문 카드에 답글 → Backend 서브 미션 재진입
위임자 없는 서브 미션이면 → S8 에 lane_blocked 항목 → Director(미션 안) 또는 방장(미션 밖)이 답글
```

**F5. 계속 쓰는 방** (PRD 시나리오 E — v0.19 가 여는 흐름)

클릭 수는 **하한**이다 **[추정]** — 각 화면이 가장 짧게 구현됐다고 가정했고, 타이핑은 세지 않았다.

| E 단계 | 화면 경로 | 클릭(하한) | 비고 |
|---|---|---|---|
| **1. 방 「결제팀」 만들고 Lead·Researcher·Writer 초대** | S5 「새 방」 → S18 「만들기」 → S7 빈 상태 「참여자 초대」 → S19 에이전트 3 체크 → 「초대」 | 2 + 5 = **7** | S18 한 줄이 **격리 없음 · 컴퓨터 미정**을 알린다(FR-2.1.1) |
| **2. "@Researcher 경쟁사 수수료 정책 어떻게 돼?"** | S7 작성창 → 전송 | **1** | 미리보기 칩: 「@Researcher 를 트리거합니다 · **미션 없음**에 들어갑니다」(FR-3.1.1). 이 전송이 **컴퓨터를 고정**하고 시스템 메시지가 그 사실을 남긴다 |
| **3. 이틀 뒤 "이걸로 보고서 쓰자" → 「이걸 미션으로」** | 메시지 「…」 → 「이걸 미션으로」 → S21 에서 assignee 고르기 → 「열기」 | **4** | goal 기본값은 그 메시지 본문. **assignee 를 안 고르면 종료 조건은 `user_approval` 단독**임을 다이얼로그가 말한다(FR-2A.1) |
| **4. 미션이 끝나고 요약이 방에 남는다** | S8 승인 항목 → 「승인」 | **2** | 우열 미션 칸이 그 미션을 가리키고 있어야 한다 — 칩 선택과 우열은 연동된다(§4.6) |
| **5. Writer 가 `colab room read --room 인프라`** | (에이전트 CLI) → 인프라 방 타임라인에 시스템 메시지 | 사람 클릭 **0** | 읽힌 쪽 사람은 **자기 방 타임라인에서** 본다. 자세히는 S23 |
| **6. 같은 Writer 가 「마케팅」 방에도** | S9 에이전트 카드 「참여 중인 방 2」 → 방 목록 | **1** | 참여자 칩 상태가 **다른 방 때문에** `working` 일 수 있다 → §4.6 「칩이 말하지 않는 것」 |

**시나리오 E 전체: 사람 클릭 15회 · 화면 이동 3회** **[추정]**. 12회가 **v0.19 에서 새로 그리는 화면**(S18·S19·S21·S22·S23) 위에서 일어난다.

### 3.4 화면 용어표 초안 (COMPONENTS §8.4 에 넣을 것)

**실제 자물쇠 반영은 R1.5 문구 전환 라운드다**(§10 v2.0). 여기 적는 것은 그 라운드가 그대로 들고 갈 초안이며, **PRD §3.2 표(Director 확정 2026-09-23)가 정본**이다. 이 절은 PRD 표에 없는 **화면 전용 파생어**를 채운다 — 자물쇠는 문자열을 재므로 표에 없는 말은 조용히 살아남는다(PR #188·#195 에서 같은 일이 있었다).

**(a) PRD §3.2 정본 — 그대로 옮긴다**

| 코드·계약 | 화면 이름 |
|---|---|
| `room` | **방** |
| `work` | **미션** |
| `lane` | **서브 미션** (옛 화면 용어 「작업 줄기」를 대체) |
| `task` | **할 일** |
| `workdir` | **작업 폴더** |
| `artifact` · `decision` | **아티팩트** · **결정 기록** |

**(b) 화면 전용 파생어 — 이 문서가 새로 정하는 것**

| 지금 화면 문자열 | 바꿀 말 | 근거 |
|---|---|---|
| 세션 (내비·제목·한 줄 설명) | **방** | FR-2 |
| 새 세션 | **새 방** | FR-2.1 |
| 세션 설정 | **방 설정** | FR-2.3 |
| 세션 취소 · 종료 · 일시정지 (S7 상단) | **미션 취소 · 미션 종료 · 미션 일시정지** | 방은 종료되지 않는다(FR-2.4) |
| 작업 줄기 · 새 작업 줄기로 보내기 | **서브 미션 · 새 서브 미션으로 보내기** | §3.2 |
| 걸린 세션 · 쓰는 중인 세션 (S13) | **쓰는 중인 방** | FR-6.1 — 작업 폴더는 방×에이전트 |
| 목표 (S7 우열) | **미션 목표** | 방에는 goal 이 없다(§3 용어표) |
| 종료 조건 진행률 | **미션 종료 조건** | 〃 |
| 세션 비용 | **미션 비용**(미션 칸) · **방 누적 비용**(방 칸) | FR-2A.5 |
| 이 세션의 diff 아티팩트 (S17 유실 경고) | **이 방의 아티팩트(diff)** | §4.16 |
| 일시정지 · 예산 / 일시정지 · 컴퓨터 연결 끊김 (S5 배지·`badge-map`) | **그대로 둔다** | `EVAL_USER.md` U12 가 못박고 COMPONENTS §8.4 가 잠근 말. 미션·서브 미션·할 일의 `paused` 는 **「일시정지」**다 |
| MacBook이 7일간 **오프라인**입니다 (§4.6 방 배너) | MacBook이 7일간 **연결이 끊겼습니다** | 같은 사유를 S5 배지는 「컴퓨터 연결 끊김」으로 쓴다 — 한 문서 안의 불일치를 없앤다 |

> **「산출물」은 쓰지 않는다**(SCR-C O·SCR-B C2, Lead 판정 1). PRD §3.2(Director 확정 2026-09-23)가 `artifact` 를 「**기존 화면 용어 그대로 — 「산출물」로 바꾸지 않는다**」로 닫았다. v0.19 초판은 반대 방향(D-11)으로 적었고, 이 판에서 **본문의 「산출물」을 전부 「아티팩트」로 되돌렸다**(§4.3 보관·삭제 확인 · §4.6 우열 제목·종료 조건·빈 상태 · §4.7 assignee 설명 · §4.16 S17 유실 경고 · §5 · §7). R1.5 자물쇠가 이 표를 근거로 삼으므로, 여기가 「산출물」로 남으면 **정본 용어가 화면에서 지워진다.**
>
> **「멈춤」과 「일시정지」의 분담**(SCR-C P, Lead 판정 5). **미션·서브 미션·할 일**의 `paused` 는 사람이 되돌리는 조작이라 **U12 가 못박은 「일시정지」**를 그대로 쓴다(`badge-map` 의 다섯 kind 공통). **방 `blocked_reason` 은 시스템(또는 방장)이 방 전체를 세우는 것**이라 **「멈춤」**을 쓴다 — 「예산으로 멈춤」·「이 방 멈춤」. 두 말이 같은 화면에 서므로 배지는 **층을 함께 적는다**(§5) — 「⏸ 일시정지 · 미션 예산」 / 「⏸ 멈춤 · 방 예산」.

**(c) 바꾸지 않는 말**

- **`Director`·`deputy`** — PRD·계약·API·DB enum 에 걸쳐 있다. 영어 그대로(Director 결정 2026-09-13). **`방장`·`부방장`은 한국어다** — 계약 식별자(`owner_user_id`)와 화면 말이 원래 다르므로 예외가 필요 없다.
- **`runtime_session_ref`가 뜻하는 「런타임의 대화 세션」** — 방·미션과 무관하다. 화면에 노출되는 자리는 서브 미션 카드 펼침의 「이어서 시작」 표시 하나뿐이고, 거기서는 **「세션」이라는 말을 쓰지 않는다**(「이전 대화를 이어받음 / 처음부터 시작」).
- **`할 일`** — `task` 의 화면 말은 그대로다. **「미션 3개 · 서브 미션 5개 · 할 일 12개」** 가 한눈에 갈리는 것이 이 이름들을 고른 이유다(§3.2).

**(d) 자물쇠가 새로 재야 할 것**

1. **옛말 0건**: `세션`(단, `runtime_session_ref` 설명 제외) · `작업 줄기` · `걸린 세션` · **`산출물`**. **`아티팩트` 는 옛말이 아니다 — 정본이다**(PRD §3.2). 옛말 목록에 넣으면 자물쇠가 계약과 반대로 잠긴다(SCR-B §4.5-3).
2. **새말 실제 사용**: `방`·`미션`·`서브 미션`·`아티팩트` 가 각각 1건 이상 — 「옛말 0건」만 재면 문구가 통째로 사라져도 초록이다(§8.5 자물쇠 확장 항목).
3. **한 글자 차이 검사**: 같은 화면에 「미션」과 「할 일」이 함께 뜰 때 서로 다른 층임이 문장에서 읽히는가 — 상단 요약 한 줄(§4.6)이 그 검사 지점이다. **「일」 단독은 옛말이다** — 「일 밖 할 일」 같은 모양이 이 검사가 잡으라는 것이다(SCR-C Q).
4. **역할어 네 개**: `방장`·`부방장`(한국어) · `Director`·`deputy`(영어)는 **넷 다 예외**이되, 역할을 말하는 문장은 **층을 함께 적는다** — COMPONENTS §8.4(SCR-C F).
5. **「멈춤」/「일시정지」의 층**: 방 배지·배너에 「일시정지」가, 미션·서브 미션 배지에 「멈춤」이 섞이지 않는가 — (b) 표의 분담 문단이 검사 기준이다.

---

## 4. 화면별 상세 명세

각 절은 같은 양식을 지킨다: **표시 정보 · 동작 · 권한 · 빈 상태 · 실시간 대상**. 신규 화면(S18~S26)도 같은 밀도로 적는다.

### 4.1 S1~S3 인증

**S1 로그인** — 이메일/비밀번호 또는 SSO. 실패 시 사유를 구분해 표시(계정 없음 / 비밀번호 불일치 / 잠김). 보호된 URL에서 왔다면 로그인 후 그 URL로 복귀.

**S2 회원가입** — 이름·이메일·비밀번호. 가입 직후 S4 온보딩.

**S3 초대 수락** — 토큰으로 워크스페이스·초대자·부여될 역할을 미리 보여준다. 미로그인이면 로그인/가입 후 자동 수락. 만료·취소된 토큰은 사유를 명시하고 초대자에게 재요청하는 버튼을 준다.

**권한** — 전부 미인증 접근 가능(S3 은 토큰). **빈 상태** — 해당 없음. **실시간** — 없음.

> **워크스페이스 초대와 방 초대는 다르다.** S3 은 워크스페이스 초대이고, 방 초대(S19)는 **이미 워크스페이스 멤버인 사람**을 방에 넣는 것이라 토큰도 메일 링크도 없다(FR-2.2 «사람은 워크스페이스 멤버만 초대할 수 있다»). 방 초대는 받은 요청의 `info` 항목과 알림으로만 알린다.

### 4.2 S4 온보딩

목적은 **F1의 15분**을 만드는 것이다. 세 단계이며 각 단계는 건너뛸 수 있으나 건너뛴 것은 홈에 체크리스트로 남는다.

| 단계 | 내용 | 완료 판정 |
|---|---|---|
| 1. 워크스페이스 | 이름 입력 (팀원 초대는 나중에) | 생성됨 |
| 2. 컴퓨터 연결 | S12를 인라인으로 — 설치 명령 2줄, 연결 대기 | 데몬이 붙고 CLI 1종 이상 감지 |
| 3. 에이전트 | 팀 템플릿 선택(리서치 팀 / 개발 팀 / 콘텐츠 팀) 또는 직접 생성 | 에이전트 1개 이상 |

**마지막 CTA가 바뀐다(v0.19)**: 「첫 세션 만들기」 → **「첫 방 만들기」** 이고, 누르면 S18(이름 한 칸)이 뜬다. 방이 만들어지면 곧바로 S7 의 **빈 방 상태**로 들어가며, 거기에 온보딩의 마지막 안내가 이어진다 — 「참여자를 초대하고 말을 걸어 보세요」(§4.6 빈 상태). **온보딩은 방을 만든 순간이 아니라 첫 멘션이 나간 순간 끝난다.**

3단계에서 **템플릿을 전면에 둔다.** 에이전트를 하나씩 만들게 하면 15분을 못 맞춘다. 템플릿은 역할·instruction만 담고 프로파일은 감지된 런타임에 맞춰 자동 매핑한다(FR-1.4).

**권한** — 로그인한 본인. **빈 상태** — 단계별 안내가 곧 빈 상태다. **실시간** — 2단계의 데몬 연결 상태.

### 4.3 S5 방 목록 (`/rooms`)

옛 Sessions 목록의 자리다. **카드에서 상태 배지와 goal 이 빠진다** — 방에는 둘 다 없다(FR-2.4). 그 자리를 **무엇이 얼마나 밀려 있는가**로 채운다.

> **정정(SCR-A §3.1 실측).** v0.19 초판은 「상태 배지·goal·**진행률·예산%**가 전부 빠진다」고 적었으나 **진행률과 예산%는 v1 카드에 원래 없다** — 그 둘은 S7 우열에만 있다(`p5-w9-01-sessions-light.png`). 실제로 카드에서 빠지는 것은 **상태 배지와 goal 둘**이다.

**한 줄 설명** (화면 제목 아래, §8.5) — 「**같은 팀과 계속 이야기하고, 끝낼 일이 생기면 미션을 엽니다. 예산·컴퓨터·격리는 방마다 따로 겁니다.**」 (실측 현재 문구 「에이전트 팀에게 맡긴 일 하나가 세션입니다. 진행을 보고 새 일을 시작합니다」를 대체한다. 앞 절만으로는 「방」이 채팅방으로 읽히므로 **뒤 절이 방에 돈과 격리가 붙는다는 것을 한 번에 말한다** — SCR-C G·§3.5.)

**표시 정보** — 카드마다:

| 칸 | 내용 | 없으면 |
|---|---|---|
| 이름 | 방 이름 | — |
| 설명 | 한 줄 설명(FR-2.1) | 비워 둔다. goal 로 채우지 않는다 |
| **안 읽음** | 내가 안 본 메시지 수 (숫자 배지). `aria-label="안 읽은 메시지 3개"` | 0이면 배지를 그리지 않는다. 모델은 `room_participant.last_read_message_id`(방 단위, 사람 행만 — §12.1-6 확정) |
| **진행 중인 미션 N** | `active` 미션 수. 0이면 「열린 미션 없음」 | — |
| 주의 배지 | 내가 답할 HITL N · blocked N · 실패 N — **내 것만** 센다. 세 숫자에 각각 라벨을 단다 | 셋 다 0이면 표시하지 않는다 |
| **방 멈춤 배지** | `blocked_reason` 이 있으면 **사유와 함께**: 「예산으로 멈춤」·「컴퓨터 연결 끊김으로 멈춤」·「루프 상한으로 멈춤」·「**방장이 멈춤**」(`manual`) | `blocked_reason` 이 null 이면 배지 없음 |
| 참여자 아바타 | 사람 + 에이전트 묶음 (최대 5 + `+N`). 묶음에 `aria-label="참여자 7명"`, `+N` 은 텍스트 | — |
| 마지막 활동 | 상대 시각 | — |
| `archived` 표시 | 보관된 방은 낮은 대비 + 「보관됨」 | 기본 필터에서 숨김 |

> **상태 배지를 쓰지 않는다.** 방의 `status` 는 `active|archived` 둘뿐이고 `archived` 는 카드 자체를 흐리게 그려 표현한다. **배지 자리는 `blocked_reason` 이 쓴다** — 사람이 카드를 훑는 이유가 "어디에 내가 필요한가"이고, 멈춘 방이 그 답이기 때문이다(§12.1-9 확정).
>
> **v1 목록이 주던 「완료」가 방 층에는 없다**(SCR-A §3.1). 세션은 `완료` 배지를 달고 필터로 걷혔지만 **방은 영원히 목록에 남는다.** 끝냈다는 사실은 **방 안의 「지난 미션 N개 ▾」**(§4.6)과 타임라인의 `summary` 카드에만 있다 — 그래서 §4.6 칩 규칙이 「열린 미션이 0 이어도 지난 미션 접힘은 그린다」를 못박는다.
>
> **카드 목록은 한 열이다**(SCR-C §4.1). COMPONENTS §8.5 가 인박스를 다열에서 뺀 근거 — 「위에서 아래로 읽고 답하는 목록이라 열이 갈리면 순서가 흐려지고, 항목마다 높이가 제각각이라 다열과 상성이 나쁘다」(PR #191 판정) — 가 v0.19 방 카드에 그대로 성립한다. 안 읽음·주의 배지·멈춤 배지가 붙는 순간 방 목록은 "고르는 격자"가 아니라 **"훑고 답하는 목록"**이 된다. 한 열이면 방 20개에서 스크롤이 길어지므로, 정렬이 **마지막 활동순 하나로 고정**된 것이 그만큼 더 중요해진다(§4.4).

**동작** — 「**새 방**」(S18) · 검색·정렬·안 읽음 필터(S25) · 카드 클릭(S7) · 카드 「…」 메뉴.

**카드 「…」 메뉴** — 「보관」·「보관 해제」·「삭제」.
- **보관**: 진행 중 할 일이 있으면 비활성 + 사유. 보관은 되돌릴 수 있고 과거는 전부 남는다는 것을 확인 문구가 말한다(FR-2.4 [V19-C]) — 「새 대화와 새 미션만 막습니다. 메시지·미션·아티팩트는 그대로 남고 검색에도 걸립니다. 언제든 되돌릴 수 있습니다.」
- **삭제**: **진행 중인 미션이 있으면 비활성** + 사유(「미션 2개가 진행 중입니다 — 먼저 끝내거나 취소하세요」). 방장·ws owner·admin 만 활성. 확인 다이얼로그는 사라지는 것을 나열한다 — 메시지·미션·서브 미션·할 일·아티팩트·결정 기록·비용 기록, 그리고 이 컴퓨터의 작업 폴더. 미병합·미커밋 작업 폴더가 있으면 서버가 `409 workdir_unmerged` — 다이얼로그가 그 폴더를 나열하고 「작업 폴더 관리」(S13) 링크를 준다. **보관과 삭제를 같은 메뉴에서 나란히 보이되, 삭제 항목에는 「되돌릴 수 없음」을 붙인다** — 이 구분이 없으면 사람이 "지우기 아까운 방"을 삭제한다(FR-2.4 [V19-C]).

**권한** — 열람은 §2.4 `visibility` 대로. 보관은 방장·부방장·ws owner·admin, 삭제는 방장·ws owner·admin. 권한이 없으면 메뉴 항목이 비활성 + 사유.

**빈 상태** — 방이 0개면 「**첫 방을 만들어 보세요**」 + 「새 방」 CTA + 예시 두 줄(「결제팀 — 결제 관련 논의와 작업」·「인프라 — 배포·모니터링」). **컴퓨터가 없어도 막지 않는다**(§2.1). 컴퓨터가 0개면 CTA 아래에 「컴퓨터를 연결하면 에이전트가 일을 시작할 수 있습니다」 + S12 링크를 **보조로** 놓는다.

**실시간** — 안 읽음 수(`room.unread`), 진행 중인 미션 수, 주의 배지, `blocked_reason` 배지(`room.updated`), 마지막 활동, 방 추가·삭제(`room.deleted`). 목록이 실시간으로 **재정렬되지는 않는다** — 정렬 키가 바뀌어도 사람이 새로고침하거나 화면을 다시 열 때 반영한다(§4.4 정렬 고정의 같은 이유).

### 4.4 S25 방 찾기 (`/rooms?q=&unread=&archived=`)

방은 **완료되지 않아 목록에서 저절로 빠지지 않는다.** 세션은 `completed` 되어 필터로 걷혔지만 방은 사람이 보관을 눌러야만 걷힌다 — 그래서 20개 문제가 세션 때보다 나쁘다(V19-A §3-3, PRD §12 «활성 세션 20개»). S5 의 상단 제어를 별도 화면으로 세는 이유는 **여기가 방 20개에서 무너지는 유일한 지점**이기 때문이다.

**표시 정보·동작**

| 제어 | 값 | 기본 |
|---|---|---|
| 검색 | 방 **이름·설명** 부분 일치 | 빈 값 |
| 정렬 | **마지막 활동순 하나**(제어 없음) | 고정 — §12.1-11 확정 |
| 안 읽음만 | 토글 | 꺼짐 |
| 내가 참여한 방만 | 토글 | **켜짐** — `workspace` 방이 전부 보이면 목록이 워크스페이스 전체가 된다 |
| 보관 포함 | 토글 | 꺼짐 (`archived` 는 검색에는 계속 걸린다, FR-2.4 [V19-C]) |

- **정렬은 마지막 활동순 하나다**(§12.1-11 확정, SCR-C R·SCR-A P-8·SCR-B C1). 「안 읽음 우선」 정렬을 **두지 않는다** — 목록이 스스로 움직이면 방 20개에서 사람이 어제 본 방을 못 찾는다. **안 읽음은 배지로만** 말하고 「안 읽음만」 토글로 거른다. 이름순도 두지 않는다(같은 이유 + 제어 하나를 줄인다).
- **검색 범위는 이름·설명 부분 일치다**(§12.1-11 확정). **메시지 본문 검색을 v1 에 두지 않는다** — `invited` 방 숨김 규칙(§2.4)과 만나면 **권한 없는 방의 본문이 결과로 새는 경로**가 되고, 제대로 하려면 권한 인식 전문 색인이 필요하다(v1.1).
- **검색 결과에서 보관된 방은 「보관됨」 칩과 함께 보인다.** 보관이 "안 보이게 하는 것"이 아니라 "새 활동만 닫는 것"이므로 검색은 덮지 않는다.
- **`invited` 방은 초대되지 않은 사람의 검색 결과에 나오지 않는다** — 존재를 숨긴다(FR-4.5 의 방 숨김 규칙과 같은 원칙). ws owner·admin 에게는 「감사 열람」 칩과 함께 보인다.
- **「내가 참여한 방만」이 기본으로 켜져 있다는 사실을 말한다**(SCR-A 막힘 5). `visibility: workspace` 가 기본이라 팀원이 만든 방은 **볼 수는 있지만 목록에 안 나온다**. 토글 옆에 한 줄 — 「**워크스페이스에 공개된 방이 N개 더 있습니다**」 + 토글을 끄는 링크. 0이면 그리지 않는다. 이 줄이 없으면 «워크스페이스 공개»의 실효 경로가 토글 하나뿐이고 아무도 그것을 모른다.

**권한** — S5 와 같다. **빈 상태** — 검색 결과 0이면 「〈말〉 에 걸리는 방이 없습니다」 + 「보관 포함해서 다시 찾기」 토글을 제안한다. **실시간** — 안 읽음 수(목록 상단 합계 포함). **정렬은 실시간으로 바뀌지 않는다.**

> **안 읽음 모델은 닫혔다**(§12.1-6 확정): **방 단위** `room_participant.last_read_message_id`, **사람 행만**. 미션·서브 미션 단위 안 읽음은 두지 않는다 — 그 단위는 **구독(알림)으로만** 내려간다(§4.17). 두 단위의 배지가 같이 뜨면 사람이 어느 것을 지워야 하는지 모른다.
>
> **[구현 주의]** 칸 하나로 배지 수를 세려면 방마다 「그 id 이후 메시지 수」 질의가 필요하다 — 방 20개면 20회다. R0·R1 이 목록 op(`listRooms`)에서 **한 번에 세어 내려주도록** 설계해야 하고(SCR-B N1 `unread_count`), 화면이 방마다 따로 묻지 않는다.

### 4.5 S18 방 만들기 (`/rooms/new`, 모달)

**칸 하나다**(FR-2.1). 이 화면의 설계 목표는 "빨리 만들기"가 아니라 **"고르지 않은 것이 무엇인지 알려 주고 빨리 만들기"** 다(FR-2.1.1, §1 원칙 6).

**표시 정보**

```
┌─ 새 방 ──────────────────────────────────────────┐
│ 이름            [ 결제팀                        ] │  ← 필수, 자동 포커스
│ 한 줄 설명(선택)[ 결제 관련 논의와 작업          ] │
│                                                   │
│ ⓘ 격리 없음 · 컴퓨터는 첫 실행 때 정해집니다      │  ← FR-2.1.1, 워크스페이스
│   — 방 설정에서 미리 바꿀 수 있습니다   [방 설정]  │     기본값을 그대로 읽어 쓴다
│                                                   │
│                          [ 취소 ]  [ 만들기 ]     │
└───────────────────────────────────────────────────┘
```

- **ⓘ 한 줄은 워크스페이스 기본값을 읽어 쓴다** — 기본 격리가 `worktree` 인 워크스페이스에서는 「워크트리로 나눔 · 컴퓨터는 첫 실행 때」로 바뀐다. 문장을 고정하지 않는다(고정하면 설정을 바꾼 워크스페이스에서 거짓말이 된다).
- **「방 설정」 링크는 만들기 **전** 에는 비활성**이고 「만든 뒤 바꿀 수 있습니다」. 만들기 전에 설정을 편집하게 하면 그것이 마법사다.
- 이름 중복은 막지 않는다(방은 slug 가 아니라 id 로 식별). 다만 같은 워크스페이스에 같은 이름이 있으면 **경고 한 줄**을 띄운다 — 작업 폴더 브랜치 이름이 `colab/<방 slug>/<에이전트 slug>` 라 사람이 헷갈린다(FR-2.3 [V19-B]). 중복 판정은 화면이 `listRooms?q=` 로 한다(별도 op 없음).
- **연결된 컴퓨터가 0개여도 만들어진다**(§2.1 m5 철회). 현재 계약의 `createSession` 은 런타임 0개에 `409 no_runtime` 을 내므로, **`createRoom` 은 그 검사를 빼야 한다** — R0 의 입력이다(부록 A, SCR-B C11). 대신 만든 뒤 S7 상단에 「연결된 컴퓨터가 없어 에이전트가 아직 일을 시작할 수 없습니다」 배너가 뜬다.

**동작** — 「만들기」 → 방 생성 → **S7 빈 방 상태로 이동**. Esc·「취소」로 닫으면 아무것도 만들지 않는다.

**권한** — 워크스페이스 멤버 누구나. 만든 사람이 **방장**.

**빈 상태** — 해당 없음(입력 폼). 단 **이름이 비면 「만들기」가 비활성** + 「방 이름을 적어 주세요」.

**실시간** — 없음.

> **마법사 3·4단계가 가르치던 것을 여기서 잃는다.** 「worktree 바인딩 규칙 그림」과 「저장소 probe 결과」는 이 모달에 들어갈 자리가 없다 — 칸 하나라는 요구와 정면으로 충돌한다. 대신 **두 번째 알림**(첫 실행 시스템 메시지, §4.6)과 **저장소가 있는 컴퓨터로 첫 실행이 일어날 때의 승인 HITL**(FR-2.1.1 세 번째 불릿)이 그 역할을 한다. 즉 **S18 은 가볍고, 무게는 첫 실행으로 미뤄졌다.** 이 미룸이 성립하려면 그 HITL 이 반드시 떠야 한다 — 그것이 v0.19 의 가장 큰 사고 방지 장치다(V19-A W-1).
>
> **그 HITL 의 화면 명세는 §4.14 의 `isolation_confirm` 항목이다**(SCR-A P-1, Lead 판정 6). v0.19 초판은 §8.1 대조표에 「세 자리 전부 ✅」라 적어 놓고 셋 중 이 하나를 §4 어디에도 명세하지 않았다 — 「셋 중 하나라도 빠지면 W-1·W-2 사고가 그대로 난다」(§9)고 적으면서 그 하나를 빠뜨린 것이다. 이제 항목 타입·문구·버튼·기본 제안이 §4.14 에 있다.
>
> **세 알림 중 「묻는」 것은 그 하나뿐이다**(SCR-A §2-B). ⓘ 한 줄은 **사전 고지(수동적)**, 시스템 메시지는 **사후 통보**다. 저장소가 없는 컴퓨터로 도는 방(민지형)에는 `isolation_confirm` 이 뜨지 않으므로 **관문이 0개**가 된다 — v1 마법사 3·4단계가 「넘어가려면 반드시 지나야 하는 화면」이었던 것에 비하면 순손실이고, 이 문서는 그것을 **감추지 않고 적어 둔다.** `none` 격리가 실제로 위험해지는 조건(저장소 존재)에서만 묻는 것이 v0.19 의 선택이다.

---

### 4.6 S7 방 화면 (`/rooms/:id`) — 핵심 화면

3열 레이아웃. 좁은 화면에서는 좌·우 열이 접히고 탭으로 전환된다. **v0.3 의 S7 과 골격은 같지만 우열이 통째로 성격을 바꾼다** — 우열 6칸(목표·종료조건·아티팩트·결정 기록·비용·세션 설정)이 **미션 칸**과 **방 전체 칸**으로 갈린다(FR-2A.5).

```
┌─ 상단 ────────────────────────────────────────────────────────────────────┐
│ 결제팀   [미션 2개 · 서브 미션 5개 · 할 일 12개]    [참여자] [방 설정] [⋯] │
│ 결제 관련 논의와 작업                                                     │
│ 미션: (전체) (미션 없음) (보고서 초안 ●) (수수료 비교 ⏸)      [+ 새 미션] │
└───────────────────────────────────────────────────────────────────────────┘
┌─ 좌 (참여자 + 서브 미션 보드) ┬─ 가운데 (타임라인) ──┬─ 우 (미션 / 방) ────┐
│ 참여자          [초대·퇴장]   │ [메시지 타임라인]     │ ── 미션 칸 ──────── │
│  👤 서연  방장                │  · 사람/에이전트 메시지│ 〈보고서 초안〉  ▾  │
│  👤 민호                      │  · 스레드             │ Director 서연       │
│  🤖 @Lead     idle            │  · HITL 카드          │ 미션 목표           │
│  🤖 @Writer   working ●       │  · blocked 질문 카드  │ 종료 조건 1/2       │
│  🤖 @Researcher waiting ⏳    │  · 시스템 메시지      │ 미션 비용 $3.20/$20 │
│                               │  · 미션 열림/닫힘 카드 │ [일시정지][종료]    │
│ 서브 미션 보드                 │  · 활동 피드(펼침)    │ ── 방 전체 ▸(접힘) ─ │
│ ┌───────────────────────────┐ │                      │ 아티팩트 7 · 결정 6 │
│ │ 질문 대기│ 사람 대기 │     │ │ ──────────────────── │ 누적 $12.40         │
│ │ 일시정지 │ 실행 중   │     │ │ [작성창]              │ (펼치면: 아티팩트 · │
│ │ 대기    │ ▸완료 ▸실패│    │ │  @멘션 · 미션 선택기   │  결정 기록 · 누적   │
│ └───────────────────────────┘ │  · 미리보기 칩        │  비용 · 맥락 기록 · │
│  (카드마다 「미션 〈…〉」 또는  │  · 새 서브 미션 토글   │  방 설정 요약)      │
│   「미션 없음」)               │                      │                     │
└───────────────────────────────┴──────────────────────┴─────────────────────┘
```

> **가운데가 기본이다**(SCR-C B). 방을 열면 포커스는 작성창에 놓이고 타임라인이 맨 아래(최신)를 보고 있다. 좌·우 열은 **묻지 않으면 답하지 않는다** — 사람이 방에 오는 이유는 읽고 말하기 위해서고, 보드와 미션 칸은 그 다음이다. 이 원칙이 우열 「방 전체 칸」 기본 접힘과 보드 `done`·`failed` 접힘의 근거다.
>
> **「내가 뭘 해야 하나」의 답이 다섯 곳에 흩어진다**(SCR-C §1.2) — ① 방 멈춤 배너 ② 좌열 서브 미션 카드 ③ 가운데 HITL·질문 카드 ④ 우열 미션 칸 배너 ⑤ 밖의 받은 요청. 같은 사건이 셋에 동시에 뜨는 것이 정상이다(미션 예산 초과 = ②+④+⑤). **그래서 상단의 「나에게 필요한 것 N」이 중복 없이 센다** — 방 배너가 「몇 개가 멈췄는지 반드시 센다」를 지키는 것과 같은 요구를 화면 자신의 중복에도 건다.

#### 상단 — 방 머리와 미션 칩

**표시 정보**

| 칸 | 내용 |
|---|---|
| 방 이름 · 한 줄 설명 | FR-2.1. goal 이 아니다 |
| **세 층 요약** | 「**미션 2개 · 서브 미션 5개 · 할 일 12개**」 — 세 층이 한눈에 갈리는 것이 §3.2 용어를 고른 이유다. 0이면 그 층을 생략한다(「미션 없음 · 할 일 1개」). 가운뎃점이 한 덩어리로 읽히지 않게 **세 수에 각각 라벨**을 단다 |
| **나에게 필요한 것 N** | 이 방에서 **내가** 누를 수 있는 것의 수 — 방 멈춤 해소 · 내가 Director·deputy·방장인 미션의 HITL·승인·조건 고치기 · 내가 중단할 수 있는 서브 미션의 질문. **중복 없이 센다**(같은 사건이 좌열 카드와 우열 배너에 동시에 떠도 1이다). 0이면 칸을 그리지 않는다. 누르면 그 첫 항목으로 스크롤·포커스 |
| **미션 칩 줄** | `(전체)` `(미션 없음)` + `active`·`paused` 미션마다 칩 하나. 칩에 상태 글리프(● 실행 중 / ⏸ 일시정지 / ⏳ 사람 대기). **⏳ 는 미션 상태가 아니라 파생이다** — FR-2A.4 의 상태 머신에 `waiting_human` 이 없다(SCR-B C10). 「그 미션에 열린 HITL 이 있다」를 뜻하고 상태 배지(`draft`/`active`/`paused`…)와는 다른 신호다 |
| 방 멈춤 배너 | `blocked_reason` 이 있으면 타임라인 위 전폭 배너(아래) |
| 감사 열람 배너 | `invited` 방을 ws owner·admin 이 볼 때(§2.4) |
| 컴퓨터 없음 배너 | 워크스페이스에 온라인 컴퓨터가 0개일 때(§2.1) |

**미션 칩의 두 가지 일** — 칩은 **거르개이자 선택자**다. 누르면 ① 가운데 타임라인이 그 미션의 메시지만 남기고 ② **우열 미션 칸이 그 미션으로 바뀐다.** 둘이 연동되지 않으면 미션이 2개 열린 순간 우열이 어느 미션을 말하는지 알 수 없다(V19-A §3 4a).

- `(전체)` 는 타임라인을 거르지 않는다. 이때 **우열 미션 칸은 「최근 활동한 미션」을 보여 주고 그 사실을 적는다** — 「최근 활동: 〈보고서 초안〉 · 다른 미션을 보려면 위 칩을 누르세요」(FR-2A.5).
- `(미션 없음)` 은 `work_id = null` 메시지만 남긴다. 이때 **우열 미션 칸은 비고 대신 「이 방에서 미션 없이 오간 대화입니다」** 를 적는다 — 미션 칸을 통째로 감추면 우열 폭이 흔들려 방 전체 칸의 위치가 매번 달라진다. **칸은 남기고 안을 비운다**(감추지 않는다, SCR-C §1.4b): 종료 조건·비용·assignee 행은 「미션이 없어 해당 없음」 한 줄로 접히고 **동작 묶음은 비활성**이다(아래 (가) 표 주석).
- **미션이 하나도 없으면**(열린 것도 끝난 것도 0) 칩 줄 자체를 그리지 않고 「+ 새 미션」만 남긴다.
- **열린 미션이 0 이고 끝난 미션이 있으면 칩 줄을 그린다**(SCR-A P-4) — `(전체)` `(미션 없음)` + 「지난 미션 N개 ▾」 + 「+ 새 미션」. **끝난 미션으로 돌아가는 문이 방 화면에서 사라지면 「지난번 보고서 업데이트」(시나리오 E-4)가 URL 을 아는 사람에게만 성립한다.**
- `completed`·`cancelled` 미션은 칩에서 빠지고 **「지난 미션 N개 ▾」** 접힘으로 간다. 방은 끝나지 않으므로 칩 줄이 무한히 길어지는 것을 막는다.
- **열린 미션이 여럿 멈추면 칩 줄이 센다**(SCR-A P-2). `paused` 미션이 2개 이상이면 칩 줄 오른쪽에 「**일시정지 2 ▾**」를 두고, 펼치면 **미션 이름 + 사유 한 줄 + 승인 권한자**를 한 목록으로 보인다 — 「〈수수료 비교〉 · 미션 예산 $20 초과 · 〈민호〉가 승인」. 칩을 하나씩 눌러야만 사유를 알 수 있으면, 방 배너에 건 규칙(「몇 개가 멈췄는지 반드시 센다」)이 미션 층에서만 깨진다.
- **접기 기준은 미션 칩만 센다**(§8.7 Q8 확정). `(전체)`·`(미션 없음)`·「지난 미션 ▾」·「일시정지 N ▾」·「+ 새 미션」은 **세지 않는다** — 미션 칩이 5개 이상이면 4개 + 「미션 N개 ▾」다. 기본 `max_concurrent_works` 가 3 이므로 상한을 올린 방에서만 발동한다.

**상단 액션** — v0.3 의 5개(일시정지·종료·참여자·Director 교체·세션 취소)가 **방/미션으로 갈린다.**

| 동작 | 어디로 | 권한 |
|---|---|---|
| **참여자** (S19) | **방** — 상단에 남는다 | 방장·부방장·ws owner·admin |
| **방 설정** (S20) | **방** — 신규 | 〃 |
| 「여기까지 정리」 | **방** — `⋯` 안 → **범위 선택 다이얼로그** | 참여자 누구나 |
| **「이 방 멈춤」·해제** | **방** — 상단 액션 줄(멈춘 뒤에는 배너 안에도) | 방장·부방장·ws owner·admin |
| **맥락 오간 기록**(S23) | **방** — `⋯` 안 + 우열 링크 | 참여자 누구나 |
| 보관 · 삭제 | **방** — `⋯` 안 | 보관·해제: 방장·부방장·ws owner·admin / 삭제: 방장·ws owner·admin |
| 일시정지 · 재개 | **미션** — 우열 미션 칸으로 내려간다 | 그 미션의 director |
| 종료(`manual`) · 취소 | **미션** — 〃 | 〃 |
| Director 교체 | **미션** — 〃 | 그 미션의 director, ws owner·admin |
| 설정 편집(goal·조건·한도) | **미션** — 〃 | 그 미션의 director |

**「여기까지 정리」의 범위 선택** (FR-2.5 [V19-C], SCR-B C9) — `⋯` 의 한 줄이 아니라 **다이얼로그를 연다.** 요약은 범위를 사람이 고르고 **그 범위를 요약 메시지에 기록**해야 원 메시지로 내려갈 수 있다.

| 칸 | 내용 |
|---|---|
| 범위 | 「최근 7일」(기본) · 「최근 30일」 · 「직접 고르기」(시작·끝 메시지를 타임라인에서 집는다) |
| 미리보기 | 「메시지 142건 · 미션 2개가 이 범위에 듭니다」 |
| 결과 안내 | 「요약은 **방에 메시지로 남고** 범위와 인용한 메시지가 함께 기록됩니다 — 요약이 틀리면 원문으로 내려갈 수 있습니다」 |

- **「내가 안 읽은 것만」 기준은 두지 않는다**(FR-2.5 [V19-C]) — 사람마다 달라 방에 남길 수 없다. 본인 미리보기로만 쓴다.
- 만들어진 `summary` 메시지는 범위 칩(「2026-09-15 ~ 09-22 · 142건」)을 달고, 누르면 그 구간으로 타임라인이 스크롤한다.

**「이 방 멈춤」** (`room.blocked_reason: manual`, FR-2.4 [V19-B], SCR-A G-11) — **v1 상단의 「세션 취소」가 간 자리다.** v0.19 초판에는 사람이 누를 수 있는 방 단위 정지가 없었다 — 미션 취소는 미션 층이고, 미션 밖 서브 미션은 카드마다 하나씩 중단해야 했으며, 보관은 진행 중 할 일이 있으면 거부됐다.

| 칸 | 내용 |
|---|---|
| 버튼 | `⋯` 안이 아니라 **상단 액션 줄에 직접** 둔다 — 급할 때 누르는 것이 메뉴 안에 있으면 안 된다 |
| 확인 다이얼로그 | 「**이 방의 진행 중인 턴 3개가 중단되고 새 트리거가 막힙니다.** 미션 2개와 미션 밖 대화 전부가 멈춥니다. 중단된 서브 미션은 `failed(cancelled)` 로 남고, **멈춤을 풀면 「다시 지시」로 이어갈 수 있습니다**」 |
| 멈춘 뒤 | 방 멈춤 배너가 `manual` 로 뜨고 그 안에 **「멈춤 해제」**. 상단 버튼도 「멈춤 해제」로 바뀐다 |
| 권한 | 방장·부방장·ws owner·admin. 그 밖에게는 비활성 + 「방장·부방장만 이 방을 멈출 수 있습니다」 |
| 기록 | 타임라인 시스템 메시지 + `activity_log` — 누가 언제 멈추고 풀었는지 |

**방 멈춤 배너** (`room.blocked_reason`, §12.1-9 확정) — 타임라인 위 전폭. **이 방의 모든 미션과 미션 밖 할 일이 멈춘 상태**라 미션 단위 배너와 무게가 다르다. 마크업은 `role="alert"` 다 — 미션 배너(`role="status"`)와 달리 읽던 흐름을 끊어도 된다(SCR-C §5.6).

| 사유 | 문구 | 해결 동작 | 권한 |
|---|---|---|---|
| `budget` | 「이 방의 예산 $50을 넘겼습니다 — **미션 2개와 대화 전부가 멈췄습니다**」 | **계속 진행 승인**(상한 상향 입력 포함) | 방장 → 기한 절반 후 부방장 → 없으면 owner 최고참 |
| `runtime_offline` | 「MacBook과 7일간 **연결이 끊겼습니다** — 이 방의 **미션 2개와 대화 전부가** 멈췄습니다」 | **컴퓨터 바꾸기**(S17) 또는 열린 미션 닫기 | 〃 |
| `loop` | 「에이전트 간 왕복이 방 상한에 닿았습니다(@Writer ↔ @Researcher 5회) — **미션 2개와 대화 전부가** 멈췄습니다」 | **계속 진행 승인** 또는 개입해 방향 바꾸기 | 〃 |
| **`manual`** | 「**〈서연〉이 이 방을 멈췄습니다**(09-22 14:03) — 미션 2개와 대화 전부가 멈췄습니다」 | **멈춤 해제** | 방장·부방장·ws owner·admin |

> **몇 개가 멈췄는지 반드시 센다.** 세션 모델에서는 하나가 멈췄지만 방에서는 폭발 반경이 다르다(FR-2A.3, V19-A W-4). 「멈췄습니다」로 끝나면 사용자가 규모를 모른다. **그 수는 문장에 보간하지 않고 칸(슬롯)으로 둔다** — 템플릿 리터럴 안의 문자열은 문구 자물쇠가 잡지 못한다(COMPONENTS §8.5 가 이미 겪은 함정).
>
> **내가 승인 권한자가 아니면 배너가 그 사실과 시각을 적는다**(FR-2A.3 위임 경로). 「**방장 〈민호〉의 승인을 기다립니다 · 14:30부터 부방장 〈서연〉이 답할 수 있습니다**」. 부방장이 없으면 그 자리에 「워크스페이스 owner 중 최고참」이 들어간다. 이 줄이 없으면 방이 며칠 멈춰 있는 이유를 아무도 모른다(SCR-A 막힘 8).

#### 좌열 — 참여자와 서브 미션 보드

**참여자** — **사람과 에이전트가 한 목록**이다(§7 `room_participant`). 사람은 아바타·이름·방 역할(방장/부방장), 에이전트는 이름·역할·상태 배지(`idle`/`working`/`waiting_human`/`error`/`offline`/`disabled` — FR-1.3)·사용 중 프로파일. 목록 머리에 **「초대·퇴장」** 버튼(S19).

> **에이전트 상태는 task 상태에서 파생되는 계산값이다** (FR-1.3). `disabled` > `offline` > `error` > `working` > `waiting_human` > `idle` 순으로 첫 번째 맞는 것. `blocked`·`paused` 서브 미션은 파생에 들어가지 않으므로 그 에이전트는 `idle` 이고, 어느 서브 미션이 왜 멈췄는지는 카드가 말한다. 칩에 부가 정보를 보이려면 상태 값이 아니라 **둘째 줄 텍스트**("서브 미션 #2 질문 대기")로 넣는다.

**칩이 말하지 않는 것 (v0.19 신규 규칙)** — 에이전트 상태는 **워크스페이스 전역 task** 에서 파생되므로, 결제팀 방의 `@Writer` 칩이 `working` 인데 실제로는 **마케팅 방 일**일 수 있다(V19-A §1-⑤c). 칩은 방을 구분하지 못한다. 화면은 이렇게 다룬다.

- **이 방에 실행 중인 할 일이 없는데 칩이 `working` 이면** 둘째 줄의 **프로파일 표시를 밀어내고** 「**다른 방에서 작업 중**」을 그 자리에 쓴다(SCR-C E). 둘째 줄은 한 줄이고 좌열은 268px 이라 층을 더하면 세 줄이 된다 — **바꿔 넣지 더하지 않는다.** 프로파일은 칩을 눌러 팝오버에서 본다. 어느 방인지는 적지 않는다 — 그 사람이 그 방을 볼 권한이 없을 수 있다(§2.4).
- 그 상태에서 이 방의 서브 미션이 `queued` 로 대기하면 대기 사유 줄에 「**이 에이전트가 다른 방 일로 꽉 찼습니다(동시 3개)**」를 적는다. `max_concurrent_tasks` 는 **방을 가로지르는 전역 상한**이기 때문이다(§12.1-5 확정).
- 그 사유의 저장 자리는 **`task.queued_reason`**(`room_lanes`·`agent_global`·`runtime`·`workspace`, PRD §3.1 확정)이다. 화면은 그 값을 사람 말로 옮겨 적는다 — 옛 §8.2 G-5 는 이 결정으로 닫혔다(§8.2).

**서브 미션 보드** — 카드를 상태별로 묶는다. 카드에는 담당 에이전트, 위임 요약, 경과 시간, `reentry_count`, 작업 폴더 참조, 그리고 **v0.19 신규: 「미션 〈보고서 초안〉」 또는 「미션 없음」**(§3.2, FR-2A.5).

**보드의 순서와 접기**(SCR-C I) — 묶음 순서는 **사람이 할 일 우선**이다: `blocked` → `waiting_human` → `paused` → `running` → `queued` → `failed` → `done`. **아래 상태 표의 순서는 설명 순서지 화면 순서가 아니다**(구현 `LaneBoard.tsx` 가 이미 이 순서다).

**`done` 과 `failed` 묶음은 기본으로 접는다**(「완료 12 ▾」·「실패 2 ▾」). 방은 끝나지 않으므로 **이 둘만 무한히 쌓인다** — 세션 모델에서는 세션이 닫히며 함께 사라지던 것이다. 나머지 다섯 묶음은 항상 펼친다(사람이 할 일이 거기 있다). 펼침 상태는 방마다 기억한다.

**미션 라벨은 양방향 규칙이다**(SCR-C §2.4). `(전체)` 보기에서는 카드마다 미션 라벨을 **보이고**, 미션 칩이 선택돼 있으면 **감춘다** — 이미 걸러진 목록에 같은 이름을 반복하는 것은 잡음이고, 라벨이 `--fs-meta`(12px 하한)라 268px 에서 두 줄로 접히면 카드마다 세로가 는다.

| 서브 미션 상태 | 카드 표시 | 가능한 동작 |
|---|---|---|
| `queued` | 대기 순번 + **대기 사유**(방 동시 상한 / 에이전트 전역 상한 / 의존 대기) | — |
| `running` | 진행 시간, 현재 동작 한 줄 | **중단하고 다시 지시** / **중단** (미션 director·deputy, 방장·부방장) |
| `waiting_human` | 어떤 HITL을 기다리는지 + 받은 요청 링크 | 응답하러 가기 |
| `blocked` | **질문 배지 + 질문 요약**, 누구의 답을 기다리는지 | 질문 카드로 이동 |
| `paused` | **"예산 초과로 대기 중"** + 초과 금액 + **어느 층의 예산인지**(미션 / **방**) — 배지는 「⏸ 일시정지 · 미션 예산」 | **계속 진행 승인**(미션 예산이면 director, 방 예산이면 방장) |
| `done` | 아티팩트 요약 | — |
| `failed` | 실패 분류(`timeout`·`runtime_offline`·`cancelled`…) | 상황별(아래) |

**`paused`는 `failed`가 아니다.** 예산 초과는 오류가 아니라 정책이고, 승인하면 **같은 재개 경로**로 이어간다. **v0.19 에서 한 줄이 더 붙는다 — 어느 층의 예산인가.** 미션 예산이면 그 미션만 멈추고, 방 예산이면 방 전체가 멈춘다(FR-2A.3). 카드가 층을 말하지 않으면 사용자가 「승인」을 눌러도 왜 나머지가 안 움직이는지 모른다.

**`failed`에서 무엇을 할 수 있나** — 실패 원인마다 사람이 할 일이 다르므로 버튼도 다르다.

| 실패 분류 | 동작 |
|---|---|
| `cancelled` (사람이 중단) | **"다시 지시"** — 새 메시지를 받아 새 할 일로 시작 |
| `runtime_offline` | **컴퓨터 복구 대기** 또는 바꾸기(§4.16) |
| `timeout`·`agent_error` | 자동 재시도가 소진된 상태. **"다시 지시"** 로 사람이 맥락을 더해 재시작 |
| `budget_exceeded` | 이 분류는 쓰지 않는다 — `paused`로 간다 |

즉 **일반적인 "재시도" 버튼을 두지 않는다.** 같은 트리거를 그대로 반복하는 것이 안전한 경우가 드물다(FR-7.1 M5).

**서브 미션 카드 펼침 — 할 일 이력.** 카드를 펼치면 할 일 목록을 시간순으로 본다: 순번(`#1`·`#2 (재지시)`·`#2-2 (재시도)`) · 트리거 메시지 · 상태·사유 · 실행(시작·종료·소요·**이전 대화를 이어받았는지**) · 비용. 좌열(268px)에서는 행을 2줄로 놓는다. 「처음부터 시작」이 반복되면 §11 지표(resume 성공률 > 90%)가 무너지는 신호이므로 눈에 띄게 표시한다.

**보드의 거르개** — 상단 미션 칩은 **보드도 거른다.** `(보고서 초안)` 을 고르면 그 미션에 매인 서브 미션만 남는다. `(미션 없음)` 은 `work_id = null` 인 것만. **`(전체)` 에서는 미션 라벨이 카드마다 보여야** 어느 미션 것인지 알 수 있다.

#### 가운데 — 메시지 타임라인

메시지 종류(`message.kind`)마다 다른 카드로 렌더한다.

| kind | 카드 | v0.19 변화 |
|---|---|---|
| `text` | 일반 메시지. 작성자 아바타, 멘션 하이라이트, 스레드 답글 수, **미션 라벨**(`(전체)` 보기일 때) | 미션 라벨 추가 |
| `hitl` | **HITL 카드** — 질문/선택지/승인 요청, 제안 기본값, 기한과 `overdue`. 응답 버튼은 권한자에게만 활성 | **수신자가 누구인지 적는다** — 미션 안이면 「Director 〈이름〉에게」, 미션 밖이면 「방장 〈이름〉에게」 |
| `blocked_q` | **질문 카드** — 어느 서브 미션의 누가 무엇을 묻는지. 이 카드가 답글의 스레드 루트(FR-6.2.1) | — |
| `system` | 시스템 메시지. 낮은 대비 | **신규 5종**(아래) |
| `summary` | **미션 완료 요약** — 「〈미션 goal〉이 끝났습니다」 + 요약 본문 + 아티팩트 링크 | v0.3 은 「세션 완료 요약」이었다. **방은 끝나지 않으므로 이 카드는 항상 미션의 것**이고 미션 이름을 반드시 적는다 |

**시스템 메시지 5종 (v0.19 신규)** — 전부 «무슨 일이 일어났는지 사람이 나중에 찾을 수 있어야 하는 것»이다.

| # | 언제 | 문구 |
|---|---|---|
| 1 | **첫 할 일이 dispatch 되어 컴퓨터가 고정될 때** | 「이 방은 〈MacBook〉에서 돕니다. 격리 방식과 컴퓨터는 이제 바꿀 수 없습니다 — 작업 폴더가 거기 묶여 있습니다.」 (FR-2.1.1) |
| 2 | **다른 방이 이 방을 읽었을 때**(읽힌 쪽) | 「〈서연〉의 요청으로 〈@Writer〉가 이 방을 읽었습니다(요약 + 최근 30건).」 + S23 링크 (FR-4.5) |
| 3 | **참고 방 링크를 걸거나 풀 때**(양쪽 방 모두) | 「〈서연〉이 〈인프라〉 방을 참고 방으로 연결했습니다.」 / 「…연결을 풀었습니다.」 (FR-4.5) |
| 4 | **방장이 자동 승계될 때** | 「〈민호〉가 워크스페이스를 떠나 〈서연〉이 방장이 되었습니다.」 (§12.1-4) |
| 5 | **참여자가 들어오고 나갈 때** | 「〈서연〉이 @Writer 를 초대했습니다.」 / 「@Writer 가 이 방에서 나갔습니다 — 진행 중이던 서브 미션 1개는 그대로 남습니다.」 (FR-2.2) |

기존 시스템 메시지(합류 결과·재진입 통보·Director 변경·정책 강등·`visibility` 변경)는 그대로 유지한다.

**미션 열림·닫힘도 타임라인에 남는다.** 「〈서연〉이 이 메시지로 미션 〈보고서 초안〉을 열었습니다」 — `system` 카드이되 **해당 미션 칩으로 가는 링크**를 단다. 미션이 방 안에서 시작되고 끝나는 것이 방 모델의 전부이므로, 그 두 사건이 타임라인에 없으면 이틀 뒤 스크롤에서 맥락이 끊긴다.

**메시지 행동 — 「이걸 미션으로」** (FR-2A.1, 신규). 각 메시지의 「…」 메뉴에 둔다.
- **사람 메시지·에이전트 메시지 둘 다** 열 수 있다. goal 기본값은 그 메시지 본문.
- 이미 미션에 속한 메시지(`work_id != null`)에서는 **비활성 + 「이미 미션 〈…〉에 속한 메시지입니다」**.
- 누르면 S21 이 뜬다(`?work=from&message=:mid`).

**타임라인 페이지네이션** — 방은 끝나지 않으므로 타임라인은 **자연히 걷히지 않는다.** 화면 규칙은 이렇다: 처음에 **최근 50건**을 싣고 위로 스크롤하면 「**이전 대화 더 보기**」로 50건씩 더 읽는다. `?around_message_id=` 로 들어오면(안 읽음 앵커·인용·검색 결과) **그 메시지를 가운데 두고 위아래 25건씩**을 싣고 상단에 「최신으로」 단추를 둔다. **「이전 대화 N건은 생략됨」 같은 잘림 문구는 쓰지 않는다** — 그것은 에이전트가 읽는 브리프의 말이고(FR-4.1), 사람에게는 「더 보기」가 정확한 표현이다.

**활동 피드** — 항목마다 한 문장: *에이전트가 [동사]를 [목적어]에 했다 → [결과]*. v1은 중추 5클래스 + 일반 툴 + 원본 레일. 진행 중 항목은 제자리 갱신, 실패는 크게·조회는 조용하게, 침묵·유휴·타임아웃도 렌더, 원본 레일 토글. 런타임이 구조화 이벤트를 주지 않으면 그 사실을 명시한다. **빈 턴 카드**(FR-7.2 v0.18)는 이력 「활동」 토글 안에만 둔다 — 보드에는 빈 턴 칸이 없다(v1.1 Lead A 판정).

**작성창** — 멘션 자동완성, **트리거 미리보기**, 「**새 서브 미션으로 보내기**」 토글, 스레드 답글 시 대상 표시. **v0.19 에서 칩이 한 가지를 더 말한다.**

**빈 방의 placeholder 가 첫 지시를 가르친다**(SCR-A P-3). 메시지가 0건인 방에서는 작성창 placeholder 에 **그 방의 첫 지시 예시**를 둔다 — 「@Lead 국내 B2B SaaS 결제 시장을 조사해서 보고서 10페이지로 정리해 줘」. **마법사 1단계 goal 필드의 placeholder 가 하던 일이 여기로 온다** — F1 의 마지막 단계이자 v0.19 가 새로 만든 정지 지점이 「빈 작성창 앞에 혼자 서는 것」이다(§3.3).

**미리보기 칩은 트리거 대상과 「귀속될 미션」을 함께 적는다** (FR-3.1.1) — 「**@Writer 를 트리거합니다 · 미션 〈보고서 초안〉에 들어갑니다**」. 미션 없음이면 그렇게 적는다. 귀속은 다음 순서로 정해지고 **사람이 보내기 전에 본다.**

| 순서 | 규칙 | 화면 |
|---|---|---|
| 1 | 작성창에서 사람이 고른 미션 | 칩 옆 **미션 선택기**(기본값 = 지금 고른 칩). **`(전체)` 보기에서는 「미션 없음」이 기본값**이고, 규칙 3 이 발동하면 선택기가 그 미션으로 바뀌면서 칩이 「**자동으로 미션 〈…〉에 들어갑니다 — 바꾸려면 선택기를 누르세요**」를 적는다(SCR-A P-5). 고르면 고정 |
| 2 | 스레드 안이면 그 스레드의 미션 | 선택기 비활성 + 「이 스레드는 미션 〈…〉의 것입니다」 |
| 3 | 멘션 대상이 이 방에서 실행 중인 서브 미션을 갖고 그것이 미션에 매여 있으면 그 미션 | 칩이 그렇게 적는다 — **자동 귀속이므로 반드시 보인다**. **판정은 서버가 한다**(`previewTriggers` 응답의 `work{id,title}` + `work_source`) — 화면이 로컬로 계산하지 않는다(SCR-B C12·E5; 구현 `Composer.tsx` 가 같은 이유로 로컬 규칙 계산을 이미 금지해 두었다) |
| 4 | 그 밖 | 「미션 없음에 들어갑니다」 |

> **3번이 이 규칙의 핵심이자 위험이다.** 조용하고 자연스럽지만 **사람이 예측할 수 없다** — 그래서 미리보기 칩이 필수다. 칩 없이 3번을 구현하면 잡담이 말없이 미션에 들어가고, 그 미션의 비용·브리프가 오염된다. **자동 귀속(3번)은 사람이 고른 것(1번)과 시각적으로 달라야 한다** — 자동 쪽 문장 앞에 「자동: 」을 붙인다(§5 미션 선택기).
>
> **각 트리거 칩의 × 버튼**(`suppress_agent_ids`)은 그대로다(FR-3.6) — 멘션은 본문에 남고 깨우지만 않는다.

#### 우열 — 미션 칸과 방 전체 칸

**갈라지는 규칙이 이 열의 전부다**(FR-2A.5, V19-A P1-8). 위는 **선택된 미션에 따라 바뀌고**, 아래는 **방 내내 그대로**다. 둘 사이에 굵은 구분선과 제목을 둔다 — 붙여 놓으면 사용자가 아티팩트도 미션의 것으로 읽는다.

> **우열은 268px 이고 미션 칸만으로 이미 세로가 찬다**(SCR-C §4.5 실측: v1 은 6칸이 1280×900 의 우열을 정확히 가득 채웠고, v0.19 는 12~15칸을 요구한다 — 지금의 2.1배). 그래서 **아래 (나) 방 전체 칸은 기본으로 접힌다**(§(나) 머리). 접기 없이 구현하면 R2 에서 되돌린다.

**(가) 미션 칸** — 선택된 미션이 있을 때만 내용이 찬다.

| 칸 | 내용 |
|---|---|
| 미션 이름 · 상태 | goal 첫 줄 + 상태 배지(`draft`/`active`/`paused`+사유/`completing`/`completed`/`cancelled`) |
| **Director** | 이름. 내가 아니면 버튼 비활성 사유가 여기서 읽힌다(§2.3) |
| 미션 목표 | goal + 성공 기준 체크리스트 |
| **종료 조건 진행률** | 조건마다 사람 말 한 줄(「아티팩트 제출 ✓」·「Director 승인 — 받은 요청에서」·「Lead 의 검토 승인 — Lead 차례」). `blocked_reason` 이 있으면 ✗ 대신 **이유**(「리뷰어가 지정되지 않았습니다 — 조건을 고치세요」)와 Director 에게 「조건 고치기」. **미션이 왜 안 닫히는지 이 칸만 보고 알 수 있어야 한다** |
| **미션 비용** | 누적 / 미션 상한 + %. 추정치면 배지. **아래 「방 누적 비용」과 다른 수임을 칸이 말한다** — 「이 미션 $3.20 / $20」 |
| 미션 동작 | 일시정지·재개 · 종료(`manual`) · 취소 · 설정 편집 · Director 교체 (권한은 §2.3 미션 층). **`(전체)`·`(미션 없음)` 보기에서는 통째로 비활성** — 아래 주석 |
| assignee | 「제출자: @Writer」 — 없으면 **「제출자 없음 — 종료 조건이 `Director 승인` 하나입니다」** (FR-2A.1) |

> **`(전체)` 에서는 미션 동작을 누를 수 없다**(SCR-C C, Lead 판정 4). 일시정지·재개·종료·취소·설정 편집·Director 교체 묶음은 **비활성**이고 사유는 「**어느 미션인지 먼저 고르세요 — 위 칩에서 미션을 누르면 이 버튼이 켜집니다**」다. `(전체)` 에서 미션 칸이 「최근 활동한 미션」을 **보여 주는** 것과 그 미션을 **끝내는** 것은 무게가 다르다 — 사람이 고르지 않은 미션이 끝나는 것은 §1 원칙 6 과 정면으로 어긋난다. `(미션 없음)` 에서도 같고, 그때 사유는 「**미션 없이 오간 대화에는 끝이 없습니다**」다.
>
> 사유는 **툴팁이 아니라 버튼 아래 글자**다(§1 원칙 5, `DisabledHint`).

**미션 `paused` 해소** — 미션 칸 최상단에 사유와 해결 버튼. 사유마다 할 일이 다르다.

| 사유 | 배너 문구 | 해결 동작 | 권한 |
|---|---|---|---|
| 예산 초과(미션) | 「이 미션의 예산 $20을 넘겼습니다 (현재 $21.40)」 | **계속 진행 승인**(상향 입력 포함) | director, 기한 절반 후 deputy |
| 시간 초과 | 「시간 상한 4시간에 도달했습니다」 | **계속 진행 승인**(연장 입력) | 〃 |
| 루프 상한 | 「에이전트 간 왕복이 상한에 닿았습니다(@Backend ↔ @QA 5회)」 | **계속 진행 승인** 또는 개입해 방향 바꾸기 | 〃 |
| 수동 | 「Director가 일시정지했습니다」 | **재개** | director |

> **방 단위 사유(`budget`·`runtime_offline`·`loop`)는 여기 오지 않는다** — 전폭 배너로 올라간다(상단). 미션 칸에 방 사유를 그리면 미션 3개에 같은 배너가 세 번 뜬다.

**(나) 방 전체 칸** — 미션 선택과 무관하게 항상 같다. **기본으로 접혀 있다**(「방 전체 ▾」, SCR-C J). 미션 칸이 "지금 뭘 해야 하나"의 답이고 방 전체 칸은 "이 방에 무엇이 쌓였나"의 답이라 매번 볼 것이 아니다. **접힌 상태에서도 머리줄에 수를 적는다** — 「**방 전체 · 아티팩트 7 · 결정 6 · 누적 $12.40**」. 펼침 상태는 방마다 기억한다.

| 칸 | 내용 | 미션 밖 것의 자리 |
|---|---|---|
| **아티팩트** | 이름·버전·타입·제출자·시각. **미션별로 묶고 「미션 없음」 묶음을 맨 뒤에** | ✅ 「미션 없음」 묶음 |
| **결정 기록** | 요약·근거·출처(HITL / 에이전트) | ✅ 같은 규칙 |
| **방 누적 비용** | 방 전체 누적 / 방 상한 + %. **미션 비용의 합이 아니다** — 미션 밖 할 일의 비용이 더해진다 | ✅ |
| **맥락 오간 기록** | 「읽음 2 · 읽힘 1」 — 누르면 S23 | ✅ |
| **방 설정 요약** | 컴퓨터 · 격리 · autonomy · 한도(예산·시간·동시 미션·동시 서브 미션) · 기본 Director · 공개 범위 · 참고 방 N개. 「방 설정」(S20) 링크 | ✅ |

> **아티팩트 이름은 방 단위다**(FR-2A.5 [V19-B]). 같은 이름을 다시 내면 **미션이 달라도 v2** 가 된다 — 방이 아티팩트의 보관소이고 사람은 "그 보고서의 최신본"을 방에서 찾는다. 목록의 버전 표시가 그 사실을 말해야 한다(「report.md **v2** · 미션 〈보고서 개정〉이 제출」).
>
> **「미션 없음」 묶음을 맨 뒤에 두는 이유**는 미션 밖 결과가 목록 위쪽을 차지하면 지금 하는 미션이 밀려나기 때문이다. 하지만 **묶음 자체를 없애면 미션 밖 할 일의 결과가 화면 어디에도 남지 않는다**(V19-A §3-2) — 타임라인 메시지는 스크롤 밖으로 사라진다.
>
> **묶을 것이 하나면 머리글을 그리지 않는다**(SCR-A P-11). 이 방에 열린 미션이 하나뿐이고 미션 밖 아티팩트가 없으면 묶음 머리글은 순수한 잡음이다. 결정 기록도 같다.
>
> **선택된 미션의 묶음을 강조한다**(SCR-C §1.4c). 아티팩트·결정 기록은 **바뀌지 않는 칸** 안에서 **미션별로** 묶이므로, 미션 A 를 고른 사람 눈앞에 B·C 의 것이 함께 있다. 선택된 미션의 묶음을 맨 위로 올리고 머리글에 `✓` 를 단다 — 목록을 훑어 "내 미션이 낸 것"을 찾게 하지 않는다.
>
> **각 묶음은 최근 5건까지 보이고 나머지는 「더 보기 N」이다.** 아티팩트 이름이 방 단위 버전(`report.md v17`)으로 쌓이고 방은 끝나지 않으므로, 상한이 없으면 미션 30개 묶음이 지금 하는 미션 앞에 선다(SCR-C §4.7).
>
> **두 비용이 합이 아니라는 것을 화면이 말한다**(SCR-C §1.4d). 우열 한 화면에 `$3.20 / $20`(미션)과 `$12.40`(방 누적)이 세로로 놓이면 사람은 반드시 더해 보고 틀린다. 방 누적 칸에 한 줄 — 「**미션 비용의 합이 아닙니다 — 미션 밖 대화 비용이 함께 듭니다**」.

**권한** — 열람은 §2.4. 미션 동작은 §2.3 미션 층. 방 동작은 §2.3 방 층. 권한 없는 버튼은 비활성 + 사유.

**빈 상태** — 방 화면은 빈 상태가 **네 가지**다. 방 만들기가 칸 하나가 된 대가라 이 넷이 마법사의 안내를 대신한다.

| 상황 | 표시 |
|---|---|
| **방금 만든 빈 방** (메시지 0 · 참여자 = 나 혼자) | 가운데에 큰 안내 — 「**이제 무엇을 하나요?**」 + 세 갈래: ① **참여자 초대**(S19, 강조) ② 그냥 말 걸기(작성창으로 포커스) ③ 미션 열기(S21). 그 아래 회색 한 줄로 FR-2.1.1 의 확정 안내 재게시 — 「격리 없음 · 컴퓨터는 첫 실행 때 정해집니다 · [방 설정]」 |
| 참여자는 있는데 메시지 0 | 「**@로 에이전트를 불러 시작하세요**」 + 참여 에이전트 멘션 칩을 눌러 작성창에 넣기 |
| 미션 0개 | 미션 칩 줄 없음. 우열 미션 칸에 「**아직 연 미션이 없습니다.** 끝을 정해 추적하고 싶은 일이 생기면 미션을 엽니다」 + **「+ 새 미션」 버튼**(상단 버튼과 같은 동작). 문장 아래 회색 한 줄로 다른 길도 적는다 — 「이미 오간 메시지에서 열려면 그 메시지의 「…」 → 「이걸 미션으로」」. **CTA 는 문장이 아니라 버튼이어야 한다**(SCR-A §2-C: 「이걸 미션으로」는 메시지 메뉴 안에 있어 이 문장을 읽은 사람이 바로 누를 것이 없었다) |
| 서브 미션 0개 | 「아직 위임이 없습니다」 — **미션이 있고 assignee 도 있으면** 「assignee 가 계획을 세우는 중입니다」, **미션은 있는데 assignee 가 없으면** 「이 미션에는 제출자가 없습니다 — 에이전트를 멘션해 시작하세요」, **미션이 없으면** 「에이전트를 부르면 여기에 나타납니다」 |

아티팩트 0 → 「아직 제출된 아티팩트가 없습니다」 · 결정 기록 0 → 「아직 기록된 결정이 없습니다」 · 맥락 오간 기록 0 → 「이 방의 맥락이 오간 적이 없습니다」.

**실시간** — 메시지 스트리밍(토큰 단위), 에이전트 타이핑 표시, 서브 미션 카드 상태, 활동 피드 제자리 갱신, 참여자 상태·**목록**(`participant.joined`·`participant.left`), **미션 칩 줄**(`work.created`·`work.updated`·`work.closed`), 미션 종료 조건 진행률, 미션 비용·방 누적 비용(`cost.updated` 두 수), **방 `blocked_reason`**(`room.updated`), 안 읽음(`room.unread`), 시스템 메시지 5종(`message.created`). 목록은 §6.

---

### 4.7 S21 미션 열기 (`/rooms/:id?work=new` · `?work=from&message=:mid`, 다이얼로그)

마법사 1·2·6·7단계가 여기로 왔다. **다이얼로그 하나이고 필수 칸은 goal 하나**다(FR-2A.1). 나머지는 접혀 있고 기본값이 **문장으로** 보인다 — 마법사가 강제로 읽히던 것을 접힘 안의 기본값 문장으로 옮긴 것이라, 그 문장이 없으면 W-3·W-4 사고가 그대로 난다.

**제목 바로 아래 회색 한 줄로 미션이 무엇인지 말한다**(SCR-C H) — 「**미션은 끝이 있는 일입니다. 목표·마칠 조건·예산·Director 가 붙고, 끝나면 요약이 방에 남습니다.**」 세 길(메시지에서 · 새 미션 · 제안) 모두 같은 문장을 쓴다. **미션을 처음 여는 사람에게 그 정의를 말할 자리가 여기뿐이다** — 마법사가 하던 일이고, 「미션」이 게임·군사 어감 때문에 "가볍게 여는 것"으로 읽힐 위험을 이 줄이 막는다(SCR-C §3.5).

**세 가지 길이 같은 다이얼로그를 연다**(FR-2A.1).

| 길 | 진입 | 다이얼로그 제목 | goal 기본값 |
|---|---|---|---|
| 메시지에서 | 메시지 「…」 → 「이걸 미션으로」 | 「이 메시지로 미션 열기」 + **인용된 원 메시지** | 그 메시지 본문 |
| 새 미션 | 상단 「+ 새 미션」 | 「새 미션」 | 빈 값 |
| 에이전트 제안 | S26(별도 화면) | — | 제안된 goal |

**표시 정보·입력**

| 칸 | 기본 | 화면 규칙 |
|---|---|---|
| **goal** (필수) | 위 표 | 비면 「열기」 비활성 |
| 제목(선택) | goal 첫 줄 | 칩·목록에 쓰인다 |
| 성공 기준(선택) | 없음 | 체크리스트 |
| **assignee** | **비어 있음** | 「**제출자를 고르면 「아티팩트 제출」이 종료 조건에 들어갑니다**」. 메시지에서 열 때 그 메시지가 멘션한 에이전트가 **하나뿐이면 그것을 제시하되 사람이 확인한다**(FR-2A.1) |
| **종료 조건** | assignee 가 있으면 `artifact_submitted(assignee) AND user_approval`, 없으면 **`user_approval` 단독** | **사람 말 문장으로** 보인다 — 「@Writer 가 아티팩트를 제출하고」 + 「Director 승인」. 기본값이 바뀌는 이유를 한 줄로: 「제출자를 고르지 않아 「Director 승인」만 걸었습니다 — 대상 없는 제출 조건은 아무도 채울 수 없습니다」 |
| Director | 방 설정 `default_director`(기본: 방장) | 워크스페이스 멤버 중 1인. **deputy(선택)** — 「취소는 즉시, 승인은 기한 절반 후 가능합니다」 |
| 예산·시간 | 방 설정 상속 | 「방 한도 $50 중 이 미션에 $20」. **작은 쪽이 이긴다**를 한 줄로(FR-2A.3) |
| `autonomy` | 방 설정(기본 `guided`) | 값의 **차이를 문장으로**(아래) |

**종료 조건 선택 — 마법사 6단계가 강제하던 두 안전장치를 그대로 옮긴다.**
- **`agent_approval` 을 고르면 리뷰어를 참여 에이전트 중에서 반드시 고른다**(S-84, 서버 422). 리뷰어 없는 조건은 아무도 승인할 수 없다.
- **`agent_approval` 단독이면** 「사람 승인 없이 완료됩니다」 경고.
- `criteria_met` 단독 불가. `criteria_met` 자체는 v1.1(비활성 + 「v1.1」 배지).

**`autonomy` 선택** — v1 동작을 실제로 바꾸는 값이므로 차이를 문장으로 적는다.

| 값 | 화면 문구 | v1 |
|---|---|---|
| `guided` (기본) | 「질문 기한이 지나면 계속 기다립니다」 | ✅ |
| `autonomous` | 「질문 기한이 지나면 에이전트가 제안한 기본값으로 진행합니다. **승인 요청은 예외로 항상 기다립니다**」 | ✅ |
| `supervised` | 「Lead의 모든 위임을 Director가 먼저 승인합니다」 | ❌ 비활성 + 「v1.1」 배지 |

**동작** — 「열기」 → 미션 생성 → 다이얼로그가 닫히고 **그 미션 칩이 선택된 상태**로 방 화면이 돌아온다 + 타임라인에 「미션을 열었습니다」 시스템 메시지. 「이걸 미션으로」로 열었으면 **원 메시지와 그 스레드의 `work_id` 가 이 미션으로 채워진다**(FR-3.1.1 규칙 2).

**동시 미션 상한** — `max_concurrent_works`(기본 3, FR-2.3)를 넘기면 서버가 `409`. 다이얼로그는 **열기 전에** 알린다 — 「이 방에 열린 미션이 3개입니다(상한 3). **열린 미션을 닫거나 상한을 올리세요**」 + 「방 설정」 링크. 열린 미션 목록을 함께 보여 어느 것을 닫을지 고르게 한다.

**권한** — 방 참여자 누구나 연다. 연 사람이 기본 Director. `archived` 방에서는 전체 비활성 + 「보관된 방에서는 새 미션을 열 수 없습니다」.

**빈 상태** — 해당 없음. 단 **참여 에이전트가 0개면** assignee 선택기가 「이 방에 에이전트가 없습니다 — 먼저 초대하세요」 + S19 링크.

**실시간** — 열린 미션 수(상한 검사용).

### 4.8 S22 미션 패널 (`/rooms/:id?work=:workId`)

S7 우열의 **(가) 미션 칸**이 곧 이 화면이다(§4.6). **별도 라우트를 두되 별도 페이지는 아니다** — PRD §6(#274)이 「방 화면 안에서 열린다 — **별도 페이지는 없고 `/rooms/:id?work=:workId` 로 바로 열 수 있다**」로 확정했다. 받은 요청·알림·시스템 메시지에서 특정 미션으로 바로 보내야 하므로 URL 이 필요하다(§3.2 각주). 열면 방 화면이 뜨고 그 미션 칩이 선택된다.

**표시 정보** — §4.6 (가) 표 그대로. **동작** — §4.6 미션 동작 표. **권한** — §2.3 미션 층.

**좁은 화면(≤1100px)에서 열 탭은 넷이다**(SCR-C T) — 「타임라인 · 보드 · **미션** · **방**」. v0.3 의 셋(타임라인·보드·우열)에서 **우열이 미션 칸과 방 전체 칸으로 갈렸기** 때문이다(§4.6). `?work=:workId` 로 들어오면 **미션** 탭이 열린 채 시작한다. 방 전체 칸을 미션 탭에 함께 쌓으면 268px 12칸이 한 탭에 들어가 스크롤이 두 배가 된다.

**지난 미션** — `completed`·`cancelled` 미션도 이 URL 로 열린다. 이때 미션 칸은 읽기 전용이고 상단에 「**끝난 미션입니다**(2026-09-20 완료)」 + 요약 메시지 링크. 방은 남고 미션은 닫히므로 **지난 미션을 다시 볼 수 있어야** 다음 주 "지난번 보고서 업데이트"가 성립한다(시나리오 E-4).

**빈 상태** — 존재하지 않거나 다른 방의 `workId` 면 「이 방에 그 미션이 없습니다」 + 방 화면으로. **실시간** — §4.6 과 같다.

### 4.9 S26 미션 제안 확인 (`/rooms/:id?work_proposal=:id`, 다이얼로그)

에이전트가 `colab work propose --goal` 로 제안한 것을 **사람이 확인해야 열린다**(FR-2A.1 — 에이전트가 스스로 미션을 열지 못한다. 무한 자기 위임을 막는다).

**표시 정보** — 제안한 에이전트·시각 · 제안 goal · **제안 근거 한 줄**(에이전트가 적은 것) · 제안이 나온 트리거 메시지 인용.

**동작** — 「**이대로 열기**」(S21 을 goal 이 채워진 채로 연다) · 「**고쳐서 열기**」(같은 다이얼로그, 편집 가능) · 「**거절**」(사유 입력, 선택).
- 거절하면 타임라인에 시스템 메시지 — 「〈서연〉이 @Lead 의 미션 제안을 거절했습니다」 + 사유. **제안한 에이전트가 그 사실을 알아야** 같은 제안을 반복하지 않는다.
- 제안은 **받은 요청에 `work_proposed` 항목**으로도 뜬다(§4.14). 방을 열지 않고 확인할 수 있어야 한다.

**권한** — 방 참여자 누구나 확인·거절. 여는 순간 **연 사람이 Director** 가 된다(제안한 에이전트가 아니다).

**빈 상태** — 이미 처리된 제안이면 「이 제안은 〈서연〉이 2026-09-21 에 **열었습니다**」 + 그 미션 링크, 또는 「…**거절했습니다**」 + 사유.

**실시간** — 제안 도착(`work_proposal.created`), 다른 사람이 먼저 처리했을 때의 상태 갱신(`work_proposal.resolved`).

> **저장 자리는 `room_read_log` 가 아니라 `work_proposal` 이다**(PRD §7, #274 — SCR-B C8·P6). 칸: `proposed_by_task_id · goal · rationale · trigger_message_id? · status(open|accepted|rejected|expired) · decided_by? · work_id?`. 이 화면의 「제안 근거 한 줄」이 `rationale`, 「이미 처리된 제안」 빈 상태가 `status`·`decided_by`·`work_id` 다. v0.19 초판 시점에는 이 표가 없어 화면이 저장할 곳이 없었다.

### 4.10 S19 참여자 초대·퇴장 (`/rooms/:id/participants`, 다이얼로그)

**사람과 에이전트를 한 다이얼로그에서** 다룬다(§7 `room_participant` 한 표). 마법사 5단계가 여기로 왔고, v0.3 의 O2 「참여 에이전트 추가·제거」가 정의를 마법사에 의탁하고 있었으므로 **그 정의도 여기가 정본이 된다.**

**표시 정보 — 두 구역**

| 구역 | 내용 |
|---|---|
| **지금 있는 사람·에이전트** | 아바타·이름·역할(방장/부방장/참여자 · 에이전트 역할)·합류 시각·현재 상태. 각 행에 「내보내기」 |
| **초대하기** | 검색 한 칸 + 두 탭(**사람** / **에이전트**) |

**사람 탭** — 워크스페이스 멤버 목록(이미 있는 사람은 제외). 「초대」를 누르면 바로 참여자가 된다(토큰·메일 없음). **부방장 지정**은 사람 행의 「…」에서 — 한 명만, 방장이 지정.

**에이전트 탭** — 워크스페이스 에이전트 목록 + 각자 **프로파일 선택**. 마법사 5단계의 두 경고를 그대로 옮긴다.

| 상황 | 화면 |
|---|---|
| `respond_to` 로 초대 불가 | 행 비활성 + **사유**(「이 에이전트는 소유자만 부를 수 있습니다」·「킬 스위치가 켜져 있습니다」) — FR-1.9 |
| 방 컴퓨터에 없는 `runtime_kind` 프로파일 | 경고 — 「〈MacBook〉에 Hermes 가 없습니다. 첫 실행에서 실패합니다 — 다른 프로파일을 고르거나 방 설정에서 컴퓨터를 바꾸세요」 |
| 컴퓨터가 아직 안 정해진 방 | 경고 대신 안내 — 「컴퓨터가 첫 실행 때 정해집니다. 프로파일이 맞는지는 그때 검사됩니다」 |
| 보관된 방 | 전체 비활성 + 「보관된 방에는 초대할 수 없습니다」 |

**동작** — 초대 · 내보내기 · 부방장 지정·해제 · 프로파일 바꾸기.

**내보내기 규칙**(FR-2.2)
- **에이전트**: 서브 미션·할 일은 **남고**(기록) 새 트리거만 막힌다. **퇴장은 취소가 아니다** — 진행 중인 턴이 있으면 「진행 중인 턴 1개는 계속 돕니다. 멈추려면 서브 미션 보드에서 중단하세요」를 확인 문구에 적는다.
- **사람**: 게시·열람 권한이 사라진다. 그 사람이 **어떤 미션의 Director 이면 내보내기를 거부**하고 「〈보고서 초안〉의 Director 입니다 — 먼저 Director 를 교체하세요」. 방장은 내보낼 수 없다(먼저 방장을 넘긴다).
- **[공백]** 나간 사람이 originator 인 할 일이 다른 방을 읽으려 하면 그 순간 `403` 이 된다(FR-4.5 [V19-C] «권한은 읽는 시점에 다시 검사한다»). 사유는 에이전트에게 구분해 전달되지만 **사람 쪽 화면에는 경고가 없다** — §8.2 G-7.
- 추가·제거는 시스템 메시지로 타임라인에 남긴다 — 로스터가 바뀌면 이후 위임 판단이 달라지므로 에이전트도 알아야 한다.

**권한** — 방장·부방장·ws owner·admin. 그 밖에게는 다이얼로그가 **읽기 전용**으로 열린다(누가 있는지는 참여자 전원이 봐야 한다).

**빈 상태** — 워크스페이스에 에이전트가 0개면 에이전트 탭에 「먼저 에이전트를 만드세요」 + S9 링크. 초대할 사람이 없으면 「워크스페이스 멤버가 당신뿐입니다」 + 설정·멤버 링크.

**실시간** — 참여자 목록·상태, 다른 사람이 동시에 초대·퇴장했을 때의 갱신.

### 4.11 S20 방 설정 (`/rooms/:id/settings`)

마법사 3·4·7단계가 여기로 왔다. **여기가 방의 유일한 안전망**이다 — 기본값이 워크스페이스에서 상속되므로 사람이 한 번도 안 보면 모든 방이 같은 값으로 돈다(FR-2.1).

| 묶음 | 항목 | 화면 규칙 |
|---|---|---|
| **공개 범위** | `visibility: workspace` 또는 `invited` | 바꾸면 시스템 메시지(§2.4). `invited` 로 바꿀 때 「지금 이 방을 보는 사람 중 초대되지 않은 N명이 더는 볼 수 없게 됩니다」 |
| **컴퓨터·격리** | `runtime_id` · `isolation`(`worktree`/`none`, `container` 는 v1.1) | **첫 dispatch 전에만 바꿀 수 있다.** 고정된 뒤에는 읽기 전용 + 「〈MacBook〉에 고정됨 — 작업 폴더가 거기 묶여 있습니다. 바꾸려면 [컴퓨터 바꾸기]」(S17). `worktree` 를 고르면 **저장소 선택** + 바인딩 규칙 설명(에이전트당 작업 폴더 1개) — 마법사 3단계의 그림이 여기로 온다 |
| **한도** | 예산(USD) · 시간 · **동시 진행 미션 수**(`max_concurrent_works`, 기본 3) · 동시 서브 미션 수(`max_parallel_lanes`, 기본 5) | 「초과는 완료가 아니라 멈춤입니다. **방 상한을 넘기면 이 방의 모든 미션이 한꺼번에 멈춥니다**」(FR-2A.3). 미션에도 걸 수 있고 **작은 쪽이 이긴다** |
| **autonomy** | `guided`/`autonomous`/`supervised` | §4.7 의 문장 표와 **같은 문구**. 방 기본값이며 미션이 덮어쓸 수 있다 |
| **기본 Director** | `default_director`(기본: 방장) | 새 미션의 Director 기본값 |
| **방장·부방장** | 방장 넘기기 · 부방장 지정 | 방장 넘기기는 확인 다이얼로그 + 시스템 메시지 |
| **참고 방 링크** | 연결된 방 목록 + 「연결 관리」 | S24 |
| **보관·삭제** | 보관 · 보관 해제 · 삭제 | §4.3 카드 메뉴와 같은 규칙·같은 확인 문구 |

**각 항목에 바꿨을 때의 영향을 한 줄로 적는다** — 예: 「동시 미션 상한을 1로 낮추면 새 미션을 열 때 기존 미션을 먼저 닫아야 합니다」.

**동작** — 값 저장(즉시 반영) · 방장 넘기기 · 부방장 지정·해제 · 참고 방 링크 관리(S24) · 보관·보관 해제·삭제. 파괴적인 것은 전부 확인 다이얼로그를 거친다.

**권한** — 방장·부방장·ws owner·admin(삭제는 방장·ws owner·admin). 그 밖에게는 **읽기 전용**으로 열린다 — 내가 못 바꾸더라도 이 방이 어떤 격리·예산으로 도는지는 봐야 한다(§1 원칙 5).

**빈 상태** — 없음. 단 컴퓨터가 아직 없으면 컴퓨터 칸에 「연결된 컴퓨터가 없습니다」 + S12 링크.

**실시간** — `runtime_id` 고정(첫 dispatch 순간 읽기 전용으로 바뀐다), 한도 잔여, 참고 방 링크 목록.

### 4.12 S24 참고 방 링크 (`/rooms/:id/settings/links`, 다이얼로그)

**없으면 ⑤가 반쪽이 된다.** FR-4.5 의 권한 2번 조건은 「그 에이전트가 그 방의 참여자이거나, **현재 방에 참고 방 링크로 연결되어 있으면**」이다. 링크를 걸 화면이 없으면 **에이전트가 참여하지 않은 방은 영원히 읽을 수 없다.**

**표시 정보**

| 구역 | 내용 |
|---|---|
| 연결된 방 | 방 이름 · 설명 · 마지막 활동 · 연결한 사람·시각 · 「연결 풀기」 |
| 방 찾아 연결 | 검색 한 칸 + 결과 목록(**내가 참여한 방만**) + 「연결」 |
| 설명 한 줄 | 「연결하면 이 방의 에이전트가 저 방을 **읽을 수** 있습니다. **쓰지는 못합니다.** 읽을 때마다 양쪽 방에 기록이 남습니다」 |

**동작** — 연결 · 연결 풀기. 둘 다 **양쪽 방 모두에 시스템 메시지**를 남긴다(FR-4.5) — 한쪽만 알면 유출 경로가 한쪽에만 보인다.

**연결해도 읽히지 않을 수 있다는 것을 말한다.** 권한은 두 조건이 **둘 다** 맞아야 하므로, 링크가 있어도 **그 할 일을 일으킨 사람이 저 방의 참여자가 아니면** `403` 이다(FR-4.5 조건 1). 다이얼로그 아래 한 줄: 「연결은 **에이전트 쪽 조건**만 풉니다. 읽는 순간 **요청한 사람**이 저 방의 참여자인지도 검사합니다」.

**권한** — 방장·부방장·ws owner·admin. 그 밖에게는 읽기 전용(연결된 방 목록은 참여자 전원이 본다 — 맥락이 어디로 샐 수 있는지는 공개 정보여야 한다).

**빈 상태** — 「연결된 참고 방이 없습니다. 이 방의 에이전트는 **자기가 참여한 방만** 읽을 수 있습니다.」 검색 결과 0이면 「내가 참여한 방 중에 없습니다 — 먼저 그 방에 참여하세요」.

**실시간** — 연결 목록(다른 방장이 반대쪽에서 풀 수 있다).

### 4.13 S23 맥락 읽기 기록 (`/rooms/:id/reads`)

**⑤의 안전장치다.** FR-4.5 는 읽기를 양쪽 방에 남기라 했는데, **읽힌 쪽에는 그 할 일이 없어 `task_event` 에 붙일 수 없다** — `task_event` 는 닫힌 스키마다(§7 v0.16 S-52). 남는 자리는 `activity_log` 와 **방 타임라인 시스템 메시지** 둘이고, 이 화면은 그 둘을 한 자리에 모은다.

**표시 정보 — 세 묶음**

| 묶음 | 내용 |
|---|---|
| **이 방이 읽은 것** | 시각 · 읽은 에이전트 · **요청한 사람**(originator) · 대상 방 이름 · 범위(요약 + 최근 N건) · 그 할 일 링크 |
| **이 방이 읽힌 것** | 시각 · 읽은 에이전트 · **요청한 사람** · **읽은 쪽 방 이름** · 범위. 타임라인 시스템 메시지와 같은 사건 |
| **거부된 시도** | 시각 · 에이전트 · 사유(「요청자가 그 방의 참여자가 아닙니다」 / 「이 에이전트가 읽을 수 없는 방입니다」). **어느 방인지는 적지 않는다** — 존재를 숨기는 규칙 그대로(FR-4.5) |

- 「이 방이 읽힌 것」의 **읽은 쪽 방 이름은 적는다.** 읽힌 쪽 사람은 자기 맥락이 어디로 갔는지 알아야 하고, 읽은 쪽 방의 존재는 이미 그 사건으로 드러났다.
- 「거부된 시도」는 **읽으려 한 방의 활동**에 남는 것이므로 이 화면에서는 「이 방의 에이전트가 시도했으나 막힌 것」만 보인다.
- 잘린 구간(`max_rooms_per_turn`·`max_tokens` 초과로 잘림, FR-4.5)이 있으면 그 행에 「분량 상한으로 잘림」 칩.

**동작** — 기간 필터 · 에이전트 필터 · 「활동 로그에서 보기」(S15 로, 같은 사건의 원본 행).

**권한** — 방 참여자 누구나 읽는다. **맥락이 새어 나간 사실은 그 방 사람 전원이 봐야 한다**(FR-4.5 의 취지). 필터·내보내기 없음.

**빈 상태** — 「이 방의 맥락이 오간 적이 없습니다. 에이전트가 `colab room read` 로 다른 방을 읽으면 여기에 남습니다.」

**실시간** — 새 읽기·읽힘·거부 행(`room_read.recorded`, 타임라인 시스템 메시지와 동시에).

> **저장 자리는 `room_read_log` 다**(PRD §7, #274 — SCR-B C7·P7). 칸: `reader_task_id · reader_agent_id · originator_user_id? · target_room_id · allowed(bool) · denied_reason? · summary_bytes · created_at`. 이 화면의 세 묶음은 **한 표를 세 방향으로 읽는 것**이다 — 「이 방이 읽은 것」은 `room_id = 이 방`, 「이 방이 읽힌 것」은 `target_room_id = 이 방`, 「거부된 시도」는 `room_id = 이 방 AND allowed = false`. **`allowed=false` 행에서는 `target_room_id` 를 화면에 내보내지 않는다**(존재 숨김, FR-4.5) — op(`listRoomReads`)의 응답에서도 `null` 로 지운다.

---

### 4.14 S8 받은 요청 (`/inbox`)

**목적**: "지금 내가 답해야 할 것"을 **방을 열지 않고** 처리한다. v0.19 에서 항목 타입이 **방·미션·미션 밖** 세 층으로 갈린다.

| 항목 타입 | 심각도 | 카드 내용 | 인라인 동작 | v0.19 |
|---|---|---|---|---|
| `hitl_request` | action_required | 맥락 한 줄(F2 표) · 질문/승인 대상 · **제안 기본값** · 기한·`overdue` · **수신자 근거**(「Director 로서」/「방장으로서」) | 답변 / 승인·거절 | **수신자 근거 추가** |
| **`isolation_confirm`** | **action_required** | 「**〈MacBook〉에는 저장소가 있습니다. 에이전트들이 같은 폴더를 함께 고치게 됩니다 — 이 결정은 되돌릴 수 없습니다**」 + 기본 제안 `worktree` + 「이 방은 아직 첫 턴을 시작하지 않았습니다」 + **첫 턴을 일으킨 사람·메시지 인용** | **「워크트리로 나눔」(기본) / 「이대로 진행」 / 「방 설정 열기」**(S20) | **신규**(FR-2.1.1 세 번째 불릿) |
| `lane_blocked` | action_required | 위임자 없는 서브 미션의 질문(FR-6.2.1) | 답글 작성 | — |
| **`work_proposed`** | action_required | 에이전트가 제안한 미션 goal + 근거 | **이대로 열기 / 고쳐서 열기 / 거절**(S26) | **신규**(FR-2A.1) |
| **`work_paused`** | attention | 미션 이름 + 사유(예산/시간/루프/수동) | 계속 승인 / **미션 열기**(S22) | 옛 `session_paused` 분할 |
| **`room_paused`** | **action_required** | 방 이름 + 사유 + **「이 방의 미션 N개와 대화 전부가 멈췄습니다」** + **「승인하면 이 방의 미션 N개가 한꺼번에 다시 돕니다 — 열린 미션 잔여 예산 합계 $X」** | 계속 승인 / 방 열기 | **신규** — 폭발 반경이 달라 심각도가 한 단계 위다(FR-2A.3) |
| `run_failed` | attention | 실패 분류·에이전트·미션(없으면 「미션 없음」) | **다시 지시** / 방 열기 | 미션 라벨 추가 |
| `runtime_offline` | attention | 머신·경과 시간, 7일 유예 잔여, **묶인 방 N개** | 연결된 컴퓨터 열기 / 컴퓨터 바꾸기 | 방 단위 |
| **`work_completed`** | info | 미션 결과 요약 | 미션 열기(S22) | 옛 `session_completed` |
| `mention` | info | 나를 멘션한 메시지 | 방 열기 | — |
| **`room_invited`** | info | 「〈서연〉이 〈결제팀〉에 초대했습니다」 | 방 열기 | **신규**(FR-2.2) |

**카드는 방과 미션 두 개의 바로가기를 준다**(PRD §6). 미션이 없는 항목은 방 바로가기 하나만.

**`inbox_item.work_id`·`lane_id` 는 §7 에 들어갔다**(#271·#274). 미션이 5개 도는 방에서 「예산 승인해 주세요」가 어느 미션 것인지 카드가 말할 수 있고, 미션별 필터·미션 단위 구독이 그 위에 선다 — 옛 §8.2 G-2·§8.3 D-6 은 이 칸으로 닫혔다.

**맥락 한 줄은 두 줄까지 쓴다**(SCR-C K). 첫 줄은 `[심각도] [타입] · 방 이름 · 미션 제목` 이고 **방 이름은 줄이지 않는다** — 말줄임은 미션 제목부터, 그다음 타입 순이다. 미션 밖 항목의 **트리거 메시지 인용은 둘째 줄**로 내리고 한 줄 말줄임한다(「"경쟁사 수수료 정책 어떻게 돼?"…」). 인용을 첫 줄에 넣으면 방 이름이 밀려 **어느 방 일인지 모르는 채로 답하게 된다** — F2 가 막으려던 바로 그 상황이다.

**`room_paused`·미션 밖 `hitl_request` 는 위임 시점을 적는다**(FR-2A.3). 내가 부방장이거나 owner 최고참이면 카드가 「**14:30부터 답할 수 있습니다**」로 비활성 상태를 먼저 보이고, 그 시각이 지나면 활성 + 「위임됨 · 방장 〈민호〉가 아직 답하지 않았습니다」. 미션 `deputy` 와 같은 규칙이다(아래).

**deputy에게 나타나는 시점** — `hitl_request` 항목은 처음에는 그 미션 Director 의 받은 요청에만 있다. **기한의 절반이 지나면 그 미션의 deputy 에게도 나타나며** 「위임됨 · 지금부터 응답 가능」으로 표시된다. 알림도 함께 보낸다.

**정렬**: `overdue` → action_required → attention → info, 각 묶음 내에서 기한 임박 순.
**필터**: 미읽음 / 액션 필요 / **방별** / **미션별**. 제어가 늘어 **두 줄**이 되므로 칩(미읽음·액션 필요)과 선택 상자(방·미션)를 줄을 나눠 놓는다. 목록은 **한 열 그대로**다(COMPONENTS §8.5).
**뱃지**: 내비에 action_required 개수만 카운트한다. info까지 세면 뱃지가 무의미해진다. **`room_paused` 가 action_required 로 올라간 만큼 뱃지 정의는 그대로 유지된다** — 방 전체가 멈춘 것은 "내가 지금 해야 할 일"이 맞다.

**권한** — 본인 것만. **빈 상태** — 「지금 답할 것이 없습니다」(부정형이 아니라 상태로). **실시간** — 항목 추가·해소, 뱃지, deputy 노출 시점 도달.

### 4.15 S9 에이전트 목록 · S10 에이전트 편집

**S9** — 카드마다 이름·역할·역할 설명·상태·기본 프로파일(런타임+모델)·소유자·`respond_to`, 그리고 **v0.19 신규: 「참여 중인 방 N」**.

- **「참여 중인 방 N」을 누르면 그 방 목록이 펼쳐진다.** 내가 볼 수 있는 방만 나오고, 못 보는 방은 개수에만 포함하되 「+ 볼 수 없는 방 1」로 적는다 — 개수를 속이면 "왜 3인데 2개만 보이나"가 되고, 이름을 보이면 `invited` 방의 존재가 샌다(§2.4).
- **이 칸이 없으면 ⑤를 사람이 화면에서 확인할 방법이 없다** — "이 Writer 가 어느 방들에 있나"를 물을 자리가 어디에도 없다(V19-A §2.2).
- 카드에 **「동시 3개 중 2개 사용 중」**(`max_concurrent_tasks`)을 함께 적는다. 이 상한은 **방을 가로지르는 전역**이라(§12.1-5 확정) 한 방의 사용자가 "내 방 에이전트가 왜 느린가"를 여기서만 알 수 있다.
- 필터(역할·런타임·상태·소유자·**참여 방**). 새 에이전트, 팀 템플릿(FR-1.4).

**S10** — 3구역(정체성 / instruction / 실행). 프로파일 목록(추가·삭제·기본 지정) — 런타임 종류 → 모델 → 옵션 → env/args → 폴백. 툴·MCP 허용 목록, `max_concurrent_tasks`, `budget_per_task`, `respond_to`(+ allowlist). 모델 목록은 데몬 probe 결과로 채우고, 런타임 능력에 없는 옵션은 비활성 + 사유.

**역할별 허용 명령**(FR-1.9.1) — 역할 카드 아래 **읽기 전용 목록**으로 그 역할이 쓸 수 있는 `colab` 명령을 보인다. **v1.1 에서 이미 구현된 화면이다**(PR #246·#247, 실측 `p5-w16-02-s10-role-commands-*`) — v0.19 초판 §8.1 대조표가 「FR-1.9.1 | — | v1.1」로 적어 화면 목록에서 빠져 있었다(SCR-C S). 방 구조로 넘어가도 이 칸은 그대로 남고, 명령 이름만 R0 계약의 `ColabCommand` enum(`room_read` 추가)을 따라간다.

**테스트 채팅** — **방이 아니다**(FR-1.8.1). 방·미션·`colab` 토큰·작업 폴더 어느 것도 만들지 않는다. 화면 문구도 「방」을 쓰지 않는다 — 「이 에이전트와 1:1로 시험 대화」.

**`respond_to: nobody`(킬 스위치)** — 별도 위험 영역. 켤 때 「실행 중인 턴이 취소되고 대기 중 할 일이 취소됩니다. 열린 사람 확인 요청은 남습니다」 + **v0.19 추가: 「참여 중인 방 N개 전부에서 멈춥니다」**.

**권한** — 생성은 멤버 누구나, 수정·삭제는 소유자 + ws admin 이상(§8.7 Q7). **빈 상태** — 에이전트 0개면 템플릿 3종을 카드로. **실시간** — 상태, 동시 사용 수.

### 4.16 S11 연결된 컴퓨터 · S12 컴퓨터 연결 · S13 작업 폴더 관리 · S17 컴퓨터 바꾸기

**S11** — 머신 카드: 이름·호스트·온라인 상태·마지막 접속·데몬 버전, 감지된 CLI 목록(종류·버전·로그인 여부·모델 수), **지금 하는 일 N**, 작업 폴더 사용량 합계, 저장소 목록(경로·remote URL·브랜치·클린 여부). 오프라인이면 경과 시간과 **7일 유예 잔여**를 표시하고, 유예를 넘기면 **「이 컴퓨터에 묶인 방 N개가 멈췄습니다」** + 컴퓨터 바꾸기 링크(v0.3 의 「세션 N개가 일시정지됨」을 대체 — FR-9.2 의 단위가 방이다).

삭제는 **진행 중인 방이 없을 때만** 가능하고, 있으면 차단 사유와 **대상 방**을 나열한다.

**S12** — 설치 명령 2줄(복사 버튼)과 연결 대기 상태(`대기 중 → 연결됨 → CLI 감지 중 → 준비 완료`). 3분 이상 연결되지 않으면 문제 해결 안내(방화벽·권한·CLI 미설치).

**S13 작업 폴더 관리** — 목록: 종류(worktree/container/dir), 소유 에이전트 또는 서브 미션, 경로·브랜치, 용량, 마지막 사용, 보존 만료일, 상태(활성/보존 중), 그리고 **「쓰는 중인 방」**(v0.3 의 「쓰는 중인 세션」을 대체 — 작업 폴더는 **방×에이전트** 단위다, FR-6.1).

**보존 기한의 기준 사건이 바뀐다.** v0.3·v0.18 은 세션 완료 시각이 기준이었는데 **방은 완료되지 않는다.** 기준은 **`workdir.last_used_at + workdir_retention_days`** 다(FR-2.3 [V19-B], V19-B NN8·위험 1). 화면은 그 기준을 그대로 적는다 — 「마지막 사용 2026-09-08 · **14일 뒤 정리 예정(2026-09-22)**」. 이 문장이 없으면 사용자가 "방이 살아 있는데 왜 지워지나"를 모른다.

수동 삭제는 가능하되 **미병합 커밋이나 미커밋 변경이 있으면 경고하고 기본 차단**한다(FR-6.4 M4). 워크스페이스 용량 상한 대비 사용률을 상단에 표시한다.

**S17 컴퓨터 바꾸기(재바인딩)** — `/runtimes/:id/rebind?room=`. 단위가 **세션에서 방으로** 바뀐다.

| 구역 | 내용 |
|---|---|
| 상황 | 「MacBook이 9일간 오프라인입니다. **〈결제팀〉 방은 8월 28일부터 멈춰 있습니다** — 미션 2개와 대화 전부」 |
| 대상 선택 | 격리 방식이 후보를 제한한다 — `none`은 온라인 컴퓨터 전부, `worktree`는 **remote URL이 같은 저장소를 가진 컴퓨터만**. 후보가 없으면 그 사실과 이유 |
| **유실 경고** | `worktree`일 때만: 「완료된 서브 미션의 코드도 원래 머신의 브랜치에만 있습니다. 새 머신에서는 **이 방의 아티팩트(diff) N개**를 적용해 복구합니다. 커밋 이력은 복원되지 않습니다」 — **순서는 말하지 않는다**(§8.2 G-4) |
| 선택 | **컴퓨터 바꾸기** / **열린 미션 모두 취소** / 취소 |

- **여러 방이 한 컴퓨터에 걸렸으면 방마다 따로 결정한다.** S11 에서 들어오면 방 목록이 먼저 뜬다.
- **v0.3 의 「세션 종료」 선택지가 「열린 미션 모두 취소」로 바뀐다** — 방은 종료되지 않는다(FR-2.4).
- **[공백] diff 적용 순서.** 방 하나에 미션 5개의 diff 가 섞여 있다. v0.3 의 문구 「이 세션의 diff 아티팩트를 순서대로」는 세션이 한 주제였기에 성립했다. 방에서 무슨 순서인지(제출 시각순? 미션별로 묶어서? 취소된 미션의 diff 는 제외?)가 PRD 에 없다 — §8.2 **G-4**(§8.3 D-8 의 나머지 절반이 여기로 합쳐졌다).

**권한** — S11·S12·S13 은 owner·admin. S17 은 **방장**(방 단위이므로 v0.3 의 Director 에서 바뀐다).

**빈 상태** — 컴퓨터 0개면 S12 로 유도. 작업 폴더 0개면 「아직 만들어진 작업 폴더가 없습니다」. 재바인딩 후보 0이면 사유를 적는다.

**실시간** — S11 온라인 상태·지금 하는 일 수·CLI 감지 / S13 용량·상태(GC 결과 반영) / S12 연결 단계.

### 4.17 S14 설정 (`/settings/*`)

| 탭 | 내용 | 권한 |
|---|---|---|
| 멤버 | 목록·초대·역할 변경·제거 | owner·admin |
| 런타임 정책 | 데몬 동시 할 일 상한, 컴퓨터별 상한 | owner·admin |
| 예산 | 워크스페이스·**방 기본** 상한, 초과 시 동작 | owner·admin |
| 루프 상한 | `max_chain_depth`(8) · `max_hops_per_hour`(60) · `max_pair_roundtrips`(5) | owner·admin |
| 컨텍스트 | **`room_history.recent_messages`(50)** · **`room_read.max_rooms_per_turn`(3) · `max_tokens`(4000)** | owner·admin |
| 작업 폴더 | 기본 격리 방식, `workdir_retention_days`(14) · `workdir_disk_quota_gb` · `runtime_offline_grace`(7일) | owner·admin |
| 보안 | 활동 로그 페이로드 마스킹(diff·셸 출력을 요약만 저장) | owner |
| 알림 | 이메일·푸시, **구독 단위**(아래) | 개인 |
| 대시보드 | PRD §11 성공 지표 10개 표 — `getWorkspaceMetrics` 를 그대로 그린다. **행마다 분모 라벨**(「미션 기준」 1·2·5 / 「방 기준」 10 / 나머지는 task·HITL 집계라 라벨 없음, §12.1-10 확정). 표본 없으면 「아직 잴 수 없음」. 표 **아래에 「관찰」 표 7행**(PRD §11 `[v0.17]`·`[V19-C]`) — `getWorkspaceObservations`, 목표치 없음 | 멤버(읽기) |

> **「컨텍스트 재사용 상한」 항목이 빠진다**(§12.1-7 확정: FR-4.4 를 **버린다**). 같은 탭의 `room_history`·`room_read` 상한은 남는다 — 그 둘은 FR-4.1·FR-4.5 의 것이다. R0 정리 대상 셋은 §7 `room_context` 표 · 계약 `Session.context[]`·`context_reuse_override` · 이 설정 항목이다(PRD FR-4.4).
>
> **「관찰」 표와 S10 「역할별 허용 명령」은 v1.1 에서 이미 구현된 화면이다**(SCR-C S, PR #246·#247 · 실측 `p5-82-s14-observations.png`·`p5-w16-01-*`). v0.19 초판의 9탭 표와 §8.1 대조표에서 빠져 있었다 — S15 승격처럼 이 둘도 v0.19 화면 목록에 자리를 갖는다.

**「방 기본값」 묶음이 결정적으로 중요해진다.** 방 만들기가 칸 하나가 되면서 **워크스페이스 기본값이 모든 방의 실제 값**이 된다(FR-2.1). 예산·기본 격리 탭에 「**이 값이 새로 만드는 모든 방의 기본값입니다**」를 명시하고, 기본 격리 옆에 「`none` 이면 에이전트들이 같은 폴더를 함께 고칩니다」를 한 줄로 적는다 — 마법사 3단계가 하던 말이 여기와 S18·S20 셋에 나뉘어 들어간다.

**구독 단위**(FR-8, §10 R2 [V19-C]) — v0.18 의 「세션 구독: 전부 / HITL만 / 종료만」은 **끝나지 않는 방에 너무 성기다.** 구독 단위를 **미션·서브 미션 스레드**로 내린다.

| 단위 | 선택지 |
|---|---|
| 방 | 전부 / 내가 참여한 미션만 / HITL만 / 끄기 |
| 미션 | 전부 / HITL만 / 종료만 (방 설정을 덮어씀) |

미션 단위 구독은 `inbox_item.work_id`·`lane_id`(§7, #271·#274)와 구독 op(`setWorkSubscription`·`setLaneSubscription`) 위에 선다. **안 읽음 배지는 방에 하나뿐이고**(§12.1-6), 미션·서브 미션 단위는 **구독으로만** 내려간다 — 두 단위의 배지가 같이 뜨면 사람이 어느 것을 지워야 하는지 모른다.

**동작** — 탭 전환 · 값 저장(즉시 반영) · 멤버 초대·역할 변경.

**권한** — 탭별로 위 표. 대시보드는 멤버 읽기, 나머지는 owner·admin(보안 탭은 owner).

**빈 상태** — 각 목록의 0건 문구(「초대한 멤버가 없습니다」 등). 대시보드는 표본이 없으면 「아직 잴 수 없음」.

**실시간** — 없다(설정은 저장 시 반영).

### 4.18 S15 활동 로그 (`/settings/audit`) — **v1.1 → v1 승격**

**승격 이유는 하나다.** FR-4.5 는 다른 방 읽기를 **양쪽 방에 기록**하라고 하는데, **읽힌 쪽에는 그 할 일이 없어 `task_event` 에 붙일 수 없다**(§7 닫힌 스키마, S-52). 남는 자리는 `activity_log` 뿐이다. S15 가 v1.1 이고 「v1 에서는 내비에 노출하지 않는다」로 남아 있으면 **§12 위험표가 FR-4.5 의 완화책으로 내건 "양쪽 기록"이 절반만 작동한다.**

**표시 정보** — 시각 · 행위자(사람 또는 에이전트) · 행위 · 대상 · 방 · 페이로드(마스킹 설정 반영).

**v0.19 에서 반드시 담아야 하는 행**

| 사건 | 왜 여기여야 하나 |
|---|---|
| **다른 방 읽기(읽은 쪽·읽힌 쪽)** | FR-4.5 — 읽힌 쪽의 유일한 영속 기록 |
| **읽기 거부(`403`)** | FR-4.5 — 대상 방 이름은 적지 않는다 |
| 참고 방 링크 연결·해제 | FR-4.5 |
| `visibility` 변경 · 감사 열람 | §2.4 |
| 방 삭제(`room.deleted`) | FR-2.6 — 방이 사라진 뒤 남는 한 줄 |
| 방장 승계 | §12.1-4 |

**동작** — 기간·행위자·방·사건 종류 필터. 내보내기는 v1.1.

> **부를 op 이 없었다**(SCR-B C6: 계약 97개 op 전수 확인 — activity 계열 0개). PRD §10 **R0 행에 `listActivityLog` 가 들어갔다**(#274). 응답 칸은 `{at, actor{kind,id,name}, action, object_ref, room{id,name}?, payload(마스킹 반영)}` 이고 권한은 owner·admin 이다(§부록 A).

**권한** — owner·admin. **방 참여자는 자기 방 몫만 S23 에서 본다** — 활동 로그 전체는 워크스페이스를 가로지르므로 권한이 다르다.

**빈 상태** — 「기록된 활동이 없습니다」. **실시간** — 없다(감사 화면은 새로고침으로 충분하고, 실시간으로 흐르면 읽을 수 없다).

> **내비 노출**: 설정 안에 둔다(`/settings/audit`). v0.3 의 「v1 에서는 내비에 노출하지 않는다」는 **철회**하되, 최상위 내비가 아니라 설정 탭으로 둔다 — 평소에 볼 화면이 아니고, 방 사람이 볼 것은 S23 이다.

---

## 5. 공통 컴포넌트

여러 화면에 반복되는 요소는 한 번만 정의한다. **캔버스에서는 이 표의 항목이 `reusable` 컴포넌트로 존재한다** — id·오버라이드 키·슬롯은 `COMPONENTS.md`에 있다.

> **캔버스 id 를 인용할 때의 주의**(SCR-C §2.6). `COMPONENTS.md` 는 v0.19.1 에서 머리를 올렸지만 **캔버스(`agent-collaboration.pen`)는 여전히 세션 모델의 P0 와이어프레임**이다. 여기서 `x1YCq`·`n9PqY` 같은 id 를 인용하는 것은 «그 카드의 자리» 를 가리키는 것이지 «그 카드의 문구가 맞다» 는 뜻이 아니다 — 한쪽이 다른 쪽을 id 로만 가리키고 내용 대조를 아무도 안 하는 구조가 D-11(「산출물」)을 낳았다. **문구의 정본은 PRD §3.2 → `COMPONENTS.md` §8.4 → 이 문서 순이다.**

| 이 표의 항목 | 캔버스 컴포넌트 | v0.19 |
|---|---|---|
| 에이전트 칩 | Agent Chip `MmxsR` | **사람 칩과 같은 목록에 선다** — 아이콘으로만 구분. 사람은 별도 컴포넌트(아래) |
| 상태 배지 | Badge `Tfz9o` | 층이 하나 늘었다(방 `blocked_reason`) |
| HITL 카드 | HITL Card `n9PqY` (타임라인) · Inbox Item `T0qdqP` (받은 요청) | **수신자 근거 슬롯** 추가(기존 `W7gS3` 권한 안내 슬롯 재사용) |
| 서브 미션 카드 | Lane Card `x1YCq` | **미션 라벨 슬롯** 추가(기존 부가 정보 `bXrz6` 재사용, 「미션 〈…〉」 또는 「미션 없음」) + `queued` 대기 사유 |
| 조건 행 | Condition Row `XMNop` | — |
| 프로파일 행 | Profile Row `ZWC8q` | S19 에이전트 탭에서도 쓴다 |
| **상태 배너** | Status Banner `zPJly` | **방 멈춤 배너**(전폭)와 **미션 멈춤 배너**(우열)로 두 크기. **방 배너는 끼어들고(`role="alert"`) 미션 배너는 조용하다(`role="status"`)** — 방이 멈추면 이 방의 모든 일이 멈춘 것이라 읽던 흐름을 끊어도 된다. **멈춘 수는 문장에 보간하지 않고 칸으로 둔다**(「미션 **2**개와 대화 전부」) — 보간하면 문구 자물쇠가 잡지 못한다(COMPONENTS §8.5) |
| 활동 피드 항목 | Activity Feed `h6pub` | — |
| **미션 칩** (Work Chip) | **신규** | 상단 거르개 겸 선택자. 상태 글리프 + 이름. 스펙은 COMPONENTS §9.1 |
| **미션 선택기** (Work Selector) | **신규** | 작성창 옆. 「귀속될 미션」을 고르고 보여 준다. 스펙은 COMPONENTS §9.2 |
| **사람 칩** (Person Chip) | **신규** | `AgentChip` 은 닫힌 타입(`AgentRole`·`AgentStatus`)이라 방장·부방장을 담을 수 없다. 같은 격자·다른 데이터. 스펙은 COMPONENTS §9.3 |
| **방 멈춤 배너** (Room Blocked Banner) | **신규** — Status Banner `zPJly` 의 두 번째 크기 | 전폭 + **카운트 슬롯**. 스펙은 COMPONENTS §9.4 |
| 확인 다이얼로그 | (컴포넌트 없음 → **일반화 권고**) | **쓰는 곳이 일곱으로 늘었다** — 방 삭제·보관·「이 방 멈춤」·참여자 내보내기·방장 넘기기·링크 해제·미션 취소·제안 거절. `DeleteSessionDialog` 의 `role="alertdialog"` 패턴을 일반화하는 것이 가장 싸다(SCR-B §3.4) |

| 컴포넌트 | 쓰이는 곳 | 규칙 |
|---|---|---|
| **에이전트 칩 / 사람 칩** | 참여자, 서브 미션 카드, 메시지, 로스터 | 아바타 + 이름 + 상태 점. 클릭 시 팝오버(역할 설명·프로파일·**참여 중인 방 수**·현재 작업). **이 방에 실행 중인 할 일이 없는데 `working` 이면 둘째 줄에 「다른 방에서 작업 중」**(§4.6) |
| **상태 배지** | 방·미션·서브 미션·할 일·에이전트·컴퓨터 | 상태별 색·아이콘을 **전 화면 통일**. 글리프가 1차 신호, 색은 보조. 글리프: running ● · waiting_human ⏳ · blocked ? · paused ⏸ · done ✓ · failed ✕ · queued/idle ○ · disabled ⊘. `paused`는 테두리만(soft), `failed`·`offline`은 채움(solid). 텍스트에 상태색을 쓸 때는 **예외 없이** 짙은 `-text` 변형. 받은 요청 심각도 글리프: `action_required` **!** · `attention` **▲** · `info` **i**. `idle`은 회색 ○ |
| **멈춘 것은 층을 함께 적는다** (v0.19) | 미션·서브 미션·방 | 배지 옆에 층을 적는다 — 미션·서브 미션은 **「⏸ 일시정지 · 미션 예산」**, 방은 **「⏸ 멈춤 · 방 예산」**. 두 말의 분담은 §3.4(b) — 사람이 되돌리는 조작은 U12 가 못박은 「일시정지」, 방 전체를 세우는 것은 「멈춤」. 층 없이 쓰면 승인해도 왜 나머지가 안 움직이는지 모른다(§4.6) |
| **HITL 카드** | 받은 요청, 방 타임라인 | 껍데기는 다르되 본문 구조는 같다. **v0.19 에서 본문에 한 줄이 는다 — 수신자 근거**(「Director 로서 답합니다」/「방장으로서 답합니다」). 미션 밖 요청이 방장에게 가는 유일한 신호다(FR-2A.1). **위임 시점이 있으면 함께 적는다** — 「14:30부터 부방장이 답할 수 있습니다」(FR-2A.3·FR-5.4). **`isolation_confirm` 도 이 카드의 한 타입이다**(§4.14) — 입력부가 「워크트리로 나눔 / 이대로 진행」 두 선택지로 교체된다 |
| **비용 표시** | 방 목록·방 화면(두 칸)·컴퓨터·할 일 | 누적/상한 + % + 추정 배지. **v0.19: 미션 비용과 방 누적 비용은 다른 수다** — 방 누적은 미션 밖 할 일을 포함한다. 두 칸이 같은 컴포넌트를 쓰되 제목이 다르고, **방 누적 칸에는 「미션 비용의 합이 아닙니다」 한 줄이 화면 문자열로 들어간다**(§4.6 — 설계 문서에만 있으면 사람이 더해 보고 틀린다) |
| **멘션 입력기** | 방 작성창, 테스트 채팅 | 자동완성 → `mention://` 링크. **트리거 미리보기 + 귀속될 미션 칩 연동**(FR-3.1.1) |
| **미션 칩** | 방 화면 상단, 서브 미션 카드, 메시지 카드, 받은 요청 | 같은 모양이 네 곳에 선다. 「미션 없음」도 같은 칩으로 — 없음을 빈칸으로 두면 «아직 안 정해짐»과 구분되지 않는다. **상단 칩 줄만 누를 수 있고 카드 안의 것은 라벨이다** — 같은 모양이므로 누를 수 있는 쪽만 커서·포커스 링을 갖는다. **선택은 색으로 표시하지 않는다**: 선택된 칩은 테두리가 굵어지고 앞에 `✓` 가 붙는다(「글리프가 1차 신호」). 칩 줄은 하나의 그룹이고 이름은 「미션 거르개」다. **칩을 누르면 가운데와 우열 두 곳이 바뀌므로 바뀐 사실을 조용한 안내(`aria-live="polite"`)로 알린다** — 「미션 〈보고서 초안〉으로 걸렀습니다 · 서브 미션 3개 · 메시지 42개」 |
| **미션 선택기** | 방 작성창 | 상태 셋 — `열림`(사람이 고른다) / `잠김`(스레드 안, 비활성 + 사유) / **`자동`**(귀속 규칙 3). **`자동`은 `열림`과 시각적으로 달라야 한다** — 문장 앞에 「자동: 」을 붙인다. 둘이 같은 문장을 쓰면 사람은 자기가 고른 것인지 시스템이 고른 것인지 모른다(§4.6) |
| **권한 비활성 버튼** | 전역 | 숨기지 않고 비활성 + **버튼 바로 아래 글자 사유**(`DisabledHint` + `aria-describedby`). **툴팁(`title=`)으로 쓰지 않는다** — `disabled` 요소는 탭 순서에서 빠져 키보드로 띄울 수 없고, 터치에서는 아예 뜨지 않는다(§1 원칙 5). **v0.19: 사유에 층을 적는다** — 「Director만 가능」이 아니라 「〈민호〉가 이 미션의 Director 입니다」(§2.3) |
| **확인 다이얼로그** | 파괴적 동작 | 무엇이 사라지는지 명시. 되돌릴 수 없으면 그렇다고 씀. **보관은 되돌릴 수 있다고 적는다**(FR-2.4 [V19-C]) |

**구현 쪽에서 무엇이 쪼개지는가**(SCR-B §3.3 — R2 계획의 입력). 신규 9화면 중 **기존 컴포넌트 조립만으로 서는 것은 0개**다.

| 지금 | 어떻게 |
|---|---|
| `SessionAside` | **둘로 쪼갠다** — `WorkPanel`(미션 목표·성공기준·종료 조건 진행률·미션 비용·미션 동작·assignee·Director) + `RoomPanel`(아티팩트·결정 기록·방 누적 비용·맥락 오간 기록·방 설정 요약, **기본 접힘**). 내부 절 단위로 이미 갈려 있어 분할선이 깨끗하다 |
| `ParticipantsDialog` | **실질 재작성.** 지금은 에이전트 전용 목록이고, S19 는 두 구역(지금 있는 사람·에이전트 / 초대하기 + 사람·에이전트 두 탭) + 내보내기 거부 규칙 + 부방장 지정이다 |
| `sessions/[id]/page.tsx` | 3열 골격(`s7__cols`·열 전환 탭)은 **그대로 살아남는다.** 바뀌는 것은 상단 머리(칩 줄·액션 분리)와 우열(둘로 쪼갬), 데이터 로딩 축(session → room + works), 그리고 **좁은 화면 탭이 3 → 4**(§4.8) |

**슬롯·prop 만 더하면 되는 것**: `LaneCard`(미션 라벨 + `paused` 층 + `queued` 사유) · `HitlCard`·`HitlBody`(수신자 근거) · `InboxItemCard`(타입 6종 추가·2종 개명 + 맥락 두 줄 + 바로가기 둘) · `PausedBanner`(두 크기 + 층 + 카운트 슬롯 + `role`) · `Composer`(미션 선택기 + 귀속 칩) · `MessageCard`(미션 라벨 + 「이걸 미션으로」) · `SessionCardMenu`→`RoomCardMenu`(보관·해제·삭제) · `DeleteSessionDialog`→`DeleteRoomDialog` · `RebindDialog`(방 단위 문구).

**새로 만들 것**: `WorkChipRow` · `RoomBlockedBanner` · `CreateRoomDialog`(S18) · `CreateWorkDialog`(S21) · `WorkProposalDialog`(S26) · `RoomLinksDialog`(S24) · `RoomReadsTable`(S23) · `RoomSettingsForm`(S20) · `RoomSearchBar`(S25) · `UnreadBadge` · `AuditLogTable`(S15) · `TransferOwnerDialog` · `ConfirmDialog`(일반화).

---

## 6. 실시간 갱신

| 화면 | 실시간 대상 |
|---|---|
| S5 방 목록 | **안 읽음**, 진행 중인 미션 수, 주의 배지, **방 `blocked_reason`**, 마지막 활동, 방 추가·삭제 |
| S7 방 화면 | 메시지 스트리밍(토큰 단위), **에이전트 타이핑 표시**, 서브 미션 카드 상태, 활동 피드 제자리 갱신, 참여자 상태·**목록**(초대·퇴장), **미션 칩 줄**(열림·닫힘·상태), 미션 종료 조건 진행률, 미션 비용·방 누적 비용, **방 멈춤 배너**, 시스템 메시지 5종 |
| S22 미션 패널 | S7 과 같다(같은 화면의 열 — 별도 구독을 열지 않는다) |
| S8 받은 요청 | 항목 추가·해소, 뱃지, deputy 노출 시점 도달 |
| S19 참여자 | 참여자 목록·상태 |
| S20 방 설정 | `runtime_id` 고정 순간(편집 가능 → 읽기 전용), 한도 잔여 |
| S23 맥락 읽기 기록 | 새 읽기·읽힘·거부 행 |
| S24 참고 방 링크 | 연결 목록(반대쪽 방장이 풀 수 있다) |
| S26 미션 제안 | 제안 도착, 다른 사람이 먼저 처리 |
| S11 연결된 컴퓨터 | 온라인 상태, 지금 하는 일 수, CLI 감지 |
| S13 작업 폴더 | 용량·상태(GC 결과 반영) |
| S12 컴퓨터 연결 | 연결 단계 |
| S15 활동 로그 | **없음** — 감사 화면은 흐르면 읽을 수 없다 |

**새 SSE 이벤트 13종 — R0 계약의 입력**(SCR-B §2.3). 현재 `STREAM_EVENT_TYPES` 는 26종이고 openapi `StreamEvent.type` enum 과 전수 일치한다. **목록에 없는 타입은 클라이언트가 조용히 버린다** — `openStream()` 이 목록에 있는 이름만 `addEventListener` 하고 `onmessage` 는 `event:` 필드 없는 프레임만 받으므로, 서버가 `event: room.updated` 를 보내면 **오류도 로그도 없이 사라진다**(v1.1 S5 카드 삭제에서 실제로 겪은 일, PR #219). **두 곳이 같은 PR 에 올라야 한다.**

| 이벤트 | payload | 무엇을 살린다 |
|---|---|---|
| `room.updated` | `Room` 부분(`blocked_reason`·`blocked_detail`·`status`·`last_activity_at`·`name`·`description`) | 방 멈춤 배너, S5 카드 |
| `room.deleted` | `{room_id}` (`session.deleted` 별칭) | S5 목록에서 제거 |
| `room.unread` | `{room_id, unread_count, last_read_message_id}` | 안 읽음 배지. **내가 다른 탭·기기에서 읽었을 때도 내려와야** 두 곳의 배지가 갈리지 않는다 |
| `work.created` · `work.updated` · `work.closed` | `WorkListItem` 부분 · `{work_id, status, summary_message_id?}` | 미션 칩 줄, 우열 미션 칸 |
| `work.completion_progress` | `{work_id, completion_progress}` (`session.completion_progress` 별칭) | 종료 조건 진행률 |
| `participant.joined` · `participant.left` | `{room_id, participant}` · `{room_id, participant_id, kind, left_at}` | 좌열 참여자 **목록**(기존 `participant.updated` 는 상태만 나른다) |
| `room_read.recorded` | `{room_id, direction, entry}` | S23, 읽힌 쪽 시스템 메시지 |
| `room_link.updated` | `{room_id, action, target_room{id,name}}` | S24(반대쪽 방장이 풀 수 있다) |
| `work_proposal.created` · `work_proposal.resolved` | `{room_id, proposal}` · `{room_id, proposal_id, action, work_id?}` | S26, S8 뱃지 |

**확장되는 기존 이벤트**: `lane.updated`(payload 에 `work_id`) · `cost.updated`(`{room_id, room_cost_usd, work_id?, work_cost_usd, estimated}` — **두 수로 갈린다**) · `hitl.created`·`hitl.updated`(`approver_spec: room_owner`) · `inbox.item_created`(`work_id`·`lane_id`).

**구독 범위도 바뀐다.** `streamEvents` 는 `?session_id=` 로 좁힌다 — **`?room_id=` 로 바꾸고 `work_id` 는 넣지 않는다.** 한 방의 미션 3개를 각각 구독하면 방 배너·참여자·안 읽음이 어느 구독에도 안 걸린다. 미션 단위 거르기는 클라이언트가 `payload.work_id` 로 한다.

**`activity.appended` 는 넣지 않는다** — S15 는 실시간이 없다(§4.18: 감사 화면은 흐르면 읽을 수 없다). 넣지 않기로 한 결정을 여기에 기록해 둔다.

**연결이 끊기면** 상단에 「실시간 연결 끊김 · 재연결 중」을 띄우고, 복구되면 놓친 이벤트를 채운다. 끊긴 채로 조용히 낡은 화면을 보여주지 않는다(§1 원칙 4).

---

## 7. 빈 상태와 오류

| 상황 | 화면 | 표시 |
|---|---|---|
| 방 0개 | S5 | 「첫 방을 만들어 보세요」 + 예시 두 줄. **컴퓨터가 없어도 막지 않는다**(§2.1) |
| **방금 만든 빈 방** | S7 | 「이제 무엇을 하나요?」 + 초대·말 걸기·미션 열기 세 갈래 + FR-2.1.1 확정 안내 |
| 참여자 = 나 혼자 | S7 좌열 | 「아직 아무도 없습니다」 + 「참여자 초대」 |
| 메시지 0 | S7 가운데 | 「@로 에이전트를 불러 시작하세요」 + 멘션 칩 |
| **미션 0개** | S7 상단·우열 | 칩 줄 없음 + 「아직 연 미션이 없습니다 — 끝을 정해 추적하고 싶은 일이 생기면 미션을 엽니다」 + **「+ 새 미션」 버튼** |
| 서브 미션 0개 | S7 좌열 | 미션 있으면 「assignee 가 계획을 세우는 중입니다」, 없으면 「에이전트를 부르면 여기에 나타납니다」 |
| 아티팩트·결정 기록 0 | S7 우열 | 「아직 제출된 아티팩트가 없습니다」 / 「아직 기록된 결정이 없습니다」 |
| **맥락 오간 기록 0** | S7 우열·S23 | 「이 방의 맥락이 오간 적이 없습니다」 |
| **참고 방 링크 0** | S24 | 「연결된 참고 방이 없습니다. 이 방의 에이전트는 자기가 참여한 방만 읽을 수 있습니다」 |
| 에이전트 0개 | S19 에이전트 탭, S9 | 템플릿 3종 카드 + S9 링크 |
| 컴퓨터 0개 | S7 배너, S20 | 「연결된 컴퓨터가 없어 에이전트가 아직 일을 시작할 수 없습니다」 + S12 |
| 받은 요청 비어 있음 | S8 | 「지금 답할 것이 없습니다」 |
| 활동 피드 없음 | S7 | 런타임이 구조화 이벤트 미지원이면 그 사실을, 아니면 「대기 중」 |
| 컴퓨터 오프라인 | S7·S11 | 경과 시간 + 유예 잔여 + **묶인 방 N개** + 컴퓨터 바꾸기 |
| 검색 결과 0 | S25 | 「〈말〉에 걸리는 방이 없습니다」 + 「보관 포함해서 다시 찾기」 |
| 권한 없음 | 전역 | 비활성 + **층이 적힌** 사유를 **버튼 아래 글자로**(툴팁 아님, §1 원칙 5). 화면 자체를 숨기지 않음 |
| `invited` 방을 못 볼 때 | S5·S25·S7 | **존재를 숨긴다** — 404 가 아니라 목록에 없다. 직접 URL 접근 시 「이 방을 볼 수 없습니다」 |

**키보드로 방에 들어오면 작성창이 첫 목적지다**(SCR-C N). 방 화면은 첫 탭에서 「**작성창으로 건너뛰기**」를 내놓는다 — DOM 순서가 좌열(서브 미션 8개 × 버튼 2 + 참여자)을 먼저 놓으므로, 없으면 말 한마디 하려고 탭을 25번 눌러야 한다. 좁은 화면(≤1100px)은 열 탭으로 갈려 이 문제가 없다 — **넓은 화면이 더 나쁘다.**

**숫자만 있는 요소에는 라벨을 단다**(SCR-C §5.5). 안 읽음 배지 `aria-label="안 읽은 메시지 3개"`(내비 뱃지가 이미 쓰는 규칙) · 주의 배지 세 숫자 각각 · 참여자 아바타 묶음 `aria-label="참여자 7명"` · 상단 세 층 요약의 세 수. 가운뎃점으로 이은 숫자 나열은 스크린리더에서 한 덩어리로 읽힌다.

---

## 8. PRD v0.19 대조

### 8.1 조항 → 화면 대조표

| PRD 조항 | 화면 | 확인 |
|---|---|---|
| FR-1.1~1.6 에이전트 등록·프로파일 | S10 | ✅ |
| FR-1.3 에이전트 상태 6종 | S7 좌열, S9 | ✅ **단 방을 구분하지 못한다** — §4.6「칩이 말하지 않는 것」 |
| FR-1.8.1 테스트 채팅 | S10 | ✅ 방이 아님을 문구로 |
| FR-1.9 `respond_to`·킬 스위치 | S10, S19 | ✅ 초대 차단 사유 표시 |
| FR-1.9.1 역할별 허용 명령 | **S10 역할 카드** | ✅ **v1.1 에서 이미 구현**(PR #246·#247) — 화면 목록에 자리를 준다(§4.15) |
| **FR-2.1 방 만들기(칸 하나)** | **S18** | ✅ |
| **FR-2.1.1 확정되는 것·안 되는 것** | **S18 한 줄 + S7 시스템 메시지 1 + S8 `isolation_confirm` 카드** | ✅ 세 자리 전부 — 셋째의 명세가 §4.14 에 생겼다(v0.19 초판에는 ✅ 주장만 있었다) |
| **FR-2.2 초대·퇴장(사람·에이전트)** | **S19** | ✅ 한 목록·한 다이얼로그 |
| **FR-2.3 방 설정** | **S20** | ✅ 7묶음 |
| **FR-2.4 방 상태·보관** | S5 카드 메뉴, S20 | ✅ 보관은 되돌릴 수 있음을 문구로 |
| **FR-2.4 [V19-B] `room.blocked_reason`**(+ `manual`) | S5 배지, S7 전폭 배너 + **「이 방 멈춤」 버튼**, S8 `room_paused` | ✅ 스키마 칸 포함(#271·#273) |
| **FR-2.5 방 요약(「여기까지 정리」)** | S7 `⋯` → **범위 선택 다이얼로그** | ✅ 범위 고르기 + 범위 기록 (§4.6 에 명세) |
| **FR-2.6 방 삭제** | S5 「…」, S20 | ✅ 진행 중 미션 있으면 거부 |
| **FR-2A.1 미션 시작(세 길)** | **S21**(둘) + **S26**(제안) | ✅ assignee 없으면 `user_approval` 단독 |
| FR-2A.2 종료 조건 | S21, S22 「조건 고치기」 | ✅ 리뷰어 필수·`agent_approval` 경고 |
| **FR-2A.3 방장 부재 위임** | §2.3, S7 배너, S8 카드 | ✅ 기한 절반 후 부방장 → 없으면 owner 최고참 |
| **FR-2A.3 한도와 `paused`(방∩미션)** | S7 전폭 배너(방) + 우열 배너(미션), 서브 미션 카드의 층 표기 | ✅ 몇 개가 멈췄는지 센다 |
| FR-2A.4 미션 상태·요약 | S22, `summary` 카드 | ✅ 「미션 완료 요약」 |
| **FR-2A.5 여러 미션 동시** | S7 상단 칩 + 우열 두 구역 | ✅ 칩 ↔ 우열 연동 |
| FR-2A.5 [V19-B] 아티팩트 이름은 방 단위 | S7 우열 아티팩트 | ✅ 버전 표시가 방 단위임을 말한다 |
| FR-2A.6 미션 삭제·취소 | S22 | ✅ 메시지는 방에 남는다 |
| **FR-3.1.1 메시지의 미션 귀속** | **작성창 미리보기 칩 + 미션 선택기** | ✅ 4규칙 전부 화면에 |
| FR-3.2 멘션 문법 | 멘션 입력기 | ✅ |
| FR-3.3 라우팅 8규칙 + lane 해소 | 트리거 미리보기 | ✅ 결과만 노출 |
| FR-3.4 취소는 UI 전용 | 서브 미션 카드 2동작 | ✅ |
| FR-3.5 루프 상한 | S14, S7 방 배너(`loop`) | ✅ |
| FR-3.6 트리거 억제 | 미리보기 칩 × | ✅ |
| **FR-4.1 방 히스토리(세 묶음)** | — | **화면 밖**(브리프). 잘림 한 줄도 브리프의 말이다 — 사람 쪽 타임라인은 「이전 대화 더 보기」(§4.6) |
| FR-4.2 결정 기록 | S7 우열 방 전체 칸 | ✅ 「미션 없음」 묶음 포함 |
| FR-4.3 아티팩트 | S7 우열 방 전체 칸 | ✅ |
| **FR-4.4 컨텍스트 재사용** | — | ✅ **v1 범위에서 버렸다**(§12.1-7 확정). 화면을 두지 않고, S14 「컨텍스트 재사용 상한」 항목도 뺀다(§4.17) |
| **FR-4.5 다른 방 읽기** | **S24**(링크) + **S23**(흔적) + S7 시스템 메시지 2·3 + S15 | ✅ 읽은·읽힌·거부 셋 다 |
| FR-5.1~5.4 HITL | S8, S7 HITL 카드 | ✅ 수신자 근거 표시 |
| **FR-5.3 사람 역할(방장 추가)** | §2.2·§2.3 네 층 | ✅ 스키마 칸 포함(#271: `room.owner_user_id`·`deputy_owner_user_id`·`visibility`, `room_participant.role`) |
| FR-5.3 `visibility` | §2.4, S20 | ✅ |
| FR-6.1 lane/workdir 분리 | S7 좌열, S13 | ✅ 작업 폴더는 방×에이전트 |
| FR-6.2 서브 미션 상태 7종 | S7 좌열 보드 | ✅ |
| FR-6.2.1 질문 경로 | `blocked_q` 카드, S8 `lane_blocked` | ✅ |
| **FR-6.3 동시성(에이전트 전역)** | S9 「동시 3개 중 2개」, 서브 미션 `queued` 사유 | ✅ 사유 칸은 `task.queued_reason`(PRD §3.1 확정) — **단 §7 `task` 행에 그 열이 안 적혀 있다**, §8.3 D-13 |
| **FR-6.4 작업 폴더 GC** | S13 「마지막 사용 + 14일」 | ✅ 기준 사건이 `last_used_at` |
| FR-7.2 활동 피드·빈 턴 | S7 가운데 | ✅ 빈 턴 카드는 이력 「활동」 토글 안에만(v1.1 Lead A) |
| §11 관찰 표 7행 | **S14 대시보드 아래** | ✅ **v1.1 에서 이미 구현**(PR #246·#247) — 화면 목록에 자리를 준다(§4.17) |
| FR-7.3 비용·예산 | 비용 컴포넌트(두 칸) | ✅ 미션/방 구분 |
| **FR-8 받은 요청** | S8 **11종**(`isolation_confirm` 추가) | ✅ `inbox_item.work_id`·`lane_id` 포함(#271·#274) |
| FR-9 컴퓨터·probe | S11 | ✅ |
| **FR-9.2 재바인딩** | **S17**(`?room=`) | ✅ **단 diff 적용 순서 미정** — §8.2 G-4 |
| **FR-4.5 저장 자리** | S23 · S15 | ✅ `room_read_log` 표(#274) + R0 `listActivityLog`(#274) |
| **FR-2A.1 제안 저장 자리** | S26 · S8 `work_proposed` | ✅ `work_proposal` 표(#274) |
| §3.2 화면 용어표 | §3.4 초안 | ✅ R1.5 에서 자물쇠로. **`artifact` = 「아티팩트」**(v0.19.1 에서 정본에 맞춤) |
| §10 R2 「안 읽음이 방 20개에서 동작」 | S5·S25 | ✅ `room_participant.last_read_message_id`(방 단위, 사람 행만 — §12.1-6 확정). 구현 단계는 R2 (M4) |
| §11 성공 지표 10개 | S14 대시보드 | ✅ 분모 확정(§12.1-10) — 1·2·5 미션 / 10 방 / 나머지 단위 없음. 행마다 라벨을 적는다 |
| §11 지표 1(첫 미션 완료 15분) | **F1 대본** | ✅ 대본이 미션 열기·승인까지 포함한다(§3.3, SCR-A P-9) |
| §12.1-11 정렬·검색 | S25 | ✅ 마지막 활동순 고정 · 이름·설명 검색 |
| §12.1-7 컨텍스트 재사용 폐기 | S14 · S21 | ✅ 화면 없음 |
| §12.1-4 방장 승계 | §2.3, 시스템 메시지 4 | ✅ |
| §12.1-5 에이전트 전역 상한 | S9, 서브 미션 대기 사유 | ✅ 표시 칸은 `task.queued_reason`(§8.3 D-13) |
| §12.1-9 `room.blocked_reason` | S5·S7·S8 | ✅ 스키마 포함 |

### 8.2 화면이 못 받는 조항 — **공백**

**억지로 만들지 않았다.** v0.19 초판은 아홉 자리를 공백으로 남겼다. PRD #271~#274 가 결정을 닫으면서 **여섯이 해소됐고**(아래 둘째 표), 검증 3건이 새로 짚은 두 자리(G-10 방장 부재 · G-11 방 긴급 정지)도 PRD 가 같은 라운드에서 닫았다. **남은 공백은 셋이다.**

| # | 공백 | 무엇이 없어서 | 화면이 지금 할 수 있는 것 |
|---|---|---|---|
| **G-3** | **방 위의 계층** — 방이 20개를 넘을 때 | v0.19 는 방을 **세션 자리에** 놓았고 그 위는 비어 있다. 옛 S16(채널)이 폐기되면서 «워크스페이스와 세션 사이» 자리도 함께 비었다 | S25 검색(이름·설명)·마지막 활동순 고정·안 읽음 배지로 버틴다. **폴더·태그를 발명하지 않는다** — v1.1/v2 기능 발명 금지 |
| **G-4** | **재바인딩 뒤 diff 적용 순서** — S17 유실 경고 | 방 하나에 미션 N개의 diff 가 섞인다. 순서 규칙이 PRD 에 없다(옛 D-8 의 나머지 절반) | 「아티팩트(diff) N개를 적용합니다」까지만 적고 **순서는 말하지 않는다**. 순서를 지어내면 복구가 틀린다 |
| **G-7** | **나간 사람 때문에 읽기가 막힐 때의 사람 쪽 경고** — S19 내보내기 | FR-4.5 [V19-C] 는 읽는 시점에 재검사하고 에이전트에게 사유를 구분해 알린다. **사람 쪽 화면 규칙은 없다** | 내보내기 확인 문구에 한 줄 경고까지는 적을 수 있다. **어느 할 일이 막힐지는 미리 셀 수 없다** — originator 가 살아 있는 task 를 가로질러 세는 질의가 없다 |

**닫힌 공백 — 무엇으로 닫혔나**

| # | 옛 공백 | 닫은 결정 | 화면이 지금 하는 일 |
|---|---|---|---|
| ~~G-1~~ | 안 읽음 모델 | §12.1-6 확정 — **방 단위** `room_participant.last_read_message_id`(사람 행만), §7 반영(#271) | S5 배지·S25 토글·내비 뱃지를 이 칸 위에 그린다(§4.3·§4.4). 미션·서브 미션 단위는 **구독으로만** |
| ~~G-2~~ | 받은 요청·알림의 미션 단위 | `inbox_item.work_id`·`lane_id`(#271·#274) | S8 맥락 한 줄·방별·미션별 필터·미션 단위 구독(§4.14·§4.17) |
| ~~G-5~~ | `queued` 사유 표시 | PRD §3.1 확정 — `task.queued_reason`(`room_lanes`·`agent_global`·`runtime`·`workspace`) | 서브 미션 카드가 그 값을 사람 말로 옮긴다(§4.6). **단 §7 `task` 행에 열이 안 적혀 있다** — §8.3 D-13 |
| ~~G-6~~ | §11 지표 10개의 분모 | §12.1-10 확정 — 1·2·5 미션 / 10 방 / 나머지 단위 없음 | S14 대시보드 10행에 분모 라벨을 **지금** 적는다(§4.17) |
| ~~G-8~~ | 컨텍스트 첨부(FR-4.4) | §12.1-7 확정 — **버린다** | **화면을 두지 않는다.** 「유지로 결정되면…」이 아니라 «v1 범위 밖»이다. 옛 대화를 참고하는 길은 FR-4.5 |
| ~~G-9~~ | 타임라인 잘림·페이지네이션 | 이 문서가 화면 규칙으로 정했다(§4.6 「타임라인 페이지네이션」) | 최근 50건 + 「이전 대화 더 보기」, `?around_message_id=` 앵커. 브리프의 잘림 문구(FR-4.1)와는 다른 말을 쓴다 |
| ~~G-10~~ | 방장 응답 없음(SCR-A P-7) | PRD FR-2A.3 확정 — 기한 절반 후 **부방장**, 없으면 **owner 최고참** | §2.3 권한 표·방 배너·S8 카드가 위임 시점을 미리 적는다 |
| ~~G-11~~ | 방 전체 긴급 정지(SCR-A P-7) | PRD FR-2.4 [V19-B] 확정 — `blocked_reason: manual` | S7 상단 「이 방 멈춤」·해제(§4.6). v1 「세션 취소」가 간 자리 |
| ~~(SCR-B)~~ | 미션 제안·다른 방 읽기의 저장 자리, 활동 로그 조회 op | PRD §7 `work_proposal`·`room_read_log` 표 + §10 R0 `listActivityLog`(#274) | S26·S23·S15 가 저장·조회할 곳을 갖는다(§4.9·§4.13·§4.18) |

### 8.3 PRD 고칠 것 — Lead 판단 요청

**이 문서는 PRD 를 고치지 않았다.** v0.19 초판이 올린 열두 자리(D-1~D-12)는 **PRD #271~#274 에서 전부 닫혔다.** 아래 첫 표가 그 확인이고, 둘째 표가 이 판에서 **새로 걸린 것**이다.

**(가) 닫힌 것 — 반영 확인**

| # | 자리 | 어떻게 닫혔나 |
|---|---|---|
| ~~D-1~~ | `room.visibility` 칸 없음 | §7 `room` 에 `visibility: workspace\|invited` 추가(#271) |
| ~~D-2~~ | `room.blocked_reason` 칸 없음 | §7 에 `budget\|runtime_offline\|loop\|manual\|null` 추가(#271·#273) |
| ~~D-3~~ | 부방장 칸 없음 | §7 `room.deputy_owner_user_id?`(#271) |
| ~~D-4~~ | `approver_spec` 에 방장 값 없음 | §7 필드 줄이 `director\|room_owner\|any_member\|user_id` 로(#273) |
| ~~D-5~~ | `room_participant.role` 이 `member` 하나 | `owner\|deputy\|member`(#271) |
| ~~D-6~~ | 안 읽음 칸 · `inbox_item.work_id` | `last_read_message_id?` · `work_id?`·`lane_id?`(#271·#274) |
| ~~D-7~~ | §6 UX 가 §3.2 용어를 안 씀 | §6 표가 「미션」·「서브 미션」으로(#269) |
| ~~D-8 (절반)~~ | §6 에 방 만들기·미션 제안·맥락 읽기 기록이 없음 | Rooms 행의 「새 방은 이름 한 칸」, 미션 행의 「미션 제안 확인」, Room 상세 행의 「맥락 읽기 기록」(#269·#274). **나머지 절반(diff 순서)은 §8.2 G-4** |
| ~~D-9~~ | §11 분모 미정 | §12.1-10 확정 + §11 본문 반영(#271·#272) |
| ~~D-10~~ | 보관 해제 권한 무기재 | FR-2.4 [V19-C] 에 「해제 권한은 보관과 같다」(#271) |
| ~~D-11~~ | 「산출물」 vs 「아티팩트」 | **PRD 가 「아티팩트」로 닫았다**(§3.2 Director 확정). **고칠 쪽은 PRD 가 아니라 이 문서였다** — v0.19.1 에서 본문 「산출물」을 전부 「아티팩트」로 되돌렸다(§3.4) |
| ~~D-12~~ | 사람 퇴장 규칙 없음 | FR-2.2 에 「열린 미션의 Director 인 사람은 먼저 Director 를 넘겨야 나갈 수 있다」(#271) |
| ~~(SCR-B P3)~~ | §6 「별도 라우트는 두지 않는다」 vs S22 | §6 미션 행이 **`/rooms/:id?work=:workId` 를 허용**으로(#274) |
| ~~(SCR-B P6·P7·C6)~~ | `work_proposal`·`room_read_log` 표, 활동 로그 op | §7 두 표 + §10 R0 `listActivityLog`(#274) |

**(나) 새로 걸린 것** — 전부 **원문 확인**이며 추정이 아니다.

| # | 자리 | 무엇이 어긋나나 | 화면에 미치는 영향 |
|---|---|---|---|
| **D-13** | §3.1 표 vs §7 `task` 행 | §3.1 이 **`task.queued_reason`**(`room_lanes`·`agent_global`·`runtime`·`workspace`)을 확정했는데 **§7 `task` 행의 열 목록에 그 칸이 없다**(`status`·`failure_kind` 까지만) | 서브 미션 `queued` 카드의 대기 사유(§4.6)가 R1 스키마에서 조용히 빠질 수 있다 — 옛 G-5 가 그대로 돌아온다 |
| **D-14** | §6 UX 표 Settings 행 | 「**컨텍스트 재사용 상한**」이 남아 있다. FR-4.4 는 §12.1-7 확정으로 **버린다**이고 그 항목을 R0 정리 대상으로 지목했다 | S14 탭 표(§4.17)에서 뺐는데 PRD §6 이 아직 요구한다 — 대조표가 서로 반대를 가리킨다 |
| **D-15** | §11 지표 5 | 분모는 **미션**으로 확정됐는데(§12.1-10) 문장은 「병렬 lane 사용 **세션**의 wall-clock 단축」 그대로다 | S14 대시보드 5행의 라벨(「미션 기준」)과 지표 이름이 다른 단위를 말한다 |
| **D-16** | §7 `hitl_request` 주석 vs 필드 | 필드 줄은 `user_id`, 바로 위 주석은 `user:<id>` 다(둘 다 `room_owner` 는 들어갔다) | R0 계약이 마지막 값의 표기를 어느 쪽으로 읽을지 모른다. 화면 영향은 없고 **계약 표기만 통일하면 된다** |
| **D-17** | §10 v1.1 목록 | 「**세션 긴급 정지** — 세션의 모든 lane 을 한 번에 취소」가 v1.1 에 남아 있는데, FR-2.4 [V19-B] 가 같은 일을 **`blocked_reason: manual` 로 v2.0 에 넣었다** | 「이 방 멈춤」(§4.6)이 v1.1 항목과 겹친다 — 어느 쪽이 정본인지 R0 전에 정해야 한다 |

### 8.4 자기 점검 — v0.19 와 모순 0

| 점검 | 근거 | 결과 |
|---|---|---|
| 방에 goal·진행률·완료가 없는가 | FR-2.4 | ✅ S5 카드에서 뺐고(§4.3), 우열에서 미션 칸으로 내렸다(§4.6) |
| 마법사가 완전히 사라졌는가 | FR-2.1, §10 R2 | ✅ S6 삭제. 7단계는 §3.1 표대로 S18·S19·S20·S21 넷에 나뉘었다 |
| 마법사가 하던 **사고 방지 장치** 넷이 살아 있는가 | V19-A §4 W-1~W-6 | ✅ 격리 설명(S18 한 줄·S20 바인딩 그림·**S8 `isolation_confirm` 카드 §4.14**) · 컴퓨터 고정 고지(S18 + 시스템 메시지 1) · 종료 조건 사람 말·리뷰어 필수(S21) · `autonomy` 문장(S20·S21) · `respond_to`/프로파일 경고(S19) · deputy(S21) |
| **세 알림 중 「묻는」 것이 명세돼 있는가** | SCR-A §2-B ③ | ✅ §4.14 `isolation_confirm` — 문구·버튼·기본 제안 `worktree` 까지. **저장소 없는 컴퓨터에서는 뜨지 않는다는 한계도 §4.5 에 적었다** |
| **`(전체)` 에서 미션이 끝날 수 있는가** | SCR-C §1.4(a) | ✅ 미션 동작 묶음이 **비활성 + 사유**(§4.6) — 사람이 고르지 않은 미션은 끝나지 않는다 |
| **비활성 사유가 키보드·스크린리더에 닿는가** | SCR-C §5.2 | ✅ §1 원칙 5 가 「툴팁」에서 **`DisabledHint`(버튼 아래 글자 + `aria-describedby`)** 로 바뀌었다 |
| **미션이 여럿 멈췄을 때 규모를 세는가** | SCR-A P-2 | ✅ 칩 줄의 「일시정지 N ▾」(§4.6) — 방 배너의 「몇 개가 멈췄는지 센다」가 미션 층에도 적용된다 |
| **끝난 미션으로 돌아가는 문이 있는가** | SCR-A P-4 | ✅ 열린 미션이 0 이어도 「지난 미션 N개 ▾」 칩 줄을 그린다(§4.6) |
| **사람이 방을 통째로 멈출 수 있는가** | SCR-A 막힘 9 · G-11 | ✅ S7 상단 「이 방 멈춤」(`blocked_reason: manual`, §4.6) |
| **방장이 자리를 비우면 방이 영구 정지하는가** | SCR-A 막힘 8 · G-10 | ✅ 기한 절반 후 부방장 → 없으면 owner 최고참(§2.3·§4.6·§4.14) |
| **우열·보드가 268px·900px 에 드는가** | SCR-C §4.4·§4.5 | ✅ 방 전체 칸 기본 접힘 + 보드 `done`·`failed` 접힘 + 아티팩트·결정 묶음당 5건 |
| 「이걸 미션으로」가 assignee 를 묻는가 | FR-2A.1, S-84 | ✅ §4.7 — 안 고르면 `user_approval` 단독이고 그 이유를 적는다 |
| 메시지의 미션 귀속을 **보내기 전에** 보는가 | FR-3.1.1 | ✅ §4.6 작성창 — 4규칙 전부 칩에 |
| 미션 밖 결과에 자리가 있는가 | FR-2A.5 | ✅ 우열 방 전체 칸의 「미션 없음」 묶음 + `(미션 없음)` 칩 |
| 미션이 2개 이상일 때 우열이 거짓말하지 않는가 | V19-A §3 4a | ✅ 칩 ↔ 우열 연동. `(전체)` 에서는 「최근 활동한 미션」임을 적는다 |
| 방 상한과 미션 상한이 구분되는가 | FR-2A.3 | ✅ 전폭 배너 / 우열 배너, 서브 미션 카드의 층 표기, 「몇 개가 멈췄는지」 |
| 다른 방 읽기의 **세 기록**이 다 있는가 | FR-4.5 | ✅ 읽은 쪽(S23) · 읽힌 쪽(시스템 메시지 2 + S23 + S15) · 거부(S23, 방 이름 없이) |
| 참고 방 링크를 걸 자리가 있는가 | FR-4.5 | ✅ S24 |
| 방장이 받는 HITL 이 화면에 있는가 | FR-2A.1 | ✅ S8 수신자 근거 + §2.3. 스키마 `approver_spec: room_owner` 도 들어갔다(#273) |
| 용어가 §3.2 표와 같은가 | §3.2 | ✅ §3.4 — 「작업 줄기」 전부 「서브 미션」, 「세션」은 문맥에 따라 방/미션, **`artifact` 는 「아티팩트」**(v0.19.1 정정) |
| **「산출물」이 화면 문자열로 남아 있지 않은가** | PRD §3.2 · SCR-C O | ✅ **0건**. 남은 7건은 전부 «쓰지 말라»는 규칙 문장·자물쇠 목록·변경 이력이다(§3.4 · §8.3 D-11 · §8.6) |
| **「안 읽음 우선」 정렬이 선택지로 남아 있지 않은가** | §12.1-11 · SCR-C R | ✅ **0건**. 남은 3건은 «두지 않는다»는 규칙 문장과 이력(§4.4 · §8.6) |
| **비활성 사유를 「툴팁」으로 말하는 자리가 남아 있지 않은가** | SCR-C M | ✅ **0건**. 남은 6건은 전부 «툴팁으로 쓰지 않는다»는 규칙 문장(§1 · §4.6 · §5 · §7)과 이력 |
| **U12 가 못박은 「일시정지」를 근거 없이 바꾸지 않았는가** | SCR-C §3.2 | ✅ 미션·서브 미션·할 일은 「일시정지」 유지, 방만 「멈춤」 — 분담을 §3.4(b) 에 적었다 |
| **기존 S 번호를 재사용하지 않았는가** | 이 과제 금지 항목 | ✅ 신규는 S18~S26. S6·S16 은 비워 둔다 |
| **v1.1/v2 기능을 발명하지 않았는가** | 〃 | ✅ **G-3(방 위 계층)·G-4(diff 순서)·G-7(퇴장 뒤 읽기 경고)을 공백으로 남겼다.** 폴더·태그·워크플로·전문 검색 색인 같은 것을 만들지 않았고, 새로 적은 것은 전부 PRD 확정(방 긴급 정지·방장 부재 위임)이나 기존 규칙의 연장(우열 접힘·보드 접힘·페이지네이션)이다 |
| **코드·계약·PRD 를 고치지 않았는가** | 〃 | ✅ diff 는 `SCREEN.md`·`COMPONENTS.md` 둘 |

**남은 모순 0.** §8.2 의 세 자리는 «모순»이 아니라 **공백**이고, §8.3(나) 의 다섯 자리는 **PRD 쪽**이다.

### 8.5 v1 컷 순서가 발동하면

| 컷 | 화면 영향 |
|---|---|
| 1. 활동 피드 축소 | S7 가운데 펼침이 메시지·플랫폼 조작·오류 + 원본 레일만 |
| 2. 자동 종료 제거 | S22 종료 조건 진행률이 체크리스트(정보 표시)로만 남고, 종료는 미션 칸의 `manual` 버튼 |
| (컷과 무관) | **「이 방 멈춤」은 컷하지 않는다** — 사람이 폭주를 멈출 마지막 수단이고 버튼 하나 + 한 줄 게이트다 |
| 3. `worktree` 제거 | S20 격리 칸이 `none` 고정. **S13·S17 이 통째로 빠진다.** 단 **S18 의 확정 안내(FR-2.1.1)는 남는다** — `none` 에서도 컴퓨터는 첫 실행에 고정되고 되돌릴 수 없다 |

### 8.6 변경 이력 (v0.3 → v0.19)

| 출처 | 변경 | 위치 |
|---|---|---|
| PRD FR-2.1, §10 R2 | **S6 새 세션 마법사 삭제.** 7단계를 S18·S19·S20·S21 넷에 나눔 + 사고 방지 장치 6종을 명시적으로 옮김 | §3.1, §3.2, §4.5·§4.7·§4.10·§4.11 |
| PRD §3.1, FR-2 | **S5 → 방 목록**(상태 배지·goal·진행률·예산% 제거, 안 읽음·진행 중 미션·멈춤 배지 추가) | §4.3 |
| PRD FR-2A.5, V19-A P1-8 | **S7 → 방 화면**(우열을 미션 칸 / 방 전체 칸으로 분할, 상단 미션 칩, 상단 액션을 방/미션으로 분리) | §4.6 |
| PRD §10 v1.1 → FR-4.5 | **S16 채널 폐기** — 방이 그 자리다. 방 **위** 계층은 공백으로 남김 | §3.2, §8.2 G-3 |
| PRD FR-4.5 + §7 닫힌 스키마 | **S15 활동 로그 v1.1 → v1 승격** — 읽힌 쪽 기록이 갈 곳이 여기뿐 | §4.18 |
| PRD FR-2.1 | **S18 방 만들기** 신규 | §4.5 |
| PRD FR-2.2 | **S19 참여자 초대·퇴장** 신규 — 사람·에이전트 한 다이얼로그 | §4.10 |
| PRD FR-2.3 | **S20 방 설정** 신규 | §4.11 |
| PRD FR-2A.1 | **S21 미션 열기** 신규 · **S26 미션 제안 확인** 신규 | §4.7·§4.9 |
| PRD FR-2A.5 | **S22 미션 패널** 신규 | §4.8 |
| PRD FR-4.5 | **S23 맥락 읽기 기록** · **S24 참고 방 링크** 신규 | §4.13·§4.12 |
| PRD §12.1-11, V19-A §3-3 | **S25 방 찾기** 신규 | §4.4 |
| PRD FR-5.3, §12.1-4 | **권한 3층 → 4층**(방 층 추가), `visibility` 절 신규 | §2.2·§2.3·§2.4 |
| PRD §3.2(Director 확정) | **용어표 초안** — 「작업 줄기」→「서브 미션」, 「세션」→방/미션 (초판의 「아티팩트」 치환 시도는 v0.19.1 에서 철회) | §3.4 |
| PRD 시나리오 E | **F5 흐름** 신규(클릭 15, 화면 이동 3) | §3.3 |
| v0.3 m5 철회 | 컴퓨터 없이도 방을 만들 수 있다 | §2.1 |
| v0.3 C1 철회 | `participant` 는 사람·에이전트 둘 다 | §2.2 |

**v0.19 → v0.19.1 (검증 3건 반영)**

| 출처 | 변경 | 위치 |
|---|---|---|
| PRD §3.2 · SCR-C O · SCR-B C2 | **「산출물」 → 「아티팩트」** 전수 되돌림 + §3.4(b) 치환 행 삭제 | §3.4, §4.3·§4.6·§4.7·§4.16, §5, §7 |
| §12.1-11 · SCR-C R · SCR-A P-8 | **S25 정렬 고정·검색 범위 축소**(「안 읽음 우선」·본문 검색 삭제) | §3.2, §4.4 |
| SCR-C M·§5.2 | **§1 원칙 5 「툴팁」 → `DisabledHint`** | §1, §5, §7 |
| SCR-C C·§1.4(a) | **`(전체)`·`(미션 없음)` 에서 미션 동작 비활성** | §4.6 |
| SCR-C P · SCR-A | **「멈춤」/「일시정지」 분담**(방=멈춤, 미션·서브 미션=일시정지), 「오프라인」→「연결이 끊겼습니다」 | §3.4(b), §4.6, §5 |
| SCR-A P-1 | **`isolation_confirm` 카드 명세** | §4.14, §4.5, §5 |
| PRD FR-2A.3 · SCR-A P-6·G-10 | **방장 부재 위임 경로**(기한 절반 후 부방장 → owner 최고참) | §2.3, §4.6, §4.14 |
| PRD FR-2.4 [V19-B] · SCR-A G-11 | **「이 방 멈춤」**(`blocked_reason: manual`) | §2.3, §4.6, §4.14(배너) |
| PRD #274 §6 | S22 `?work=:workId` 허용 확인 | §4.8 |
| PRD #271~#274 | **공백 6·PRD 지적 12건 해소**, 새 D-13~D-17 | §8.2, §8.3 |
| PRD §11 · SCR-A P-9 | **F1 대본이 미션 열기·승인까지** | §3.3 |
| SCR-A P-2·P-3·P-4·P-5·P-10·P-11 | 미션 층 규모 세기 · 첫 지시 placeholder · 지난 미션 문 · `(전체)` 선택기 기본값 · `room_paused` 잔여 예산 · 묶음 하나면 머리글 없음 | §4.6, §4.14 |
| SCR-C A·B·D·E·G·H·I·J·K·L·N·Q·S·T | 「나에게 필요한 것 N」 · 가운데 기본 · §3.2 「종류」 열 · 칩 둘째 줄 바꿔 넣기 · S5 한 줄 설명 · S21 정의 한 줄 · 보드 순서·접기 · 방 전체 칸 접힘 · 인박스 두 줄 · 미션 칩 규칙 · 건너뛰기 · 「일 밖」→「미션 밖」 · S14 관찰 표·S10 허용 명령 · 좁은 화면 탭 4개 | §3.2, §4.3·§4.6·§4.7·§4.14·§4.15·§4.17, §5, §7 |
| SCR-B §1·§2·§6.1 | **화면→op 부록 A 신설** · SSE 13종 표 · C1~C12 반영 | 부록 A, §6, §4 전반 |
| SCR-C §4.7 · SCR-B | 밀도 규칙(타임라인 페이지네이션 · 아티팩트·결정 묶음당 5건) | §4.6 |

### 8.7 남아 있는 확인 필요 사항

| # | 질문 | 왜 화면에서 걸리나 | 잠정안 |
|---|---|---|---|
| ~~Q1~~ | ~~비참여 멤버 열람~~ | **FR-5.3 v0.19 `visibility` 로 해결** | ✅ §2.4 |
| ~~Q2~~ | ~~세션 삭제~~ | **FR-2.6 방 삭제로 해결**(+ 보관이 생겼다) | ✅ §4.3 |
| Q3 | 에이전트 아카이브 후 과거 방 표시 | `archived_at`은 스키마에 있으나 UI 규칙이 없다 | 과거 메시지의 칩은 유지 + 「보관됨」. 신규 초대 목록에서 제외 |
| ~~Q4~~ | ~~테스트 채팅이 세션인가~~ | **FR-1.8.1** | ✅ 방이 아니다 |
| Q5 | 푸시 알림이 v1인가 | FR-5.2 | 웹 + 이메일은 v1, **푸시는 v2** |
| Q6 | 모바일 대응 범위 | 3열 축소 규칙 | 데스크톱 우선, **받은 요청 응답만** 모바일 웹. 좁은 화면의 열 탭은 **넷**(타임라인·보드·미션·방, §4.8). **미션 칩 줄이 좁은 화면에서 가로 스크롤**인지 접힘인지는 R2 구현에서 |
| Q7 | 에이전트 수정·삭제 권한 | FR-1.5 가 "Agents 화면에서만"까지만 | 소유자 + ws admin 이상. `activity_log` 에 남긴다 |
| ~~Q8~~ | ~~미션 칩이 몇 개까지 한 줄인가~~ | **확정**(SCR-C §4.3 가 짚은 셈 규칙 포함) | **미션 칩만 센다** — 5개 이상이면 4 + 「미션 N개 ▾」. `(전체)`·`(미션 없음)`·「지난 미션 ▾」·「일시정지 N ▾」·「+ 새 미션」은 세지 않는다(§4.6) |
| **Q9** | **미션 밖 대화가 길어지면** | `(미션 없음)` 칩 하나에 몇 달 치 잡담이 쌓인다 | v1 은 그대로 둔다 — **타임라인 페이지네이션이 답이고 그 규칙은 §4.6 에 적었다**(최근 50건 + 「이전 대화 더 보기」). **스레드로 나누는 것은 발명이므로 하지 않는다** |
| **Q10** | **`(전체)` 우열이 가리키는 「최근 활동한 미션」이 내 관심사와 다를 때** | 미션 3개 방에서 사람이 가장 오래 머무는 보기가 `(전체)` 다(SCR-C §4.4) | v1 은 그대로 둔다 — 칸이 「최근 활동」임을 적고 동작은 비활성이다(§4.6). **「내가 Director 인 미션 우선」 같은 개인화는 발명이므로 하지 않는다** |
| **Q11** | **방 목록이 한 열이면 방 20개에서 스크롤이 길다** | §4.3 이 인박스와 같은 판정(한 열)을 받았다 | 마지막 활동순 고정 + 안 읽음 거르개로 버틴다. **다열로 되돌리려면 COMPONENTS §8.5 의 PR #191 판정을 함께 뒤집어야 한다** — 디자인 확인 |

---

## 9. 다음 단계

1. **§8.3(나) 의 다섯 자리를 Lead 가 판단한다.** **D-13**(`task.queued_reason` 이 §7 `task` 행에 없다)은 **R0·R1 스키마 전에** 정해져야 한다 — 서브 미션 대기 사유가 여기 걸린다. D-14·D-15 는 §6·§11 의 문장 정리, D-16 은 계약 표기 통일, **D-17**(v1.1 「세션 긴급 정지」 ↔ v2.0 `blocked_reason: manual`)은 어느 쪽이 정본인지의 문제다.
2. **부록 A 의 화면→op 표를 R0 의 입력으로 넘긴다.** 신규 op 28개(N1~N26) · 확장 12개 · SSE 13종이고, **op 이름은 SCR-B 초안 그대로이며 Lead 가 R0 에서 확정한다.** 특히 셋은 컷할 수 없다 — `previewTriggers` 확장(E5, 귀속 규칙 3 을 서버가 판정) · `createRoom` 의 런타임 0개 허용(C11) · SSE enum 과 `STREAM_EVENT_TYPES` 를 같은 PR 에(M2).
3. **S7 방 화면을 와이어프레임으로 만든다.** 나머지 화면이 여기서 파생되는 규칙(미션 칩 ↔ 우열 연동·권한 비활성 사유의 층·우열 접힘·실시간)을 공유한다. **칩 ↔ 우열 연동이 실제로 읽히는지**와 **268px 에 미션 칸이 드는지**가 관건이다.
4. **§3.4 용어표를 `COMPONENTS.md` §8.4 로 옮긴다**(R1.5 = R2 서브 순서 (e)). 자물쇠는 「옛말 0건」 + 「새말 1건 이상」 두 방향을 다 재고, **「아티팩트」를 옛말 목록에 넣지 않는다** — 넣으면 계약과 반대로 잠긴다.
5. **S18 + 첫 dispatch 시스템 메시지 + `isolation_confirm` 카드 세 자리를 한 묶음으로 구현한다.** 셋 중 하나라도 빠지면 W-1·W-2 사고가 그대로 난다 — 이 셋은 따로 컷할 수 없는 한 덩어리다. **이제 셋 다 §4 에 명세가 있다**(§4.5·§4.6·§4.14).
6. **`COMPONENTS.md` §9 의 신규 컴포넌트 4개**(Work Chip · Work Selector · Person Chip · Room Blocked Banner)와 **`badge-map` 의 `room`·`work` kind** 를 R0 계약 값과 함께 확정한다 — `badge-map` 은 닫힌 union 이라 값이 늦으면 타입 오류로 막힌다.
7. 컷 순서(§8.5)가 발동해도 **S18 의 확정 안내와 「이 방 멈춤」은 남긴다.**

---

## 부록 A. 화면 → 계약 op 표 (R0 의 입력)

> **출처는 `plan/research/SCR19_impl.md`(SCR-B) §1 이고, op 이름은 그 초안 그대로다.** 이 부록은 **「화면이 무엇을 부르는가」의 목록**이지 계약이 아니다 — **이름·경로·스키마는 Lead 가 R0 에서 확정한다.** 화면이 먼저인 이유는 §머리말 「계약(R0)은 화면이 부르는 것에서 나온다」다.
>
> 판정 요약: 기존 op 그대로 **41** · 이름 별칭 **4**(`rebindSession`·`getSessionCost`·`setSessionSubscription`·`getMessage`) · 단위 이동 별칭 **6**(session 동사 → work) · 기존 op 확장 **12** · **신규 28**(N1~N26 — N5 `archive/unarchive`·N26 `block/unblock` 은 짝이라 op 은 28개. 리뷰 #276 R1-1 정정).
>
> **「세션 op 을 방 op 으로 개명하면 된다」가 성립하지 않는다.** `SessionListItem` 16칸 중 **goal·status·paused_reason·director·completion_progress·cost_usd·budget_usd·cost_estimated 여덟 칸이 미션의 것**이라 방 목록 카드로 쓸 수 없다(§4.3 이 「상태 배지와 goal 이 빠진다」고 적은 그 칸들이다). 별칭은 **op 이름**에만 걸고 스키마는 새로 만든다.

### A.1 화면별

| 화면 | 부르는 op | 판정 |
|---|---|---|
| **S5 방 목록** | `listRooms`(N1) | **신규** — 응답 `RoomListItem` 은 새 스키마 |
| | `archiveRoom`·`unarchiveRoom`(N5) · `deleteRoom`(N6) | 신규 2 + `deleteSession` 확장(거부 사유가 「진행 중 미션」) |
| **S25 방 찾기** | `listRooms` 파라미터 — `q`·`unread_only`·`participating`(기본 true)·`include_archived` | N1 에 포함. **`sort` 는 없다**(마지막 활동순 고정) · **본문 검색 없다**(§4.4) |
| **S18 방 만들기** | `createRoom`(N2) | **신규** — `createSession` 은 `{title,goal,isolation,participants}` 필수라 별칭 불가. **런타임 0개에도 201**(§2.1 m5 철회, SCR-B C11) |
| **S7 상단** | `getRoom`(N3) · `listWorks`(N15) · `markRoomRead`(N9) · `summarizeRoom`(N10) · `archiveRoom`·`deleteRoom`·`transferRoomOwner`(N7) | 신규. `summarizeRoom` 은 `{from_message_id?, to_message_id?, since?}` → `Message(kind: summary)` + `scope` |
| | **`blockRoom`·`unblockRoom`** | **신규**(N26) — 「이 방 멈춤」(`blocked_reason: manual`, §4.6). SCR-B 초안에 없던 것이고 PRD FR-2.4 [V19-B] 확정에서 나온다 |
| **S7 좌열** | `listParticipants`(E1) · `listLanes`(E2) | 확장 — 사람 행·`kind`·방 역할 / `work_id` 필터 + `Lane.work_id`·`work_title`·`queued_reason` |
| | `listLaneTasks`·`cancelLane`·`restartLane`·`listTaskEvents` | 기존 그대로 |
| **S7 가운데** | `listMessages`(E3) · `postMessage`(E4) · `previewTriggers`(E5) | 확장 — `?work_id=`·`?around_message_id=` / 본문 `work_id?` / 응답에 `work{id,title}` + `work_source: chosen\|thread\|running_lane\|none` |
| **S7 우열 (가)** | `getWork`(N17)·`updateWork`(N18) + 단위 이동 별칭 6(`pauseWork`·`resumeWork`·`completeWork`·`cancelWork`·`deleteWork`·`changeWorkDirector`) | 신규 2 + 별칭 6 |
| **S7 우열 (나)** | `listArtifacts`(E6)·`listDecisions`(E7) · `getRoomCost`(별칭) · `listRoomReads`(N14) | 확장 — 응답에 `work_id`(「미션 없음」 묶음). 미션 비용은 `Work.cost_usd` 로 따로 온다 |
| **S21 미션 열기** | `createWork`(N16) | 신규 — `409 max_concurrent_works`(열린 미션 목록을 `Problem` 확장에) · `422 reviewer_required`·`reviewer_not_participant`(기존 검증 재사용) |
| **S22 미션 패널** | `getWork`(N17) | 신규. 라우트는 `/rooms/:id?work=:workId`(PRD §6 #274) |
| **S26 미션 제안** | `listWorkProposals`(N19)·`getWorkProposal`(N20)·`resolveWorkProposal`(N21) · (에이전트) `createWorkProposal`(N22, R3) | 신규 4. 저장은 `work_proposal` 표(§7) |
| **S19 참여자** | `addParticipant`(E1b, `{agent_id \| user_id}` oneOf) · `removeParticipant`(E1c, 경로 `{participantId}` + 거부 규칙) · `updateParticipant`(E1d, `room_role`) · `setRoomDeputy`(N8) · `listMembers` | 확장 4 + 신규 1 |
| **S20 방 설정** | `updateRoom`(N4) · `transferRoomOwner`(N7) · `setRoomDeputy`(N8) · `checkRepo` | 신규 3 + 기존 1. `409 runtime_pinned`(첫 dispatch 뒤 `runtime_id`·`isolation`) |
| **S24 참고 방 링크** | `listRoomLinks`(N11)·`createRoomLink`(N12)·`deleteRoomLink`(N13) | 신규 3. `403 not_participant_of_target` |
| **S23 맥락 읽기 기록** | `listRoomReads`(N14) | 신규 — `direction=out\|in\|denied`. **`denied` 에서는 `other_room` 을 반드시 `null`**(존재 숨김) |
| **S8 받은 요청** | `listInbox`(E8) · `InboxItemType`(E9) · `respondHitlRequest`·`markInboxRead`·`getInboxSummary` | 확장 2 + 기존 3. enum 에 **신규 6**(`work_proposed`·`work_paused`·`room_paused`·`work_completed`·`room_invited`·**`isolation_confirm`**), 개명 2 |
| **S9 에이전트 목록** | `listAgents`(E10) | 확장 — `room_count`·`rooms[]`(볼 수 있는 것만)·`hidden_room_count`·`concurrent_used/max` |
| **S11·S13·S17** | `listRuntimes`·`listRuntimeWorkdirs`·`rebindSession`·`listRuntimeCandidates`(E11) | 별칭 + 확장 — `?session_id=`→`?room_id=`, 「묶인 방 N개」, `Workdir.room_id` |
| **S14 설정** | `getWorkspaceSettings`·`updateWorkspaceSettings`(E12) · `setRoomSubscription`(별칭) · `setWorkSubscription`(N23)·`setLaneSubscription`(N24) · `getWorkspaceMetrics`·`getWorkspaceObservations` | 확장 1 + 신규 2 + 기존 3. **`context_reuse.*` 는 정리 대상**(FR-4.4) |
| **S15 활동 로그** | `listActivityLog`(N25) | **신규** — 계약에 활동 로그 조회 op 이 하나도 없었다(op 97개 전수 확인). PRD §10 R0 에 들어갔다(#274) |

### A.2 신규 op 의 주요 응답 칸 (초안)

| # | op | 응답(주요 칸) |
|---|---|---|
| N1 | `listRooms` | `id·name·description·status·blocked_reason·unread_count·active_work_count·attention{hitl_open,blocked,failed}·participants[{kind,id,name,avatar_url}]·my_room_role·last_activity_at` |
| N3 | `getRoom` | N1 + `visibility·owner_user_id·deputy_owner_user_id·runtime_id?·runtime·isolation·limits·autonomy·default_director_user_id·blocked_detail·counts{works_active,lanes_active,tasks_active}·unread_count·my_room_role·my_capabilities[]` |
| N9 | `markRoomRead` | `{room_id, unread_count: 0}` — 요청은 `{last_read_message_id}` |
| N14 | `listRoomReads` | `{at, direction, agent{id,name}, originator_user{id,name}, other_room{id,name}\|null, scope{summary,recent_n}, truncated, task_id?, denied_reason?}` |
| N15 | `listWorks` | `{id,title,goal,status,paused_reason,director,assignee_agent_id,completion_progress{met,total},cost_usd,budget_usd,last_activity_at}` |
| N17 | `getWork` | `Session` 의 goal 쪽 칸 전부 + `room_id`·`completion_progress`·`paused_detail`·`my_work_role: director\|deputy\|member` |
| N19 | `listWorkProposals` | `{id, agent{id,name}, proposed_goal, rationale, trigger_message_id, status, resolved_by, resolved_at, reject_reason}` |
| N25 | `listActivityLog` | `{at, actor{kind,id,name}, action, object_ref, room{id,name}?, payload(마스킹 반영)}` — 권한 owner·admin |
| N26 | `blockRoom`/`unblockRoom` | `Room` + 시스템 메시지. 요청은 `{reason: manual}` / 없음 |

### A.3 이 표가 R0 에 넘기는 세 가지

1. **컷 불가 3건** — `previewTriggers` 확장(E5: 귀속 규칙 3 은 서버 상태를 봐야 한다) · `createRoom` 의 런타임 0개 허용(C11) · SSE enum 과 `STREAM_EVENT_TYPES` 를 **같은 PR 에**(§6).
2. **SSE 13종 + 구독 파라미터 `session_id`→`room_id`**(§6).
3. **저장 자리 셋은 이미 닫혔다** — `work_proposal`·`room_read_log` 표와 `listActivityLog`(PRD #274).
