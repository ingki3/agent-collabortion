# 화면 설계 #01 — 정보 구조와 화면 명세

| 항목 | 내용 |
|---|---|
| 문서 버전 | **v0.3 (Draft)** — 화면 리뷰 #01 + 디자인 리뷰 #01·#02 반영 |
| 위치 | **이 파일이 화면 설계의 SSOT다.** 이전 버전과 리뷰는 `screen/` |
| 작성일 | 2026-09-03 |
| 근거 | `PRD.md` **v0.10** (SSOT). 모든 화면·기능·상태는 PRD의 FR 번호로 역참조한다 |
| 리뷰 | 화면 리뷰는 `screen/`, 와이어프레임(디자인) 리뷰와 후속 조치는 `design/`. `screen/SCREEN_REVIEW_01.md` — 충돌 4건(C1~C4)·누락 6건(O1~O6)·인용 오류 9건(m1~m9) 전건 반영. PRD 쪽 공백 7건은 **PRD v0.9에 먼저 반영**했고 이 문서는 그 결과를 따른다 |
| 범위 | v1(MVP) 화면 전체 + v1.1 이후로 미루는 화면의 자리 표시 |
| 목적 | 화면 목록, 화면 간 연결, 각 화면의 기능·표시 정보·권한을 확정해 UI 디자인과 API 설계의 공통 기준을 만든다 |

> **읽는 법.** §1~§3은 전체 구조(화면 지도·권한·상태), §4는 화면별 상세 명세, §5~§7은 화면을 가로지르는 규칙(공통 컴포넌트·실시간·빈 상태), §8은 PRD 대조 결과다. 화면 하나만 볼 사람은 §2 권한 표와 §4의 해당 절만 읽으면 된다.

---

## 1. 설계 원칙

PRD에서 직접 따라오는 제약이다. 화면을 그릴 때 이 다섯 가지가 계속 충돌 판정 기준이 된다.

1. **작업의 중심은 세션이다.** 워크스페이스에는 에이전트·런타임·세션이 있고, 사람이 실제로 시간을 보내는 곳은 세션 상세 하나다. 나머지 화면은 세션을 시작하거나(Agents·Runtimes·새 세션) 세션 밖에서 응답하기 위해(Inbox) 존재한다.
2. **파괴적 동작은 화면 조작으로만 한다.** 어떤 메시지도 진행 중인 턴을 취소하지 않는다(FR-3.4). 취소는 lane 카드의 버튼이고, 그래서 lane 보드는 장식이 아니라 **유일한 제어판**이다.
3. **침묵도 렌더한다.** 이벤트가 없는 것도 정보다(FR-7.2). 빈 목록·대기·응답 없음은 모두 명시적 상태로 그린다. 빈 화면을 그대로 두지 않는다(§7).
4. **권한은 숨기지 않고 비활성으로 보인다.** Director만 쓸 수 있는 버튼도 다른 멤버에게 보이되 비활성이고, 이유를 툴팁으로 말한다. 누가 결정권을 갖는지가 화면에서 읽혀야 한다.
5. **런타임 능력에 따라 화면이 강등된다.** 구조화 이벤트를 주지 않는 런타임에서는 활동 피드가 텍스트로 떨어진다(§8.2.6). 이때도 "이 런타임은 툴 단위 로그를 제공하지 않습니다"를 표시해 침묵과 구분한다.

---

## 2. 사용자 상태와 접근 권한

### 2.1 인증 상태

| 상태 | 접근 가능 | 진입 시 |
|---|---|---|
| **로그아웃** | 로그인, 회원가입, 초대 수락 | 보호된 URL 접근 시 로그인으로 보내되 **원래 URL을 기억**했다가 로그인 후 그 자리로 복귀 |
| **로그인, 워크스페이스 없음** | 워크스페이스 생성, 초대 대기 목록 | 온보딩(§4.2)으로 강제 이동 |
| **로그인, 워크스페이스 있음, 런타임 없음** | 전체 화면 접근 가능하나 세션 생성 불가 | Runtimes 화면의 "Add a computer"로 유도. 세션 생성 버튼은 비활성 + 이유 표시. **PRD보다 엄격한 화면 결정이다**(m5) — PRD는 `none` 격리에서 `runtime_id`를 비운 채 생성하고 첫 task가 `queued`로 대기하는 것을 허용하지만, 실행되지 않을 세션을 만들게 두면 사용자가 원인을 모른 채 기다린다 |
| **로그인, 준비 완료** | 전체 | 마지막으로 본 세션 또는 Sessions 목록 |

세션 생성이 런타임을 요구하는 이유는 세션이 `runtime_id`에 묶이기 때문이다(FR-2.1, C4). 런타임이 하나도 없으면 세션을 만들 수 없다.

### 2.2 역할

역할은 **세 층**으로 나뉘고 서로 독립이다. 화면 권한을 판정할 때 셋을 각각 봐야 한다.

| 층 | 값 | 근거 |
|---|---|---|
| 워크스페이스 역할 | `owner` / `admin` / `member` | §7 `member.role` |
| 세션 역할 | `director` / `deputy` / **워크스페이스 멤버** | FR-5.3 |
| 에이전트 소유 | 에이전트별 `owner_id` | FR-1.9 |

> **`participant`는 에이전트 전용 용어다** (PRD FR-5.3, P7). 사람에게는 쓰지 않는다 — `session_participant`는 에이전트 테이블이고, `delegate`의 대상도 에이전트다. 화면 문구·코드·API에서 사람을 가리킬 때는 "멤버"를 쓴다.
>
> **세션에 사람을 "초대"하는 절차는 없다.** 워크스페이스 멤버는 누구나 세션을 열람하고 게시할 수 있다. 비공개 세션은 v1.1이며 그때 `observer` 역할이 함께 도입된다. v0.1은 `participant`/`observer`를 임의로 만들어 PRD가 허용한 게시를 막았는데, 이는 규칙 위반이었다(C1).

### 2.3 화면·동작별 권한 매트릭스

| 동작 | owner | admin | member | 비고 |
|---|---|---|---|---|
| 워크스페이스 설정 변경 | ✅ | ✅ | ❌ | 예산·루프 상한·보존 기한 |
| 멤버 초대·역할 변경 | ✅ | ✅ | ❌ | owner 강등은 owner만 |
| 런타임 추가·삭제 | ✅ | ✅ | ❌ | 활성 세션이 걸린 런타임은 삭제 차단(FR-9.2) |
| 에이전트 생성 | ✅ | ✅ | ✅ | 생성자가 `owner_id` |
| 에이전트 수정·삭제 | ✅ | ✅ | 소유자만 | **PRD 미정 — §8.2 Q7** (m3) |
| 에이전트를 세션에 초대 | 에이전트의 `respond_to` 값이 정한다 | | | FR-1.9 |
| 세션 생성 | ✅ | ✅ | ✅ | 생성자가 기본 Director |
| **Director 교체** | ✅ | ✅ | 현재 Director만 | t-5 |

세션 내부 동작은 워크스페이스 역할이 아니라 **세션 역할**이 정한다.

| 세션 내 동작 | director | deputy | 워크스페이스 멤버 | 근거 |
|---|---|---|---|---|
| 세션 열람 | ✅ | ✅ | ✅ | FR-5.3 |
| 메시지 게시·멘션 | ✅ | ✅ | **✅** | FR-5.3 — 멤버 메시지도 라우팅된다 |
| HITL 응답(`question`) | ✅ | 기한 절반 경과 후 | ❌ | FR-5.4 |
| HITL 승인(`approval`) | ✅ | 기한 절반 경과 후 | ❌ | 자동 진행 없음 |
| lane "중단하고 다시 지시" / "중단" | ✅ | **즉시** | ❌ | FR-3.4, t-3 |
| 세션 일시정지·재개·종료(`manual`) | ✅ | ❌ | ❌ | |
| 예산 초과 후 계속 승인 | ✅ | 기한 절반 후 | ❌ | FR-7.3 |
| 참여 에이전트 추가·제거 | ✅ | ❌ | ❌ | FR-1.5 (O2) |
| Director 교체 | ✅ | ❌ | owner·admin만 | t-5 |

> **v1 HITL 타입은 `question`과 `approval` 둘이다** (PRD §10). `choice`·`info`는 v1.1에서 **같은 카드 컴포넌트에 입력부만 교체**되어 추가된다(C4). 그래서 §5의 HITL 카드는 "타입별 입력부 교체 가능"으로 설계한다.

> **deputy의 비대칭**: 취소는 즉시, 승인은 기한 절반 이후다. 폭주를 멈추는 것은 지연될수록 손해가 커지고, 승인은 Director의 판단을 우선 기다려야 하기 때문이다(t-3). UI에서 승인 버튼이 비활성일 때 "Director 응답 대기 중 · 14:30부터 승인 가능"처럼 **언제부터 가능한지**를 함께 보여준다.

---

## 3. 화면 지도

### 3.1 전체 구조

```
[로그아웃]
  로그인 ──── 회원가입
     │           │
     └─→ 초대 수락 ─┐
                    ↓
[로그인]        온보딩 (워크스페이스 없음 / 런타임 없음일 때만)
                    ↓
        ┌───────────┴────────────────────────────────┐
        │            앱 셸 (좌측 내비 + 상단바)         │
        ├────────────────────────────────────────────┤
        │  Sessions ──→ 새 세션 마법사 ──→ Session 상세 │
        │      ↑                              │  ↑     │
        │      └──────────────────────────────┘  │     │
        │  Inbox ────────────────────────────────┘     │
        │  Agents ──→ Agent 편집 ──→ 테스트 채팅        │
        │  Runtimes ──→ Add a computer                 │
        │            └→ Workdir 관리                   │
        │  Settings (owner·admin)                      │
        └────────────────────────────────────────────┘
```

### 3.2 화면 목록과 우선순위

| # | 화면 | URL | v1 | 진입 경로 |
|---|---|---|---|---|
| S1 | 로그인 | `/login` | ✅ | 미인증 접근, 로그아웃 |
| S2 | 회원가입 | `/signup` | ✅ | 로그인 화면 |
| S3 | 초대 수락 | `/invite/:token` | ✅ | 이메일 링크 |
| S4 | 온보딩 | `/onboarding` | ✅ | 워크스페이스·런타임 없음 |
| S5 | **Sessions 목록** | `/sessions` | ✅ | 내비, 로그인 후 기본 |
| S6 | **새 세션 마법사** | `/sessions/new` | ✅ | Sessions, 빈 상태 CTA |
| S7 | **Session 상세** | `/sessions/:id` | ✅ | 목록, Inbox, 알림 |
| S8 | **Inbox** | `/inbox` | ✅ | 내비(뱃지), 알림 |
| S9 | Agents 목록 | `/agents` | ✅ | 내비 |
| S10 | Agent 편집 | `/agents/:id` · `/agents/new` | ✅ | Agents, 세션 마법사 |
| S11 | Runtimes | `/runtimes` | ✅ | 내비, 온보딩 |
| S12 | Add a computer | `/runtimes/new` | ✅ | Runtimes |
| S13 | Workdir 관리 | `/runtimes/:id/workdirs` | ✅ | Runtimes 카드 |
| S14 | Settings | `/settings/*` | ✅ | 내비(owner·admin) |
| S17 | 런타임 재바인딩 (다이얼로그) | `/runtimes/:id/rebind?session=` | ✅ | S11 카드, S7 `paused` 배너 |
| S15 | 활동 로그(감사) | `/settings/audit` | v1.1 | Settings |
| S16 | 채널 목록·상세 | `/channels/*` | v1.1 | §12 검토 항목 |

> v1.1로 미루는 S15·S16은 §3.1 지도에 자리만 잡아두고 v1에서는 내비에 노출하지 않는다. 나중에 끼워 넣을 때 정보 구조를 다시 흔들지 않기 위해서다.

### 3.3 주요 사용자 흐름

**F1. 신규 사용자 → 첫 세션 완료** (목표: 15분, §11)
```
S2 회원가입 → S4 온보딩(워크스페이스 생성)
           → S12 Add a computer (설치 명령 2줄 복사 → 데몬 연결 대기)
           → S11 Runtimes (CLI 감지 확인)
           → S9/S10 에이전트 등록 (템플릿에서 팀 통째로)
           → S6 새 세션 마법사 → S7 Session 상세 (goal 자동 트리거)
```
가장 이탈이 많을 지점은 **S12의 데몬 연결 대기**다. 터미널에서 명령을 실행해야 하고 결과가 즉시 보이지 않는다. 연결될 때까지 폴링하며 "대기 중 → 연결됨 → CLI 감지됨"을 단계로 보여준다. v1이 구동하는 것은 **Claude Code·Hermes 2종**이므로(Antigravity는 v1.1) 감지 결과에 그 둘의 상태를 명시한다(m1).

**F2. HITL 응답** (지표: 중앙값 30분 내)
```
알림/뱃지 → S8 Inbox → 항목 선택 → 인라인 응답 (또는 S7로 이동해 맥락 확인 후 응답)
                                  → 응답 즉시 해당 task 재개, 항목은 목록에서 사라짐
```
Inbox에서 **맥락 없이 답할 수 있어야** 한다. 질문·선택지·에이전트의 제안 기본값·세션 goal 한 줄이 카드 안에 다 있어야 하고, 더 봐야 할 때만 세션으로 간다.

**F3. 방향 전환** (시나리오 C)
```
S7 → 메시지 작성 → 전송 (실행 중이면 큐잉, 턴은 죽지 않음)
   또는 lane 카드 → "중단하고 다시 지시" → 멘션 채워진 작성창 → 전송 → 턴 취소 후 재시작
   또는 lane 카드 → "중단" → 확인 → lane failed(cancelled)
```

**F4. 자식 에이전트의 질문** (FR-6.2.1)
```
Backend가 blocked → 서버가 질문 카드 게시 + 위임자(Lead) 즉시 깨움
                  → Lead가 질문 카드에 답글 → Backend lane 재진입
위임자 없는 lane이면 → S8 Inbox에 lane_blocked 항목 → Director가 답글
```

---

## 4. 화면별 상세 명세

### 4.1 S1~S3 인증

**S1 로그인** — 이메일/비밀번호 또는 SSO. 실패 시 사유를 구분해 표시(계정 없음 / 비밀번호 불일치 / 잠김). 보호된 URL에서 왔다면 로그인 후 그 URL로 복귀.

**S2 회원가입** — 이름·이메일·비밀번호. 가입 직후 S4 온보딩.

**S3 초대 수락** — 토큰으로 워크스페이스·초대자·부여될 역할을 미리 보여준다. 미로그인이면 로그인/가입 후 자동 수락. 만료·취소된 토큰은 사유를 명시하고 초대자에게 재요청하는 버튼을 준다.

### 4.2 S4 온보딩

목적은 **F1의 15분**을 만드는 것이다. 세 단계이며 각 단계는 건너뛸 수 있으나 건너뛴 것은 홈에 체크리스트로 남는다.

| 단계 | 내용 | 완료 판정 |
|---|---|---|
| 1. 워크스페이스 | 이름 입력 (팀원 초대는 나중에) | 생성됨 |
| 2. 컴퓨터 연결 | S12를 인라인으로 — 설치 명령 2줄, 연결 대기 | 데몬이 붙고 CLI 1종 이상 감지 |
| 3. 에이전트 | 팀 템플릿 선택(리서치 팀 / 개발 팀 / 콘텐츠 팀) 또는 직접 생성 | 에이전트 1개 이상 |

3단계에서 **템플릿을 전면에 둔다.** 에이전트를 하나씩 만들게 하면 15분을 못 맞춘다. 템플릿은 역할·instruction만 담고 프로파일은 감지된 런타임에 맞춰 자동 매핑한다(FR-1.4).

### 4.3 S5 Sessions 목록

**표시 정보** — 행마다: 상태 배지, 제목, goal 한 줄, Director 아바타, 참여 에이전트 아바타 묶음, 진행률(종료 조건 충족 수/전체), 누적 비용 및 예산 대비 %, 마지막 활동 시각, **주의 배지**(HITL 대기 N · blocked N · 실패 N).

**세션 상태 배지** (FR-2.3)

| 상태 | 표시 | 색 의미 |
|---|---|---|
| `draft` | 초안 | 중립 |
| `active` | 진행 중 (+ 실행 중 lane 수) | 진행 |
| `paused` | 일시정지 **+ 사유** (예산/시간/루프/런타임 오프라인/수동) | 주의 |
| `completing` | 마무리 중 | 진행 |
| `completed` | 완료 | 성공 |
| `cancelled` | 취소됨 | 중립 |

`paused`는 **사유를 배지에 함께** 쓴다. 원인마다 사용자가 할 일이 다르다 — 예산이면 승인, 런타임 오프라인이면 재바인딩(FR-9.2).

**기능** — 필터(상태·Director·참여 에이전트·런타임·기간), 정렬(최근 활동·비용·생성일), 검색(제목·goal), 새 세션 CTA.

**빈 상태** — 런타임이 없으면 "먼저 컴퓨터를 연결하세요"로 S12를 가리키고, 있으면 새 세션 CTA를 크게 놓는다.

### 4.4 S6 새 세션 마법사

PRD의 순서를 그대로 따른다: **goal → Director → 격리 → 런타임 → 참여자·프로파일 → 종료 조건 → 한도·autonomy**. 각 단계는 되돌아갈 수 있고 마지막에 요약을 보여준다.

> **v0.3에서 격리와 런타임의 순서를 바꿨다** (디자인 리뷰 #02 C4′). 격리가 런타임 후보를 제한하고(worktree → 저장소 있는 머신만), 런타임이 참여자 프로파일 후보를 제한하므로 의존 순서는 격리 → 런타임 → 참여자다. v0.2 순서에서는 3단계 "자동 선택"에 "worktree면 5단계에서 다시 골라야 함"이라는 되돌림 주의문이 필요했는데, 그 문장의 존재 자체가 순서가 틀렸다는 증거였다.

| 단계 | 입력 | 검증·주의 |
|---|---|---|
| 1 goal | 제목, goal(필수), 성공 기준 체크리스트(선택), 첨부 컨텍스트(문서·URL·파일·이전 세션) | 이전 세션 첨부 시 요약 토큰 상한을 표시(FR-4.4) |
| 2 Director | 워크스페이스 멤버 중 1인(기본 생성자), **deputy**(선택) | deputy는 취소는 즉시, 승인은 기한 절반 후 가능함을 안내(t-3) |
| 3 격리 | `worktree`(저장소 선택) / `none` — **v1은 2종**, `container`는 v1.1 | worktree 선택 시 workdir 바인딩 규칙(에이전트당 1개)을 그림으로 설명. **이 선택이 4단계의 런타임 후보를 결정한다** |
| 4 **런타임** | 온라인 런타임 중 선택, 또는 **"자동 선택(첫 실행 시 고정)"** | `worktree`면 remote URL이 일치하는 저장소를 가진 머신만 후보이고 자동 선택은 비활성. `none`이면 자동 선택 가능 — 첫 task dispatch 시 고정(FR-2.1 M10). 저장소 경로·remote URL·클린 여부를 데몬 probe로 검증해 표시(FR-9) |
| 5 참여자 | 에이전트 다중 선택 + 각자 프로파일 + assignee 지정 | 선택한 런타임에 없는 `runtime_kind`의 프로파일은 경고. `respond_to`로 초대 불가한 에이전트는 목록에서 사유와 함께 비활성 |
| 6 종료 조건 | `artifact_submitted` / `agent_approval` / `user_approval` / `manual` 조합(AND·OR) | 기본값 `artifact_submitted(assignee) AND user_approval`. **`agent_approval` 단독이면 "사람 승인 없이 완료됩니다" 경고**(m-m) |
| 7 한도·자율성 | 예산(USD), 시간, 최대 task, 최대 병렬 lane, **`autonomy`** | 초과는 완료가 아니라 `paused`임을 명시. 추정 비용만 주는 런타임이면 하드 컷을 하지 않음을 알림(FR-7.3) |

**`autonomy` 선택** (C3, FR-2.1) — v1 동작을 실제로 바꾸는 값이므로 선택지에 **차이를 문장으로** 적는다.

| 값 | 화면 문구 | v1 |
|---|---|---|
| `guided` (기본) | "질문 기한이 지나면 계속 기다립니다" | ✅ |
| `autonomous` | "질문 기한이 지나면 에이전트가 제안한 기본값으로 진행합니다. 승인 요청은 예외로 항상 기다립니다" | ✅ |
| `supervised` | "Lead의 모든 위임을 Director가 먼저 승인합니다" | ❌ 비활성 + "v1.1" 배지 |

승인(`approval`)은 `autonomy`와 무관하게 절대 자동 진행되지 않는다(FR-5.4). 이 사실을 `autonomous` 설명에 함께 적어 오해를 막는다.

> **`criteria_met`은 v1 목록에 없다**(v1.1). 6단계 선택지는 네 개다.

### 4.5 S7 Session 상세 — 핵심 화면

3열 레이아웃. 좁은 화면에서는 좌·우 열이 접히고 탭으로 전환된다.

```
┌─ 좌 (참여자 + lane 보드) ─┬─ 중앙 (타임라인) ────────┬─ 우 (진행·산출물) ─┐
│ 참여자                     │ [메시지 타임라인]         │ Goal & 성공 기준     │
│  @Lead    idle             │  · 사람/에이전트 메시지   │ 종료 조건 진행률     │
│  @Backend working ●        │  · 스레드                 │  ✅ artifact 제출    │
│  @QA      blocked ?        │  · HITL 카드              │  ⬜ Director 승인    │
│                            │  · blocked 질문 카드      │                      │
│ Lane 보드                  │  · 시스템 메시지          │ 아티팩트 (버전 표시)  │
│ ┌────────────────────────┐ │  · 활동 피드(펼침)        │  patch_v1.diff       │
│ │ queued   │ running   │ │ │                           │                      │
│ │ waiting  │ blocked   │ │ │ ───────────────────────── │ 결정 기록            │
│ │ done     │ failed    │ │ │ [작성창]                  │ 비용 $3.20 / $20     │
│ └────────────────────────┘ │  @멘션 · 새 lane 토글     │ 런타임: MacBook      │
└────────────────────────────┴───────────────────────────┴──────────────────────┘
```

#### 좌열 — 참여자와 lane 보드

**참여자**: 이름, 역할, 현재 상태 배지(`idle`/`working`/`waiting_human`/`error`/`offline`/`disabled` — FR-1.3), 사용 중 프로파일. `offline`과 `disabled`는 아이콘과 문구를 다르게 한다(원인이 다름).

> **에이전트 상태는 task 상태에서 파생되는 계산값이다** (PRD FR-1.3 v0.10, 디자인 리뷰 C1·C1′). `disabled` > `offline` > `error` > `working`(running task 있음) > `waiting_human`(waiting_human task 있음) > `idle` 순으로 첫 번째 맞는 것. `blocked`·`paused` lane은 파생에 들어가지 않으므로 그 에이전트는 `idle`이고, 어느 lane이 왜 멈췄는지는 lane 카드가 말한다. 칩에 부가 정보를 보이려면 상태 값이 아니라 **둘째 줄 텍스트**("lane #2 질문 대기")로 넣는다 — 칩 폭을 넘기지 않게 Role 텍스트는 줄바꿈되는 컴포넌트다(N2).

**lane 보드**: lane 카드를 상태별로 묶어 보여준다. 카드에는 담당 에이전트, 위임 요약, 경과 시간, `reentry_count`(재진입했다면), workdir 참조가 들어간다.

| lane 상태 | 카드 표시 | 가능한 동작 |
|---|---|---|
| `queued` | 대기 순번 | — |
| `running` | 진행 시간, 현재 동작 한 줄 | **중단하고 다시 지시** / **중단** (director·deputy) |
| `waiting_human` | 어떤 HITL을 기다리는지 + Inbox 링크 | 응답하러 가기 |
| `blocked` | **질문 배지 + 질문 요약**, 누구의 답을 기다리는지 | 질문 카드로 이동 |
| **`paused`** | **"예산 초과로 대기 중"** + 초과 금액 | **계속 진행 승인**(director, 기한 절반 후 deputy) |
| `done` | 산출물 요약 | — |
| `failed` | 실패 분류(`timeout`·`runtime_offline`·`cancelled`…) | 상황별 (아래) |

**`paused`는 `failed`가 아니다** (C2, FR-7.3 M9). 예산 초과는 오류가 아니라 정책이고, Director가 승인하면 **같은 재개 경로**로 이어간다. 실패로 그리면 사용자가 작업이 날아간 줄 알고 다시 시작한다. 승인 버튼은 Inbox의 `session_paused` 항목과 **같은 동작**이다.

**`failed`에서 무엇을 할 수 있나** (m6) — PRD의 재시도는 자동이고(FR-7.1, 2~3회) **사람이 누르는 재시도는 정의가 없다.** 실패 원인마다 사람이 할 일이 다르므로 버튼도 다르다.

| 실패 분류 | 동작 |
|---|---|
| `cancelled` (사람이 중단) | **"다시 지시"** — 새 메시지를 받아 새 task로 시작. 이전 지시를 그대로 되돌리지 않는다(FR-3.4 B) |
| `runtime_offline` | **런타임 복구 대기** 또는 재바인딩(§4.9) |
| `timeout`·`agent_error` | 자동 재시도가 소진된 상태. **"다시 지시"** 로 사람이 맥락을 더해 재시작 |
| `budget_exceeded` | 이 분류는 쓰지 않는다 — `paused`로 간다 |

즉 **일반적인 "재시도" 버튼을 두지 않는다.** 같은 트리거를 그대로 반복하는 것이 안전한 경우가 드물기 때문이다(부분 실행 위험, FR-7.1 M5). 사람은 항상 "다시 지시"로 맥락을 더한다.

> `waiting_human`과 `blocked`는 **아이콘과 문구를 확실히 구분**한다. 전자는 사람을, 후자는 다른 에이전트를 기다린다(FR-6.2). 사용자가 "내가 뭘 해야 하나"를 즉시 판단할 수 있어야 한다.

**lane 카드 펼침 — task 이력** (O3). PRD는 task 정체성을 세밀히 정했는데(재지시는 새 task + `restarted_from_task_id`, 재시도는 `attempt` 증가, HITL 재개는 새 attempt) 그것을 볼 곳이 없으면 `reentry_count`가 숫자로만 남는다. 카드를 펼치면 task 목록을 시간순으로 보여준다.

| 열 | 내용 |
|---|---|
| 순번 | `#1`, `#2 (재지시)`, `#2-2 (재시도)` — 재지시는 새 task, 재시도는 같은 task의 attempt |
| 트리거 | 어떤 메시지가 이 task를 만들었나 (클릭 시 타임라인의 해당 메시지로) |
| 상태·사유 | 종료 상태와 `failure_kind` |
| 실행 | 시작·종료 시각, 소요, **런타임 세션 resume 여부**(콜드 스타트였다면 표시) |
| 비용 | task별 토큰·비용 |

이 표는 **정보 5종**의 정의다. 열 배치는 화면 폭에 맞춘다 — 좌열(268px)에서는 5열이 들어가지 않으므로 **행을 2줄로**(1줄: 순번·상태·비용, 2줄: 트리거·실행) 놓는다(디자인 리뷰 #02 N1).

resume 실패가 반복되면 §11 지표("재진입 시 resume 성공률 > 90%")가 무너지고 있다는 신호이므로, 콜드 스타트를 눈에 띄게 표시한다.

#### 중앙 — 메시지 타임라인

메시지 종류(`message.kind`)마다 다른 카드로 렌더한다.

| kind | 카드 |
|---|---|
| `text` | 일반 메시지. 작성자 아바타, 멘션 하이라이트, 스레드 답글 수 |
| `hitl` | **HITL 카드** — 질문/선택지/승인 요청, 에이전트의 제안 기본값, 기한과 `overdue` 표시. 응답 버튼은 권한자에게만 활성 |
| `blocked_q` | **질문 카드** — 어느 lane의 누가 무엇을 묻는지. **이 카드가 답글의 스레드 루트**다(FR-6.2.1) |
| `system` | 시스템 메시지(합류 결과, 재진입 통보, Director 변경, 정책 강등 등). 시각적으로 낮은 대비 |
| `summary` | 세션 완료 요약 |

각 에이전트 메시지에는 **"활동 보기"** 펼침이 붙어 그 run의 활동 피드를 인라인으로 연다(FR-7.2).

**활동 피드** — 항목마다 한 문장: *에이전트가 [동사]를 [목적어]에 했다 → [결과]*. v1은 중추 5클래스 + 일반 툴 + 원본 레일을 렌더한다.

- 진행 중 항목은 **제자리에서 갱신**한다(대기 → 실행 중 → 완료/실패). 상태 줄을 쌓지 않는다.
- 실패는 크게, 조회는 조용하게.
- 침묵·유휴·타임아웃도 "대기 중…", "응답 없음"으로 렌더한다.
- 원본 레일 토글로 가공 없는 출력을 본다.
- 런타임이 구조화 이벤트를 주지 않으면 메시지 카드 + 원본 레일만 남기고 **그 사실을 명시**한다.

**작성창** — 멘션 자동완성(`@` 입력 시 에이전트·멤버 목록), **트리거 미리보기**, **"새 lane으로 보내기" 토글**(t-2, 켜면 전송 버튼 옆에 표시), 스레드 답글 시 대상 표시.

**트리거 미리보기와 개별 억제** (O4, FR-3.6) — 보내기 전에 "이 메시지는 @Backend를 트리거합니다 · 실행 중이라 큐잉됩니다"를 칩으로 보여주고, **각 칩에 × 버튼**을 둔다. 누르면 그 에이전트를 `suppress_agent_ids`에 넣어 이번 메시지에서만 트리거하지 않는다. 멘션은 본문에 남으므로 읽는 사람에게는 보이되 깨우지는 않는다 — "참고로 알아두라"는 의도를 표현하는 유일한 수단이다.

> 트리거 미리보기는 FR-3.3의 8개 규칙과 lane 해소 규칙의 결과를 **보내기 전에** 사람이 확인하는 장치다. 규칙이 복잡하므로 미리보기 없이는 예측 불가능하다.

#### 우열 — 진행 상황

goal과 성공 기준, **종료 조건 진행률**(조건별 충족 여부와 누가 충족시킬 차례인지), 아티팩트 목록(버전 포함), 결정 기록, **비용**(누적/예산, 추정치면 배지), 런타임과 격리 방식, 세션 설정 요약.

**`paused` 해소** (O6) — 세션이 `paused`면 우열 최상단에 사유와 해결 버튼을 띄운다. **사유 5종마다 할 일이 다르므로** 버튼도 다르다.

| 사유 | 배너 문구 | 해결 동작 | 권한 |
|---|---|---|---|
| 예산 초과 | "예산 $20을 초과했습니다 (현재 $21.40)" | **계속 진행 승인** (상한 상향 입력 포함) | director, 기한 절반 후 deputy |
| 시간 초과 | "시간 상한 4시간에 도달했습니다" | **계속 진행 승인** (연장 시간 입력) | 〃 |
| **루프 상한** | "에이전트 간 왕복이 상한에 도달했습니다 (@Backend ↔ @QA 5회)" | **계속 진행 승인** 또는 **개입해서 방향 바꾸기**(lane 중단·재지시로 유도) | 〃 |
| 런타임 오프라인 | "MacBook이 7일간 오프라인입니다" | **재바인딩** 또는 **세션 종료** (§4.9) | director |
| 수동 일시정지 | "Director가 일시정지했습니다" | **재개** | director |

루프 상한은 승인만 하면 같은 핑퐁이 반복되므로, 배너에 **어떤 두 에이전트가 몇 번 주고받았는지**를 함께 보여 사람이 원인을 판단하게 한다.

#### 상단 액션

| 동작 | 권한 | 비고 |
|---|---|---|
| 일시정지 / **재개** | director | `paused` 상태에서 재개 버튼으로 바뀐다(O6). 일시정지는 진행 중 턴을 마치게 두고 대기시킨다(드레인, FR-2.3) |
| 종료(`manual`) | director | 확인 필요. 진행 중 lane이 있으면 개수를 명시 |
| **참여 에이전트 관리** | director | 아래 |
| 설정 편집 | director | goal·성공 기준·한도·`autonomy`. 런타임과 격리는 **변경 불가**(workdir이 묶여 있다) |
| Director 교체 | director, owner·admin | 시스템 메시지로 기록 |

**참여 에이전트 추가·제거** (O2, FR-1.5) — 에이전트가 `ask_human`으로 "이 역할이 필요하다"고 요청하면 Director가 여기서 추가한다. 마법사 4단계와 **같은 선택 UI**를 다이얼로그로 띄운다.

- `respond_to` 초대 권한 검사를 동일하게 적용한다(FR-1.9). 초대 불가한 에이전트는 사유와 함께 비활성.
- 프로파일 선택도 함께 받는다. 세션 런타임에 없는 `runtime_kind`면 경고.
- **제거는 진행 중 lane이 없을 때만** 가능하다. 있으면 lane을 먼저 끝내거나 중단하라고 안내한다.
- 추가·제거는 시스템 메시지로 타임라인에 남긴다 — 로스터가 바뀌면 이후 위임 판단이 달라지므로 에이전트도 알아야 한다.

### 4.6 S8 Inbox

**목적**: "지금 내가 답해야 할 것"을 세션을 열지 않고 처리한다. 정의는 "나에게 열려 있는 HITL 요청 + 기한이 임박한 항목"의 합집합(FR-5.4).

| 항목 타입 | 심각도 | 카드 내용 | 인라인 동작 |
|---|---|---|---|
| `hitl_request` | action_required | 세션·에이전트·질문 또는 승인 대상, **제안 기본값**, 기한·`overdue` | 답변 / 승인·거절 |
| `lane_blocked` | action_required | 위임자 없는 lane의 질문(FR-6.2.1) | 답글 작성 |
| `session_paused` | attention | 사유(예산/시간/루프/런타임) | 계속 승인 / 세션 열기 |
| `run_failed` | attention | 실패 분류·에이전트 | **다시 지시** / 세션 열기 — 일반 재시도 버튼은 두지 않는다(§4.5, 디자인 리뷰 C4) |
| `runtime_offline` | attention | 머신·경과 시간, 7일 유예 잔여 | Runtimes 열기 / 재바인딩 |
| `session_completed` | info | 결과 요약 | 세션 열기 |
| `mention` | info | 나를 멘션한 메시지 | 세션 열기 |

**deputy에게 나타나는 시점** (O5, FR-5.2) — `hitl_request` 항목은 처음에는 Director의 Inbox에만 있다. **기한의 절반이 지나면 deputy의 Inbox에도 나타나며** "위임됨 · 지금부터 응답 가능"으로 표시된다. §2.3의 "14:30부터 승인 가능" 툴팁과 같은 시점이고, deputy가 자기 Inbox만 보고도 알 수 있어야 하므로 알림도 함께 보낸다.

**정렬**: `overdue` → action_required → attention → info, 각 묶음 내에서 기한 임박 순.
**필터**: 미읽음 / 액션 필요 / 세션별.
**뱃지**: 내비의 Inbox에 action_required 개수만 카운트한다. info까지 세면 뱃지가 무의미해진다.

### 4.7 S9 Agents 목록 · S10 Agent 편집

**S9** — 카드마다 이름·역할·역할 설명·상태·기본 프로파일(런타임+모델)·소유자·`respond_to` 값. 필터(역할·런타임·상태·소유자). 새 에이전트, **팀 템플릿**(FR-1.4).

> `.agent.md` 가져오기/내보내기와 Build with AI는 **v1.1**이다. v1의 S9에는 폼과 템플릿만 둔다.

**S10** — 3구역.

| 구역 | 내용 |
|---|---|
| 정체성 | 이름(멘션 라벨, 워크스페이스 내 유일), 역할 태그, **역할 설명**(다른 에이전트의 로스터에 노출되므로 위임 판단 근거가 된다고 안내), 아바타 |
| instruction | 시스템 프롬프트 편집기. `lead` 역할 선택 시 "코디네이션 프로토콜이 자동 추가됩니다"를 알림(§8.3) |
| 실행 | **프로파일 목록**(추가·삭제·기본 지정) — 각 프로파일은 런타임 종류 → 모델 → 옵션(effort 등) → env/args → 폴백 프로파일. 툴·MCP 허용 목록, `max_concurrent_tasks`, `budget_per_task`, `respond_to`(+ allowlist) |

프로파일 편집기에서 **모델 목록은 데몬 probe 결과**로 채운다(§8.2). 런타임 능력에 없는 옵션(effort 미지원 등)은 비활성 + 사유 표시(§8.2.6).

**테스트 채팅** — 세션을 만들지 않고 이 에이전트와 1:1로 시험 대화. 실행 경로(ACP/CLI)와 사용 토큰을 함께 보여줘 설정이 맞는지 확인시킨다.

**`respond_to: nobody`(킬 스위치)** 는 별도 위험 영역에 두고, 켤 때 "실행 중인 턴이 취소되고 대기 중 task가 취소됩니다. 열린 HITL은 남습니다"를 확인시킨다(FR-1.9).

### 4.8 S11 Runtimes · S12 Add a computer · S13 Workdir 관리

**S11** — 머신 카드: 이름·호스트·온라인 상태·마지막 접속·데몬 버전, **감지된 CLI 목록**(종류·버전·로그인 여부·모델 수), 실행 중 task 수, **workdir 사용량 합계**, 저장소 목록(경로·remote URL·브랜치·클린 여부 — FR-9). 오프라인이면 경과 시간과 **7일 유예 잔여**를 표시하고, 유예를 넘기면 "이 런타임에 묶인 세션 N개가 일시정지됨"과 재바인딩 링크를 띄운다(FR-9.2).

삭제는 활성 세션이 없을 때만 가능하고, 있으면 차단 사유와 대상 세션을 나열한다.

**S12** — 설치 명령 2줄(복사 버튼)과 **연결 대기 상태**. `대기 중 → 연결됨 → CLI 감지 중 → 준비 완료` 단계를 실시간으로 보여준다. 3분 이상 연결되지 않으면 문제 해결 안내(방화벽·권한·CLI 미설치)를 편다.

**S13** — workdir 목록: 종류(worktree/container/dir), 소유 에이전트 또는 lane, 경로·브랜치, 용량, 마지막 사용, **보존 만료일**, 상태(활성/보존 중). 수동 삭제 가능하되 **미병합 커밋이나 미커밋 변경이 있으면 경고하고 기본 차단**한다(FR-6.4 M4). 워크스페이스 용량 상한 대비 사용률을 상단에 표시한다.

### 4.9 S17 런타임 재바인딩 다이얼로그 (O1)

**진입**: S11 런타임 카드의 "세션 재바인딩", 또는 S7 `paused` 배너의 "다른 런타임으로 재바인딩".
**권한**: Director (세션 단위). 여러 세션이 걸렸으면 세션마다 따로 결정한다.

데이터 유실을 다루므로 다이얼로그지만 명세가 필요하다(FR-9.2).

| 구역 | 내용 |
|---|---|
| 상황 | "MacBook이 9일간 오프라인입니다. 이 세션은 8월 28일부터 일시정지 상태입니다" |
| 대상 선택 | 격리 방식이 후보를 제한한다 — `none`은 **온라인 런타임 전부**, `worktree`는 **remote URL이 같은 저장소를 가진 런타임만**(경로 문자열이 아니라 remote URL로 판정, F). 후보가 없으면 그 사실과 이유를 표시 |
| **유실 경고** | `worktree`일 때만: "완료된 lane의 코드도 원래 머신의 브랜치에만 있습니다. 새 머신에서는 이 세션의 diff 아티팩트 N개를 순서대로 적용해 복구합니다. 커밋 이력은 복원되지 않습니다" |
| 선택 | **재바인딩** / **세션 종료(`cancelled`)** / 취소 |

재바인딩을 실행하면 새 workdir을 만들고 첫 프롬프트에 아티팩트 재적용 지시를 넣는다. 진행 중이던 lane은 콜드 스타트한다(`runtime_session_ref`가 무효이므로). 아티팩트·메시지·결정 기록은 서버에 있어 대화 맥락은 온전하다.

### 4.10 S14 Settings

| 탭 | 내용 | 권한 |
|---|---|---|
| 멤버 | 목록·초대·역할 변경·제거 | owner·admin |
| 런타임 정책 | 데몬 동시 task 상한, 런타임별 상한 | owner·admin |
| 예산 | 워크스페이스·세션 기본 상한, 초과 시 동작 | owner·admin |
| 루프 상한 | `max_chain_depth`(8) · `max_hops_per_hour`(60) · `max_pair_roundtrips`(5) | owner·admin |
| 컨텍스트 | `context_reuse.max_summary_tokens`(2000) · 아티팩트 포함 여부 | owner·admin |
| Workdir | 기본 격리 방식, `workdir_retention_days`(14) · `workdir_disk_quota_gb` · `runtime_offline_grace`(7일) | owner·admin |
| 보안 | **활동 로그 페이로드 마스킹**(diff·셸 출력을 요약만 저장) | owner |
| 알림 | 이메일·푸시, 세션 구독 기본값(전부/HITL만/종료만) | 개인 |

기본값은 모두 PRD의 값을 그대로 쓴다. 각 항목에 **바꿨을 때의 영향**을 한 줄로 적는다(예: 루프 상한을 낮추면 정상 위임이 막힐 수 있음).

---

## 5. 공통 컴포넌트

여러 화면에 반복되는 요소는 한 번만 정의한다. **캔버스에서는 이 표의 항목이 `reusable` 컴포넌트로 존재한다** — id·오버라이드 키·슬롯은 `COMPONENTS.md`에 있다. 화면을 추가할 때 손으로 그리지 말고 인스턴스를 쓴다.

| 이 표의 항목 | 캔버스 컴포넌트 |
|---|---|
| 에이전트 칩 | Agent Chip `MmxsR` |
| 상태 배지 | Badge `Tfz9o` (Lane Card·Message Card·HITL·Inbox Item·Condition Row·Profile Row 안에 중첩) |
| HITL 카드 | HITL Card `n9PqY` (타임라인) · Inbox Item `T0qdqP` (인박스) — 입력부 구조는 같고 껍데기가 다르다 |
| 비용 표시 | (컴포넌트 없음 — 텍스트 + 바) |
| 멘션 입력기 | (컴포넌트 없음 — 작성창은 화면당 하나) |
| 활동 피드 항목 | Activity Feed `h6pub` (Message Card 슬롯 안) |
| 권한 비활성 버튼 | 각 컴포넌트 버튼의 `opacity:0.4` 오버라이드 + 안내 텍스트 슬롯 |
| 확인 다이얼로그 | (컴포넌트 없음 — S17만 존재) |
| — | Lane Card `x1YCq` · Condition Row `XMNop` · Profile Row `ZWC8q` · Status Banner `zPJly` (이 표에 없던 것. §4.5·§4.4·§4.7에서 파생) |

| 컴포넌트 | 쓰이는 곳 | 규칙 |
|---|---|---|
| **에이전트 칩** | 참여자, lane 카드, 메시지, 로스터 | 아바타 + 이름 + 상태 점. 클릭 시 프로필 팝오버(역할 설명·프로파일·현재 작업) |
| **상태 배지** | 세션·lane·task·에이전트·런타임 | 상태별 색·아이콘을 **전 화면 통일**. **인박스 심각도 배지의 색은 심각도가 아니라 항목의 원인 상태를 따른다** — `attention`이 `session_paused`면 분홍, `runtime_offline`이면 빨강. 글리프(`! ▲ i`)가 심각도를 말하고 색은 원인을 말한다(리뷰 #03 N4). 같은 단어가 다른 층에서 다른 뜻이면(예: `paused`) 층을 함께 표기. **글리프가 1차 신호, 색은 보조**(디자인 리뷰 #01 §7) — 6색으로는 `paused`/`failed`가 색상환 27°밖에 안 떨어져 색각 이상에서 구분 불가. 글리프: running ● · waiting_human ⏳ · blocked ? · **paused ⏸** · done ✓ · **failed ✕** · queued/idle ○ · disabled ⊘. `paused`는 테두리만(soft), `failed`·`offline`은 채움(solid)으로 무게를 나눈다. 텍스트에 상태색을 쓸 때는 **예외 없이** 짙은 `-text` 변형을 쓴다(`$s-run-text` `$s-wait-text` `$s-block-text` `$s-pause-text` `$s-done-text` `$s-fail-text`). 원색 중 일부는 대비를 통과하지만, 규칙에 예외를 두면 잊힌다(m4). 인박스 심각도 글리프: `action_required` **!** · `attention` **▲** · `info` **i** — 뱃지가 action_required만 세는 규칙과 시각적으로 맞춘다(N4). `idle`은 회색 ○("할 일 없음", `done`의 초록이 아님 — N5) |
| **HITL 카드** | Inbox, 세션 타임라인 | 와이어프레임에서는 **껍데기가 다르다**(HITL Card `n9PqY` / Inbox Item `T0qdqP`) — 인박스는 세션명·심각도가 더 필요하다. 질문·제안 기본값·기한·권한·버튼의 **본문 구조는 같아야** 하며, 하이파이에서 그 본문을 하위 컴포넌트로 빼서 둘 다 품게 한다(F2: 인박스에서 맥락 없이 답한다). 디자인 리뷰 #03 K2 |
| **비용 표시** | 세션 목록·상세, 런타임, task | 누적/상한 + % + 추정 배지. 추정이면 반드시 배지 |
| **멘션 입력기** | 세션 작성창, 테스트 채팅 | 자동완성 → `mention://` 링크 삽입. 트리거 미리보기 연동 |
| **활동 피드 항목** | 세션 타임라인 | §4.5의 렌더 규칙. 클래스별 아이콘 고정 |
| **권한 비활성 버튼** | 전역 | 숨기지 않고 비활성 + 툴팁으로 사유와 조건("Director만 가능", "14:30부터 가능") |
| **확인 다이얼로그** | 파괴적 동작 | 무엇이 사라지는지 명시. 되돌릴 수 없으면 그렇다고 씀 |

---

## 6. 실시간 갱신

PRD가 WebSocket/SSE를 전제하므로(§8.1), 화면마다 무엇이 실시간인지 정한다.

| 화면 | 실시간 대상 |
|---|---|
| S5 Sessions | 상태 배지, 비용, 주의 배지, 마지막 활동 |
| S7 Session 상세 | 메시지 스트리밍(토큰 단위), **에이전트 타이핑 표시**, lane 카드 상태, 활동 피드 제자리 갱신, 참여자 상태, 비용, 종료 조건 진행률 |
| S8 Inbox | 항목 추가·해소, 뱃지 |
| S11 Runtimes | 온라인 상태, 실행 중 task 수, CLI 감지 |
| S13 Workdir 관리 | 용량·상태 — GC 실행 결과가 반영되어야 한다(m9) |
| S12 Add a computer | 연결 단계 |

**연결이 끊기면** 상단에 "실시간 연결 끊김 · 재연결 중"을 띄우고, 복구되면 놓친 이벤트를 채운다. 끊긴 채로 조용히 낡은 화면을 보여주지 않는다(§1 원칙 3).

---

## 7. 빈 상태와 오류

| 상황 | 화면 | 표시 |
|---|---|---|
| 런타임 0개 | S5·S6 | "먼저 컴퓨터를 연결하세요" + S12 링크. 세션 생성 비활성 |
| 에이전트 0개 | S6 4단계 | 템플릿 3종을 카드로 제시 |
| 세션 0개 | S5 | 첫 세션 안내 + 시나리오 예시 |
| Inbox 비어 있음 | S8 | "지금 답할 것이 없습니다" (부정형이 아니라 상태로) |
| lane 0개 | S7 | "아직 위임이 없습니다. assignee가 계획을 세우는 중입니다" |
| 활동 피드 없음 | S7 | 런타임이 구조화 이벤트 미지원이면 그 사실을, 아니면 "대기 중" |
| 런타임 오프라인 | S7·S11 | 경과 시간 + 유예 잔여 + 재바인딩 경로 |
| 권한 없음 | 전역 | 비활성 + 사유. 화면 자체를 숨기지 않음 |

---

## 8. PRD 대조 결과

### 8.1 PRD 기능 → 화면 매핑

| PRD | 화면 | 확인 |
|---|---|---|
| FR-1.1~1.6 에이전트 등록·프로파일 | S10 | ✅ |
| FR-1.3 에이전트 상태 6종 | S7 좌열, S9 | ✅ `offline`/`disabled` 구분 |
| FR-1.7 `.agent.md` | — | v1.1 |
| FR-1.9 `respond_to`·킬 스위치 | S10 위험 영역 | ✅ |
| FR-2.1 세션 생성 폼 | S6 7단계 | ✅ `runtime_id` 포함 |
| FR-2.2 종료 조건 | S6 6단계, S7 우열 | ✅ v1 4종 |
| FR-2.3 세션 상태 | S5 배지, S7 상단 | ✅ `paused` 사유 표시 |
| FR-3.2 멘션 문법 | 멘션 입력기 | ✅ |
| FR-3.3 라우팅 8규칙 + lane 해소 | 트리거 미리보기 | ✅ 결과만 노출 |
| FR-3.4 취소는 UI 전용 | S7 lane 카드 2동작 | ✅ 권한 제한 |
| FR-3.5 루프 상한 | S14 | ✅ |
| FR-4.3 아티팩트 | S7 우열 | ✅ |
| FR-5.1~5.4 HITL | S8, S7 HITL 카드 | ✅ v1은 `question`·`approval` 2종 |
| FR-5.2 deputy 위임 알림 | S8 (기한 절반 후 노출) | ✅ O5 |
| **FR-5.3 사람 역할** | §2.2·§2.3 | ✅ **PRD v0.9 기준** — 멤버 게시 가능 |
| FR-6.2 lane 상태 **7종** | S7 lane 보드 | ✅ `blocked`·`paused` 포함 (PRD v0.9 P5) |
| FR-6.2.1 질문 경로 | `blocked_q` 카드, Inbox `lane_blocked` | ✅ |
| FR-6.4 workdir·GC | S13 | ✅ 삭제 차단 조건 |
| FR-7.2 활동 피드 | S7 중앙 | ✅ v1 클래스 |
| FR-7.3 비용·예산 | 비용 컴포넌트, S7 우열 | ✅ 추정 배지 |
| FR-8 인박스 7종 | S8 | ✅ |
| FR-9 런타임·probe | S11 | ✅ 저장소 정보 |
| FR-9.2 재바인딩 | **S17 다이얼로그** | ✅ O1 — 후보 제한·유실 경고·두 선택 |
| FR-3.6 트리거 억제 | S7 작성창 미리보기 칩 × | ✅ O4 |
| FR-1.5 참여자 추가 | S7 상단 액션 | ✅ O2 |
| FR-3.4 B task 정체성 | S7 lane 카드 펼침 | ✅ O3 |
| FR-1.8.1 테스트 채팅 | S10 | ✅ PRD v0.9에서 정의됨 |

### 8.2 화면 설계에서 새로 드러난 확인 필요 사항

PRD에 정의가 없어 화면을 그리려면 정해야 하는 것들이다. **UI 결정이 아니라 제품 결정**이므로 PRD에 반영이 필요할 수 있다.

> v0.1에서 올린 질문 중 **PRD 쪽 공백 7건(P1~P7)은 PRD v0.9에 반영됐다.** 특히 P7(사람 역할)은 Q1의 답이기도 하다 — 워크스페이스 멤버는 열람과 **게시 모두 가능**하다. 아래 표는 아직 남은 것만 담는다.

### 8.3 v1 범위에서 화면이 빠지는 항목

컷 순서(§10)가 발동하면 화면도 함께 줄어든다. 미리 대응을 정해둔다.

| 컷 | 화면 영향 |
|---|---|
| 1. 활동 피드 축소 | S7 중앙의 펼침이 메시지·플랫폼 조작·오류 + 원본 레일만. 파일 편집·셸은 원본 레일로 |
| 2. 자동 종료 제거 | S7 우열의 종료 조건 진행률이 체크리스트(정보 표시)로만 남고, 종료는 상단 `manual` 버튼 |
| 3. `worktree` 제거 | S6 5단계가 격리 선택 없이 `none` 고정. **S13 Workdir 관리와 S17 재바인딩 다이얼로그가 통째로 빠진다** (재바인딩의 복잡성은 대부분 worktree 때문이다 — `none`은 다른 머신으로 옮기면 그만) |

---

### 8.4 변경 이력 (v0.2 → v0.3)

와이어프레임 리뷰 두 번(`design/DESIGN_REVIEW_01.md`, `design/DESIGN_REVIEW_02.md`)이 이 문서로 되돌린 항목이다. 캔버스가 문서보다 먼저 옳았던 경우가 둘 있다(C4, N1).

| 출처 | 변경 | 위치 |
|---|---|---|
| 디자인 리뷰 #01 C4 | `run_failed` 인라인 동작 "재시도" → **"다시 지시"** — §4.5가 이미 "일반 재시도 버튼은 두지 않는다"고 했는데 §4.6 표가 모순이었다. 캔버스가 옳았다 | §4.6 |
| 디자인 리뷰 #01 C1 → #02 C1′ | 참여자 칩의 에이전트 상태를 **task에서 파생되는 계산값**으로 정의(PRD FR-1.3 v0.10). #01 반영 시 넣은 "집계하지 않는다" 문장이 FR-5.2와 모순이어서 #02에서 파생 규칙으로 교체 | §4.5 |
| 디자인 리뷰 #01 §7 | 상태 배지 **글리프 1차·색 보조** 원칙, soft/solid, `-text` 변형 | §5 |
| 디자인 리뷰 #02 C4′ | 마법사 순서 **격리 → 런타임 → 참여자**. 캔버스 S6-3의 되돌림 주의문이 순서 오류의 증거였다 | §4.4 |
| 디자인 리뷰 #02 N1 | task 이력은 "정보 5종"이고 열 배치는 폭에 맞춘다(좌열은 2줄 행) | §4.5 |
| 디자인 리뷰 #02 N4·N5·m4 | 인박스 심각도 글리프 `! ▲ i`, `idle` 회색, `-text` 변형은 예외 없이 | §5 |

| # | 질문 | 왜 화면에서 걸리나 | 잠정안 |
|---|---|---|---|
| ~~Q1~~ | ~~비참여 멤버 열람~~ | **PRD v0.9 FR-5.3에서 해결** — 워크스페이스 멤버는 열람·게시 모두 가능, 비공개 세션은 v1.1 | ✅ 반영됨 |
| Q2 | **세션 삭제가 가능한가** | S5에 삭제 동작을 둘지 결정 필요. PRD 상태 머신에 `deleted`가 없다 | v1은 삭제 없음. `cancelled` 후 보관만 |
| Q3 | **에이전트 아카이브 후 과거 세션 표시** | `archived_at`은 스키마에 있으나 UI 규칙이 없다 | 과거 메시지의 칩은 유지하고 "보관됨" 표시. 신규 초대 목록에서 제외 |
| ~~Q4~~ | ~~테스트 채팅이 세션인가~~ | **PRD v0.9 FR-1.8.1에서 해결** — 세션 아님, `colab` 토큰 미발급, 임시 workdir | ✅ 반영됨 |
| Q5 | **푸시 알림이 v1인가** | FR-5.2가 "이메일/푸시 알림 옵션"을 이미 말한다(m2). 남은 것은 푸시의 v1 포함 여부뿐 | 웹 + 이메일은 v1, **푸시는 v2** |
| Q6 | **모바일 대응 범위** | 3열 레이아웃의 축소 규칙이 필요 | v1은 데스크톱 우선, Inbox 응답만 모바일 웹에서 동작(F2 지표가 30분이므로 이동 중 응답이 필요) |
| Q7 | **에이전트 수정·삭제 권한** | FR-1.5는 "Agents 화면(사람)에서만"까지만 정하고 누가 남의 에이전트를 고칠 수 있는지 말하지 않는다. §2.3 표가 임의로 "소유자만"을 넣었다(m3) | 소유자 + workspace admin 이상. 남의 에이전트 수정은 `activity_log`에 남긴다 |

## 9. 다음 단계

1. **상태 배지·색 체계를 디자인 토큰으로 먼저 확정한다.** 세션·lane·task·에이전트·런타임 다섯 층이 같은 어휘(`paused`·`waiting_human`·`failed`)를 쓰므로 나중에 바꾸면 전 화면에 번진다. 특히 `paused`(사람의 판단 대기)와 `failed`(끝남)의 시각적 거리가 멀어야 한다(C2).
2. **S7(Session 상세)을 와이어프레임으로 만든다.** 나머지 화면이 여기서 파생되는 규칙(상태 배지·권한 비활성·실시간·HITL 카드)을 공유한다. lane 보드 → 카드 펼침(task 이력) → 활동 피드 3단 깊이가 실제로 읽히는지가 관건이다.
3. **S12(Add a computer)의 연결 대기 흐름을 별도로 프로토타이핑한다.** F1의 최대 이탈 지점이고, 성공 지표(15분)가 여기서 갈린다.
4. 남은 Q2·Q3·Q5·Q6·Q7을 결정한다. Q7(에이전트 수정 권한)은 PRD FR-1.5에 한 줄 추가가 필요하다.
5. 컷 순서(§8.3)가 발동하면 화면 범위도 함께 줄인다. 특히 컷 3(`worktree` 제거)은 **S13과 S17이 통째로 빠진다** — 와이어프레임 착수 전에 스파이크 1 결과를 확인한다.
