# 캔버스 컴포넌트 카탈로그

| 항목 | 내용 |
|---|---|
| 문서 버전 | **v0.19.2** — 구조 전환(방·미션) 반영 + V19-GAP(PRD #280) 라벨 정정. §1~§7 은 **세션 모델 P0 와이어프레임의 기록**이고, **§8·§9 가 현행 규약이다** |
| 대상 | `agent-collaboration.pen` 와이어프레임 **v0.4** (P0, **세션 모델**) + `web/` 구현 |
| 근거 | `PRD.md` **v0.19** §3.2 화면 용어(정본) · `SCREEN.md` **v0.19.2** §5 공통 컴포넌트, `design/DESIGN_REVIEW_01.md` §6.2 · `design/DESIGN_REVIEW_02.md` §4 (컴포넌트화 요청). 리뷰·후속 조치 이력은 `design/` |
| 작성일 | 2026-09-04 (v0.19.1: 2026-09-22 — SCREEN 검증 3건 반영 · v0.19.2: 2026-09-23 — `manual` 라벨 「직접 멈춤」, 루프는 방 사유만) |
| id 신뢰 범위 | **v0.4부터 인스턴스 id는 이 문서와 캔버스만 신뢰한다.** 후속 #01·#02에 적힌 인스턴스 id(`NnWRc`, `e1Lzj` 등)는 자기 교체로 전부 바뀌었으므로 이력으로만 읽는다(K4). 컴포넌트 **정의** id는 바뀌지 않는다 |
| 목적 | 캔버스의 `reusable` 컴포넌트 11개를 **id·오버라이드 키·슬롯·사용처**까지 기록해, 다음 사람이 화면을 추가하거나 카드를 고칠 때 손으로 그리지 않고 인스턴스를 쓰게 한다 |

> **v0.19.1 에서 머리를 올린 이유**(SCR-C §2.6). 이 문서의 근거 줄은 오래 `SCREEN.md` **v0.3** 이었고 본문은 `S6 마법사`·`세션 완료 summary`·`걸린 세션`을 그대로 인용한다. 그런데 SCREEN v0.19 §5 는 이 카탈로그의 **캔버스 id**(`x1YCq`·`n9PqY`·`zPJly` …)를 계속 인용한다 — **한쪽이 다른 쪽을 id 로만 가리키고 내용 대조는 아무도 안 하는 구조**이고, 그것이 D-11(「산출물」 vs 「아티팩트」)을 낳았다. **§1~§7 의 문구·화면 이름은 세션 모델 시절의 기록으로 읽고, 실제 규약은 §8(문구·글자·색·레이아웃)과 §9(v0.19 신규 컴포넌트)에서 읽는다.** 문구의 정본 순서는 **PRD §3.2 → 이 문서 §8.4 → `SCREEN.md`** 다.

> **이것은 디자인 시스템이 아니다.** 토큰(색 18개 + 글꼴 1개, `X:` 임포트 제외)과 도메인 컴포넌트만 있다. 기초 컴포넌트(버튼·입력·탭)는 임포트된 `X:` nitro 라이브러리에 있지만 와이어프레임에서는 쓰지 않았다. 하이파이로 갈 때 "nitro를 기초로 쓸지"를 결정한다.

---

## 1. 무엇이 바뀌었나

v0.3까지 lane 카드·HITL 카드·인박스 항목·메시지 카드·조건 행·프로파일 행·배너는 **화면마다 손으로 그린 개별 프레임**이었다. 같은 카드가 S7·S7-P·S7-D에 3벌씩 있어서, 리뷰 #02의 task 이력 수정을 두 번 따로 했다.

v0.4에서 이것들을 `reusable` 컴포넌트로 올리고 **모든 화면을 인스턴스로 재조립**했다. 손으로 그린 카드는 남아 있지 않다(검증: 정규식 전수 탐색 0건). 이제 Lane Card 정의 하나를 고치면 21개 인스턴스에 반영된다.

| 컴포넌트 | id | 인스턴스 | 쓰이는 곳 |
|---|---|---|---|
| Badge | `Tfz9o` | 28 + 컴포넌트 내부 | 전 화면 상태 배지 (v0.2부터) |
| Agent Chip | `MmxsR` | 12 | S7 3종 참여자 |
| App Nav | `D6yb65` | 13 | 앱 셸 화면 전부 |
| **Lane Card** | `x1YCq` | 21 | S7 3종 lane 보드 (7장씩) |
| **Message Card** | `CYRKG` | 18 | S7 3종 타임라인 (6장씩) |
| **HITL Card** | `n9PqY` | 3 | S7 3종 타임라인 |
| **Inbox Item** | `T0qdqP` | 8 | S8 |
| **Condition Row** | `XMNop` | 7 | S6 (5), S7 우열 (2) |
| **Profile Row** | `ZWC8q` | 2 | S10 |
| **Status Banner** | `zPJly` | 3 | S7-P paused, S6 경고, S17 유실 경고 |
| **Activity Feed** | `h6pub` | 3 | Message Card의 슬롯 안 (S7 3종 Lead 메시지) |

캔버스 위치: 기존 3개는 (0,0)~(0,140), 범례는 x=300. 새 8개는 **x=1700부터 시작하는 컴포넌트 선반**에 세 줄로 놓았고 각 정의 위에 이름·id 라벨이 있다(`Component Labels` 프레임). 컴포넌트는 항상 화면(y≥1120) 위에 둔다.

| 줄 | y | 컴포넌트 (왼쪽부터) |
|---|---|---|
| 1 | 60 | Lane Card · Message Card · HITL Card · Activity Feed |
| 2 | 420 | Inbox Item · Condition Row |
| 3 | 600 | Profile Row · Status Banner |

> v0.4 초판은 범례 오른쪽 x=1194에 두었는데 범례가 1317px로 넓어지면서 Lane·Message·HITL Card를 덮었다(리뷰 #03 직후 사용자 지적). 최상위 프레임끼리의 겹침은 자동 검사로 0건 확인.

---

## 2. 컴포넌트별 명세

오버라이드는 인스턴스의 `descendants` 맵에 **노드 id**를 키로 넣는다. 컴포넌트 안의 Badge처럼 중첩된 ref는 `"내부refId/배지노드id"` 경로로 접근한다. 부분을 끄려면 `{enabled:false}`, 켜려면 `{enabled:true}` — 정의에서 꺼둔 슬롯은 인스턴스에서 켜야 보인다.

### 2.1 Lane Card `x1YCq`

lane 보드의 카드. 7개 lane 상태를 하나의 정의로 표현한다. 폭 268(좌열), 인스턴스는 `width:"fill_container"`.

| 부분 | id | 기본 | 오버라이드 |
|---|---|---|---|
| 에이전트 이름 | `R3fKpX` | 켬 | `content` |
| 상태 배지 (Badge ref) | `HF1VS` | 켬 (기본 `queued` ○ 회색 — 오버라이드를 빠뜨려도 "실행 중"으로 오독하지 않게, m4) | `HF1VS/zlCEw` 라벨 · `HF1VS/IOnJ0` 색 · `HF1VS/EnjHf` 글리프+색. soft(paused): `HF1VS: {fill:$bg, stroke:색, strokeWidth:1.5}`. solid(failed): `HF1VS: {fill:색, stroke:색}` + 라벨·글리프 `fill:$bg` |
| 요약 | `PDkU3` | 켬 | `content` |
| 부가 정보 | `bXrz6` | **끔** | `{enabled:true, content, fill}` — 상태색 `-text` 변형 |
| 동작 묶음 | `b7sPL` | **끔** | `{enabled:true}` |
| 버튼 1·라벨 | `mEolH` · `WmIHX` | 켬 | 라벨 `content`. 비활성 표현은 `mEolH: {opacity:0.4}` |
| 버튼 2·라벨 | `jphJo` · `ac2d1` | 켬 | 버튼 하나만 쓰면 `jphJo: {enabled:false}` |
| task 이력 슬롯 | `wG4Pf` | **끔** | `{enabled:true}` 후 `Insert(인스턴스id+"/wG4Pf", …)` 로 2줄 행을 넣는다 (§3) |

상태별 조합:

| 상태 | 배지 | 부가 | 버튼 |
|---|---|---|---|
| `queued` | ○ `$ink-3` | — | — |
| `running` | ● `$s-run` | (deputy 시점: "취소는 즉시 가능") | 중단하고 다시 지시 · 중단 |
| `blocked` | ? `$s-block` | "? @위임자의 답을 기다림" | — |
| `paused` | ⏸ soft | "계속 진행 승인 필요" | 계속 진행 승인 |
| `waiting_human` | ⏳ `$s-wait` | "⏳ Director 승인 대기 · Inbox" | 응답하러 가기 |
| `done` | ✓ `$s-done` | — | — |
| `failed` | ✕ solid | 실패 분류·재시도 소진 | 다시 지시 |

### 2.2 Message Card `CYRKG`

타임라인 메시지. `text`·`system`·`blocked_q`·`answer` kind를 배지와 테두리로 구분한다. 폭 580, 인스턴스 `fill_container`.

| 부분 | id | 기본 | 오버라이드 |
|---|---|---|---|
| kind 배지 (Badge ref) | `YHA5b` | **끔** | `{enabled:true}` + `YHA5b/zlCEw` `YHA5b/IOnJ0` `YHA5b/EnjHf` |
| 작성자 | `J0mWUr` | 켬 | `content` |
| 메타 | `rIri9` | 켬 | `content` |
| 본문 | `YydN1` | 켬 | `content` |
| 슬롯 | `XMTib` | **끔** | `{enabled:true}` 후 `Insert(인스턴스id+"/XMTib", Activity Feed ref)` |

카드 자체 오버라이드: 질문 카드(`blocked_q`)는 인스턴스에 `padding:12, fill:$bg, stroke:$s-block, strokeWidth:1`.

본문(`YydN1`)은 **마크다운**이다(PRD FR-3.1, 2026-09-15 Director 지적 · W-12). 웹은 `web/lib/markdown.tsx`(의존성 없음 · React 요소만)로 문단·`#`~`###` 제목·굵게/기울임/인라인 코드·코드 블록·목록(1단 중첩)·인용·표·http(s) 링크를 그리고, 멘션 링크는 기존 칩 그대로다. 메시지 안 제목은 `--fs-card` 이하(§8.2 안), 코드 블록·인용·표의 배경·테두리는 `--surface`·`--line` 토큰만(§8.3 두 벌). HTML 태그는 문자 그대로(XSS 0). 시스템·요약·「작성 중…」 델타(열린 펜스는 열린 채로)도 같은 렌더러, HITL 질문은 인라인 문법만.

kind별 조합 (K3):

| kind | Kind 배지 | 카드 오버라이드 | 슬롯 |
|---|---|---|---|
| `text` (사람·에이전트 일반) | 끔 | 없음 (투명) | 에이전트 메시지면 Activity Feed |
| `system` | `system` · `$ink-3` | 없음 | — |
| `blocked_q` (질문 카드, 스레드 루트) | `질문 → @위임자` ? `$s-block` | `padding:12, fill:$bg, stroke:$s-block` | — |
| `answer` (질문 답글) | `answer` ↳ `$ink-3` | 없음 | — |
| `summary` (세션 완료) | `summary` ✓ `$s-done` | `fill:$surface` | — (v1 화면에 예시 없음) |

### 2.3 HITL Card `n9PqY`

승인·질문 요청 카드. S7 타임라인과 S8 인박스가 **같은 컴포넌트를 쓰지는 않는다** — 인박스는 세션명·심각도가 더 필요해 Inbox Item이 따로 있다. 실제로 본문 구조도 같지 않다(여기는 `g71PvC` 제안 기본값 슬롯, Inbox Item은 `fDXjQ` 부가 텍스트로 대신). **하이파이에서는 HITL 본문(질문·제안·기한·버튼)을 하위 컴포넌트로 빼서 둘 다 품게 한다** — F2(인박스에서 맥락 없이 답한다)의 원래 의도다. SCREEN §5도 그렇게 고쳤다(리뷰 #03 K2). 폭 580.

| 부분 | id | 기본 | 오버라이드 |
|---|---|---|---|
| kind 배지 | `cHpam` | 켬 | `cHpam/zlCEw` (`HITL · approval` / `HITL · question`) |
| 작성자 | `Lqg7w` | "시스템" | 에이전트 질문이면 에이전트명 |
| 메타 | `i2BcH` | | `content` — 시스템 발행이면 "… 때문에 발행 · source: system" |
| 질문/대상 | `JzPyQ` | | `content` |
| 제안 기본값 | `g71PvC` | **끔** | `{enabled:true, content}` — `question` 타입은 필수(FR-5.1) |
| 기한·권한 | `cmq2b` | | `content` |
| 버튼 1(주)·라벨 | `qw6Mj` · `wE8wV` | 켬 | 라벨. 비활성 `qw6Mj: {opacity:0.4}` |
| ~~버튼 옆 메모~~ | ~~`V55koW`~~ | — | 용도 없어 **삭제**(리뷰 #03 K1). 버튼 옆 짧은 안내는 `W7gS3` 권한 안내로 |
| 버튼 2·라벨 | `r7AS3m` · `X7yhTl` | "거절" | |
| 권한 안내 | `W7gS3` | **끔** | `{enabled:true, content}` — deputy 시점의 "🔒 22:31부터 승인 가능" |

### 2.4 Inbox Item `T0qdqP`

인박스 항목 7종을 하나로. 폭 1172.

| 부분 | id | 기본 | 오버라이드 |
|---|---|---|---|
| 심각도 배지 | `flvU9` | | `flvU9/zlCEw` `flvU9/IOnJ0` `flvU9/EnjHf` — `action_required` **!** · `attention` **▲** · `info` **i** |
| 종류 | `SzQ6z` | | `content` |
| 세션 | `chBBl` | | `content` ("· 세션명") |
| 기한 | `ZHNoQ` | | `content`. overdue면 `fill:$s-fail-text, fontWeight:700` |
| 본문 | `I6r6T` | | `content` |
| 부가 | `fDXjQ` | **끔** | `{enabled:true, content}` |
| 버튼 1(주) | `N1gvMt` · `mYkDk` | 켬 | 라벨 |
| 버튼 2 | `wocsZ` · `JZCMt` | **끔** | `{enabled:true}` + 라벨 |
| 버튼 3 | `bIbO0` · `w4w1Uf` | **끔** | 〃 |

카드 오버라이드: `action_required`는 인스턴스에 `stroke:심각도색, strokeWidth:2`.

타입별 조합 (K3). **심각도 배지의 색은 항목의 원인 상태를 따른다** — 글리프가 심각도, 색이 원인(리뷰 #03 N4). 다음 사람이 `attention`을 빨강으로 통일하려 들지 않도록 규칙으로 둔다.

| 타입 | 심각도 · 글리프 · 색 | 버튼 | 부가 |
|---|---|---|---|
| `hitl_request` approval | action_required ! `$s-wait` | 계속 진행 승인 · 거절 · 세션 열기 | deputy 응답 가능 시점 |
| `hitl_request` question | action_required ! `$s-wait` | 답변 보내기 · 세션 열기 | 에이전트 제안값 + autonomy 결과 |
| `lane_blocked` | action_required ! `$s-block` | 답글 작성 · 세션 열기 | — |
| `session_paused` | attention ▲ `$s-pause` | 계속 진행 승인 · 세션 열기 | 재개 방식 |
| `runtime_offline` | attention ▲ `$s-fail` | 재바인딩 · 세션 종료 | — |
| `run_failed` | attention ▲ `$s-fail` | 다시 지시 · 세션 열기 | — |
| `mention` | info i `$s-run` | 세션 열기 | — |
| `session_completed` | info i `$s-done` | 세션 열기 | — |

### 2.5 Condition Row `XMNop`

종료 조건 한 줄. S6 마법사(테두리 있음)와 S7 우열 진행률(테두리 없음) 둘 다 쓴다. 폭 808.

| 부분 | id | 기본 | 오버라이드 |
|---|---|---|---|
| 체크 | `V8cDma` | ☐ | `content` (☑ / ☐ / —) |
| 이름 | `VLDQ9` | | `content`, S7에서는 `fontSize:12` |
| 설명 | `oKmnM` | | `content`, S7에서는 `fill:$ink-3` |
| 옵션 드롭다운 | `NayQT` · `QphSd` | **끔** | `{enabled:true}` + 라벨 |
| v1.1 배지 | `GzFvX` | **끔** | `{enabled:true}` |

S7 우열용 오버라이드: `stroke:"#00000000", padding:[2,0]`. 선택된 조건은 `stroke:$ink`, 비활성(v1.1)은 `opacity:0.45`.

### 2.6 Profile Row `ZWC8q`

에이전트 편집의 런타임 프로파일 한 줄. 폭 808.

| 부분 | id | 오버라이드 |
|---|---|---|
| 이름 · 런타임 · 모델 · 옵션 · 폴백 | `pPGCI` `T1edb` `epf1b` `luoSl` `Yla1S` | `content` |
| 기본 배지 | `I4fSYw` | **끔** → `{enabled:true}`. 글리프 ★ `$ink` — 상태색을 쓰지 않는다(리뷰 #02 N4, #03 m5) |

기본 프로파일 행은 `stroke:$ink`.

### 2.7 Status Banner `zPJly`

세션 `paused` 사유 배너, 마법사 경고, 재바인딩 유실 경고. 폭 268(우열), 인스턴스 `fill_container`. 정의는 주황(경고)이고 색은 인스턴스에서 바꾼다.

| 부분 | id | 기본 | 오버라이드 |
|---|---|---|---|
| 제목 | `v5FTca` | | `content`, `fill` (`-text` 변형) |
| 설명 | `KAlIg` | | `content` |
| 입력 | `t42cy` · `M7KnB` | **끔** | `{enabled:true}` + 값 — "새 상한: $30" |
| 버튼 1·2 | `kCPXF` → `f0epr`·`atFpz`, `IWssW`·`ynXhu` | **끔** | `kCPXF: {enabled:true}` + 라벨 |
| 권한 각주 | `CDR7n` | **끔** (기본 문구 "권한 각주 — 예: …") | `{enabled:true, content}` — 켜면 반드시 `content`를 넘긴다 |

색 변형 — paused: `fill:#FCE7F3, stroke:$s-pause`, 제목 `$s-pause-text`. 유실 경고: `fill:#FEE2E2, stroke:$s-fail`, 제목 `$s-fail-text`.

### 2.8 Activity Feed `h6pub`

메시지 카드 슬롯에 들어가는 "활동 보기" 펼침. 행 5개 슬롯(기본 4개 켬). 폭 580.

| 부분 | id | 오버라이드 |
|---|---|---|
| 제목 (run · 런타임 · resume) | `dMOdM` | `content` |
| 행 1~5 | `hLPBV` `qGpJs` `Tlj89` `lSHWZ` `AopVY` | 안 쓰는 행 `{enabled:false}` |
| 행 아이콘 1~5 | `BD3Sc` `TlATI` `Jvslm` `hRKb2` `tI1NY` | `content` — **이벤트 종류**다: ● 동작(플랫폼 조작·편집·셸), ○ 턴 생명주기(시작·종료). FR-7.2의 "제자리 갱신(대기 → 실행 중 → 완료)"은 같은 행의 **문장 끝 결과**로 표현하고 아이콘을 바꾸지 않는다(K6) |
| 행 문장 1~5 | `k6xMtp` `P2NA7A` `PanD2` `DDDbU` `he2xa` | `content` — "동사 · 목적어 → 결과" |
| 강등 안내 | `Bxz9s` | **끔** → 구조화 이벤트 없는 런타임에서 `{enabled:true}` (SCREEN §1 원칙 5) |

---

## 3. 슬롯에 내용 넣기 — 인스턴스 안에 Insert

Lane Card의 task 이력, Message Card의 활동 피드처럼 **인스턴스마다 다른 하위 트리**가 필요한 곳은 슬롯 프레임을 두고 인스턴스 경로로 Insert한다.

```js
inst = Insert(laneBoard, {type:"ref", ref:"x1YCq", descendants:{..., "wG4Pf":{enabled:true}}})
Insert(inst + "/wG4Pf", {type:"frame", name:"TR", layout:"vertical", ...})   // 슬롯 안에 자식 추가
Insert(msgInst + "/XMTib", {type:"ref", ref:"h6pub", descendants:{...}})    // 슬롯에 다른 컴포넌트 인스턴스
```

이 방식의 한계: 슬롯 내용은 인스턴스마다 따로 관리된다(S7·S7-D의 task 이력은 여전히 두 벌). 그래도 **카드 껍데기는 하나**라 카드 스타일 변경은 전파된다.

---

## 4. 알려진 도구 특성

- **정의의 `enabled:false` 슬롯이 `fill_container`면 검사기가 "not inside a flexbox layout" 경고를 낸다.** 꺼진 노드를 레이아웃 밖으로 보기 때문이다. 인스턴스에서 켜면 정상이다. 무해하지만 매 execute마다 8줄씩 나온다. 없애려면 슬롯 정의에서 `width`를 빼고 인스턴스에서 `{enabled:true, width:"fill_container"}`로 함께 주면 된다 — 인스턴스 40여 개를 다시 손봐야 해서 보류.
- **`Insert`로 만든 ref는 앱이 레이아웃을 돌리지 않는다 — 스크린샷도 PNG Export도 빈칸이다.** 공간은 차지하고 `ctx.bounds`도 맞지만 텍스트·중첩 배지가 그려지지 않는다. `placeholder` 토글이나 컴포넌트 정의 속성 토글로는 풀리지 않았다. **`Replace(id, Get(id, {depth:30}))` — 자기 자신으로 교체하면 즉시 렌더된다.** 이번 라운드에서 인스턴스 62개 전부에 적용했다. id가 바뀌므로 다른 곳에서 인스턴스 id를 참조하고 있으면 안 된다. 슬롯 자식이 있는 인스턴스는 `depth`를 충분히 줘야 한다 — 얕게 읽으면 하위가 `"..."`로 잘려 Replace가 그것을 그대로 쓴다.
- **정의(reusable) 프레임은 만든 직후 스크린샷에서 텍스트가 빈칸이다.** 정의의 자식을 `Update`로 한 번 건드리면(예: 기본 배지 오버라이드 변경) 그 뒤로는 렌더된다 — 리뷰 #03 반영 때 Lane Card에서 확인. 정의를 `Replace`하면 id가 바뀌어 모든 `ref:`가 끊기므로 **절대 하지 않는다**; 렌더가 필요하면 Update로 재해석을 유도한다.
- 하나의 `execute`에서 `const`로 선언한 헬퍼는 다음 호출에 남지 않는다. 8개 영역을 병렬 호출로 재조립할 때 `lane()`·`msg()`·`item()` 헬퍼를 호출마다 다시 정의했다.

---

## 5. 검증

| 항목 | 방법 | 결과 |
|---|---|---|
| 손으로 그린 카드 잔존 | 정규식 전수 탐색 (`^(Lane \|Msg \|Item \|Cond \|P default\|P fast)`) | 0건 ("Lane Board" 컨테이너 3개만 매칭 — 카드 아님) |
| 인스턴스 수 | ref 전수 집계 | 표 §1과 일치 |
| 슬롯 내용 | `resolveInstances:true`로 트리 읽기 | Lead 메시지 슬롯에 AF ref, Backend lane 슬롯에 History 4행 |
| 레이아웃 문제 | `ctx.problems` 전수 (스크롤 영역 제외) | 0 |
| 레이아웃 문제 (전체) | `ctx.problems` 전수 | S13 상태 열 `V` 1건 15px 클립(스크롤 아님) → 경로 열 320으로 축소해 해소(리뷰 #03 m6·m7) |
| 렌더 | 인스턴스 62개를 `Replace(id, Get(id,{depth:30}))`로 자기 교체 후 스크린샷 | 전부 정상. 슬롯 내용(task 이력 4행, AF 4행)도 교체를 거쳐 보존됨. 정의 프레임만 스크린샷에서 빈칸(§4) |

---

## 6. 다음 라운드에서 할 수 있게 된 것

컴포넌트가 생겨서 후속 #01 §4에 보류했던 항목의 비용이 내려갔다.

| 항목 | 방법 |
|---|---|
| O10 런타임 능력 강등 표시 | Activity Feed의 `Bxz9s` 켜고 행 4개 끄기 — 인스턴스 하나 |
| O9 실시간 연결 끊김 배너 | Status Banner 인스턴스를 앱 셸 상단에 |
| O8 빈 상태 8종 | 별도 컴포넌트 "Empty State" 하나 추가 |
| N6 lane 보드 접기 | Lane Card는 그대로, 보드를 상태별 접이식 그룹 컴포넌트로 |
| O7 참여 에이전트 다이얼로그 | 마법사 5단계(참여자) 행을 컴포넌트로 만든 뒤 재사용 |

---

## 7. 구현에서 드러난 빈칸 (v0.5, 2026-09-05 — `web/components/badge-map.ts` PR #6)

Badge를 코드로 옮기며(PLAN P0-a W) 문서가 답하지 않은 것들이다. 코드는 아래 "임시 결정"대로 두었고, 디자인 확인 후 이 표와 `.pen`을 함께 갱신한다.

| # | 빈칸 | 임시 결정 (코드) | 확인 필요 |
|---|---|---|---|
| 1 | soft 변형의 테두리 — 문서는 paused만 "테두리만"이라 했고 나머지 soft의 테두리는 무언급 | 모든 soft에 1px 같은 색 테두리, paused는 채움 없이 1.5px 테두리만 | 디자인 |
| 2 | agent `error ⚠` 글리프 — 리뷰 후속 #01에는 있으나 §2 조합표에는 없음 | ⚠ solid | 조합표에 추가 |
| 3 | task `deferred`·`dispatched`·`preparing`, session `completing` 글리프 미정의 | deferred ○ / dispatched·preparing ● / completing ● | 글리프 결정 |
| 4 | 한국어 라벨 출처 | SCREEN §4.3(세션)·§4.5(lane)·§4.6(인박스) 문구에서 선택, 없는 것은 신설(배정됨·준비 중·보류) | SCREEN에 표로 고정 |
| 5 | `disabled` 색 | neutral(`--ink-3`) — 상태색 아님 | 확인 |
| 6 | PLAN "색 19개" 표기 | 실제는 색 18 + 글꼴 1 = 변수 19 | PLAN 문구 |

---

## 8. 시인성 개선 (v0.6, 2026-09-08 — Director 실사용 지적)

Director 가 실제로 쓰면서 "시인성이 떨어진다"고 지적했다. 코드에서 잰 근거와 그에 대한 결정이다.
시안: **Colab UI 개선 시안**(디자인 캔버스, 2026-09-08) — 실제 `tokens.css`·`app.css` 값에서 뜬 현재/제안 비교.

### 8.1 무엇이 문제였나 (측정)

`web/` 전체 CSS 의 `font-size` 사용 횟수: **12px 46 · 11px 35 · 13px 26 · 14px 13 · 10px 5 · 20px 2**.
**86% 가 13px 이하**이고 10~11px 이 40 곳이다. 본문 표준(16px)은커녕 최소선(14px)에도 못 미친다.
제목 계층도 20px 하나뿐이라 그다음이 바로 13px 이다 — 무엇이 중요한지 크기로 구분되지 않는다.

### 8.2 글자 계단 (토큰으로 고정)

크기를 손으로 적지 않는다. 토큰 5개를 `tokens.css` 에 두고 컴포넌트는 그것만 쓴다.

| 토큰 | 값 | 용도 | 이전 |
|---|---|---|---|
| `--fs-title` | 24px | 화면 제목(`.h1`) | 20px |
| `--fs-card` | 17px | 카드·항목 제목 | 13~14px |
| `--fs-body` | 14px | 본문·버튼·입력 | 12px |
| `--fs-sub` | 13px | 보조 설명·부가 줄 | 11px |
| `--fs-meta` | 12px | 메타·배지 — **하한** | 10~11px |

**10px·11px 은 폐기한다.** 40 곳이지만 대부분 카드 안 메타 줄이라 일괄 치환이 된다.
행간도 함께 올린다: 여러 줄이 되는 설명은 `line-height: 1.5` 이상(지금은 기본값이라 붙어 있다).
`--fs-meta` 보다 작은 값을 새로 쓰지 않는다 — 필요하면 그 정보가 화면에 있을 이유부터 다시 본다.

### 8.3 다크 테마 (v1 범위로 승격)

`tokens.css` 주석의 "다크 테마는 v1 범위 밖"을 **철회한다**. 색 18개를 두 벌로 만든다.

- 전환: `@media (prefers-color-scheme: dark)` + `[data-theme]` 수동 지정(설정에서 시스템 따름 / 밝게 / 어둡게).
- 기본 6은 뒤집는 것이 아니라 **따로 고른다**: `--bg #0f0f11` · `--surface #18181b` · `--line #2e2e33` · `--ink #f4f4f5` · `--ink-2 #a1a1aa` · `--ink-3 #71717a`.
- **상태색 12개는 어두운 배경 전용 값이 필요하다.** 지금 값을 그대로 쓰면 `--s-run`(#2563eb)·`--s-block`(#7c3aed)이 읽히지 않는다. 어두움 쪽 제안: 진행 `#60a5fa` · 대기 `#fbbf24` · 완료 `#4ade80` · 실패 `#f87171` · 질문 `#c4b5fd` · 일시정지 `#f472b6`.
- soft 배경(`--soft-alpha` 12%)은 어두움에서 대비가 죽는다 — 어두움에서는 20% 로 둔다.
- **어두움 토큰 정정 1건(T-W9 측정)**: `--dk-ink-2` #a1a1aa 가 soft 카드 위 `--surface` 평면에서 wait 4.41·done 4.45 로 미달 → **#a6a6ae**(최악 4.68, `--bg` 위 7.92). 자물쇠가 평면을 재는 이상 "지금 그 자리가 없다"는 이유로 남기지 않는다.
- **v0.19 가 더하는 평면 둘(SCR-C §2.5)** — 자물쇠(`contrast.test.ts`)가 지금 재는 평면은 ① `--bg` 위 ② `--surface` 위 ③ soft 카드가 `--surface` 위일 때다. 여기에 **④ 선택된 미션 칩**(`--bg` 배경 + 테두리, `app-nav__item[aria-current]` 와 같은 모양 — 그 위의 상태 글리프가 새 평면)과 **⑤ 방 멈춤 배너**(soft 배경이 `--bg` 위에 직접 — 우열 미션 배너는 `--surface` 위라 같은 색쌍이 두 평면에 놓인다)를 더한다.
- **보조 줄에 `--ink-3` 를 쓰지 않는다.** 「최근 활동: 〈보고서 초안〉 · 다른 미션을 보려면 위 칩을 누르세요」 같은 우열 보조 문장은 텍스트다 — `--ink-3` 는 어두움에서 3.96:1 이라 **텍스트 금지**이고 `.muted-3` 는 이미 `--ink-2` 로 올려 두었다.
- **밝음 토큰 정정 2건(T-W7 측정)**: 밝음 팔레트에도 4.5:1 미달이 있었다 — `--s-wait-text` #b45309(soft 위 4.42) → **#92400e**(6.25), `--s-done-text` #15803d(4.39) → **#166534**(6.24). 인박스 카운트 배지는 solid 채움에 흰 글자라 3.19 였다 → **soft 배경 + `-text` 글자**로 바꾼다. `agent-collaboration.pen` 은 P0 와이어프레임이므로 이 값들과 어긋나도 SSOT 는 이 문서다.

### 8.4 문구 — 사용자의 말로

원칙 하나: **사용자의 다음 행동을 바꾸지 않는 정보는 접거나 숨긴다.** 어댑터 버전·프로토콜 번호·내부 식별자는
문제가 났을 때만 필요하므로 「자세히 보기」 안에 둔다.

| 지금 | 바꿀 말 |
|---|---|
| Workdir 관리 | 작업 폴더 |
| 실행 중 task 0 | 지금 하는 일 없음 |
| 걸린 세션 · 쓰는 중인 세션 | 쓰는 중인 **방** (R1.5 — 작업 폴더는 방×에이전트) |
| Runtimes | 연결된 컴퓨터 |
| Sessions · Inbox · Agents · Settings | **방** · 받은 요청 · 에이전트 · 설정 (R1.5 — 「세션」은 옛말) |
| Add a computer | 컴퓨터 연결 |
| ACP protocol v1 · 어댑터 0.74.0 · 브리프: … | (자세히 보기 안으로) |
| 툴 차단 수단 없음 — 프로파일의 툴 허용 목록이 강제되지 않습니다 | 도구 제한을 걸 수 없는 **컴퓨터**입니다 |
| lane · task · attempt · HITL 이 그대로 노출된 문구 | 각 화면의 말로 — **lane 은 「서브 미션」**(v0.19 §3.2 — 옛 「작업 줄기」를 대체), task 는 "할 일", HITL 은 "사람 확인" |
| **세션**(내비·제목·설명) | **방** 또는 **미션** — 문맥이 가른다. 「방」은 끝나지 않는 대화 공간, 「미션」은 끝이 있는 목표(PRD §3.2) |
| ~~아티팩트 → 산출물~~ | **바꾸지 않는다.** PRD §3.2(Director 확정 2026-09-23)가 「기존 화면 용어 그대로 — **「산출물」로 바꾸지 않는다**」로 닫았다. **「아티팩트」를 옛말 목록에 넣으면 자물쇠가 계약과 반대로 잠긴다** |
| 일시정지 · 런타임 오프라인 (S5 배지) | **일시정지 · 컴퓨터 연결 끊김** — `EVAL_USER.md` U12 가 못박은 말 |
| 런타임 | **컴퓨터** — 산문까지 전부. `EVAL_USER.md` 의 기대 문구도 이 말로 고쳤다(U12 등) |

**R1.5 화면 용어표 (v0.19 — SCREEN §3.4 (a)(b) 를 옮겨 왔다, 자물쇠 반영 완료 2026-09-24).** PRD §3.2 가 정본이고, 아래 (b) 는 표에 없는 화면 전용 파생어다. 식별자(`session_id`·`/sessions/*`·코드 변수)는 바꾸지 않는다 — 사람에게 보이는 문장만.

(a) PRD §3.2 정본

| 코드·계약 | 화면 이름 |
|---|---|
| `room` | **방** |
| `work` | **미션** |
| `lane` | **서브 미션** (옛 화면 용어 「작업 줄기」를 대체) |
| `task` | **할 일** |
| `workdir` | **작업 폴더** |
| `artifact` · `decision` | **아티팩트** · **결정 기록** |

(b) 화면 전용 파생어

| 옛 화면 문자열 | 바꾼 말 |
|---|---|
| 세션 (내비·제목·한 줄 설명) | **방** |
| 새 세션 | **새 방** |
| 세션 설정 | **방 설정** |
| 세션 취소 · 종료 · 일시정지 (S7 상단) | **미션 취소 · 미션 종료 · 미션 일시정지** — 방은 종료되지 않는다 |
| 작업 줄기 · 새 작업 줄기로 보내기 | **서브 미션 · 새 서브 미션으로 보내기** |
| 걸린 세션 · 쓰는 중인 세션 (S11·S13) | **쓰는 중인 방** |
| 목표 · 종료 조건 진행률 (S7 우열) | **미션 목표 · 미션 종료 조건** |
| 세션 비용 | **미션 비용**(미션 칸) · **방 누적 비용**(방 칸) |
| 이 세션의 diff 아티팩트 (S17 유실 경고) | **이 방의 아티팩트(diff)** |
| 보고서 제출 · 담당 에이전트 (종료 조건 편집기 · S21) | **아티팩트 제출 · 제출자** (#303 NN1) |
| 일시정지 · 예산 / 일시정지 · 컴퓨터 연결 끊김 (S5 배지) | **그대로 둔다** — U12 가 못박은 말 |
| MacBook이 7일간 **오프라인**입니다 (방 배너 · S17 상황) | MacBook이 7일간 **연결이 끊겼습니다** |

**문구는 테스트로 못박는다.** T-W7 이 `typography.test.ts` 로 px 리터럴을 0 으로 고정했듯, §8.4 표를 읽어 "옛말이 화면 문자열에 0건"을 재는 테스트를 둔다 — 없으면 이 표는 한 번 지키고 다음 판에서 조용히 풀린다(실제로 PR #188 에서 같은 파일의 옛 상수 하나가 사람 눈에만 걸렸다).

**R1.5 자물쇠가 재는 다섯 가지(v0.19)** — SCREEN §3.4(d) 와 같은 목록이다. 1·2·3·5 는 `web/lib/wording.test.ts`(「R1.5 화면 용어」 블록과 §8.4 표의 행), 1 은 서버·데몬 자물쇠(`server/internal/wording` · `daemon/internal/wording` — 같은 표)도 잰다.
1. **옛말 0건**: `세션`(단 `runtime_session_ref` 설명 제외) · `작업 줄기` · `걸린 세션` · `산출물`.
2. **새말 1건 이상**: `방`·`미션`·`서브 미션`·`아티팩트`.
3. **한 글자 차이**: 「일」 단독(「일 밖 할 일」 같은 모양)이 남아 있지 않은가.
4. **역할어 네 개**가 층과 함께 쓰이는가. — 기계로 재지 않는다(문장 뜻의 문제라 정규식이 잡지 못한다). 리뷰 항목.
5. **「멈춤」/「일시정지」의 층**이 섞이지 않는가.

> **쪼갤 자유가 없다**(SCR-B §4.4). `web/lib/mock/server-wording/*` 자물쇠가 웹 목 문장을 **`server/` Go 소스 리터럴과 글자 단위로** 대조하므로(`SERVER_ROOT` 를 `readFileSync` 로 직접 읽는다), 서버와 웹을 다른 PR 로 나누면 중간 PR 이 반드시 빨개진다. 그리고 `wording.test.ts` 의 `ROWS` 에 **`Sessions → 세션`** 행이 있어 「세션」이 **새말로 등록돼 있었다** — 옛말로 뒤집는 순간 그 행이 자기모순이 되므로 **표와 문구가 같은 PR 이어야 했다**(R1.5 에서 `Sessions → 방` 으로 뒤집었다). 규모는 화면 문자열 107 · 웹 테스트 90 블록 · 서버 Go 40파일 · e2e 셸 20파일이고, 완화책은 **커밋으로 쪼개는 것**뿐이다: (1) 자물쇠 표 교체 → (2) 웹 화면 문구 → (3) 웹 목 + 서버 Go → (4) 테스트 단언 → (5) e2e 셸 → (6) 스크린샷 재촬영.

**예외 하나 — 역할명.** `Director`·`deputy` 는 PRD·계약·API·DB enum 에 걸쳐 있어 화면만 바꾸면 용어가 둘이 된다. **Director 결정(2026-09-13): 영어 그대로 둔다.** 문구 자물쇠도 이 두 단어는 예외로 남긴다.

**역할어는 한 화면에 넷까지 선다(v0.19).** 방 층은 한국어(`방장`·`부방장`), 미션 층은 영어(`Director`·`deputy`)이고 **둘은 상하가 아니라 다른 축**이다 — 방장이 어느 미션의 Director 가 아닐 수 있고 그 반대도 된다. 문구 자물쇠는 네 단어를 모두 예외로 두되, **역할을 말하는 문장은 층을 함께 적는다**: 「서연 · **방장**」 / 「**이 미션의** Director 서연」. 층 없이 「Director」만 적으면 방에 미션이 셋일 때 어느 미션의 말인지 화면에서 읽히지 않는다. 비활성 사유도 같다 — 「Director만 가능」이 아니라 「〈민호〉가 이 미션의 Director 입니다」(SCREEN §2.3·§5).

**「멈춤」과 「일시정지」는 층으로 갈린다(v0.19).** **미션·서브 미션·할 일**의 `paused` 는 사람이 되돌리는 조작이라 **U12 가 못박은 「일시정지」**를 그대로 쓴다 — `badge-map` 의 `paused` 라벨은 다섯 kind 공통으로 「일시정지」다. **방 `blocked_reason` 은 시스템(또는 방장)이 방 전체를 세우는 것**이라 **「멈춤」**을 쓴다(「예산으로 멈춤」·「이 방 멈춤」). 둘이 한 화면에 서므로 배지는 층을 함께 적는다 — 「⏸ 일시정지 · 미션 예산」 / 「⏸ 멈춤 · 방 예산」. **U12 기대 문구를 고치지 않고 「멈춤」으로 통일하려 들면 자물쇠가 깨진다**(SCR-C §3.2).

**같은 사유는 같은 말로 쓴다.** 컴퓨터가 끊긴 상태는 배지·배너·인박스 어디서나 **「컴퓨터 연결 끊김」**이다 — 「오프라인」을 섞어 쓰지 않는다(`PausedBanner.tsx` 의 `runtime_offline` 문구가 정본).

**한 화면 안에서 언어를 섞지 않는다.** 지금은 메뉴가 영어(Sessions)인데 옆 버튼이 한국어(새 에이전트)다. 한국어로 통일한다.

### 8.5 레이아웃·대비 (후속)

- 빈 상단 바(47px)를 없앤다 — 넣을 것이 정해지기 전까지 자리만 차지한다.
- 화면 제목 아래 **한 줄 설명**을 둔다(그 화면이 무엇을 하는 곳인지).
- 비활성 버튼은 왜 비활성인지 버튼 근처에서 말한다(지금은 빈 상태 카드에만 있다).
- 목록·카드에 상태 점·아이콘을 붙여 훑기 쉽게 한다. 아이콘은 stroke 기반 16px, 한 벌로.
- **넓은 화면에서 콘텐츠가 좌상단에만 몰리지 않게 한다** — 카드 목록은 가용 폭에 따라 열을 늘리고(`repeat(auto-fill, minmax(…))`), 본문에는 읽기 좋은 최대 폭을 둔다. **단 인박스(받은 요청)는 한 열이다** — 위에서 아래로 읽고 답하는 목록이라 열이 갈리면 순서가 흐려지고, 항목마다 답 입력·버튼이 있어 높이가 제각각이라 다열과 상성이 나쁘다(PR #191 판정). 열 최소 폭은 토큰 하나(`--card-min`)로 두어 두 파일에 흩어지지 않게 한다.
- 다섯 화면의 한 줄 설명은 **그 화면에서 할 수 있는 일**을 말한다(동어반복 금지 — "설정을 바꿉니다" 가 아니라 무엇을 바꿀 수 있는지).
- **자물쇠를 한 겹 넓힌다**(PR #186·#188 리뷰): 글자·문구 테스트가 못 보는 자리 — 템플릿 리터럴 `${…}` 안의 문구, solid 배지 조합의 대비, soft 카드가 `--surface` 위에 놓이는 평면 — 을 같은 방식으로 덮는다. 새 문구가 실제로 쓰이는지도 잰다(지금 자물쇠는 "옛말 0건" 만 잰다).
- **방 목록도 한 열이다(v0.19).** 인박스를 다열에서 뺀 근거(위 PR #191 판정)가 v0.19 방 카드에 그대로 성립한다 — 안 읽음·주의 배지·멈춤 배지가 붙는 순간 방 목록은 "고르는 격자"가 아니라 "훑고 답하는 목록"이 된다(SCREEN §4.3, SCR-C §4.1).
- **수를 문장에 보간하지 않는다(v0.19).** 「미션 **2**개와 대화 전부가 멈췄습니다」의 `2` 는 **슬롯**이다 — 템플릿 리터럴 안에 넣으면 문구 자물쇠가 그 문장을 못 잡는다. 같은 규칙이 「완료 12 ▾」·「방 전체 · 아티팩트 7 · 결정 6」·「나에게 필요한 것 3」에 적용된다.

---

## 9. v0.19 신규 컴포넌트 (문서 스펙 — 코드는 R2)

**이 절은 문서 스펙이다.** `web/` 코드와 `.pen` 캔버스는 R2 에서 따라온다(PRD §10 R2 서브 순서). 근거는 `SCREEN.md` v0.19.2 §5 와 `plan/research/SCR19_design.md` §2.2.

**먼저 — 기존 어휘 안에 드는 것 5개**(새 컴포넌트가 **아니다**):

| 신규 요구 | 기존 자리 |
|---|---|
| 서브 미션 카드의 **미션 라벨** | Lane Card `x1YCq` 의 **부가 정보 `bXrz6`**(정의에서 꺼 둔 슬롯) |
| HITL 카드의 **수신자 근거**(「Director 로서」) | HITL Card `n9PqY` 의 **권한 안내 `W7gS3`**(꺼 둔 슬롯) |
| 에이전트 칩의 **「다른 방에서 작업 중」** | `agent-chip__line2`(이미 있는 둘째 줄) — **더하지 않고 바꿔 넣는다**(§9.3 주의) |
| 미션 종료 조건 진행률 | Condition Row `XMNop` |
| 확인 다이얼로그(방 삭제·보관·「이 방 멈춤」·퇴장·방장 넘기기·링크 해제·제안 거절) | `DeleteSessionDialog` 의 `role="alertdialog"` 패턴 — **일곱 곳으로 늘었으므로 일반화한다** |

### 9.1 Work Chip (미션 칩) — 신규

- **쓰이는 곳**: S7 상단 칩 줄(거르개+선택자) · 서브 미션 카드 라벨 · 메시지 카드 라벨 · 받은 요청 카드 · 작성창 미리보기. SCREEN §5 가 「같은 모양이 네 곳에 선다」고 못박았다.
- **상태 5**: `unselected` / `selected` / `(전체)` / `(미션 없음)` / `disabled`(지난 미션).
- **부분**: 상태 글리프(`●`/`⏸`/`⏳` — `badge-map` 과 같은 글리프) · 이름(미션 **제목**, 말줄임 12자) · 선택 표식.
- **문구**: 특수 칩 둘은 글자 그대로 `전체`·`미션 없음`. **`⏳` 는 상태가 아니라 파생**(열린 HITL 존재)이므로 상태 배지와 같은 말로 읽히지 않게 툴팁이 아닌 `aria-label` 로 「사람 대기」를 준다.
- **규칙 4**
  1. **선택은 색만으로 표시하지 않는다** — 테두리 굵기 + 앞에 `✓`(§8.4 「글리프가 1차 신호, 색은 보조」). 프로그램적으로는 `aria-pressed`(또는 `role="tab"` + `aria-selected`).
  2. **거르개로 쓰일 때만 누를 수 있다.** 카드 안의 것은 라벨이다 — 같은 모양이므로 **누를 수 있는 쪽만** 커서·포커스 링을 갖는다.
  3. **칩 줄은 하나의 그룹이고 이름은 「미션 거르개」다**(`aria-label`).
  4. **누르면 가운데와 우열 두 곳이 바뀌므로 `aria-live="polite"` 로 알린다** — 「미션 〈보고서 초안〉으로 걸렀습니다 · 서브 미션 3개 · 메시지 42개」. 화면으로는 연동이 보이지만 **소리로는 안 들린다**.
- **접기**: 미션 칩이 5개 이상이면 4 + 「미션 N개 ▾」. **그 셈에 `(전체)`·`(미션 없음)`·「지난 미션 ▾」·「일시정지 N ▾」·「+ 새 미션」은 넣지 않는다**(SCREEN §8.7 Q8).
- **글자**: `--fs-body`(상단 전폭이라 여유 있음). **카드 안 라벨은 `--fs-meta`(12px 하한)** 이고, 그래서 **미션 칩이 선택돼 있으면 카드 라벨을 감춘다**(양방향 규칙, SCREEN §4.6).

### 9.2 Work Selector (미션 선택기) — 신규

- **쓰이는 곳**: 작성창 미리보기 칩 옆, 한 자리.
- **상태 3**: `열림`(사람이 고른다) / `잠김`(스레드 안 — 비활성 + 「이 스레드는 미션 〈…〉의 것입니다」) / **`자동`**(FR-3.1.1 규칙 3 — 멘션 대상의 실행 중 서브 미션이 미션에 매여 자동 귀속).
- **문구**: 「**이 메시지는 미션 〈…〉에 들어갑니다**」 / 「**미션 없음에 들어갑니다**」.
- **규칙**: **`자동` 은 `열림` 과 시각적으로 달라야 한다** — 문장 앞에 「자동: 」을 붙인다. SCREEN §4.6 이 규칙 3 을 「이 규칙의 핵심이자 위험 — 사람이 예측할 수 없다」고 스스로 적었는데, 두 경우가 같은 문장을 쓰면 사람은 자기가 고른 것인지 시스템이 고른 것인지 모른다.
- **기본값**: 지금 고른 칩. **`(전체)` 보기에서는 「미션 없음」**이고, 규칙 3 이 발동하면 그 미션으로 바뀌면서 「바꾸려면 선택기를 누르세요」를 덧붙인다.
- **비활성 사유**는 `DisabledHint`(버튼 아래 글자), 툴팁 아님.

### 9.3 Person Chip (사람 칩) — 신규

`AgentChip` 은 **닫힌 타입**이다 — `role?: AgentRole`(`lead|researcher|writer|engineer|reviewer|custom`), `status?: AgentStatus`(6종). **방장·부방장은 `AgentRole` 에 없고 사람에게는 `AgentStatus` 가 없다.** SCREEN §2.2 가 「참여자 목록은 한 목록」이라고 한 이상 **같은 모양·다른 데이터**를 담을 자리가 있어야 한다.

- **부분**: 아바타(아이콘으로 사람/에이전트 구분) · 이름 · **방 역할 칩**(`방장`/`부방장`/없음) · 둘째 줄(「〈미션 이름〉의 Director」 · 합류 시각).
- **상태**: 사람에게 실행 상태는 없다. **빈 배지 자리를 남기지 않는다** — 에이전트 칩과 같은 격자를 쓰되 배지 슬롯이 꺼진다.
- **문구**: 방 역할은 한국어(`방장`·`부방장`), 미션 역할은 영어(`Director`·`deputy`) — §8.4 의 네 역할어 규칙 그대로, **층을 함께 적는다.**
- **에이전트 칩 쪽 주의**: 둘째 줄은 이미 `lead · claude_code · claude-sonnet-5 · 서브 미션 실행 중` 네 토막이고 268px 에서 두 줄로 접힌다. 「다른 방에서 작업 중」은 **프로파일 표시를 밀어내고 그 자리에 쓴다** — 층을 더하면 세 줄이 된다. 프로파일은 칩을 눌러 팝오버에서 본다.

### 9.4 Room Blocked Banner (방 멈춤 배너) — Status Banner `zPJly` 의 두 번째 크기

SCREEN §5 가 「**방 멈춤 배너**(전폭)와 **미션 멈춤 배너**(우열)로 두 크기」라고 이미 적었다. 스펙에 없던 것은 **카운트 슬롯**이다.

- **카운트 슬롯**: 「미션 **2**개와 대화 전부가 멈췄습니다」의 `2` 가 들어갈 자리. SCREEN 이 「몇 개가 멈췄는지 반드시 센다」를 강한 규칙으로 걸었으므로 그 수는 **문장 안의 보간이 아니라 슬롯**이어야 한다(§8.5).
- **무게를 마크업이 말한다**: 방 배너 `role="alert"`(끼어든다) · 미션 배너 `role="status"`(조용히). 지금 `PausedBanner` 는 둘 다 없다.
- **사유 4종**: `budget` · `runtime_offline`(문구는 「컴퓨터 연결 끊김」) · `loop` · **`manual`**(방장·부방장·ws owner·admin 이 건 긴급 정지 — 해제 버튼이 배너 안에 있고, **HITL 없이** 건 사람과 같은 권한자가 푼다, PRD FR-2.4). **루프 상한은 이 배너에만 온다** — 미션 배너의 사유는 예산·시간·수동 셋이다(PRD §5·FR-3.5).
- **위임 줄**: 내가 승인 권한자가 아니면 「방장 〈민호〉의 승인을 기다립니다 · 14:30부터 부방장 〈서연〉이 답할 수 있습니다」(FR-2A.3).
- **대비**: soft 배경이 `--bg` 위에 직접 놓이는 **새 평면**이다 — §8.3 의 ⑤.

### 9.5 `badge-map` 에 kind 2개 추가 (문서 결정 — 코드는 R2)

`web/components/badge-map.ts` 의 `BadgeKind` 는 **닫힌 union** 이고(`lane | task | session | agent | inbox`) 파일 머리가 「여기 없는 kind/value 조합은 TypeScript 타입 오류다」라고 적었다. v0.19 는 둘을 더 요구한다. **닫힌 union 이라 값이 늦으면 타입 오류로 구현이 막히므로 R0 계약 값과 함께 확정한다.**

| 새 kind | 값 | 라벨 | 글리프·톤 |
|---|---|---|---|
| **`room`** | `blocked_reason`: `budget` · `runtime_offline` · `loop` · `manual` (+ null = 배지 없음) | 「예산으로 **멈춤**」 · 「**컴퓨터 연결 끊김**으로 멈춤」 · 「루프 상한으로 멈춤」 · 「**직접 멈춤**」 | ⏸ soft. `manual` 도 같은 글리프 — 사람이 걸었다는 것은 **라벨**이 말한다. **라벨에 역할을 넣지 않는다** — 방장·부방장·ws owner·admin 누구든 걸 수 있으므로(PRD FR-2.4) 역할 이름을 붙인 라벨은 거짓이 된다. 누가 멈췄는지는 배너가 이름으로 말한다(SCREEN §4.6) |
| **`work`** | `draft` · `active` · `paused` · `completing` · `completed` · `cancelled` | 기존 `session` 6종과 **같은 말**(「진행 중」·「**일시정지**」·「완료」…) | 기존 `session` 과 같은 글리프 |

> **`session` kind 를 재사용해 이름만 바꾸지 않는다.** 이관 기간에 두 kind 가 공존하므로(별칭 한 판 유지, §10 R0), `session` 을 그 자리에 두고 `work` 를 따로 더한 뒤 R4 에서 `session` 을 뺀다. 이름만 바꾸면 옛 kind 를 가리키는 테스트·목이 조용히 통과한다.
>
> **`room` 의 라벨에 「일시정지」를 쓰지 않는다** — §8.4 의 층 분담 그대로다. `work`·`lane`·`task` 의 `paused` 는 「일시정지」, `room` 의 `blocked_reason` 은 「멈춤」이다.
